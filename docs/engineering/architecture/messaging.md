# Messaging — the state cell and the notification cards (architect design 2026-08-29)

This file is the ruling for BOTH halves of the messaging redesign: the STATE CELL, row 8's own text in the clock's own run, which carries what is true right now; and the NOTIFICATION CARDS, the top-right stack, which carry what just happened. The design record with his words is `tmp/notifications_design.md`; the mockups he picked from are `tmp/previous/messaging_mockups/` (`cards_AB.png`'s look 1 is the card, `bar_ground_AB.png`'s look A the ground he picked for the bar). The cards landed first; a STATUS BAR on the window's foot landed the next day and the tab row's STATUS CHAIN — the four-tier ladder the state strings and the events had shared since 2026-08-13 — was deleted with it (the tab row paints tabs and nothing else); and that same evening the architect ruled the bar off and its state text folded into row 8, where it is.

## The split: state and events

Every message the product emits is one of two things, and the split is the file's opening rule because it decides the surface. STATE is what is true right now, replaced as it changes, never timed out and never cleared by a key press — the progress line and the `h` walk line (the RESOLVED READOUT was the third and retired whole with the bar). STATE LIVES IN ROW 8'S STATE CELL. An EVENT is something that happened: an act was answered with a sentence, or something the user was not watching finished badly. EVENTS ARE NOTIFICATION CARDS. The architect is "not a big fan of notifications": the cards are minimal, and the inventory below is closed.

The split is also why the state surface has NO TIMEOUTS. The retired transient class was an event on a state surface, and it inherited every fault of that: it was hidden under a progress line, REVEALED STALE when the line above it cleared, wiped by the next key press, and invisible altogether under the `h` view's top-tier line. Nothing in the state cell can go stale, because nothing in it is ever a claim about a past moment.

## The state cell — row 8, in the clock's own run

"Status bars are generally the last row" (architect 2026-08-29) — every DAW and KWave agree, and kdenlive's own is the model: `tmp/previous/screenshots/kdenlive/kden3.png` shows the bar at its foot, `kden2.png` two text cells butted with padding and no separator. Messages were stuffed into the tab row in the first place because the returned Pi rig's 1024x600 had no room; the tablet has room. **A LANE ON THE WINDOW'S FOOT IS WHAT THAT REASONING BUILT, AND IT LASTED ONE DAY.** The architect saw it that evening and ruled it off: he prefers the state text on the toolbar he already reads, and the bar duplicated that panel's own foot. THE STATE TEXT IS ROW 8'S OWN CELL NOW, and the whole tenth lane — `status_bar_area`, `status_bar_content_area`, `kStatusBarContentPx`, the three height accessors, `paint_status_bar` and the window-foot seam `kRedesignBottomLine` — is deleted. The vertical stack is EIGHT lanes again, seven top plus the one bottom row on the foot, and gap 2 got the bar's 33 authored px back the same way it had given them.

THE CELL IS NOT A CELL ANY MORE — **the lower left is ONE MONOSPACE RUN** (architect 2026-08-31): `| sep | kTransportSepToClockPx | 00:00.100 | Updating... |`, the clock, a LITERAL PIPE and the state text in one string, in the clock's own monospace face at the row's size, on the clock's own baseline, from the clock's own origin. The pipe is a character in the string and not a painted separator, so the SEPARATION IS TYPOGRAPHY and there is no gap left to measure. With no state string standing, the clock paints alone exactly as it always has.

WHAT THAT SUPERSEDES, in the two days the state text was a neighbouring cell: it was a SANS run in `kRedesignLabel` on the row's own centred baseline (not the clock's, whose authored 1 px nudge is a monospace-cell correction), laid a SECOND READ of `kTransportSepToClockPx` off the clock — `| sep | gap | 02:42.608 | gap | <state text> |`, one constant and no new number, with no separator between them (kden2's way: two runs and padding alone) — and then, from 2026-08-30, laid INK TO INK, because one authored number read twice still read wrong: the architect saw `Rendering...` standing about two pixels further from the digits than the separator stands from them, the SIDE BEARINGS falling on opposite sides of the same gap (the clock's cell is an ADVANCE sum whose last column is air, and the sans run opens with its own left bearing), measured at 100 % as 9.0 px of ink on the left against 11.625 on the right. The correction placed the text's origin so that (clock ink right → text ink left) equalled (separator → clock ink left), through the one ink reader `text_shape::ink_extents_px` on the clock's memoised SPECIMEN. **ALL OF IT RETIRES WITH THE PIPE**: the constant's third reader (it keeps this row's separator → clock distance and the render player's modal row's three), the specimen's `ink_left` / `ink_right` pair, `text_shape::ink_extents_px` and `InkExtents` themselves — that ink road had exactly one consumer and no other layout here wants ink, every other one wanting a reserved CELL, which is `width_px`'s job — and the second face at the site. The clock's reserved WIDTH and its damage box stay the advance's, untouched.

UNCHANGED BY THE MOVE: the run is CLIPPED, never ellipsised (a cairo rectangle clip, the folder overlay rows' precedent), where the row's right-hand button cluster begins less one `icon_row_pad_x` — the clip is the STATE's and not the clock's, so the digits alone still paint unclipped and the row's crop-at-its-floor rule is untouched. The painter is `paint_bottom_row_buttons_and_clock`'s, laid out with the clock it is now part of; the `h` walk line keeps its own small composer (`history_walk_line`) beside it.

THE CONTENT is exactly the retired bar's LEFT cell: `queue_progress_text` (`Loading...` / `Rendering...` / `Rendering N of M <label>...` / `Updating...`) or the `h` view's walk line (`n/N <label> Scale: [-]… [+]…`). THE TWO CAN COEXIST AND THE WALK LINE WINS — the recorded coexistence, kept: nothing STARTS a render from inside the `h` view (both render chords are off its allowlist since 2026-08-08), but a render or a target preview dispatched BEFORE the visit runs on through it — bare `h` refuses only for a checkpoint in flight, cancels no render and clears no progress text, and `GuiTargetRender` gates on the `T` audio view, which the mode does not move. So the walk line stands while the view does, and the progress line is back the moment it closes.

THE RESOLVED READOUT RETIRED WHOLE with the bar (architect 2026-08-29). It was the bar's RIGHT cell for that one day, the bottom strip's before that and the tab row's chain's in between — `~= 1.28*1.0112 (from a.23 @ 00:19.208)`, the selected pass or reference marker's resolved tempo. Nothing displays a resolved value any more. What replaced it is a pair of acts on the same subject, the selection's focus: **bare `j`** copies the pasteable value to the system clipboard (the global `Ctrl+C` that used to do it is unbound again; the editors' ctrl-exact `Ctrl+C` / `Ctrl+X` / `Ctrl+V` are untouched), and **Shift+`j`** JUMPS to the marker that value came from — the pass's owner or the ref's definition — on the OTHER A/B tab, so a reference and its definition stand one Ctrl+Tab apart. The jump is FIVE acts since the architect's evening pass that day — `c` on the tab it leaves, the tab switch, the single-select on the source index, the marker land, `c` again — so both tabs end framed, each on its own half of the pair. One bottom-row button, **Copy resolved value** (the verb group's seventh, seated after the Measure), is `j` at its lift and Shift+`j` at its shift-click or long press. The parser's own composer went with the readout: `compute_hover_popup_text` is `resolved_marker_payload` now, returning the payload alone plus the source marker's index, its prefixes and its parenthetical provenance deleted (a granted frozen touch, architect approval 2026-08-29).

THE ROW YIELDS WHOLE TO A MODAL, so the state text is HIDDEN while a prompt, a dialog editor, the render player or the picker stands, and the live string is back the instant it closes — architect-accepted at the fold, and the one thing the separate lane bought that the cell does not. It is also why the render player's load-under-a-running-render refusal is a CARD: the progress line that used to be its explanation is not on screen under that modal.

THE DAMAGE OWNER is `Viewport::invalidate_status_cell_area` (renamed from `invalidate_status_bar_area` with the fold), and it takes THE BOTTOM ROW'S LANE WHOLE: the state text reserves no width of its own — it runs on from the clock's last digit and clips at the right block — so a shorter new string must erase a longer old one and there is no fitted box to erase inside. The lane's rect covers the clock cell's as a superset, so a route touching both spells them and pays once. ITS CALLERS SHRANK TO THE PROGRESS LINE'S WRITERS: `input_render_dispatch`'s three and `target_render`'s six. Every call that served the READOUT alone — the selection routes, the marker-value writes, iteration mode's two, the flag editor's, the undo restore's, the drags', the trim commits', the propagate tails', the nudge tail's — is deleted with it. The `h` walk line has no site of its own and never did: the mode's edges invalidate the whole window, as the file loader's `Loading...` does.

## The two classes

- NORMAL — a small card that leaves on its own `kNotificationMs` (5000 ms, `gui_input.h`, beside `kHoldBeatMs` and deliberately NOT derived from it: a card's life is neither a hold nor a second tap, and Plasma's own default is what it takes; a duration, so it rides no scale) after ITS PUSH — every card is on screen from that instant since the queue retired (2026-08-30), so there is no later surfacing for a clock to wait for — or at its X, or at a BUMP. The pointer resting on it PAUSES the clock (the remaining life is banked at hover-enter and re-armed at hover-leave; the pointer-left hook ends a hover too; glass has no hover and no finger ever holds a clock). A card whose deadline has already passed when the pointer arrives is NOT banked — the deadlines are polled, so a pointer can land in the gap between the deadline and the tick, and hover pauses a clock rather than resurrecting one: that card leaves on the next poll as an unhovered one would.
- CRITICAL — never TIMED out and never BUMPED. No clock, and nothing that comes after takes it down: a later success raises nothing and clears nothing, AND NO BUMP EVER REMOVES ONE (architect 2026-08-30: "critical cards keep standing"). What does take one down is a DELIBERATE dismissal, and all three roads reach it: its X on the pointer, BARE ESC as the stack's OLDEST card — the key reads no class (2026-08-31) — and CTRL+ESC with the whole stack (2026-09-01), the one act that reaches a critical card without a pointer; the two Esc rulings are below. The four checkpoint outcomes (`Checkpoint failed: nothing was committed` / `Checkpoint failed: files written but not committed` / `Checkpoint could not be confirmed` / `Checkpoint committed; push failed`) are its only producers.

No remaining-time bar, no actions, no title/body split, no sound. The glyph is what tells the classes apart at a glance: `dialog-information` on a normal card, `dialog-error` on a critical one.

## The look and the placement

The stack sits top-right, `kPanelPadPx`-class air (the folder overlay's `pad_px`) below ROW 1 (the menu row, whose right-floating view bar carries the S+W / T+P / T+W radios) AND THE SAME NUMBER in from the window's right edge — the two margins are ONE constant read twice (architect 2026-08-29; the right margin was `icon_row_pad_x()` for the cards' first day and read wider than the air above them; a card's INTERNAL pad is a different concept and a different number, the one derived pad below) — newest on top, growing DOWN over whatever lies there (the tab row's right stretch, the icon row's empty right, the thin lanes, the waveform). THE STACK IS UNCAPPED AND EVERY CARD IN IT IS VISIBLE (architect 2026-08-30, retiring the three-card cap and the queue with it: "cards bump each other off the screen — newest on top, the oldest leaving; critical cards keep standing"). What limits it is THE ROOM, counted in one-line cards — `notification_capacity(a)` = `floor((room + gap) / (card + gap))`, at least 1, a pure function of the window and the scale like the room itself: 20 at a 1080 px window and 100 %, 11 on the tablet's 1440 px panel at 225 %, far more than the architect will ever stack. A push past that count BUMPS the oldest NORMAL card and nothing else — never a critical one, and never the card just pushed, which is the answer to the act the user has this instant performed (reachable, not theoretical: the capacity is 4 at 1080 px and 350 %, where four checkpoint verdicts can stand). The capacity counts ONE-LINE cards because no pure function of the window can know a line count, so two overflows escape it — a burst of WRAPPED cards, and CRITICALS the bump will not touch — and both take the same answer, THE PAINTER'S CLIP AT THE ROOM'S FOOT: the overflow paints on down and is cut there rather than over the bottom row, and the painter publishes its rects clipped too, so nothing under that foot is claimed by a card either. One rule, not a second cap.

Every constant a card reads, none of its own but TWO (`notifications.h` — `kNotificationMinWidthPx` and `kNotificationMaxLines`; `kNotificationVisibleMax` was a third until the stack went uncapped 2026-08-30 and the room's own capacity replaced it):
- height = `icon_row_content_h_px()` — the icon row's 46: the 32 px button box plus its 7 px margins — THAT IS THE ONE-LINE HEIGHT, and a card of `n` lines is it plus `(n − 1)` face line-heights (`cairo_scaled_font_extents(font).height`, `nearbyint`ed, read once per paint);
- the gap between cards = `kIconBtnGapPx` (2), which the room's capacity count reads too;
- THE PAD = `notification_pad_px()` (7 at 100%), ONE NUMBER FOR ALL SIX DISTANCES (architect 2026-08-30, his first symmetry pass — he saw the X's side margin exceed its top/bottom margin): left edge → glyph box, glyph box → text, text → X box, X box → right edge, top → boxes, boxes → bottom. Nothing is authored for it: it IS `(notification_card_h_px() − kIconBtnPx) / 2`, the icon row's own centering that the card's height already comes from, so a retune of either moves all six together. Where the difference is odd the integer floor puts the extra pixel BELOW the boxes, the icon row's own arithmetic and not a second rule. The glyph and the X each sit in a `kIconBtnPx` (32) box at that pad, the `kIconGlyphPx` (22) glyph at the box's own `(32-22)/2` inset. `icon_row_pad_x()` and the folder overlay's icon-to-name gap `kRowIconGapPx` — the card's two foreign constants until that day — LEFT the painter with it; the overlay's own rows keep their gap, that being their surface;
- width = the content's (four pads, two boxes, the shaped text) clamped to [`kNotificationMinWidthPx` 272 authored px, `kNotificationMaxWidthPx` 640 authored px]. THE FLOOR IS THE UNDO CARD'S OWN WIDTH (architect 2026-09-01): he steps undo and redo deliberately as a sanity check, and the two walls' cards — `There is nothing to undo` and `There is nothing to redo`, one letter apart — came out a few pixels apart and the difference read as a flicker between two presses of the same shape. THE MEASUREMENT, at 100 % in the card's own face (the one sans at `redesign_font_size_px`, 16 px): the undo sentence shapes to 172.73 px and the redo one to 169.16, and the card adds its chrome — four pads and two button boxes, 4 × 7 + 2 × 32 = 92 — for 265 px and 262 px. 272 IS THE NEXT MULTIPLE OF 8 ABOVE THE WIDER, a clean authored round-up with 7 px of air over the sentence that set it, so a face retune of a pixel or two cannot silently put the pair back at two widths; and because the floor is AUTHORED px scaled like the sentence, the relation holds at every `gui_scale`. Every shorter sentence joins them at the one width, which is the point. IT DOES NOT MEET THE CEILING and changes nothing about the clamp: `notification_card_max_w_px` already floors its window safety here, so `floor ≤ ceiling` holds as before and a window narrower than the floor still keeps the floor and lets the card overhang. THE CEILING IS THE LAPTOP'S OWN, AUTHORED AND SCALED (architect 2026-08-31): 640 is what the retired `window / 3` gave on the 1920 px laptop, so the width the laptop already had is the canonical one and `scaled_px` carries it to every device — the fraction was DEVICE pixels and starved the tablet, whose 1440 px panel at 225 % gave a 480 px card for text shaped half again as large. A CARD MAY NOW COVER BUTTONS on the tablet, accepted in his own words ("it's okay if it covers up some buttons"): a card is a sentence to read and it leaves on its own. The WINDOW survives only as a SAFETY under the ceiling — the window less the stack's two `kPanelPadPx` margins — and never below the floor, that number being the stack's ROOM as well, which the painter clips to and the damage owner erases; a window narrower than the floor keeps the floor and lets the card overhang, the contrived window this design declines to cater for;
- ground `kModalFieldGround` #141618 (the player's and the picker's band ground — "that colour stands out"; the dropdown's ground was rejected as "what's already in use throughout") under a 1 px `kRedesignTabLine` #4c4e51 outline (the popup's own border), through the one popup box painter `paint_popup_chrome` at the popup's radius;
- ink `kRedesignLabel`, the one sans at the redesign's size, the FIRST line's baseline the one-line card's through `redesign_baseline` over that first band;
- THE WRAP (architect 2026-08-30): a sentence that does not fit the text's room GROWS DOWNWARD UNDER THE TWO ICONS to at most `kNotificationMaxLines` (3, an authored count — his "a few"). The glyph box and the X box keep the one-line card's placement and NOTHING REFLOWS beside them; each further line sits one line-height lower; the air under the last line is the first line's own by construction, so the one pad still rules every edge. The WIDTH rule is unchanged — the whole sentence's shaped width inside the same clamp — so a card that fitted one line still does. Breaking is GREEDY AT ASCII SPACES ON MEASURED ADVANCES, each candidate line re-shaped as one run through the shaping chokepoint so the run that is measured is the run that is painted (`notification_text_lines`, the one wrap owner, beside the painter); a break's space and any leading spaces are consumed and every other byte is carried verbatim — the lines are substrings, never a rejoin. A SINGLE WORD WIDER THAN THE ROOM stands alone and clips at the run's right edge, and THE LAST PERMITTED LINE TAKES THE WHOLE REMAINDER and clips the same way — no word is ever silently dropped, only cut where the eye can see the cut. No ellipsis (the folder overlay rows' precedent) and no memo: shaping runs at paint, as the overlay's rows already do;
- the X wears the icon button's hover face — the 1 px `kRedesignAccent` outline through `redesign_face_box`, the icon row's own — while the pointer is in its box; the card's body wears no hover face.

The three glyphs are fresh verbatim transcriptions into the roster (`icons.h`, which `kIconCount` counts — 54 at the cards' landing, 55 since the Copy resolved value button's edit-copy later the same day; that constant is the one authority): `status/22/dialog-information` and `status/22/dialog-error` (symlinks in Breeze Dark onto `data-information` / `data-error`, copied resolved into `assets/icons/breeze/`) and `actions/22/window-close`. The two dialog files are TWO-COLOUR — a rounded plate in a scheme class (`.ColorScheme-Accent` #3daee9, `.ColorScheme-NegativeText` #da4453: the roster's existing `kIconAccent` and `kIconNegativeText`) under a glyph in the literal `#fff` — and the roster paints each path in the table's own ink, so the card's painter colours nothing: the plate is Breeze's own blue or red. Their plates are `<rect rx="2">` elements and take the table's four-number derivation (the `<rect>` precedent tool-rect-selection set), the glyph paths verbatim.

## The layer

Cards paint in `on_redraw`'s step 13, AFTER the flag editor's box and BEFORE the dropdown — above every lane, the waveform, the keyboard slot and the folder overlay's band, BELOW the three surfaces that stay topmost: the dropdown and the tooltip (the two pointer-transient floaters) and the modal row. One painter, `paint_notifications`, unconditional on every frame for the floating surfaces' reason: it PUBLISHES each visible card's rect and X box (`AppState::Notifications::painted`), and a skipped run would strand a stale publication.

## The hit

THE X, AND ONLY THE X, DISMISSES A CARD ON THE POINTER (architect 2026-08-29, superseding the design's tap-anywhere) — AND SINCE 2026-08-31 THE X HAS A KEYBOARD TWIN, BARE ESC (architect). The succession is exact and the 2026-08-29 ruling is untouched: it says where a PRESS may land on a card, and the key lands on no card at all. Esc DISMISSES THE STACK'S OLDEST CARD — the bottom, not the top, in his own reasoning: "clearing the newest would let the oldest stick around preferentially", so the key empties the stack from the back the way the clock does. It reads no class: A CRITICAL CARD IS DISMISSED LIKE ANY OTHER, Esc being the X's twin and the X taking any class (the bump is the act that skips criticals, and it is not this one). THE TWO DIFFER IN RANK, deliberately: the X's press claim sits ABOVE EVERY VEIL because a card must be dismissable under any modal, while Esc sits UNDER all of them — it is the LAST of bare Esc's EIGHT places (region-scrub-esc.md is the Esc owner; the src enumeration is at `on_key`, the arm at `handle_plain_bare_keys`), so the key reaches the stack only when no prompt, editor, player, picker or dropdown is standing and no render is in flight. With an empty stack the arm is the ruled silence it has always been (the list below).

AND CTRL+ESC CLEARS THE WHOLE STACK, CRITICALS INCLUDED (architect 2026-09-01) — the X's BULK FORM, the one act that reaches a critical card without a pointer, and therefore the act that clears exactly what the X's CLOCK never touches. That asymmetry is deliberate and RECORDED SO A REVERSE IS EASY: an explicit chord is a decision where a timeout would be an accident, and if the criticals should ever survive it, `GuiNotifications::dismiss_all` is the one line to change. IT SITS AT THE OPPOSITE END OF THE RANKING FROM ITS BARE SIBLING: bound at the HEAD of `on_key`, above every gate, editor, player, picker, dropdown, drag and loading state, for the reason the X's own pointer claim sits above every veil — a card must be dismissable under any modal, and the chord is aimed at nothing else (it authors nothing, reads no tab, opens and closes no surface, so nothing below it has anything to protect). The head is also the seam with the FEWEST EDITS: the player's and the picker's routers are each "the whole vocabulary" while they stand and would otherwise have had to enumerate it beside Ctrl+S and Ctrl+Q, and the four gates would each have needed a hole. Ctrl-exact, one-shot (`repeat_eligible` names no Escape shape in any state), read-only-legal and `h`-legal — the gates it sits above are the gates that would have refused it — and SILENT WITH AN EMPTY STACK, the state it asks for being already true and visibly so. IT ADDS NO PLACE TO BARE ESC'S EIGHT: it is a different chord, and the eight-place contract and its arm are untouched. It joins `chord_is_bound`'s inventory for completeness alone — no gate below the head can ever be asked about it. The pointer's own rule, unchanged: a press anywhere on a visible card is consumed whole — arms nothing, moves nothing, lands no playhead, opens no drag, reaches nothing underneath — and the LEFT press on the card's X box dismisses it; any other button over a card is consumed in the veil's own manner. ONE RULE ON BOTH BACKENDS: the router cannot fork on tap versus click (no origin bit rides a press — `GuiInputState` carries modifiers alone), so the rule must be one, and the X's box is the icon row's 32 px button box, already the product's glass target, which is why no finger-fattened body target exists.

The claim sits at `on_button_press`'s head, after the tooltip hide and the menu row's disarm (a press anywhere still does both) and ABOVE EVERY VEIL — the on-screen keyboard's claim, the prompt's, the folder overlay's, the player's, the picker's, the dialog editors' — because a card must be dismissable under any modal. It is not an exception to the veil: a card is not a reach into the veiled surface, it is the message about the act the veil stands over (the record at the retired reach-through's site, `input_pointer.cpp`). The press is the act (content acts the moment its identity is certain; a card press can mean one thing), so the release owes nothing and no arm is left standing.

The geometry a card is HIT by is the geometry it was PAINTED with: the one hit owner reads the publication and then asks the live stack whether that id is still there — which since 2026-08-30 is the whole test, "in the stack", the queue clause having had the queue as its only producer (published geometry may only SELECT; live state decides — `ModalDialogGeometry`'s doctrine) — so a card that expired or was BUMPED between the paint and the press is hit by nothing: it is never dismissed twice, and one already gone is never dismissed at all (`dismiss` asks the same test of its argument), and the press is claimed by nothing and falls through to the surface underneath, whose pixels are what the next paint shows there anyway. A card under the open dropdown's box yields to it (the one pointer-owning surface that paints above the cards), in the one owner `notification_card_at`.

- CURSOR: the zone map's arm 0 — the Arrow over a card, whatever lies under it (the card is opaque to the pointer).
- MOTION: the hover is re-derived at the head of the motion handler, above every branch, like the menu row's exit; the X's face is the only thing it paints. THE OPACITY IS CARRIED BY THE TWO HOVER WALKS A CARD CAN STAND OVER, not by an early return there: the roster's (`recompute_redesign_button_hover`) and the folder overlay band's (`update_folder_overlay_hover`) each ask the one hit owner and answer "nothing under the pointer", so no button beneath a card wears a hover face or starts a tooltip dwell. It has to live at the roster's walk rather than at the motion, because that walk's SECOND caller is the tick and a pointer RESTING on a card is exactly the case; an early return at the motion would also freeze a live gesture whose pointer merely crossed a card.
- WHEEL: swallowed over a card (`wheel_context` answers -1, first and above every veil, as the press claim ranks above them). A card is not a scroll surface and it is opaque, so a detent over one neither reaches the waveform under it nor scrolls a band it overlaps; -1 rather than 0 keeps the platform from growing sub-detent remainder there.
- TOUCH: the one-finger translation delivers the press only once the disambiguation window has resolved to the pointer (a tap, or an off-zone hold or crossing), which is what keeps a two-finger landing off it; and the pan zone answers FALSE on a card (`touch_point_in_pan_zone`), so a finger landing on one resolves to the pointer translation and reaches the claim rather than becoming the phone-model pan.

## The clock and the damage

The clock rides the run loop's existing deadline tick, polled: `GuiNotifications::fire_if_due` at the HEAD of `main.cpp`'s `on_tick`, above every early return that body has — the startup load's, the render player's fork, the loading/blank guard and the playing-only guard. A card is a message about something that has ALREADY happened, so nothing the tick does below it can be a precondition for retiring one, and every one of those returns is a mode a card can stand over: an idle window must still retire an expired card, and the render player's own `No audio device; the wav cannot be played` card must not stand for the whole player session. The tick's software deadlines are FOUR — key repeat and the touch disambiguation window (`GuiInputCore::tick`), the audition's rests, the cards — and none is scheduled; all read `monotonic_ms()`. The same call re-answers the hover from the remembered pointer, so a card that slid up under a resting pointer — which is what a bump or an expiry above it does — starts pausing without a motion.

Damage has one owner, `Viewport::invalidate_notification_stack`: every stack change (a push, a dismissal — the X, bare Esc, or Ctrl+Esc emptying it whole — an expiry) damages the stack's ROOM — `notification_stack_bound`, the maximum card width from `kPanelPadPx` under row 1 down to that same `kPanelPadPx` above the bottom row's lane — which erases what stood and admits what comes without shaping a glyph off the paint clock; the hover face takes `invalidate_rect` on the X box alone. IT IS THE ROOM AND NO LONGER A TIGHT BOUND since the wrap landed (2026-08-30): it was "three cards of one line each", which a wrapped card outgrows, and no pure function of the window can know a card's line count — that needs a shaped run, and shaping needs the paint's own font. So the damage owner takes the whole space the stack may grow into and THE PAINTER CLIPS TO IT, which is what keeps a contrived stack off the bottom row; a window with no room between row 1 and that lane answers a zero height and paints no card at all.

## The chokepoint, the duplicates, the bump

`GuiNotifications::notify` is the ONE writer of a card; every producer below calls it and nothing else pushes.

DELIBERATE PRESSES STACK THEIR DUPLICATES (architect 2026-09-01, retiring the unconditional dedup of 2026-08-30): each PHYSICAL press pushes its OWN card, so a wall hit three times shows three cards. That count IS the confirmation — "I am very deliberate with my presses" — and it is the one thing the roster's grey cannot give him, a key's refusal being what he is reading at a wall. THE ONE CARVE-OUT IS A HELD INPUT'S SYNTHESIZED REPEATS, which coalesce exactly as everything used to: a text identical to a card already in the stack in the same class removes that card and re-pushes it AT THE TOP with a fresh clock, in both classes alike. The rule is *multiples are for distinct presses, not for a 30 Hz flood* — a burst at the compositor's cadence is one gesture, not thirty presses, and letting it stack would empty the room of every other card in half a second.

THE SEAM IS THE KEY EVENT'S OWN BIT, not a parameter and not a global: `GuiInputState::synthesized_repeat` — whose TWO producers, the held KEY (`GuiInputCore::maybe_fire_repeat`) and the held BUTTON (`tick_chrome_press_repeat`), both dispatch THROUGH `on_key` — is read once at that body's head and parked in `AppState::Notifications::held_repeat_dispatch` by an RAII guard that restores it at every one of that body's returns, the early gate returns included. `notify` is its one reader. It is dispatch-scoped for a reason: the raise sites are everywhere on that road (the gates, the ops' returned `GuiOpRefusal` sentences, the editors' red flashes), so threading a parameter down every chain would be a dozen signatures for one fact that arrives once, at the top, with the event — while a bit written where the event arrives cannot drift from it, and cannot be read stale by a worker's verdict, a pointer gesture or a paint, all of which see false and stack like the deliberate presses they answer.

WHY A REPEAT MOVES ITS CARD rather than re-arming it where it stands (the 2026-08-30 reasoning, unchanged): "in the stack" is not "on screen". An overflowing stack paints past the room's foot and the painter clips it there, so a card can be live and wholly invisible — and a re-arm in place would answer the act the user is this moment performing with nothing visible at all. The top is the one place the answer is certain to be seen, and it is also what a repeat means to the reader: the top says the last time it happened.

THE BUMP IS UNCHANGED AND NOW LOAD-BEARING: repeated presses of one refusal fill the room with copies of one sentence instead of refreshing a single card, so the walk runs on ordinary use and not only in a burst of different events. It needs nothing new for that — the victims are still the oldest NORMAL cards, never a critical one and never the card just pushed, and the count is still the room's — and what the user sees at a wall is the stack topping out at the capacity with the newest press on top, which is the picture the count is for.

THE BUMP replaced the queue on 2026-08-30. A push inserts at the top and starts that card's clock; then, while the stack is past `notification_capacity`, the OLDEST NORMAL card is removed — the walk runs from the back, SKIPS every critical card, and STOPS ABOVE THE CARD JUST PUSHED, so the two things that never leave at a push are a critical card (by the ruling) and the sentence answering the act the user has this moment performed (by that sentence's whole reason for existing). When neither leaves a victim the loop simply stops and the overflow paints past the room's foot, where the painter's clip cuts it. A bumped card that was hovered drops the hover. NOTHING WAITS UNSEEN any more: `expiry_ms` 0 means "no clock" and belongs to critical cards alone, `notification_visible` is "in the stack", and `start_visible_clocks` / `is_visible_index` are deleted with the queue that produced them.

The stack is a member of the per-project `AppState`, so a reopen starts with an empty stack by construction — the chip's own per-project scope, and no clear site exists because none is needed.

## What is notified, and what is not

NORMAL cards, every one a sentence answering an act or reporting a background act's outcome (the `notify` callers, re-grepped 2026-08-29):
- the three phase-reset pastes' `Stopped at …` reports (`phase_reset_propagate.cpp`) and the measure paste's; and beside them THE TWO PASTES' SHARED `Nothing matched, so nothing was pasted` (`kNothingMatched`, its two readers `paste_apply`'s matched==0 arm and `paste_state_apply`'s pair_count==0 arm — a CLEAN PARTIAL WALK is not this and stays silent, having pasted what it had). THE CARD OUTLIVED THE SWITCH THAT PRODUCED IT (architect 2026-08-31, R10): it was raised on 2026-08-30 because both pastes ended on the ALWAYS-SWITCH to target view, a change of scene that reads exactly like a paste, and the architect answered the misleading half of the success rule by RETIRING THE SWITCH on every produced-nothing path instead of the sentence — those arms now skip `land_paste_in_target_view` (skipped, not stubbed: every run that WROTE a block still lands unchanged), so a paste that wrote nothing leaves the view exactly where it stood and the card is the whole answer. THE PRODUCED-NOTHING FAMILY IS FOUR SHAPES ACROSS THE TWO PASTES: `paste_apply`'s matched==0 (its `kNothingMatched` case and its block-0 divergence, one early return), and in `paste_state_apply` the pair_count==0 case plus a LABEL DIVERGENCE or a MARKER COUNT MISMATCH at block 0 — the last two closing the asymmetry with the sibling on the architect's own call the same day, under the one test `applied_pairs == 0` (blocks that completed their pass, which the state paste counts even where every flag already matched). AND THE OTHER HALF IS RULED THE OTHER WAY DELIBERATELY: a run that PAIRED blocks and left the store BYTE-EQUAL still lands, on both pastes — a paste that paired stands as a paste and the switch shows a true result; what the switch was retired for is a paste that touched no block at all. The MEASURE paste has no analogue on either side: it switches no view at all and a matched-nothing run there is silent;
- `History is unavailable` — the commit-title editor's Enter into a mode that closed, and the failed-scan arrival that closes the view under the user (with the store's reason appended);
- the `h` view's `'` load refusals, `Load in place refused: …` (the strict sidecar load, the past-EOF wall, the Local walk's number) and `Revert refused: …` — stderr-only until 2026-08-29;
- THE RENDER PLAYER'S LOAD ACT'S OWN REFUSALS, in that same form and since 2026-08-30 (`load_render_entry_in_place`): the running-batch self-guard, a missing or non-regular entry wav, each of the three sidecars failing to read or parse, and the past-EOF wall — one local `refuse` owner composes each sentence once for the card AND for the stderr line, which alone carries the full path. The act said nothing and the CALLER said `Load refused`, a sentence that told the user nothing the dead player did not; that caller now says nothing at all, exactly as the `h` view's arms' caller already did;
- the Open project picker's three refusals (`A checkpoint is still publishing; try again when it finishes`, the project model's sentence, the dry run's reason), the picker staying open;
- Synchronize's refusals (`sync_path is not set` — the device config's key named by its own spelling, and `A synchronization is already running`, a sentence since 2026-08-30) and its worker's every FAILURE — the `Synchronizing to …` dispatch notice was DROPPED at the cards' landing (a process line is state, and the verdict follows within seconds) and A SUCCESS SAYS NOTHING SINCE 2026-08-30 (architect: "if it succeeds, we don't necessarily need [a notice]"), the `Synchronized N files to …` count deleted from the outcome and moved to the silent list below; the two volume-discovery sentences (`No removable volume mounted`, the several-volumes list) went with the discovery itself the same day;
- the render player's `Nothing to play: no renders under tmp/` (it named both output folders until 2026-09-01, when the player moved inside `tmp/` and stopped listing `render/`; the sentence's shape is unchanged), `Only a render can be loaded in place; a folder carries no recipe` (it named the deliverable and `..` too until 2026-09-01, when the player moved inside `tmp/` and the `..` row became the Up button — a refusal names only the subject a highlight can still be), `Cannot load in place while a render is running` (the load under a running or parked render — silent for the one day the status bar stood at the window's foot and painted through the player's modal, and a card since the state cell moved onto row 8, which the player's own modal row takes whole; `validation_topology.md`'s row), `This wav does not have the source's sample rate and channel count`, `This wav holds no samples`, `No audio device; the wav cannot be played` and the frozen reader's own words (`GuiRenderPlayer::status`, the thin road). THE SIX SHORT ONES WERE LONGER-FORMED on 2026-08-30 (architect, with the wrap: "cryptic one-liners say what they mean") — `No renders to play`, `Only batch renders load in place`, `Render running`, `Not the source's rate and channel count`, `Empty wav`, `No audio device` — none of them a change of meaning, each a sentence a first-time reader can decode;
- `Target render failed` (the preview's failure is an event the user was not watching; a cancelled preview is the product's own doing and says nothing);
- THE GATES' OWN CARDS (architect 2026-08-30: "go very verbose — a card for every refusal / no-op that is silent today"). A press a standing state swallowed now says WHICH state swallowed it, and where the chord itself is what was wrong it is NAMED, through the ONE chord speller `spell_chord` (`gui_input.h`, read by the THREE of these sites that name a chord and by nothing else — the editor gate, the `h` allowlist and the read-only lock; `spell_modifiers` beside it has no caller of its own, being the prefix half: modifiers first in the fixed order Ctrl, Alt, Shift, then a printable key by its own character UPPER-CASED — UNCONDITIONALLY, modifier or none, so `Ctrl+S` and a bare `V` are spelled alike — or a named key by its name. THAT CASE RULE MADE A ONE-DAY ROUND TRIP AND THE SUCCESSION IS RECORDED WHOLE: upper-cased at the cards' own landing (architect 2026-08-30 — a chord stands alone inside a sentence rather than in parentheses after a verb, where a lone lowercase letter reads as prose instead of as a key, and no capital could be mistaken for a shifted press because THE SHIFT IS ALWAYS SPELLED, `Shift+F` being the shifted one and `F` the key); LOWERCASED FOR THE BARE LETTER on the morning of 2026-09-01 (architect: with no modifier named, the letter's case is the only shift signal the sentence carries, so `V is not available on a read-only tab` was held to imply a Shift the press never had and the truthful card was `v is not available on a read-only tab`); and UPPER-CASE AGAIN that same day (architect 2026-09-01, the standing ruling), on the evidence of KDENLIVE ITSELF — the design reference this GUI is modelled on writes `Ctrl+S` and a bare `T` alike in uppercase in its own accelerator convention, and the product follows its reference. THE SHIFT AMBIGUITY IS AN ACCEPTED COST, not an oversight: the morning's objection is sound and was weighed and lost, the idiom the reader already knows from the application beside this one beating a spelling unambiguous only on its own terms. AND THE ROSTER'S TOOLTIPS ARE A DIFFERENT SURFACE THIS RULING DOES NOT REACH — their `(v)`-style chord suffixes are hand-written literals in `kToolbarChords` under `redesign_button_tooltip`'s own standing rule (`app_state.h`), they advertise a BOUND chord in advance rather than naming a press that just happened, and they stay LOWERCASE; two surfaces, two conventions, each with its own owner, and neither is evidence about the other (said at the speller too, so a later hand does not unify them). THE NAME TABLE IS THE BOUND SET'S AND NOTHING WIDER (architect 2026-08-31, his DELETE, closing decision 84's open question): the bound vocabulary's own non-printables and no others — `Space`, `Return`, `Tab`, `Esc`, `Del`, `Home`, `End`, `PgUp`, `PgDown`, `Up`, `Down`, `Left`, `Right`, FIFTEEN case labels for THIRTEEN names, `Return`/`KpEnter` sharing `Return` and `Tab`/`IsoLeftTab` sharing `Tab` — every other bound key being a printable ASCII character `spell_chord` upper-cases from the keysym itself. IT WAS THREE BLOCKS WIDER (architect 2026-08-30, closing codex round C's LOW-1 and narrowed the same day by round D's MED-2): THE KEYPAD WHOLE (`Keypad +`, `Keypad 5`, `Keypad End` — both faces of the dual-legend keys, the layout deciding which arrives), the editing and system block (`Insert`, `Menu`, `Print Screen`, `Scroll Lock`, `Pause`, `Num Lock`, `Compose`, …) and a laptop's vendor strip (`Volume Up`, `Brightness Down`, `Media Play`, …), in a `GuiSpellKeys` namespace beside `GuiKeys` — the merely NAMEABLE beside the BOUND — with `Backspace` among the bound table's own arms, which the editors consume themselves and no gate can name. THE UNBOUND-KEYS RULING TOOK THEIR LAST PRODUCER that same 2026-08-30 evening and they stood one more day as "one owner of what a key is CALLED, complete rather than caller-shaped"; the architect ruled that keeping OUT — an arm exists iff a producer exists (`validation_topology.md`), and the deletion changes NO BEHAVIOUR at all, the ignoring of a keypad or a vendor press never having lived in the speller but in the dispatch, which binds none of them and answers them with the silence either way. `That key`, the last resort for a value with no legend this product reads out, WENT WITH THEM, those blocks having been the only thing that could still reach it: the speller has two arms and no third, and `every_bound_key_is_spellable` — a `consteval` beside `chord_is_bound`'s own anchors — PINS the implication at compile time, so a binding added on a key with neither a name nor a printable character breaks the build instead of putting a control byte on a card. THE VENDOR SPACE round D had accepted as unnamed (`XF86Calculator`, browser Back/Forward, Power, Sleep, WLAN, keyboard brightness, Touchpad Toggle) is owed nothing either, an unbound key being answered by silence whatever its name, so the table is not chased wider and a future binding on one of these keys brings its name with it. THE XKB ROAD STAYS REJECTED — deriving the display name from `xkb_keysym_get_name` at the Wayland boundary would put a STRING on the key event and widen the platform seam for a card the Android backend (which produces named `GuiKeys` only through `synthesize_key`) could never spell the same way, and the two hosts must answer one press with one sentence. NO F-KEY ARM, the Wayland boundary dropping F1..F35 before delivery and Android producing named `GuiKeys` only). EIGHT SITES, FIVE SENTENCES (re-greped 2026-08-30, after the unbound-keys ruling took eight sites and five sentences off this list): `Close the editor first: <chord> is ignored while it is open` (the keyboard-modal editor gate, for the CHORDS it swallows — a TYPED CHARACTER is the editor's own and says nothing, which needs no test of its own because printable insertion is a class `text_editor::classify_key` OWNS, so the gate's blocked set is chords by construction: every ctrl/alt spelling the editor does not bind, the character-less bare and shifted keys, and Tab / Shift+Tab under the ringless flag editor); `Keys are ignored during a drag` (ONE literal `kKeysDuringDrag` at the FOUR sites that swallow the keyboard for a gesture — the editor text drag's gate, the pointer gestures' drag-modal gate, and, since 2026-08-30, THE RENDER PLAYER'S AND THE PICKER'S OWN ARMS above it, which outrank that gate and so ask its question themselves for the band's row press and the scrub's marker drag — naming no chord, the chord not being what was wrong); `No audio is loaded yet`; `<chord> is not available in the history view` (the card is the on_key CALL SITE'S, never the predicate's, `history_mode_key_blocked` being asked once per button per frame by the roster's face walk). AND A GATE'S GENERIC SENTENCE IS FOR A CHORD THAT MEANS NOTHING IN THE STATE, NEVER FOR ONE THE STATE MERELY CANNOT RUN RIGHT NOW (architect 2026-09-01: "look for instances where it lies by saying it's not allowed when, in fact, it's only contextually not allowed") — SO A GATE'S MEMBERSHIP TEST IS THE CHORD'S ALONE, pure vocabulary with no state terms, and a chord the mode owns is admitted outright while its ACT answers the contextual refusal with the true reason (its own card, or the ruled silence where the refusal is a benign one-dimensional already-at-state with a greyed face). THE INSTANCE HE HIT WAS CTRL+S IN THE `h` VIEW: the mode's own Save-and-Commit chord was admitted only while the act could run — a non-empty head delta and no checkpoint in flight — so a committed-and-current session was told the view's own save chord was "not available in the history view". Both terms moved to the act (`open_history_commit_editor`: `A checkpoint is still publishing; try again when it finishes` for a worker mid-act, SILENCE for an empty head delta) and the Save button's grey moved with them, off the derived partition and onto `history_checkpoint_actionable` at its own face arm — the same one decision, one house over. TWO CONDITIONAL ADMISSIONS REMAIN IN THAT GATE and neither lies, each forking out of the generic sentence with its own true reason (bare `'` and bare `v`, below); the READ-ONLY LOCK'S ONE STATE TERM is honest for a different reason — `horizontal_arrow_step_lock_admits` decides WHICH act Left / Right means (the marker nudge with a selection standing, the playhead step without), and the lock really does refuse the nudge, so naming the lock names the true cause. `<chord> is not available on a read-only tab` (the KEY's card alone — a GREYED button's press dies at `arm_redesign_press`'s disabled line and never reaches the gate, the grey being the roster's own message under the truthful-buttons ruling; a LIT button whose chord the lock drops is answered exactly as its key is). IT NAMES THE CHORD AND NOT THE LOCK ALONE since 2026-08-30 (architect, closing codex round C's MED-1), and it is KEPT that way after the unbound-keys ruling because it names what was pressed, which is what a user who just pressed it is looking for. `read_only_key_blocked` is THE COMPLEMENT OF AN ALLOWLIST, so it answers true for a bound authoring chord the lock is holding back AND for a chord unbound everywhere — bare `4`, an unbound modifier decoration; when every unbound press elsewhere was told so, saying nothing here would have left that second class unanswered and a bare `This tab is read-only` would have stated a WRONG CAUSE for it. THE UNBOUND-KEYS RULING DISSOLVED BOTH HORNS AND MED-1's ACCEPTED COST WITH THEM: the second class now takes the silence it takes everywhere (`chord_is_bound` is the gate's first test), and what is left is a sentence about a chord that really would have acted, or carded its own reason, ON A WRITABLE TAB IN THE SAME STATE. THAT SECOND CLASS HAD A SECOND MEMBER UNTIL 2026-09-01 (architect, U4): a chord bound nowhere IN THIS STATE, which for this product means the `h` view's seven shapes pressed outside the view — bare `v` on a locked tab in S+W was his own instance, answered `V is not available on a read-only tab` where a writable tab said nothing at all. `chord_is_bound` reads the view's bit now, so those seven take the same silence here; the gate's MEMBERSHIP (`read_only_key_blocked`) learned nothing and is still the chord's alone. AND WHERE THE ACT WOULD ALSO HAVE REFUSED FOR ITS OWN REASON — bare `S` in T+W, `I` in the P column, a verb with no selection — THE LOCK STILL SPEAKS, being the OUTERMOST STATE, a mode entered on purpose exactly as the `h` view is: the ruling's target is the chord that means NOTHING in the state, not the chord that means something and is refused twice, on the three standing precedents (the faces compose `!read_only && <the act's predicate>` with the lock first, the `h` view cards `S is not available in the history view` in T+W under the same reasoning, and ONE CARD PER PRESS raises at the outermost site that has the reason). The rejected alternative stays rejected on its own reasoning — a membership list of the authoring chords to fork the sentence on would be a second spelling of the bindings whose drift would lie — and `chord_is_bound` is not that list: it spells the bindings' EXISTENCE, decides only whether a card is OWED and never which cause is named, so its own drift costs a missed card and never a wrong one. `kTabReadOnlyCard` (`notifications.h`) stays the product's ONE CROSS-FILE CARD LITERAL with THREE readers in three translation units — the Settings dropdown's own opener, the render player's Load in place and the `h` view's bare `v`, the three sites that KNOW THEIR ACT and so need no chord in the sentence; this gate left that list the day it gained the speller. AND EVERY ONE OF THESE FIRES ONLY FOR A CHORD THE PRODUCT BINDS (architect 2026-08-30, the unbound-keys ruling — the deduction rule below), AND BINDS IN THE STANDING MODE (architect 2026-09-01, U4): each gate on the main dispatch asks `chord_is_bound` (`gui_input.h`, one owner beside the speller) before it composes anything, so a press nothing binds dies at its gate in silence and no gate can claim to be the reason a key did nothing when it would have done nothing anyway — and the `h` view's seven shapes (bare `g`, bare `u`, bare `v`, `,` and `.` with and without shift), which bind nothing outside that view, take that same silence at the read-only lock, the two drag gates and the loading gate. THE INSTANCE WAS `V is not available on a read-only tab` in S+W (the revert act, which a WRITABLE tab in that state ignores without a word); the predicate was state-blind from 2026-08-30 to that day, all four gates asking it, so all four went truthful in the one edit with no site of theirs touched. THE TWO THAT DO NOT ASK are the player's and the picker's own drag arms: each stands inside a mode whose router IS the vocabulary — bare `r` flips Repeat one in the player — and this inventory does not know it, so those two answer every swallowed key. A CARD NAMING THE CHORD THE USER PRESSED IS NOT A GESTURE HINT: it says what happened, never what to press instead. KEY REPEAT RIDES THE HELD-REPEAT CARVE-OUT — a held key whose repeats are eligible re-fires its gate at the compositor's cadence and the stack keeps ONE card for the sentence, re-pushed at the top with a fresh clock, so a hold reads exactly as a single press, while DISTINCT PRESSES of the same refusal each push their own card since 2026-09-01 (the chokepoint's section above);
- THE VERBS' OWN REFUSALS (architect 2026-08-30, the strictness ruling's second step — the first was the gates', above). Where a gate says which STATE swallowed a press, these say what the ACT wanted and did not have, and every one of them names a subject or a view rather than a chord. THE HOME-VIEW BINDING answers in the warp column's own words — `Markers are placed in source view` (the bare `s` drop in T+W; Shift+S never meets it, going to target view first — its own refusal since 2026-08-30 is `Already in phase reset view`, the crossing chord pressed with the P column already standing) and `Markers are moved in source view` (the bare Left/Right nudge on a warp marker in target view). THE FOUR P-COLUMN SENTENCES RETIRED later that day when S+P opened (`Phase resets are placed / moved / edited / deleted in target view` — the P column authors in both audio views now, the S-view drop taking no lead-in). THE SUBJECT REFUSALS name what to select — `Select a marker to enable or disable`, `Select a marker to delete`, `Select a marker to convert`, `Select a warp marker to edit its line`, `Select a marker to edit its measure`, `Select a warp marker to change its tempo` — THE TAB WALK'S OWN `There is no marker to step to` LEFT THIS SET ON 2026-08-31 with the one-dimensional silences below, the ACT unchanged: it still REFUSES WHOLE AT A WALL, asking the landing owner `marker_walk_actionable` before it selects anything, so a step with nothing ahead writes nothing at all — no select, no land, no recentre — where before 2026-08-30 it fell through and re-landed the playhead on the standing focus under a GREYED button; the face and the act read one predicate, which is now the whole cue, and the Ctrl+Shift+Tab march takes the same answer at each of its two cycles with the tab switch between them still acting. THE COLUMN REFUSALS name the column — `Phase resets carry no tempo to inherit` (Ctrl+N in P), `Grid iterations work on warp markers` (bare `i` in P). THE VALUE FACTS — `One of the selected markers cannot take this tempo change` (the group step's all-or-nothing scan, KEPT carded on 2026-08-31 because a group edit is not one-dimensional: its effect would have spread across every selected flag), `In target view only a marker that owns its tempo can be stepped` and `That marker shares its frame with another` (the singleton cent step's two target-view payload refusals), and `A label reference has no tempo of its own` (that same step's remaining NO-CHANGE arm — a fact about the marker's KIND, not a wall its value rests on). THE WALLS LEFT THIS SET ON 2026-08-31, every one of them a one-dimensional refusal already at its state: `The marker is already at the edge` (both position nudges), `The playhead is already at the trim start` / `…at the trim end` and the ctrl pair's `…at the start of the song` / `…at the end of the song` (the Home / End jumps and the `h` view's pair), and `The tempo is already at its limit` (the singleton step at the bracket's own end). The ACTS are unchanged — the jumps still refuse only when NOTHING would change, a live transport, a selection to clear or a shown trim overlay counting as change through `playhead_end_jump_actionable`, and the two nudges and the step still return `std::nullopt` where they returned a sentence — and each of their buttons greys on the same predicate, which is now the whole cue. THE FOUR CLIPBOARD CHORDS, each naming its own payload at its first gate and sharing the rest — `Phase resets are copied from warp markers` / `Measures are copied from warp markers`, `Select the markers whose phase resets to copy` / `Select the markers whose measures to copy`, `Select one consecutive run of markers`, `Phase resets are pasted onto warp markers` / `Measures are pasted onto warp markers`, `Nothing has been copied yet`, `Select exactly one marker to paste onto` (Ctrl+Alt+Shift+P takes its sibling's three verbatim). THE VALUE PAIR — `The focused marker has no resolved value to copy` (bare `j`, both of its refusals), `The focused marker has no resolved value` and `There is no source marker to jump to` (Shift+`j`). UNDO AND REDO — `There is nothing to undo` / `There is nothing to redo`, and `That step belongs to the other tab, which is read-only` for the cross-tab case the active tab's own lock cannot explain. AND THREE SINGLES — `This tab is read-only` from the SETTINGS DROPDOWN's item click (the one road that reaches the settings editor's own lock gate; bare `;` dies at the keyboard gate, which since that evening names the chord instead — `; is not available on a read-only tab`, the two roads saying the same fact in the two voices their sites can afford), `A checkpoint is still publishing; try again when it finishes` from Ctrl+S with the PICKER standing (one literal shared with the picker's open act, the roster being greyed and unreadable as state there), and `Nothing matched, so nothing was pasted` from a phase-reset paste that paired no block at all — ONE literal with TWO readers since that evening (`kNothingMatched`, `phase_reset_propagate.cpp`): the PLACEMENT paste whose destination produced no block, and the STATE paste (Ctrl+Alt+Shift+P) whose lockstep loop never ran; both end on the always-switch to target view, a change of scene that looks exactly like a paste, so a run that wrote nothing must say so. A clean PARTIAL walk stays silent in both, since it pasted what it had, and a `Stopped at …` report wins where there is one. ONE CARD PER PRESS is the rule the whole set is built on: where a refusal is asked twice on one road the OUTERMOST site that has the reason raises it and the inner one stays a silent belt, which is why the marker ops' own leading returns say nothing. THE OPS DO NOT KNOW ABOUT CARDS: the authoring cluster's acts COMPOSE their refusal sentence and RETURN it (`GuiOpRefusal`, `warpmarkers_ops.h`) for the dispatch arm to raise, so the fact stays where it lives and this freeze-adjacent code keeps no notification surface;
- THE BPM GATE'S TEN ARMS (architect 2026-08-30, the strictness ruling's third step). Bare `m`'s ladder is ten tests deep and its whole answer was one indistinguishable non-response; NINE of them now name the rule the selection broke, one card per press carrying the FIRST failure — `BPM iterations work on warp markers` (the P column), `BPM iterations work in source view` (off warp's home, the mode being deliberately outside the W+target status/value family), `Select the markers whose sections to sweep` (an empty selection), `The selection no longer matches the markers` (the index belt, carded precisely because its state is the one the screen does not show), `Select one consecutive run of markers` (the contiguity gate — the CLIPBOARD family's own literal `kSelectOneRun`, whose fifth reader this is: the same test on the same set for the same reason), `A label reference lies inside or at the end of the span`, `The first selected marker must own its tempo and be enabled` (`bpm_popup_eligible_marker`), `The first selected marker shares its frame with another` (the coincident collapse, which renders as one synthetic 1.00 owner) and `Selected markers carry different tempos` (the one-tempo rule). THE TENTH IS THE ONE SILENCE, and it is unreachable: `enter_bpm_mode`'s own bail re-asks four of the gate's tests and adds only "bpm mode already standing", which cannot be — the mode never rests without its editor, and while that editor stands `m` is a typed character that never reaches the dispatch. The editor's own later refusals (the bracket walls at the commit) are the flag editor's and are untouched here;
- the GRID ITERATIONS' CELL-CAP refusal, `Grid iterations refused: more than 1000 cells (cap 1000). Narrow the marker brackets and retry.` (architect 2026-08-30; its subject took the menu row's own name in the 2026-08-31 rebrand) — the rest of the sentence is unchanged and the refusal is still pre-mutation, iteration mode and the brackets surviving for correction; it wore the dismiss-only ERROR_NOTICE modal until that day, the pre-split surface for a sentence the user had to be shown, and that prompt kind retired whole with this move.
- THE MODES, THE TRIM FAMILY, PLAYBACK AND THE RENDER CHORDS (architect 2026-08-30, the strictness ruling's fourth step, which empties the silent list of everything but the ruled silences above). THE RENDER PLAYER speaks through `GuiRenderPlayer::status` like its siblings — the DECODE's own refusals, which report a fact no surface shows, plus the LOCK's own sentence on Load in place. ITS TWO SHARED SENTENCES ARE RETIRED (2026-08-31, R5): `No render is loaded to play` and `Start playback before seeking` were spelled once at the mode's header (`render_player.h`) because several acts and one POINTER site each said them, and both went silent whole under the one-dimensional rule below, the header keeping only their record — bare `'`'s card since the row's disabled face landed later on 2026-08-30 (the button greys on the lock and on a recipe-less highlight through `render_player_button_enabled`, its consumed lift saying nothing; the running-render card stays both roads'). Its MODIFIED press on the scrub track stays silent, the overlay's pad-and-gap silence one surface over. THE PROJECT PICKER answers `Choose a project first` (Enter on an empty band, which is every folder under `projects_path` refused by the model), and the opener's two state refusals `Close the render player first` and `Close the editor first`, the second reachable only through the File menu's row under the pointer-transparent flag editor; a standing PROMPT and a standing PICKER are silent there, a prompt veiling everything and being its own answer, a second Open project asking for what is on screen. THE `h` VIEW: `A checkpoint is still publishing; try again when it finishes` on bare `h` (the picker's own literal, third reader, the stderr line kept — the LAST of the ruled silences to fall) and on CTRL+S IN THE VIEW since 2026-09-01 (the fourth reader — the commit act's own opener saying what its allowlist admission used to swallow), `History is unavailable: <reason>` when the ENTRY refuses (the same composed line the module's stderr already printed, raised by the one entry owner — `kHistoryUnavailable` moved to `history_diff.h` for it), and TWO SENTENCES THE ALLOWLIST GATE FORKS OUT OF ITS GENERIC ONE — `There is nothing to load` (bare `'` on an empty walk) and `Select a change to revert` / `This tab is read-only` (bare `v`, forked on the two terms its admission composes) — because those two admissions are CONDITIONAL ON STATE, so the chord is not what was wrong. THEY ARE THE WHOLE OF THAT LIST SINCE 2026-09-01, when Ctrl+S's two terms left the gate for the act under the membership-is-the-chord's-alone ruling above: they keep theirs because the one decision that refuses each chord is also the one that greys its button through the derived partition, and neither sentence lies. AND THE COMMIT ACT'S OWN TWO now answer where every act's refusal answers — the publishing sentence for a checkpoint in flight, and NOTHING for an empty head delta, the benign one-dimensional refusal already at its state whose cue is the greyed Save-and-Commit face. THE TRIM FAMILY: `A trim bound must stay inside its partner` (the trim bar's ctrl / ctrl+shift bound set, raised by the ACT and never by `trim_bound_click_frame`, which the cursor map asks once per motion) — AND IT IS THE FAMILY'S ONLY SENTENCE SINCE 2026-08-31 (architect, R9), the two SUCCESS reports beside it having gone silent under the rule below: `The trim covers the whole song` (bare `[`'s SHOW over a full window — never a refusal, the overlay going up as always, and the overlay going up IS the confirmation) and `The trim bounds crossed; the window was reset to the whole song` (a REPORT rather than a refusal, raised at `auto_clear_crossed_trim`, the one owner every former reaches — the trim bar's endcaps snapping to the song's extremes is the visible cue on every road, and the trim is scratch viewport, render-modifying but benign, a full window having zero impact on the output). Both stood for the one day of 2026-08-30; both resets and both shows behave exactly as they did. PLAYBACK: `Playback is unavailable on this device`, at the ONE LAUNCH BODY `launch_playback_window` (so Space, the scrub's audition and the A/B audition's bounded plays share it), asked FIRST because a launch with no device otherwise succeeds in silence — `GuiPlayback::device_unavailable` read, no bit added — and it is the launch's ONE sentence since 2026-08-31, when its former companion `There is nothing left to play from here` went silent with the other one-dimensional refusals (the playhead resting visibly at the view's end, the Play button greying on that very predicate); both of the position refusal's sites — the launch body's playable gate and `toggle_playback`'s verdict-identical target-view pre-sum gate — refuse exactly as they did, and the constant `kNothingLeftToPlay` that spelled the sentence once for the pair is deleted. THE DEVICE QUESTION COMPOSES INTO BOTH PRE-LAUNCH GATES (2026-08-30, closing codex round D's MED-1): `toggle_playback` asks it ahead of that pre-sum gate, so a dead device at the domain's end is never answered with silence where the device is the real fact, and `GuiAbAudition::start` asks it ahead of its two tab gates and so ahead of the first `c` and the tab switch, so Shift+Space on a dead device leaves the user on his own tab with his own camera — the promise that a refused act moves nothing. The launch body's check stays the BELT (it is the scrub launch's only gate), each of the three returns, and the sentence is ONE literal `kPlaybackDeviceUnavailableCard` (`playback_lifecycle.h`, beside the cluster whose fact it is), so one press raises one card; `Wait for the target preview to finish rendering` from Space's play edge AND the waveform scrub's own gate (`kTargetPreviewNotReadyCard`, `notifications.h` — two predicates, `preview_ready` and `is_updating`, one fact); and the A/B audition's TWO press-time refusals, IN ORDER: the device sentence above (its own preflight's, asked ahead of the pair) and `One of the two tabs has nothing to play from here` (one act over a pair, so which half could not play is not the answer). IT WAS THREE UNTIL 2026-08-31, when `An audition is already running` was DELETED WITH ITS ARM (architect): a Shift+Space that finds the act standing is no longer a refusal at all but the act's STOP, routed into the one stop body bare Space's own fork reaches, so it says nothing under the success rule below — the silence ending is the answer, and the play/stop button had been wearing STOP for the act's whole duration all along (conventions.md carries the toggle). THE RENDER CHORDS: `Turn off grid iterations to render one file` (Ctrl+Alt+Shift+R, refused inside the route so the key and the Render button's shift press are one refusal), `A marker's iteration bracket runs backwards` and `No iteration ranges are authored` (the sweep's two pre-dispatch refusals, each beside the stderr line it keeps), and `Could not create the render folder \`<folder>\`: <message>` from ONE composer (`render_folder_creation_card`, `renders_dir.h`) shared by the three dispatchers that make a batch folder — the iteration sweep, the BPM sweep and the miscellaneous cell's allocator, whose card is the allocator's own so both of its roads answer — the path shown as its BASENAME by the rule above while the stderr lines keep the whole of it.
- EVERY RED FLASH'S REASON (architect 2026-08-30, the strictness ruling's fifth step). A field that turns red says only THAT it refused; the card says WHY, and the sentence has ONE COMPOSER with TWO READERS — the stderr line the site already printed and the card — so a retune cannot make them disagree. The stderr line keeps the offending TEXT after the sentence; the card drops it, that text standing on screen in the red field the refusal leaves open. The card never carries the `warptempo_gui: ` prefix. THE FLAG-EDITOR CLUSTER (`flag_editor.cpp`, which took a `GuiNotifications&` for it, the settings editor's own arrangement — the reason is composed where the fact is): `Measure rejected: <reason>` (the measure grammar), `Edit rejected: malformed iteration bracket`, `Edit rejected: <reason>` (the canonical-line parse and the duplicate `label_def`), `Edit rejected: iteration bracket cells leave the tempo bracket [<lo>, <hi>]`, and the BPM editor's three — `BPM edit rejected: invalid syntax`, `BPM edit rejected: derived tempo or scale outside its bracket (…)`, `BPM edit rejected: a marker outside the span would leave the tempo bracket [<lo>, <hi>] once rescaled`. THE SETTINGS EDITOR'S FIVE, all under `Settings edit rejected: …`: its two `reject` lambdas (the GUI-kind vocabulary's reason, and the line shape's), `unknown engine key '<key>'`, `key '<key>' has invalid value '<value>': <reason>`, and the source-clobber guard's `this would make the render output overwrite the source file (<basename>); choose a different title`. THE TWO THAT ARE ONLY A CARD, having never had an stderr line and gaining none: the commit title's `Enter a title for the checkpoint` and the offset editor's `Enter a whole number with no leading zeros`. THE MEASURE PASTE'S TWO travel out of the act as a `GuiOpRefusal` (`apply_measure_paste` returns the sentence instead of a `bool`, the reason having been discarded on the way before): `Measure rejected: <reason>` for a clipboard entry that will not parse and `That offset would put a measure outside the allowed range` for the bracket. AND THE TWO CAPACITY REFUSALS, which live in `text_editor.cpp` — the pure editor over one buffer, which has no `GuiNotifications` and wants none — so each REPORTS and the dispatch layer cards: `replace_selection` answers `false` and `apply_editor_clipboard` says `The pasted text is too long for this field`, while the typing path answers the new `KeyAction::OverCapacity` and `route_modal_editor_key`, the one route every editor's keys pass through, says `This field is full`. The settings recall's own `replace_selection` call is the one caller that stays silent, and the asymmetry is recorded at it: that text is the product's own, not the user's.

- THE THREE CLIPBOARD SUCCESSES (architect 2026-08-30, closing the strictness arc's invariant that an accepted press shows something). THE SUCCESSES THAT CARD ARE EXACTLY THE CLIPBOARD WRITES, because nothing paints a clipboard — and since the resolved readout retired with the status bar, nothing paints a resolved value either: `Copied the resolved value` at bare `j`'s write (`copy_focused_marker_value`, whose Copy resolved value button dispatches that same chord at its lift), `Copied the selected markers' phase resets` at Ctrl+P's arm and `Copied the selected markers' measures` at Ctrl+/'s (the Edit menu's two Copy rows dispatching those chords). The card is the DISPATCH ARM's in both propagates, beside the gate sentences it follows — the copy bodies carry none, unlike the pastes. AND A COPY THAT CAPTURED NOTHING SAYS THAT INSTEAD: both copies take `warp_marker_propagates` (labeled AND effectively enabled), so a run of unlabeled or disabled markers passes every gate and leaves an EMPTY clipboard, which one shared sentence answers — `No labeled, enabled markers are selected, so nothing was copied`, the copy-side twin of the pastes' `Nothing matched, so nothing was pasted`. THE EDITORS' OWN Ctrl+C IS NOT ONE OF THE THREE (architect the same day): an editor is its own world with that world's conventions, and its copy stays silent, its Ctrl+V over an empty clipboard with it;

CRITICAL cards: the four checkpoint outcomes and nothing else.

NOT a card, by ruling:
- A BENIGN REFUSAL OF A ONE-DIMENSIONAL COMMAND ALREADY AT ITS STATE (architect 2026-08-31, the refinement arc's opening rule and the first thing to leave the carded set the strictness arc had filled): a refusal cards UNLESS it alters no output and refuses a command that moves ONE THING IN ONE PLACE, where one glance at that one place already answers whether anything happened. THE COUNTER-CLASS IS THE TEST: a command whose effect would have spread across the screen keeps its card even where its button greys, "because they can be subtle and required me to check the whole screen" — which is why undo and redo over an empty stack, the GROUP tempo step's `One of the selected markers cannot take this tempo change`, Shift+S in the P column, the propagate partial-stop reports, the clipboard successes and the render player's substantive refusals all stay carded. THE TWELVE THAT WENT SILENT, each an ACT UNCHANGED with only its sentence removed (the constants that spelled them deleted where nothing else read them): the Home / End jumps in both forms and the `h` view's pair (`playhead_end_jump_actionable`, `run_playhead_end_jump` and the mode's own arm), the marker walk at an empty store or a wall (`cycle_marker_focus`), the SINGLETON tempo step at the bracket's end (`adjust_tempo_cents`, returning `std::nullopt` where it returned `The tempo is already at its limit`), Space with nothing left to play at both its sites (`kNothingLeftToPlay` deleted), the history walk's two walls, the `h` diff cycle's two ends and its empty arm (`cycle_history_diff_flag_focus`, whose local `report_wall` went with them), Shift+`[` over an already-full window (`handle_trim_clear_both`'s identity guard), the picker's row on the project already open (which still CLOSES — the window title names it), the render player's two folder-end skips (`kFirstInFolder` / `kLastInFolder` deleted) and its Backspace at the root, the position nudge at its wall in BOTH columns (`nudge_selected_markers` / `nudge_selected_phase_resets`, both returning `std::nullopt`), and — that evening, R5 — THE RENDER PLAYER'S WHOLE IDLE FAMILY: every raise of `No render is loaded to play` (Play with nothing bound, the two folder walks' no-item arm, every seek road) and of `Start playback before seeking` (R41's dead slider: the two seek keys, bare Home, bare End, the car's absolute `SeekTo`, and the scrub press's two gates, which meet the seek's own refusals at their own gate because the press must not ARM the handle drag either), both constants deleted with the last of their raises and `play_button_act`'s own copy of `toggle_pause`'s guard deleted with its card. THE REFUSALS ARE UNCHANGED THERE — an idle seek still refuses, which is what keeps `resume_frame` 0 at every idle rest by construction. WHERE A FACE PREDICATE ALREADY GREYS THE BUTTON that grey is the standing cue — the player's Play and Home grey on exactly these states (`render_player_button_enabled`; its Stop greyed with them until that button retired 2026-09-01) — and where none does — the picker's row, the player's Backspace, the player's scrub — the surface itself is (a window title, a listing with no `..` row, a slider resting at its left end under a zeroed clock). Each site carries a one-line comment naming this rule; the reverse is per item, restoring its sentence where it was raised;
- A SUCCESS WHOSE RESULT IS ON SCREEN (architect 2026-08-31, the refinement arc's second rule and the twin of the one above): a success is SILENT where the screen shows truthfully what happened, and it cards only where NOTHING shows (the clipboard three, above) or where what shows WOULD MISLEAD — and in that second case the misleading thing is what gets fixed where it can be. THE TWO THAT WENT SILENT (R9), both trim reports, both acts unchanged: `The trim covers the whole song` at bare `[`'s show over a full window (`handle_toggle_trim_region` — the overlay going up over the whole song, its bounds at the song's edges, IS the confirmation) and `The trim bounds crossed; the window was reset to the whole song` at `auto_clear_crossed_trim` (the trim bar's endcaps snapping to the extremes is the cue on every road, and the trim is scratch viewport — render-modifying but benign, a full window having zero impact on the output). AND THE ONE THAT KEPT ITS CARD BY LOSING ITS MISLEADING HALF (R10): the two pastes' `Nothing matched, so nothing was pasted` stands, and the always-switch to target view under it retired on every produced-nothing path (the bullet above);
- a render's completion ("that would get annoying");
- A SUCCESSFUL SAVE (architect 2026-08-30, ruling the clipboard exception above narrow): the dirty mark leaving the window title IS the save's visible answer — his words, "the disc writes — there is something that paints, the dirty dot goes away" — and a clean tab shows no mark to begin with, so Ctrl+S stays silent on success. A CLEAN PROPAGATE WALK with it: the walk lands visibly in target view, and only a walk that stopped early or matched nothing reports;
- A SUCCESSFUL SYNCHRONIZATION (architect 2026-08-30, on the render's own precedent: a render served silently publishes silently, and a mirror that worked is the same kind of quiet). Its FAILURES are still cards;
- AN UNBOUND CHORD, WHEREVER IT IS PRESSED AND WHATEVER SWALLOWED IT (architect 2026-08-30, the arc's last ruling and THE DEDUCTION RULE the rest of this list rests on): "bound keys either show an effect or a card, so an unbound key is identified by its silence". Every other refusal in the product answers now, so a press that raises no card and changes nothing on screen can only be a press with no binding — a stronger answer than a sentence saying so, and one that costs no card for a mistyped modifier. IT RETIRED THE `<chord> is not bound` CLASS WHOLE: the strict-modifier tail and `handle_plain_bare_keys`' default (the two halves of one sentence), the render player's and the picker's two catch-alls each, `Rows open on a plain click` on a folder-overlay row, and the pointer's own `<modifier>+click is not bound here` — six sentences, eight sites, all consumed-silent again, the ruling stated once at the strict-modifier tail (`input_handler.cpp`, and in `conventions.md`) and pointed at from each. THE SURVIVING GATE CARDS ENFORCE IT: each gate on the main dispatch asks `chord_is_bound` (`gui_input.h`) first, so an unbound chord under an editor, a drag, the loading state, the `h` view or a read-only tab is silent there too — a gate may not claim to be the reason a key did nothing when it would have done nothing anyway. THE INVENTORY IS HAND-KEPT and that is its whole cost: the dispatch is an if-ladder across three translation units with its modifier tests inline, so `chord_is_bound` spells the membership a second time, A NEW BINDING JOINS IT, and drift is a MISSED CARD (never a wrong act — nothing routes through the predicate) with codex as the backstop. WHAT COUNTS AS BOUND is what `on_key`'s own road claims IN THE STANDING MODE (architect 2026-09-01, U4, narrowing the 2026-08-30 "in ANY state"): the `h` view's seven shapes (bare `g`, bare `u`, `,` / `.` with and without shift, bare `v`) are bound WHILE THE VIEW STANDS, because that view ADDS to the dispatch rather than replacing it, and unbound outside it, because their whole binding sits behind two mode tests — `handle_history_mode_key`'s own mode return and the revert arm's — so the predicate carries that ONE state term and the gates say outside the view exactly what `handle_plain_bare_keys`' default and the strict-modifier tail already said (bare `h`, the toggle, is claimed in every state and reads no term); the RENDER PLAYER'S and the PICKER'S keys are NOT, each router being the whole vocabulary while its mode stands — bare `r` flips Repeat one in there and binds nothing outside it (bare `v` was this example's own until the player's Stop retired 2026-09-01, and the letter did not go unbound with it: it is the `h` view's REVERT now, one of the seven shapes above and a member of `chord_is_bound` in that exact bare spelling);
- NOTHING FROM THE STRICTNESS ARC'S INVENTORY IS LEFT ON THIS LIST BY DEFERRAL (2026-08-30): the STRICT-MODIFIER NO-OPS left it that morning, THE OFF-HOME DROPS AND THE REST OF THE VERBS' REFUSALS the same day, `m` ON A BAD RUN that afternoon with nine of its ten arms carded, and BARE `h` WITH A CHECKPOINT PUBLISHING — the last of them — that evening, when the modes' own step landed; the unbound chords then came BACK to it that night by the ruling above, which is a decision and not a deferral. What stays silent below stays silent BY RULING or for want of a producer, never because its step has not run yet;
- THE REFUSALS WITH NO PRODUCER, re-derived 2026-08-30 rather than carded on the inventory's word: the two DROPS' past-EOF walls (`drop_marker`, `drop_phase_reset_at_position`) — every drop road authors AT or BEFORE the playhead, which rests in `[0, total-1]` by its writers' own clamps, so no press can reach either wall — the position nudges' shared prologue, whose refusals are the loading gate's card, an empty selection the dispatcher routes elsewhere, and belts against the never-parked rule; and `enter_bpm_mode`'s five-bail recheck, every arm of which the `m` gate has already carded one level up (ONE CARD PER PRESS — the outermost site that has the reason raises it). An error arm exists iff a producer exists (`validation_topology.md`), so these stay silent and say why at their sites;
- TOP-LEVEL BARE ESC WITH AN EMPTY STACK (architect 2026-08-30), the one silence the gates' ruling deliberately left standing on the key dispatch for a BOUND chord (the unbound ones came back to this list the same evening): Esc is the product's dismissal, bound in EIGHT places since 2026-08-31, and reaching `handle_plain_bare_keys`' own Escape arm means there was nothing modal standing to dismiss and no render to cancel. THAT ARM'S OTHER HALF IS AN ACT NOW — the oldest card's dismissal, place (e), the hit section above — so what stays silent is the press that finds no card there either: a retraction, already answered by the surface that did not close, which a card would answer with a complaint. It is an arm of its own rather than the unbound default's for exactly that reason;
- A MODIFIED PRESS ANYWHERE ON THE FOLDER OVERLAY'S BAND — its pad, a gap between rows, and since the unbound-keys ruling A ROW as well: nothing on the band binds a modifier, so the press claims the band, arms nothing and says nothing (the row test the one-day `Rows open on a plain click` needed went with that card). The render player's MODIFIED PRESS ON THE SCRUB TRACK is the same silence one surface over;
- THE T+W FLAG DRAG, the one positional refusal that stayed silent when the keyboard's own gained sentences (2026-08-30; the WARP column's alone since S+P opened the same day): the home-view gate sits at the marker drag's THRESHOLD CROSSING, so what is refused is a gesture that never begins — the flag does not move, which is the answer, and the press that armed it has already selected the marker and landed the playhead. Its keyboard twins (the bare `s` drop, the Left/Right nudge) card, because a key that does nothing shows nothing;
- A GREYED BUTTON'S LIFT (the truthful-buttons ruling's own division, 2026-08-30): the grey IS the message on the roster and the card is the message on the keyboard, so a disabled button's press dies at `arm_redesign_press`'s disabled line, arms nothing and dispatches no chord. A LIT button whose chord a gate then drops is answered exactly as its key is;
- THE TARGET-VIEW ENTRY GATE's refusal (architect 2026-08-30). The gate stays — a silent-wrong guard: the preview must never receive a map the builder refuses — and its MESSAGE goes. The `t` road and the T radios now stay in source view and print ONE stderr line with the builder's own words, `warptempo_gui: Target view entry refused: <error>`, which is what the LOAD ROAD's `active_audio_view=T` restore always did bar the line itself: one predicate (`validate_target_view_entry`), two callers, one answer. No on-screen surface exists for it BY RULING, not by omission — its refusals are unreachable from program-written input (loads are strict, the resolver normalizes ahead of the build, authored tempo is integer cents in [25, 400]), and what would catch a builder/resolver disagreement is the preview's own `Target render failed` card;
- the loader's fatal exits (the adversarial class: stderr and exit 1);
- every QUESTION — the prompts and the dialog editors;
- the peaks-cache rebuild lines (`audio.cpp`, stderr; they self-heal);
- what is TRUE NOW — the two state strings.

## The basename rule

A message that carries a filesystem path names the FILE — its basename, or the batch cell's `N_tag/NN` id — never a full path: the subject is the highlighted row on screen, and the clip is the backstop. Synchronize's sentences name their path RELATIVE TO THE MIRROR'S TWO ROOTS (`<sync path's last component>/<path under it>` on the stick, the path under the project folder in the project — the one composer `shown`, `external_sync.cpp`; the full path stays on stderr), which is `external_sync.h`'s rule 1's naming clause. THE PICKER'S TWO PRODUCERS NAME THEM AT THE WRITER, not at the picker: the PROJECT MODEL's refusals name the FOLDER NAME (`resolve_project`, one `name` for every sentence it composes) and the DRY RUN's name the project folder and the file — `550 - 1/07 - Menuetto.settings` — through the one composer `shown_project_path` (`device_config.h`, beside the `projects_path` key it refuses to spell; `sidecar_present`'s stat refusal takes it too, being one of those reasons by way of the dry run). ONE SENTENCE PER REASON AND NO ROAD FORK: the FATAL roads that also print these words — startup's resolve and `gui_main`'s reopen, both stderr and exit 1 — print the same shortened sentence, the projects path being the device config's one setting and the same for every project on the machine, and startup's own zero case is what names it out loud (`No project under <projects_path>`). The `h` view's load refusals name repo-relative sidecar paths already, and THE RENDER PLAYER'S LOAD ACT took the rule on 2026-08-30 when its arms became cards: each names the batch cell by `render_entry_id`'s own `N_tag/NN.wav` spelling or the sidecar by its file name, the full path staying on the stderr line the same `refuse` owner writes.

## What retired, and why

- `AppState::transient_status_message`, the TRANSIENT class — hidden under a progress line, revealed stale when it cleared, wiped by the next key press, and invisible altogether under the `h` view's top-tier line; its clear at `on_key`'s head, its tier in the retired `paint_status_chain`, its "no writer may stamp under a progress line" discipline and every damage call that served it alone. A card cannot be hidden, so "it would be invisible" is no longer a reason for silence, and the gates that refused silently on that ground were re-decided each on its own merits (the picker's three and Synchronize's reports in the `h` view: cards; the player's load under a running render was left silent for the progress line and became a card that evening, when the fold put that line under the player's own modal).
- `AppState::critical_error_message`, the PERMANENT CRITICAL CHIP — the tab row's paint-only red box at the chain's left (2026-08-09 to 2026-08-29), its painter, its re-derived box, its one clearing route (a later established success) and its fill-only-an-empty-slot condition. The chip was a SLOT holding the repository's last answer; a card is an EVENT the user closes once read.
- `status_chain_pad_x` whole, and `paint_status_chain` with it. The chip-to-text gap went with the chip; the painter, the tab row's call to it, its right-alignment, its tabs-win-by-paint-order collision rule and the last three tiers of its ladder went the next day with the STATUS BAR.
- THE STATUS BAR ITSELF, one day old (architect 2026-08-29): the tenth lane on the window's foot, `status_bar_area` / `status_bar_content_area`, `kStatusBarContentPx` and the three height accessors, `paint_status_bar` and its two-cell layout, and the window-foot seam `kRedesignBottomLine`, whose crop provenance was corrected in the same breath — the near-black last row of `row_7_text.png` is the xfce4-panel's top edge under kdenlive's window and was never a kdenlive border, so the seam is retired for good rather than pending another ruling. What survives is the LEFT cell's two strings, in row 8's own state cell.
- THE DISMISS-ONLY ERROR NOTICE (`ERROR_NOTICE`, `GuiPrompt::open_error_notice`), retired whole 2026-08-30 with its Esc-only response set and its lone `OK` button. It was the pre-split surface for a sentence the user had to be shown, and the split left it with nothing to carry: its TWO synchronous callers became a NORMAL CARD (the iteration sweep's cell cap) and a SILENT ARM (the target-view entry gate), both listed above. `present()` is FIVE sites now (re-grepped 2026-08-30: the unsaved-work prompt, its save-failed restatement, the paste confirmation and the load confirmation's two raisers), Esc's places are unchanged, the four `ModalDialogOwner`s are unchanged, and `PromptInitialFocus` stays a REQUIRED parameter — every surviving prompt is a QUESTION.
- THE QUEUE AND THE THREE-CARD CAP, three days old (architect 2026-08-30): `kNotificationVisibleMax`, the demotion block in `notify`, `start_visible_clocks`, `is_visible_index`, `notification_visible`'s second clause and the painter's count-bounded walk. A queued card was one the user had a right to read and could not see, whose clock had not started; the cap that produced it was three because the design guessed at a stack's size, and the ROOM is the honest answer. What replaced it is one function and one loop — `notification_capacity` and the bump — and the painter's clip, which the wrap had already made necessary, now covers the overflow the count cannot see as well.
- THE RESOLVED READOUT, the bar's right cell and the third state string: nothing displays a resolved value any more. Bare `j` copies it and Shift+`j` goes to the marker it came from; the parser's composer is `resolved_marker_payload`, the payload and the source index alone.
