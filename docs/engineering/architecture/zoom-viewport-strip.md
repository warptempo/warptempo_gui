# Pixel Anchoring, Waveform Width, Viewport, Continuous Zoom, Strip Lanes and Drags

> UPDATED 2026-08-01 for the completed kdenlive redesign (rows 1-7 — kdenlive-redesign.md is authoritative for every lane's geometry, colors, buttons and painters). This file remains authoritative for: pixel anchoring, the waveform width/viewport/zoom machinery, the strip-drag mechanics and capture, the framing double-click, follow suppression, and the scanner.

Moved verbatim from CLAUDE.md in the 2026-07-25 slim-down (project feature-complete). This file is now the authoritative home for this detail; CLAUDE.md carries only a digest and a pointer here. Keep it current with landed rulings.

### Gesture Commits are Pixel-Anchored
Every pixel-stepping POSITION gesture (the bare Left/Right marker-lane nudges in each column's home view — warp in source, phase reset in target; in W+TARGET the bare arrows are a CONSUMED REFUSAL since contortion ruling 8 deleted the tempo-image step that lived there) anchors to the on-screen column grid: read the item's currently PAINTED column (`painted_column_of_source_frame`, the stem painters' own math), step by whole columns, commit that column's time (`authored_frame_at_column`, which funnels through `snap_authored_frame`). The painted move is exactly the commanded number of columns per press, unconditionally — the whole-frame rounding error is just under 0.291 px at the deepest zoom under the value brackets and the 44100 floor (0.5 / 1.72265625 at the exact bracket edge — codex round 1 corrected the older "at most 0.29" wording, which was not a strict bound), and re-anchoring each press means nothing accumulates (per-press independent rounding would paint occasional 0/2 px jumps). THE ONE-COLUMN GUARANTEE IS A GRID-FINENESS PROPERTY, not a gesture-family property (architect 2026-07-24, the lesson of the min-step churn): it holds because the authored FRAME grid is finer than the pixel grid (≥ 1.72 source frames per target pixel at the deepest zoom, so every adjacent-column target is representable). IT APPLIES TO THE POSITION GESTURES AND NOTHING ELSE, and the trap it documents already bit once: the only other gesture that ever claimed it was the W+target TEMPO-IMAGE STEP, which authored CENTS — a grid COARSER than pixels on ordinary spans (one cent moves the image ~2+ px) — so the guarantee INVERTED there and that gesture needed a minimum-directional-cent rule of its own; it is deleted (contortion ruling 8), and the surviving tempo surface, the bare Up/Down cent step, makes no pixel claim at all (it steps ONE CENT per press by definition). Mouse drag commits snap the release to its painted column (frozen-map coordinates; stored equals shown), uniform in both views at all zooms; mid-gesture positions stay free. EXACTLY ONE ITEM MOVES PER GESTURE (contortion ruling 9, architect 2026-07-29 — horizontal movement is a focus act, groups are never moved): a nudge steps the FOCUS (a 2+ selection collapses to it first, in the twins' shared prologue) and a drag moves the marker it grabbed, so a drag commit is one column snap plus the integer wall belt and nothing more. The RIGID GROUP COMMIT this replaced is recorded here only as a do-not-re-propose: it column-snapped the GRABBED member alone and rode every other member on a uniform active-domain delta D through `snap_authored_frame`, with D re-clamped into the motion's wall intersection at release and the grabbed member re-derived from the clamped value by plain rounding (walls winning over the grid for that one saturated case), and D never recomputed from the re-derived frame — the codex round-6 re-quantization trap. All of it went with the group drag; the release-time D clamp in particular existed ONLY to keep other members off a wall the group had stopped at, so deleting it restores the pre-group singleton commit exactly. The motion loop's verbatim short-circuit survives and still tests the CLAMPED delta, not the raw pointer delta (codex round 6: an outward drag of a half-offscreen flag clamps to zero effective motion but a raw-delta test would write two-hop residue and let commit column-snap a forbidden inward jump). Walls win over the grid everywhere (walls are integer frames). Propagate paste rounds plainly WITHOUT pixel anchoring (computed, view-independent positions — recorded at the site). Wall semantics are one regime per column under the home-view binding (see that paragraph): each column's POSITION nudge runs in its HOME view only (the 2026-07-24 "third exception" — a both-views warp position nudge — was RE-RULED AWAY the same day: no warp position authoring in target view; in W+target the bare arrows are a consumed refusal since ruling 8, see the home-view binding): the warp nudge in SOURCE view CLAMPS its step into the nudged marker's own wall headroom (creep-to-the-wall, walls exactly reachable); the phase-reset nudge runs only in target view and since 2026-07-30 CLAMPS the same way — the unified wall policy (authoritative at position_nudge.h's head): SINGLETON STEPS CLAMP ONTO THEIR WALL with post-clamp identity a silent no-op, GROUP PRESSES REFUSE WHOLE as group rigidity; the old "one regime per column at its home" split is retired. Both regimes are singleton-scoped now: the group forms (the warp intersection over members, the phase whole-press refusal over members) went with the group nudge, contortion ruling 9.

The waveform's EFFECTIVE width is quantized to a multiple of 16 px (`waveform_area` floors `a.width` to `w - (w%16)`, leaving a ≤15 px inert right gutter that `render_background` fills automatically over the full-width damage). 16 = `1600/gcd(44100,1600)`, the strictest grid step among standard sample rates (every standard rate's step divides 16), so at a multiple-of-16 width `logical_spp·W` is integral at every INTEGER numeric zoom level and `painter_samples_per_pixel == logical_spp` exactly there — making the GUI's pixel-anchored grid coincide with the migration tool's frame-0 grid regardless of the compositor-configured width. (The zoom level is a CONTINUOUS double as of 2026-07-19 — see the zoom paragraph below; a fractional rest rides the painter quantization `nearbyint(spp·W)/W`, and the bit-exactness claims hold at the integer rungs, notably the working zoom (2.0) every snap gesture lands on.) All grid consumers (the plate/flag caches and the live trim pass, `samples_visible`, the viewport snap, pointer placement, pixel-anchored commits, and the flag/trim-bound column mapping + hit-test — the last threaded a `waveform_width` param through `iterate_visible_flags_impl` so trim geometry never drifts from stems) route through `waveform_area(a).w`; only strip backgrounds and invalidation damage use the full `a.width`. No-op at the deployment widths (1920/2560/3840 are all multiples of 16).

The VIEWPORT itself is snapped to whole-pixel (grid) boundaries at numeric zoom levels — the single chokepoint is `clamp_viewport_start` (main.cpp), which every viewport mutation already funnels through immediately after writing `viewport_start_sample`. It snaps to the nearest grid point using the SAME `painter_samples_per_pixel` (now a declared free function in `warp_frame_map_view.h`, shared with `authored_frame_at_column`), then clamps into `[0, max_start_grid]` where `max_start_grid` is the smallest grid point ≥ `max_start = total − visible` — so the FLUSH-RIGHT viewport also rests on the grid, showing <1 px of inert padding past EOF (subsumed in the last painted column; `get_peak_range` clamps past-EOF reads to silence, so no paint change). This is what lets an exact-grid marker commit stay valid at maximum scroll. The snap runs at EVERY level (the retired fit-file mode's skip is gone; at the effective zoom-out ceiling max_start ≈ 0 and the snap parks the start at 0). Consequence: source-view marker and click-playhead commits land EXACTLY on the frame-0 authoring grid (no ±1 residue), via the shared single-rounding basis `source_grid_position_at_column` (`warp_frame_map_view.h`): recover the grid-snapped viewport's column index `m = nearbyint(viewport_start/q)` and round ONCE, `nearbyint((m+col)·q)`, instead of the two roundings `nearbyint(viewport_start + col·q)`. Both `authored_frame_at_column`'s SOURCE branch (guarded `ctx.domain==Source`, funnels through `snap_authored_frame`) and the two click→playhead sites (a shared `playhead_frame_at_click_column` in input_pointer.cpp) use it, so click-drop, drag, and nudge cannot drift. So GUI zoom-level-2 authoring agrees with the migration tool's frame-0 grid BIT-FOR-BIT (the ±1 became 0 once Phase A/B guaranteed every rest viewport is a true grid point). The m-recovery is exact for product-reachable audio lengths (at the deepest numeric zoom `q ≥ ~27.5` frames/px, far above the double ULP), NOT over the whole int64 range. `painted_column_of_source_frame` is UNCHANGED and the round-trip `painted_column(authored_frame_at_column(col))==col` still holds (`|residue| ≤ 1/q < 0.037` px). Target view keeps its inverse-map form (no source-grid claim). The playhead is NOT snapped except at a click; arrow-key fine-tuning (`move_playhead_pixels`) still authors off-grid, deliberately. This made the continuous-pan fractional-sample accumulator redundant — it was removed, and the Alt+drag scroll-drag was retired outright at the time (2026-07-19; pan moved to the strip rows — see the strip-row paragraph below), whose drags likewise apply an absolute viewport per event and let the snap re-anchor. THAT RETIREMENT WAS ITSELF REVERSED LATER and this sentence had gone stale (caught in the 2026-07-28 docs sweep): the pan-strip ROW is the thing that is retired now, and pan lives back on the waveform grab — a captured, continuous 1:1 `ScrollDragState` (`app_state.h`), navigation-class, matched against the same surface's ctrl zoom and mirrored by the wheel's stepped pan. IT WAS ALT+DRAG when this was written, one of the two alt gestures that survived the 2026-07-28 alt retirement precisely because that pan/zoom pairing works, AND THE PAIRING IS WHAT OUTLIVED THE MODIFIER: the eighth glass ruling (2026-08-12) moved the grab-pan onto the PLAIN drag and the stepped pan onto the PLAIN wheel, emptying alt's pointer vocabulary, and the one-model ruling (2026-08-14) folded the ctrl zoom into the same ONE nav drag as its live modifier phase — so the pan and the zoom are now two phases of one gesture rather than two gestures on two modifiers (the paragraphs below own both models).

ZOOM IS CONTINUOUS (Ableton-style free rest, architect 2026-07-19): the zoom level is a real-valued DOUBLE exponent resting anywhere in the ONE continuous domain `[kMinZoom, kMaxZoom]` = [1.0, 17.0] — the fit-file 0.0 SENTINEL is ELIMINATED (legacy of the retired digit-key zoom era; there is no fit MODE, no sentinel branches anywhere). ms-per-pixel = `0.625·2^(level−1)` (computed in `samples_per_pixel_at`; the table is deleted), so integer rungs reproduce the historical 2x ladder bit-for-bit. `kMaxZoom` = 17.0 is DERIVED from audio_io's structural source caps, not invented (derivation at the constant, settings_file.h): the RIFF uint32 data limit bounds any loadable source at ≈16232 s (24-bit stereo at the 44100 floor), and 17 is the provably smallest whole level covering that at the 640 px minimum window. The EFFECTIVE per-file ceiling is `effective_max_zoom_level` = `clamp(1 + log2(total·1000/(0.625·sr·width)), kMinZoom, kMaxZoom)` — the fit-equivalent level, where spp·width == total — so FULL ZOOM-OUT RESTS AT WHOLE-SONG-VISIBLE for every loadable file (a multi-hour file just gets a coarse pyramid plate); the ceiling is a GUI-runtime clamp (tick reclamp + on_resize), deliberately NOT schema-checked. `kWorkingZoomLevel` = 2.0 (RENAMED from kSnapZoomLevel — say "working zoom", not "level 2") is the ruled fine-tuning zoom where the authoring-grid bit-exactness claims hold: `c` lands EXACTLY on it, and it alone among the marker-navigation commands: THE TAB FAMILY DOES NOT ZOOM (architect 2026-08-05 — the working-zoom landing that family carried for one commit, one expression in `cycle_marker_focus` mirrored by the history view's diff-flag cycle, was REVERTED the same day, "no zoom on Tab": the walk lands and recentres at whatever level it was pressed at, in both families, because a walk must not re-frame the view under the reader). `0` IS NO LONGER A TOGGLE (architect 2026-08-05, replacing the working-zoom ↔ full-out two-way): below the effective ceiling it goes to FULL ZOOM OUT, and pressed AT the ceiling it RUNS THE `c` COMMAND (`run_overview_command`, RENAMED from run_zoom_toggle_command — its own route: the trim-bar double-click runs the separate span-framing `run_span_framing_command` described below). THE SECOND ARM IS `c` ITSELF, not a bare centre (architect 2026-08-05, superseding the bare `center_viewport_on_playhead` that arm carried for its first day — that call is structurally a no-op at the ceiling, since `samples_visible == total` by the fit-level solve leaves no pan room, so the round trip was lost). AND SINCE 2026-08-18 IT IS `c` WITH THE LEVEL `0` WAS PRESSED AT SUBSTITUTED FOR THE WORKING ZOOM — BARE `0` REMEMBERS WHERE IT CAME FROM (architect). `ViewState::zoom_recall_level` is a `std::optional<double>` PER TAB: the ZOOM-OUT arm STAMPS the live level it is leaving (it is strictly below the ceiling there, so a stamp can never name full zoom-out and make the return trip a no-op), and the AT-THE-CEILING arm SPENDS it — `c`'s own recipe with the level as a parameter, so the round trip goes out to the whole song and back to the MAGNIFICATION THE USER WAS WORKING AT. `value_or` is the whole of the empty-slot rule: with nothing stamped the arm is `c` VERBATIM, which is the session's opening state and the project that opens ALREADY zoomed out. The slot can only be found empty at the ceiling when the ceiling was reached some OTHER way, since every arrival there by this key stamps on the way. THE RESTORE IS THE LEVEL ALONE, not the window — `c`'s centring decides where, exactly as it does for the working zoom (asked and answered directly). THE STAMP IS RESOLVED AT THE ARM, against the LIVE ceiling (`clamp_zoom_level`), AND FALLS BACK TO `kWorkingZoomLevel` WHEN IT CANNOT MOVE THE ZOOM (2026-08-18): `apply_zoom_change`'s pre-clamp is a BOUND, NOT A RESCUE — a level stamped while it was legal sits above a ceiling that has since FALLEN (a narrower window, an S/T flip into a shorter domain), the pre-clamp hands such a request back the ceiling, and the ceiling is the level this arm is standing on, so the equality early-return fires and the round trip moves NO ZOOM AT ALL while legal magnifications remain below. A stamp that cannot move the zoom is worth no more than an empty slot and spends as one — which is exactly what an unstamped tab and "opened already zoomed out" already answer with. The genuinely immovable case (a file whose effective ceiling is at or below the working zoom) answers as it always has: `c` lands and centres, no zoom change. NOTHING IS CLEARED on that path: an unusable stamp is unusable only against the CURRENT ceiling — widen the window or flip the domain back and it is right again — and the next press below the ceiling overwrites it, so the stamp keeps its single writer. `run_overview_command` IS THE STAMP'S ONE WRITER: no manual `=`/`-`, no wheel, no drag, no `c`, no touch gesture and no load path writes it, and nothing clears it; a load in place or a source load drops it structurally with the rest of the band (both replace the whole `ViewState`). SESSION SCRATCH, deliberately absent from `kSettingsOrder` — the ONE member of `ViewState` that is not persisted; do not "fix" the omission with a key row. So `0` twice is OVERVIEW, then the level it was pressed at, on the focus, and the focus semantics are `c`'s own rather than a second, weaker centring. ONE OWNER, ONE FORK: `run_center_command` (input_handler.cpp) holds BOTH recipes — the live one (the trim region overlay's hide, `repair_last_selected`, the jump onto the focused marker, ITS `target_zoom_level` PARAMETER — `kWorkingZoomLevel` by default, the stamped level when bare `0` passes one — and the centre) and the `h` history view's mode-local re-expression (re-land on the mode's diff-flag focus, working zoom, centre) — and picks between them on the mode bit, so its three callers (the live `c` arm, the mode's `c` claim, and this second arm) share one decision instead of spelling it each. THE TWO ARMS SIT ON OPPOSITE SIDES OF THE PURE-VIEWPORT LINE and that is the whole of the change: the ZOOM-OUT arm is still a PURE VIEWPORT COMMAND (architect 2026-07-30, reversing the 2026-07-29 clear+collapse as "overscoped") — it touches neither selection nor region nor playhead, a group and its span cross it intact, and it never stops playback (the pure-viewport-move class of the keyboard stop rule, playback_lifecycle.h) — while the second arm carries `c`'s regime whole, region clear and focused-land stop included, stated at `c` and nowhere restated. Both arms read the resting cursor. Manual steps (`=`/`-`, and `Ctrl`+wheel via `zoom_steps` — one whole level per completed detent, up = in, the same two bodies coalesced per pointer frame) walk ±1.0 whole level from the current — possibly fractional — rung, no integer snapping, saturating constructively at the floor (recenter-on-playhead at the floor) and the effective ceiling. `c` = MARKER JUMP + WORKING ZOOM: with a focused marker it first jumps the playhead DIRECTLY onto it via `jump_playhead_to_focused_marker` — the factored Tab-family tail, and the Tab family and this one command are the ONLY routes that land the playhead exactly on a marker — then snaps to the working zoom and recenters; with none it keeps snap-and-center-on-playhead. The Tab family does NOT end the same way (above): the shared tail recenters at whatever level it is CALLED at, and `c` is the one caller that sets the working zoom after it returns. `tab_X_zoom` persists as a DOUBLE under the min-0 `format_value_double` round-trip gate with vocabulary exactly `[kMinZoom, kMaxZoom]` (settings_file.{h,cpp}, architect-granted frozen touches; a stored `0` REFUSES — pre-release ruling, no migration, the architect hand-edits old files; the default and first-open template write `2`). The LIVE level can never rest above the effective ceiling — the clamp lives at the TOP of `clamp_viewport_start` (`clamp_zoom_level`, main.cpp), the one chokepoint every zoom write already funnels through immediately after assigning, with `apply_zoom_change` pre-clamping its request so a ceiling-bound `c`/`0` is a true no-op; the tick and on_resize reclamps keep only trigger + side-effect roles and delegate. PARKED bands are the deliberate exception (ruling 2026-07-20): an inactive tab's zoom commit stores the schema-legal request VERBATIM and Ctrl+S serializes it — the ceiling is width- and domain-dependent, so a store-time clamp against today's context could destroy a value honorable at activation; parked zoom is display-scratch like the persisted viewport/playhead, and the activation-time chokepoint is the sole owner of honoring it.

THE TOP STRIP IS SEVEN LANES, THE BOTTOM ONE (the redesign's roster — geometry/colors/buttons in kdenlive-redesign.md, all on the gui_scale axis; the TOOLBAR ROW's 44+1 was deleted 2026-08-12 at the grand relayout's roster commit, its four buttons the icon row's first group now, and the OVERVIEW STRIP moved up out of the bottom strip at that relayout's COMMIT B the same evening, so the top lanes sum 196 at 100% — 195 until the overview lane gained its top border on 2026-08-13): top, edge→waveform — MENU ROW (34+1 margin, PINNED at the window top, with FLEXIBLE GAP 1 under it), then THE CENTERED BLOCK — TAB ROW (30+1), ICON ROW (46+1), OVERVIEW STRIP (24 content + 1px of border at EACH edge since 2026-08-13 = 26, ONE fixed tiny height on every host; `kOverviewHeightPx`, render.h, and the dedicated paragraph at the end of this file), TRIM BAR (9 authored x `kTrimBarScalePercent`, the ONE lane with a factor of its own — RESTING AT 100 since the seventh glass ruling (the 150 finger-target experiment lived one commit on 2026-08-12; render.h owns the constant and the record), so 9px at 100%; the strip total and waveform top move with whatever it reads through the lane accumulation), RULER (28), MARKER LANE (20, flush on the waveform), then the WAVEFORM itself, whose own thick bottom border is the block's bottom edge; then FLEXIBLE GAP 2; bottom — THE UNIFIED BOTTOM ROW (2026-08-12, rows 8 and 9 merged; kdenlive-redesign.md's unified-row record), ONE 47px lane AT THE WINDOW'S FOOT (1px border-top — commit B's "thin border" — + 46 content; it was 51 = 1 + 50 until 2026-08-14, when the row was ruled to TAKE THE ICON ROW'S METRICS and its height, pads and gap all became delegations to that row's own scaled accessors — `bottom_row_content_h_px` returns `icon_row_content_h_px`, so the two lanes cannot drift): the transport three at the left with the monospace clock behind their separator (LEFT-ANCHORED since 2026-08-18, centred in the lane before it), and flush right the five single-marker verbs (the MARKER MEASURE joining them on bare `/` since 2026-08-19) with ADD TO SELECTION behind them, the marker-walk three and the arrow four — sixteen buttons, every one painted in every state since that relayout deleted the cluster swap that put the history companions in the arrows' slots inside the `h` view. THE STATUS CHAIN IS NOT A TENANT HERE any more — it moved to the TAB ROW 2026-08-13 (the dirty dot lives in the window title, as it always has). `strip_row_rect(a, top_strip, lane)` stays the single geometry owner over the per-lane height tables (`top_strip_h`/`bottom_strip_h`, each now including its own flexible gap; `waveform_area` takes each separately). THE WAVEFORM IS CENTERED AND HAS A MAXIMUM HEIGHT since 2026-08-12 (the seventh glass ruling gave the clamp; COMMIT B gave the centering and took the clamp 550 → 500 — kdenlive-redesign.md's commit-B section owns the vertical rule): the leftover past the lanes is `win_h − top lanes − bottom row`, the waveform takes `min(kWaveformMaxHeightPx-scaled, leftover)` (render.h, 500 authored, riding gui_scale — 4K at 200% = 1000, the architect's own example), GAP 1 = `max(0, win_h/2 − top lanes − W/2)` so THE WAVEFORM'S MIDPOINT IS THE WINDOW'S ("the labwc titlebar above and the panel below offset each other" — centered within the app surface, no titlebar arithmetic), and GAP 2 is the remainder; where centering is infeasible both gaps floor at 0 and the WAVEFORM absorbs the shortfall, one clean formula with no second constant. AT 100%: 1920x1080 = 35 menu / 93 blank / 162 block / 500 waveform / 243 blank / 47 row (the bottom row went 51 → 47 on 2026-08-14 when it took the icon row's metrics; the four pixels came out of gap 2 and the waveform did not move — kdenlive-redesign.md's row-4/bottom-row swap section carries the re-derivation from the one vertical owner) (the waveform still spanning y 290..790 about the midline 540 — the clamp fixes its height and the midpoint rule its centre, so the overview lane's new border row came out of GAP 1 and the waveform did not move); the Pi's 1024x600 = 35 / 0 / 162 / 356 / 0 / 47, the waveform unclamped, paying that row itself, and both gaps floored. (The stacks read 95 / 160 / 500 / 239 and 0 / 160 / 354 / 0 from commit B until 2026-08-13; every number here is re-derived from the lane table rather than adjusted.) Both strip heights include their gap, which is the whole of how `waveform_area`'s `h − top − bottom` arithmetic yields the centered clamped height, and every horizontal rule in this file — the effective-width floor, the zoom ceiling, the grid — is untouched by it. BAND CLAIMS, top to bottom (rows 0-3's button claims and the four-row wheel-inert band are kdenlive-redesign.md's): the TRIM BAR claims plain (consume a pending span-framing double-click FIRST — see the double-click passage below — else arm an endcap/bridge drag; a click that never becomes a drag is a CONSUMED NOTHING apart from seeding that double-click at its motionless release), CTRL (set the BEGIN bound at the clicked column), and CTRL+SHIFT (set the END bound — the one Ctrl+Shift pointer claim; shift-exact stays transparent; the pair always rests under always-set trim); the RULER band is the NAVIGATION SURFACE's lane member (architect 2026-08-12, the EIGHTH glass ruling, pan-primary — superseding that morning's sixth-ruling ruler region former after one day): its plain press is the PENDING CLICK / GRAB-PAN, its shift press the SWEEP (the "region former" until 2026-08-18, when the span it drew became the TRIM itself — it writes the trim per motion event under no width rule at all; region-scrub-esc.md owns the model), its ctrl press the ONE NAV DRAG opened in its ZOOM phase (the strip drag's ctrl entry until 2026-08-14, the live-modifier ruling), its motionless click the deferred playhead placement; `region-scrub-esc.md` and pointer-hit-testing.md own the vocabulary, no double-click surface here. (THE TRIM SURFACE ARC's merged band — `top_trim_surface_area`, the band region former, the ctrl/alt bridge displacement, the ruler entry's deletion — lived one day, 2026-08-11..12, and was REVERTED whole at the architect's order; the ruler rework then landed the same day on top of the revert; pointer-hit-testing.md carries both records.) The MARKER LANE's empty stretches are the surface's second lane member (marker-ui.md): plain = the pending click / grab-pan (the motionless release runs the deferred placement AND seeds the marker-create double-click candidate), shift = the trim sweep, ctrl = the zoom, double-click = create+select+land. All modal gates sit above the claims; read-only refuses NOTHING on this band since 2026-08-07 — the navigation-class claims were always allowed and the trim bound sets and drags joined them with the ruling that trim is BAND rather than authored content (`read_only_key_blocked`). THE DUAL-AXIS STRIP DRAG IS DELETED WHOLE, AND EVERYTHING UNTIL THE STEM PARAGRAPH BELOW IS ITS RECORD RATHER THAN A LIVE CONTRACT (architect 2026-08-15, redesigning the overview lane: "zoom is not what I'm looking for in the overview strip, because the overview strip can indirectly control zoom by directly controlling the outline box" — the lane paragraph at the END OF THIS FILE is authoritative for what the lane does now, and THE ONE NAV DRAG's paragraph below for the desk's live zoom). ITS LAST ENTRY WAS THE OVERVIEW LANE'S CTRL PRESS, which binds nothing since that day; the state, the arm, the per-event apply, its term in `any_pointer_gesture_active`, the lane's Zoom cursor arm, the anchor stem's third producer and the vertical rate constant `kZoomStripPxPerLevel` all went with it, and the nav surface's own ctrl press had already left one day earlier (2026-08-14, the live-modifier ruling). WHAT SURVIVED IT, named so this record cannot be misread as a live system: the SONG ANCHOR and the ABLETON EDGE TRICK, which the nav drag's zoom phase carries, and `apply_strip_drag_zoom`, still the family's one application chokepoint (three callers — the nav drag's zoom phase, the touch pinch, the overview lane's box-edge drags). THE MODEL, kept revivable: IT WAS DUAL-AXIS AND SONG-ANCHORED to the pressed song position (v6, fifth pass 2026-07-20 — the planner-designed model reinstated after the fourth-pass ZOOM-ONLY trial tested poorly; genealogy v1 d187b50 → v2 screen-anchored a7a63bc → v3 5bd1810 → v4 axis-lock family → v5 zoom-only a9560aa → v6 5e8ddb7; the accepted Ableton compromise being the loss of an anchor dragged far offscreen): `anchor_sample` was captured at press and NEVER re-derived. The model was INCREMENTAL on the LIVE level/viewport with NO press baseline (the axis-lock staleness class died with it) — per event, from the deltas since the previous motion position (the crossing event folding the whole accumulated delta, last_x/last_y still at press): (1) PAN at the old level, `vp = viewport − effective_dx·spp_old` (grab sign — drag right reveals earlier content), WALL-CLAMPED to `[0, max_viewport_start_grid(app, audio)]`, the SAME right-wall owner clamp_viewport_start rests at (shared so a press at the flush-right grid rest derived vp exactly at the rest and the edge rebind below could not corrupt anchor_sample there; the interior grid-snap was deliberately NOT reproduced — sub-pixel residue self-heals on the next event, only the WALL had to be exact — and `max_viewport_start_grid` outlived the gesture as clamp_viewport_start's one reader); (2) ZOOM incrementally, `new_level = zoom_level − dy/kZoomStripPxPerLevel` (drag DOWN zoomed in; `kZoomStripPxPerLevel` was 60.0, architect-tunable, and the surviving drag-zoom rate is `kNavZoomPxPerLevel` at 200.0 on the HORIZONTAL axis) — BOTH AXES PLAIN AND SIMULTANEOUS for its last day, the pre-lock model again from 2026-08-14 (the segment axis lock deleted with its whole ladder, the deletion paragraph below; this lane's deliberate ctrl gesture never produced the wrist-arc complaint the lock existed for) — clamped to `[kMinZoom, effective_max_zoom_level]`; (3) the anchor's drifted column under the wall-clamped vp with the ABLETON EDGE TRICK — clamp the column into `[0, W−1]` and, when the clamp engages, REBIND anchor_sample to that edge pixel's frame (the zoom focus never leaves the screen; a pan that pushes it to an edge PINS it there as the edge's content — this half is LIVE, carried by the nav drag's zoom phase); (4) `apply_strip_drag_zoom(new_level, anchor_sample, anchor_col, final)`. THE CAPTURING DRAG'S PRESS PAINTS THE STEM, AND THAT IS ALL IT DOES — ruled on the strip drag (architect 2026-08-05) and LIVE ON THE ONE NAV DRAG'S CTRL ARM, which is the capturing zoom gesture now (`arm_nav_zoom_press`; the playhead jump that briefly rode the same press was ROLLED BACK 2026-08-06). The anchor stem — the HEADLESS ZOOM STEM, the same painter and the same pixels, no head — stands from the PRESS rather than from the slack crossing (each producer's term in `paint_strip_drag_anchor` is its own gesture record alone, and whatever arms it owes the first frame's full waveform-area damage), so the press shows the pivot it captured instead of the affordance appearing only once the drag crosses the 8px gate. THE ONE COST is the stem's ERASE, which every end of the gesture now owes — the clean release, a lost button and the force-end finalizer each spell the same full waveform-area damage, the MOVED arm getting it from its final apply instead. THERE ARE TWO PRODUCERS OF THAT ONE STEM since 2026-08-15 — three between the pinch joining on 2026-08-14 and the overview lane's ctrl strip drag, the ORIGINAL producer, being deleted with the lane's zoom — and `paint_strip_drag_anchor` is the one painter for both: (1) THE NAV DRAG'S ZOOM PHASE (`scroll_drag.active && scroll_drag.zooming` — from a ctrl-armed press or from a ctrl-down edge mid-drag, gone again at the ctrl-up edge); and (2) THE TOUCH TWO-FINGER PINCH (`touch_nav_zoom.seated`, added later the same day at the architect's own request from the rig — "add a zoom stem to the zoom on the touchpad just so I can see exactly what's going on, because at the edges there are some strangeness, it seems like" — so THE TWO SURFACES SHOW THE SAME AFFORDANCE; touch.md's two-finger section carries it). EACH GATE IS THE GESTURE RECORD AND NOTHING ELSE, which is the family's one shape, and BOTH ANCHOR IDENTICALLY: a song frame re-projected against the DISPLAYED basis every paint, so the SLIDE under a clamped zoom — the song moving out from under a held frame while its stem walks across the screen — is visible on glass exactly as it is under the mouse, and `render_strip_anchor_stem`'s edge clamp draws the pin. PRECEDENCE IS DECLARED rather than left to the expression (a held mouse capture and a glass contact are not structurally impossible together): the nav drag's zoom phase, then the pinch — the CAPTURING gesture first, a capture owning the pointer for its whole life and being the more committed act. (It read strip drag, then the nav zoom phase, then the pinch while there were three producers, and the selection was an if/else chain; with two it is a ternary again and the rule is the same either way.) THE PINCH OWES DAMAGE AT BOTH ENDS like the other two, and NEITHER END IS FREE: its SEAT damages at its own site because a seating frame is not in general an APPLIED frame at all (the seat is taken above the gesture's exact-no-op return since 2026-08-14, so two fingers landing and sliding together seat and apply nothing) and because, even where it does apply, `apply_strip_drag_zoom`'s mid-gesture true-no-op return drops a pinch that begins SATURATED AT A WALL, which would leave that pinch stemless at exactly the edges the stem was asked for; and its CLEAR damages through `clear_touch_zoom_seat`, a clear being reachable on a frame that applies nothing at all (touch.md carries both). THE 2026-08-06 ROLLBACK, recorded because the shape it removed stood for a day and a half: the strip drag briefly LANDED THE PLAYHEAD on its anchor — first on the motionless release (2026-08-05), then on the mousedown for every zoom motion (2026-08-06), with an anchor-follow re-seat keeping the cursor on a rebound anchor — and the architect reverted the whole feature the same day, it being buggy while audio played and making the `h` view's ctrl press read as broken. `jump_playhead_to_strip_anchor` and the `place_playhead_at_frame` hoist are DELETED, the rebind moves the anchor and nothing else again, and the gesture is NAVIGATION-CLASS as it always was. "MOVE THE PLAYHEAD HERE" LIVES ON SHIFT+CLICK (the waveform placement press, region-scrub-esc.md) — one gesture, one job. IDENTITY PROOFS: pure pan (dy=0) is EXACT — new_level == old, apply reproduces bit-for-bit the vp the column was derived from, and AT a wall the clamped vp equals the resting vp so a saturated pan is a TRUE NO-OP; pure zoom (dx=0) pivots the rescale around the anchor's fixed (possibly edge-pinned) column; a both-unchanged event is a true no-op the entry point skips. Every event — level-changed or not, pure pan and wall-saturated zoom included — runs the one synchronous full rebuild (the incremental pan fast path was deleted 2026-07-26; see the mid-gesture repaint paragraph). PAN LIVES ON THE PLAIN DRAG OVER THE WHOLE NAVIGATION SURFACE since 2026-08-12 (the eighth glass ruling, PAN-PRIMARY: "I'm always going to be zoomed in... I want to be able to pan", so the most common gesture takes the primary drag on desk and glass alike; the alt+drag that carried this machinery from the pre-arc grab is DELETED, and with the alt+wheel stepped pan moving onto the PLAIN wheel, ALT'S POINTER VOCABULARY IS EMPTY — conventions.md owns the alt inventory). The machinery is the old grab whole (`ScrollDragState` — now also the PENDING CLICK record, its motionless release running the deferred placement; region-scrub-esc.md owns the click half), per-event `nearbyint(dx·spp)` → `scroll_viewport(−delta, continuous)`, release / button-lost / force-end resync + end (Esc does nothing — pointer gestures have no cancel since 2026-07-29), read-only allowed — and since 2026-07-30 a grab-pan DURING PLAYBACK suppresses follow for the session, through the scroll_viewport funnel it rides (the rule at the end of this file). THE FORK IS THE STANDING 8px GATE: sub-threshold the press stays a pending click and the pan applies nothing; the crossing begins the pan AND its pointer capture (deferred off the press so a click never blinks the cursor), folding the whole press→crossing delta into its first event. With ONE sanctioned delta, since generalized: it drains a busy worker and rebuilds synchronously — originally a distinct pan mode, but since 2026-07-26 every pan/zoom frame of every class (wheel and PageUp/PageDown included) takes the same drained synchronous full rebuild, so the old async busy-worker fallback (the convicted mid-gesture staleness mechanism) has no successor and no distinct pan mode exists. THE GRAB-PAN CAPTURES THE POINTER (2026-07-21, opus session — the earlier no-capture form superseded; the capture begins at the THRESHOLD CROSSING since the pending-click fork): the same begin/end_strip_pointer_capture the strip drag uses, so the cursor swallows and the pan travels infinitely; PAN-ONLY (no zoom axis, no anchor stem — a pan has no pivot), and it restores the cursor at the pointer's NOTIONAL position on release, never at the raw traveled x (2026-08-14 — the raw travel carries the whole off-window debt and the compositor's clamp of it is applied once, which is exactly what left the cursor stranded at the edge; the notional position and its single-owner rule are below). A CTRL-EXACT PRESS ON THE NAVIGATION SURFACE — the waveform, either half, plus the ruler and the marker lane's empty stretches since 2026-08-12 — IS THE ONE NAV DRAG'S CTRL ENTRY since 2026-08-14 (arm_nav_zoom_press): the same pending/drag the plain press arms, opened in the ZOOM phase with the pivot seated and the stem painted at the press, ctrl live mid-gesture in both directions (the deletion paragraph below owns the model; a ctrl press on a FLAG stays the membership toggle, and a motionless ctrl press-release commits nothing). THE OVERVIEW STRIP's CTRL press was that dual-axis drag's last home and BINDS NOTHING since 2026-08-15, the gesture having been deleted whole with the lane's zoom (the overview paragraph at the end of this file owns the lane). THE CAPTURING DRAG — the nav drag, and the deleted strip drag before it — CAPTURES THE POINTER (Ableton-style, architect 2026-07-19): at the press the cursor HIDES (the "swallowed" feedback) and locks via the standard wayland-protocols pointer-constraints-unstable-v1 + relative-pointer-unstable-v1 (both OPTIONAL registry binds — a compositor without them degrades silently to clamped absolute motion); while locked the platform advances an UNBOUNDED virtual position from the accelerated relative deltas and delivers it through the normal on_motion path — COALESCED to ONE delivery per pointer frame (the wheel model: the position updates immediately per delta, only the callback defers to `on_pointer_frame`; button events never defer, and both release-before-motion orders are safe because the position is always current) — so pan/zoom travel is infinite, the gesture layer is coordinate- and rate-agnostic, and a sensor torrent costs one gesture event per frame; on release the cursor reappears at the ANCHOR STEM's column for a gesture that ENDED IN ITS ZOOM PHASE (the deleted strip drag's rule, live on the nav drag: the GUI hands the platform the stem's surface x via `set_capture_restore_x` each event — the edge-trick rebind pins the stem while the raw travel keeps going, so restoring at the raw travel would strand the cursor PAST the stem) and at the NOTIONAL position for the grab-pan (no stem; `capture_restore_x_override_.value_or(notional_pointer_x_)` since 2026-08-14 — the continuously clamped position rather than the travel ledger, so the cursor comes back where the pointer notionally is; the pivot paragraph below owns that split), y frozen at the press row (set_cursor_position_hint + commit before the lock destroy); THE RESTORE WRITES THE TRACKED POINTER POSITION BACK TO THAT HINT (2026-08-05, closing a real defect): the travel used to outlive the lock in `pointer_x_`/`pointer_y_`, and since button events carry no coordinates in the protocol they are DELIVERED at those fields, while the unlock warp comes back as no `wl_pointer.motion` — so a click made before the user next physically moved was routed at the travel's END. The architect hit it as a single visible click in the RULER band flipping the marker view (the ruler was then the strip drag's second entry — deleted for good 2026-08-12 after the merge/revert round trip, the defect record standing and the fix entry-agnostic): a ruler press armed the drag, a drag UP to zoom walked the virtual position out of the ruler into row 4's icon band (one zoom level is 60 px, the bands 20-70 px apart), the release drew the cursor back on the ruler at the press row, and the next click landed on the W radio — whose chord is bare `p`. Every roster button and the bare-`e` synthesized click had the same exposure; the alt-pan's was wider, setting no restore-x override. The hint is the right value because it is where the cursor is DRAWN, so position and pixels now agree; `pointer_position_unknown_` is deliberately NOT cleared with it (the coordinates are an estimate, the compositor still the authority — see the cursor-drop span in `region-scrub-esc.md`), and no motion is synthesized; the compositor-revoke `unlocked` event and pointer-capability loss both tear down cleanly, and every gesture exit path calls the idempotent end hook (`begin/end_strip_pointer_capture`, the kick_waveform_* installable-hook pattern, wired in main.cpp). Mid-gesture repaint is SYNCHRONOUS per pointer frame (`Viewport::apply_strip_drag_zoom`, final=false — a REVERSAL of the phase-1 async-supersede ride, replaced after labwc testing showed the plate hitching in bursts against the smooth live-basis overlays; per-frame coalescing plus the sync path's inline flag-cache rebuild (trim paints live since 2026-07-25) is what makes it affordable): EVERY event — level-changed or not, pan-only included — runs the one synchronous full rebuild (`kick_waveform_sync`, the keyboard zoom's per-press cost; architect ruling 2026-07-26, the smooth-movement conversion: no special handling during movement vs at-standstill, so the incremental pan fast path, its memmove + edge strip, the fast-flick valve, and `kick_waveform_pan` are all DELETED — the step-2 direct plate writer made the full render per-pointer-frame affordable, the license zoom frames had already proven); the async supersede slot belongs to the worker/preview system (load/resize/follow/drift) only. No per-event predictor resync; the terminating event (release / button-lost, or the force-end `finalize_active_drags` on Ctrl+Q / resize / WM-close — Esc no longer ends a gesture, 2026-07-29) runs ONE synchronous rebuild + predictor resync, and a motionless press-release commits nothing (the drag record's own `moved` gates finalization). A press becomes a DRAG only past a LATCHED Chebyshev threshold (`kDragMovedThresholdPx` = 8 px from the press since the 2026-07-24 unification — renamed from the strip-only name when the region drag adopted the same gate; a marker-specific 8px split existed briefly and was RE-UNIFIED 2026-07-24 second pass: ONE generic 8px gate for every press-becomes-drag surface (strip/region/trim/marker/tempo — kMarkerDragMovedThresholdPx deleted; per-surface re-split is the recorded fallback); below it events are ignored outright, moved stays false; once moved the gate never re-engages — no anchor dead zone): under capture the relative-pointer stream delivers every sub-pixel sensor tick, so an ungated click would always classify as moved and starve double-click detection; the crossing event folds the whole accumulated dx (last_x still at press_x). Strip drags never touch playhead or selection and have nothing to restore anywhere (navigation — they apply continuously, and no pointer gesture has a cancel since 2026-07-29), and the strip drag's own state replaced `ScrollDragState` everywhere at the time (`any_pointer_gesture_active` included, so wheels stay swallowed mid-drag; the name came back to `ScrollDragState` alone when that gesture was deleted, 2026-08-15). DOUBLE-CLICK on the TRIM BAR (the whole lane — endcaps, bar and empty space alike; REHOMED 2026-07-31 from the deleted zoom lane onto the trim band — it spanned the merged trim surface for the arc's one day, 2026-08-11..12, and came back to the lane with the revert; the ruler has NO double-click surface — hand-rolled through the shared surface-tagged `DoubleClickCandidate`, surface=TrimBar: the band press records its point in `TrimBarPressSeed` (above the band's read-only return while that return existed; it is deleted 2026-08-07), and the MOTIONLESS release seeds the candidate — both-axis slack AND no live trim drag (the merged band's third term, "no moved band region drag", left with the band — no region drag can arm on this lane now), the "stayed a click" spellings equal by construction since `kDoubleClickSlackPx` = `kDragMovedThresholdPx` = 8; the next plain trim-band press within `kDoubleClickMs` = 500 and the slack CONSUMES AT THAT PRESS (2026-08-17, with the whole double-click family — "if I'm double clicking specifically to do the double click action, I would never double click into a drag") and then falls through to the band's ordinary cap/bridge arm, seeding nothing, so the cadence stays second-press-only; THE ACCEPTED COST is recorded because trim has no undo — a consumed second press that then crosses into a cap/bridge drag proceeds FROM THE FRAMED VIEW, the framing having moved the viewport under the held button, and the architect's ruling is that the drift-into-drag double-click is a nonexistent use case while the frame-then-drag is the user's own two-act gesture; a moved drag, button-lost, the force-end finalizer, load, or any intervening keyboard OR WHEEL command clears it (a bare Esc clears it only as the keyboard command it now is — a no-op that still passes the top-of-dispatch candidate clear) — the wheel clear is audit C8, 2026-07-24: on_wheel joined on_key's top-of-dispatch candidate clear, since a wheel between two clicks moves content under the pointer): the action ZOOMS TO A SPAN, split from the bare `0` key (2026-07-21, opus session). It runs `run_span_framing_command` (renamed from run_zoom_double_click_command at the rehoming), which only ever FRAMES a span, never the fine working zoom: a PROPER trim sub-window wins (THE REGION ARM IS DELETED, 2026-08-18: the overlay and the trim window are one span now, so the two arms that used to compete frame identically and one answers both) (always-set trim, 2026-07-30: the full window is the old unset state and falls through to the whole-song arm; compute_trim_samples owns the normalization) mapped into the ACTIVE domain (`displayed_or_live_target_map` — identity on the empty source-view map, so one call covers both views), else the whole song (full zoom-out). A 2.5%-of-span margin on EACH side for the trim case (5% total), none for the whole song (already the whole song at the effective ceiling). The fit level is `effective_max_zoom_level`'s formula with the span in place of total, clamped to `[kMinZoom, effective ceiling]` (a zoom-OUT ceiling and a zoom-IN floor, so a tiny span may go deep while a span wider than the song saturates at whole-song-visible), and the start CENTERS the margined span in the window (the shared framer `frame_span_into_view`, 2026-07-25: start = span midpoint minus half the painter-quantized visible width at the fit level — degenerating to the margined span's start whenever the fit level is unclamped, and centering a floor-saturated tiny span instead of the former left-align; shared with the group undo/redo restore's offscreen framing) — both applied through `Viewport::apply_zoom_to_start` (clamp_zoom_level + clamp_viewport_start, NO playhead recenter, unlike apply_zoom_change). It is IDEMPOTENT (target==current is a no-op, so a second double-click with the viewport unchanged does nothing while any pan/zoom re-frames), one-shot navigation, playhead/selection untouched, read-only allowed. IT IS ONE COMMAND IN EVERY STATE AGAIN (2026-08-18): the `h` history view's trim bar stopped displaying the viewed checkpoint's diff span, so its double-click runs THIS command over the tab's real trim window like everywhere else, and the mode's two framing owners (`frame_viewed_commit_diff_span`, and `frame_history_view_whole_song` behind its empty-delta arm) are DELETED, caller-less. It served two commands from 2026-08-05 until then, the mode picking one at the press. The shared consume test is `trim_bar_double_click_at`, and the mode's press records the same `TrimBarPressSeed`, its release-side seeder needing no mode arm since no trim drag can go live in there. Bare `0` keeps its own route (`run_overview_command` — full zoom-out, else `c` at the stamped recall level, since 2026-08-05 and 2026-08-18) and `c` its marker-jump working zoom. The wheel bindings since 2026-08-12 are TWO EXACT CHORDS. THE PLAIN WHEEL IS THE STEPPED PAN (the eighth glass ruling) — the alt+wheel's exact old body ("alt+wheel is step pan... that comes from Reaper"; the samples_visible/kViewportLeadDivisor stride through the scroll_viewport funnel, follow suppression included) over the waveform, the top strip and the overview lane alike — with THE PLAIN WHEEL ZOOM DELETED ("superseded by the ctrl-drag zoom... we already have the =/- hotkeys") and ALT+WHEEL DELETED with alt's whole pointer vocabulary. AND `CTRL`+WHEEL IS THE ZOOM STEP (architect-ruled the same day, in the field session that produced the axis lock below): one whole level per completed detent, up = in, down = out, dispatching the `=`/`-` commands' own bodies through `Viewport::zoom_steps` (the coalesced form — its final state equals calling `zoom_in()`/`zoom_out()` once per detent, floor recenter and ceiling saturation included; it was producer-less for the few hours between the plain zoom's deletion and this binding). IT TAKES THE KEYBOARD ZOOM'S SEMANTICS, NOT THE DRAG'S, deliberately: it CENTERS on the playhead (the scanner while one runs, the resting cursor otherwise — `apply_zoom_change`'s own split) instead of pivoting on the column under the pointer, and it suppresses follow no more than the keys do, being a zoom that centers ON the scanner; the pointer-anchored pivot is what the ctrl-DRAG is for. Pure viewport move otherwise — no playhead write, no selection change, no region clear, no playback stop, read-only-legal. THIS IS NOT A REVERT of the morning's deletion: what died is the PLAIN wheel zoom, the plain wheel remains the pan, and the new binding sits on a chord that bound nothing — the ctrl-drag zoom's own detent-sized sibling, same modifier, same axis, same surfaces. It routes through `wheel_context` like the pan, so every refusal (prompt, dropdown, dialog modals, loading/empty audio, any live pointer gesture, the wheel-inert bands) comes free and unchanged — the predicate reads position and state only, never a modifier, so ctrl cannot change WHERE the wheel is live, only what it does there — and the platform's sub-detent accumulator already keys its remainder on the modifier chord and clears it on any modifier change, so remainder grown while panning can never complete a detent as a zoom. `=`/`-` keep the keyboard zoom steps, and every OTHER modified wheel (alt, shift, mixed) is swallowed. (The chip-row Alt+wheel trim-end move was already RETIRED, phase 2, 2026-07-20, and `wheel_context`'s chip-row region collapsed with it.) THE PLAYBACK SCANNER RENDERS AT A SUB-FRAME POSITION (2026-07-21, opus session): the scanner's drawn pixel comes from a continuous double `GuiPlayback::cursor_precise()` (the predictor's extrapolated position), not the integer `playhead_scanner_sample`, so it slides smoothly under a strip-drag zoom's per-frame viewport rescale instead of jittering on integer-frame steps (smoothness over accuracy — zooming is ballparking). The INTEGER sample stays the domain/change-detection anchor (follow/centering targets, the timestamp readout — the loop-wrap consumer died with looping, 2026-07-30), and cursor_precise matches cursor() branch-for-branch (stopped/suspended/pre-anchor/advancing/end-clamped/total-clamped/domain-offset). THE RESTING MIRROR IS RETIRED (audit C4, 7433706): the scanner value fields are meaningful ONLY while playhead_scanner_active — UNCONDITIONALLY since 2026-07-29 (contortion ruling 10a deleted the natural-end endpoint hold, the rule's one sanctioned exception: a stopped scanner deactivates immediately, natural end taking the same stop path every manual stop takes — ONE STOP BODY since 2026-07-30, `stop_playback_if_playing` (the hand-spelled `playback.stop()` + `restore_playhead_to_lsp` pair that Space's edge and the natural-end tick both carried collapsed onto it; `restore_playhead_to_lsp` is deleted, its one surplus over the shared body being a top-strip invalidate verified dead — the scanner paints no top-strip pixel — and the quiescence fence's fence-before-flag-clear ordering surviving inside the helper verbatim); since 2026-07-29 the natural-end branch's own follow-scroll tail is deleted too, so both stop edges leave the viewport exactly where playback left it (architect, "the simplest symmetry"; the tail's dead override guard and its scroll-BACK behaviour, both flagged, died with it rather than being fixed) — and the scanner line vanishes from the predictor's last painted column a few pixels short of the end bound — the accepted delta, one 60Hz frame of endpoint paint not worth two AppState flags, a tick→on_redraw→tick handshake, three teardown sites and six clears); the scanner's consumers all gate on it being active, resting values are stale by contract, and the stop paths deactivate without snapping — only the launch seed and the per-paint predictor advance write them. EVERY PAN SUPPRESSES FOLLOW FOR THE SESSION (architect 2026-07-30, "follow — agree"): `scroll_viewport`'s changed branch sets `follow_overridden_for_session` when playback is live — the funnel covering PageUp/PageDown, the plain wheel's stepped pan, touchpad scroll and the plain-drag grab-pan — and `apply_strip_drag_zoom` carries the same line because that chokepoint writes the viewport itself, bypassing the funnel (the song-anchored zoom axis fires it too, deliberately: that zoom carries the view off the scanner, unlike the keyboard zoom, which centers ON the scanner and is a non-producer); before this the flag's "manual-pan suppression" named a producer class that did not exist and panning away during playback was impossible with follow on. The producer inventory lives at the flag's declaration (app_state.h); the chase resumes at the next launch or an explicit `f` re-enable. ALL THREE VIEW SWITCHES ARE ONE SYNCHRONOUS-REBUILD CLASS since 2026-07-30: `p` joined `t` and Ctrl+Tab at `kick_waveform_sync` (the run loop services the frame callback before the timerfd tick's fingerprint check, so `p`'s bare invalidate blitted the leaving column's flags under the entering column's live passes for one frame; the settings `active_markers_view=` twin rides the same function; inventory at viewport.h's declaration). THE SCANNER MACHINERY ABOVE IS the foundation for the SCRUB, which SHIPPED 2026-07-23 — the waveform LOWER HALF's audition, the sole owner of the "scrub" name since the 2026-07-28 vocabulary ruling, and a motionless-release CLICK ACT rather than a press since 2026-08-13, when the two halves became one navigation surface (its 2026-08-01 full-height BARE RIGHT sibling died 2026-08-12 with the right button's unbinding; region-scrub-esc.md owns the statement) — and which launches the scanner independently of the cursor.

THE DIRECTIONAL SEGMENT AXIS LOCK IS DELETED — THE LIVE MODIFIER SUPERSEDES IT (architect 2026-08-14, the ONE-MODEL ruling: both surfaces speak ONE sentence — PAN BY DEFAULT, ADD THE ZOOM MODIFIER AT ANY TIME, DROP IT AT ANY TIME — the modifier being a SECOND FINGER on glass and CTRL on the desk; the ruling's original framing said the touch panel was the PRIMARY surface — "the touch screen is absolutely going to be essential to my workflow... whatever concessions are made to support symmetry on the touch screen and make it a first-class citizen are worth it in the long run" — and the desk its translation, and THAT FRAMING IS CORRECTED later the same day: the two surfaces are CO-EQUAL in the warp / phase-reset sense, the LAPTOP going first, "way more precise and much faster", while the panel is the more natural one and the one available on the go, neither being the model the other translates, and asymmetry between them ACCEPTED WHERE GENUINE — the architect expects the drag-zoom to be the biggest such asymmetry and says that is fine). THE ONE NAV DRAG (ScrollDragState, app_state.h, the code contract): the navigation surface's press — plain or ctrl — arms ONE gesture; while ctrl is up each motion event's dx pans 1:1 and dy is discarded, while ctrl is held each event's dx zooms (dx/kNavZoomPxPerLevel, about the seated pivot, RIGHT zooming in — the pinch's own sign, flipped hours after the rotation landed) and dy is discarded — THE ROTATION AND ITS SIGN, ruled later the same day and recorded in their own paragraph below; AS FIRST RULED THE ZOOM READ dy (dy/kZoomStripPxPerLevel) AND DISCARDED dx, which is the premise several sentences in this paragraph argue from, each marked where it matters — and CTRL'S EDGES TAKE EFFECT MID-GESTURE in both directions — the button never lifts. THE ZOOM PHASE ALSO FREEZES THE POINTER'S OWN X, AND THAT IS A SECOND STATEMENT RATHER THAN THE DISCARD RESTATED (architect 2026-08-14, from the rig, after a pointer-confinement experiment run purely as a diagnostic and reverted whole — the lock stays, the cursor stays hidden, travel stays unbounded in both axes: "Zoom allows diagonal movement. If I'm zooming in and out, it's actually moving the x position of the cursor, and it shouldn't be. I've been operating under the assumption that the zoom control would lock the x position... There was no problem at all with our calculation of the cursor. It's just that we're missing a clamp on the zoom. We need to clamp to zero horizontal movement on zoom"). WHY THE WORDING IS WHAT HID IT (the argument as it was made, on the vertical-zoom model the ROTATION below superseded — kept because it is how the freeze came to exist and because it is still true of that model; the freeze's reason under the rotation is restated in that paragraph and is STRONGER, not weaker): "the zoom discards dx" is TRUE OF THE GESTURE and FALSE OF THE POINTER — the view ignored the sideways travel while the pointer's notional position went on accumulating every pixel of it, and because nothing on screen answered that travel it was invisible. Two consumers then read the debt: a later ctrl DOWN seated the pivot far from where the pointer was believed to be, so the stem jumped, and a zoom→pan switch (which drops the stem override) left the release restoring the cursor out where the hand had never visibly been. IT IS A POSITION RULE, NOT A DELTA RULE, and the distinction is the whole fix: the notional position simply does not advance while the phase is frozen, while THE TRAVEL LEDGER IS UNTOUCHED — not one delta expression changed, and BOTH PHASES' TRAVEL IS STILL UNBOUNDED (which the rotation later made load-bearing rather than incidental: the zoom's own axis is the frozen one now), and the ctrl edges stay jump-free for the reason they already were (`apply_nav_zoom_at` advances `last_x` every event, so the first plain event after a switch measures its dx from the pointer's own position). THE FREEZE LIVES WHERE THE POSITION IS ACCUMULATED — `GuiPlatform::set_notional_x_frozen`, gated only at the notional half of `on_relative_pointer_motion` — because suppression has to happen at the accumulation: there is exactly one notional position and a GUI-side correction would have to subtract the discarded travel on the DELIVERY cadence, which is the second-position shape codex round 17 deleted (and which cannot agree with the raw-event clamp at a wall). The platform still applies NO GESTURE POLICY: it never works out that a zoom phase is running, it is TOLD, exactly as it is told the restore x and the restore kind, and this is the fourth member of that told-not-inferred family. Scoped to the capture (cleared at both capture edges, no-op uncaptured), asserted at the 8px crossing — the ctrl edges a sub-threshold press took reached no capture — and re-asserted at every ctrl edge from the one mode-sync body; the overview lane's dual-axis strip drag never asserted it and ran unfrozen by construction, and that gesture is deleted (2026-08-15), so the nav drag is the only asserter there has ever been. ACCEPTED PRECISION, one pointer frame wide: raw events accumulate as they arrive while the bit is set at delivery time, so the frame carrying a ctrl-down advances the notional x by that frame's own lateral travel first — the same grain the pivot seat already reads at. THE MODE FOLLOWS THE MODIFIER AT ITS OWN EDGE, motion or no motion (architect 2026-08-14, from the rig — "if I let go of control, the zoom stem should disappear. It doesn't disappear until I start moving the mouse"; the same-day model that read the mode per MOTION EVENT ONLY, and its "so no modifier-edge hook exists" clause, are SUPERSEDED): ONE body, `sync_nav_drag_mode`, with TWO callers — the run loop's SETTLED-STATE tail (main.cpp's `set_loop_settled_hook`, beside the pointer cursor's owner and by its own argument: a loop boundary is after every write, so one consumer answers every route that can move the modifier, the keyboard-focus loss that moves it with no event included) and the top of the MOTION arm, which is not redundant with it because a dispatch batch can carry the modifiers event and then a motion with no loop tail in between, and that motion must apply in the mode already held. THE PLATFORM PAYS THE ATTRIBUTION HALF: a modifier edge FLUSHES any staged captured motion through the ordinary pointer-frame flush before the modeled bits change (`on_keyboard_modifiers`, and `forget_keyboard_state` for the same reason), so relative motion that arrived under one meaning is delivered under it rather than re-attributed at the next frame boundary — the one-frame wrong-axis lurch the live-modifier model exists to prevent (codex round 16). THE RE-SEAT RULE, one per direction, is what makes every switch jump-free: at ctrl DOWN the zoom pivot SEATS at the pointer's current notional column, EVERY time (the anchor stem paints there at the edge itself, and a ctrl-armed press seats and paints from the PRESS — the stem-at-press ruling kept), while the LEVEL cannot jump because the zoom is a per-event delta off the live level; at ctrl UP nothing re-seats, STRUCTURALLY — the pan is incremental on dx from last_x, which both phases keep current, so the first plain event pans from the pointer's own position — the stem erases, the capture's restore-x override clears back to the pan's own fallback (the notional position, which is now the WHOLE fallback — there is no second arm) and the restore KIND re-stamps to Pan. THE PIVOT IS A SONG POSITION AGAIN, AND THE SEAT IS STILL SCREEN-BASED — the two halves of the question were ruled separately and only one of them moved (architect 2026-08-14, from the rig, reversing the pivot half of `69735b46` on his own evidence): `anchor_sample` is a frame in the active display domain, its COLUMN re-derived from the live viewport at every zoom event and the frame held stationary under it while the level changes, while THE SEAT is unchanged — each ctrl-down still places the stem at the pointer's own notional column, visible or invisible, and what it writes is the song frame under that column at that instant. THE CASE THAT FORCED IT, his own: zoom in near the left edge so frame 0 goes far offscreen, release ctrl and pan until the pivot's content sits mid-screen, then hold ctrl and drag UP. The viewport walks left until frame 0 comes back and CLAMPS — "which is good" — and from that instant a SCREEN pivot is a lie, because the song keeps sliding out from under a stem that does not move: "the second drag motion is basically irreversible... if I were to continue holding control and drag downward, I would be zooming into a section much later than where I did the second zoom". HIS FIX, IN HIS WORDS: "pin the cursor to the song position when zoom is happening, such that if the viewport clamps because we've reached end of file or zero, the cursor stays in the song position that it is doing the zooming from, so that any zoom is fully reversible". THE TWO MODELS ARE THE SAME THING UNTIL SOMETHING CLAMPS, which is exactly why this took so long to surface and why it is not a reversal of the screen seat: a zoom phase has NO PAN TERM AT ALL, so the frame under a fixed column cannot move on its own, and the models can diverge ONLY where the viewport saturates at frame 0 or at the right wall — the case above. Away from the walls the behaviour is what it always was. STATE THE PROPERTY EXACTLY, because "fully reversible" is stronger than what a wall can give: WHAT IS INVARIANT FOR THE PHASE IS THE ANCHORED FRAME. Off the walls that is the strict identity — the pivot's column re-derives to the value it was placed at, so zooming out and back in by the same travel reproduces the earlier viewport. AT a saturated wall the viewport cannot come back the same way at all (pinned at 0 or at the right wall, the view is determined by the level alone), and what survives is the FOCUS: the stem's column slides with its content while the frame under it never changes, so the reversed drag zooms into the section it came from. That second half is precisely what the screen column lost. THE EDGE TRICK COMES BACK WITH THE SONG ANCHOR, and it is the behaviour the architect had named admiringly two turns earlier ("as the part of the waveform that the stem is on moves off screen, the stem would clamp — that was a very intuitive design"): a pivot column pushed outside `[0, W-1]` pins at the edge pixel and REBINDS the anchor to that pixel's frame, so the zoom focus never leaves the screen. Recorded honestly: the rebind is the ONE thing that breaks exact reversibility, being a lasting mutation of the anchor — and it is deliberate, engaging only once the anchored content has been pushed OFF the visible span, where there is nothing left on screen to be reversible about. THE SCREEN-COLUMN MODEL IS THEREFORE SUPERSEDED IN PART AND ITS LINEAGE IS KEPT: `69735b46` made the pivot a screen column on the touch panel's evidence — "the zoom stem should remain where it was on the screen relative to screen x/y, not where it was on the waveform. Because the next time I press control, if I have dragged the waveform, I would expect the zoom stem to be placed where I left it relative to the screen" and "on the touch panel, if I drag-pan, then pinch zoom, then drag-pan some more, then pinch zoom in the same place where I did last time, that pinch zoom is applied relative to the SCREEN position" — and THOSE STATEMENTS ARE ABOUT THE SEAT, which is exactly what survives: where the next zoom starts from IS the screen position of the cursor. What could not be seen from there was the clamped case, a zoom-only phase never panning. So the Ableton song anchor is no longer "the road not taken" for this gesture; the overview lane's own ctrl drag implemented the same shape throughout, which made it the living reference for both halves, edge trick included, until its deletion left the nav drag and the pinch as the only implementations. WHAT REMAINS UNSETTLED is the seat under a DUAL-AXIS ctrl phase, which would pan while zooming and so would have to re-ask where the pivot belongs rather than inherit this answer. AND THE TOUCH PINCH IS RULED NOW, later the same day, so this file no longer holds it out of scope: it HAD the same clamped-zoom question in principle while it re-derived its anchor from the live centroid every frame, and the architect answered it the way he answered this one — "when the two-finger touch is first registered, it picks the point on the waveform, and the zoom pivot stays there no matter where the two fingers move on the screen." The pinch now SEATS a song frame at the first applied two-finger frame's centroid, holds it for the phase and re-derives its column each frame, edge clamp-and-rebind included; the parity is his own model stated whole — "I imagine my right hand as being the same right hand on the touchpad, and then the left hand reaches for the control key and implicitly does the opposite" — so the ctrl phase and the pinch differ only in what plays the modifier's part. touch.md's two-finger section is authoritative for the glass half; the one difference that survives is the SEAT'S TRIGGER (a ctrl-down here, the pinch's own registration there, cleared at every downgrade and every end). THE ZOOM STEM IS PLACED WHEREVER THE CURSOR IS WHEN CONTROL GOES DOWN, VISIBLE OR INVISIBLE, AND THAT IS THE WHOLE RULE (architect 2026-08-14, from the rig, superseding his own anticipate-the-teleport seat of hours earlier: "how about if we say that the zoom stem is placed wherever the cursor happens to be when the user presses control? Simpler rule... as far as the drag, pan, same rules as current — those are pretty intuitive and work well"). It re-seats at every ctrl down, at the pointer's own notional column, and asks nothing about where the release will put the cursor. NO LIVE RE-SEAT INSIDE A ZOOM PHASE is needed or wanted — the sentence still stands and now means something narrower, the pivot being a song frame: THE SEAT is written once per ctrl-down and never re-read from the pointer during the phase, which is safe because the phase freezes the pointer's x and so writes no notional position at all, while THE COLUMN is re-derived every event by design and the EDGE REBIND is the single route that rewrites the anchor mid-phase; a future dual-axis ctrl phase, which would pan while zooming, breaks the freeze's premise and would have to revisit it. THE SUPERSEDED SEAT AND ITS FALSE PREMISE, recorded so it is not re-derived (and MOOT now that the release has no fork at all — see the wrap below): the seat briefly read THE POSITION THE RELEASE WOULD RESTORE THE CURSOR TO instead — the release's own teleport-on-clamp fork, hoisted into one accessor the seat and the release shared — on the reasoning that a runaway drag's cursor "is going to end up teleporting there anyways", so the pivot should meet it at the grab column. THE PREMISE WAS FALSE EVEN THEN: that teleport was THE PAN'S RULE ALONE, since the zoom phase stamps the stem override on every one of its events and the override OUTRANKED the clamp fork, so after any zoom the cursor came back ON THE STEM wherever the stem is and the fork was never reached. Seating the pivot at the grab column did not agree with a teleport that was going to happen — it MANUFACTURED one. THE CTRL-DOWN POP IS DELETED, AND SO IS THE HOME IT POPPED TO (architect 2026-08-14, from the rig, replacing his own pop and its re-home of hours earlier rather than extending them: "I want to undo that idea"). It existed to answer a pointer PINNED at a wall — "zooming from a pinned stem position is essentially useless, because the whole point of zoom is to see what's going on on both sides, before and after your current viewport, and zooming while pinned only shows you half of that equation" — and the WRAP below removes the pinning itself, so there is nothing left for a pop to fix. CTRL THEREFORE MEANS ONE THING AGAIN: it seats the stem where the cursor is, full stop, which is the simple-rule seat above with the complication taken back off it. DELETED WITH IT, in full, so a reader does not hunt for them: the last notional write's clamp verdict, the capture's remembered home column, the shared homecoming expression the release and the ctrl edge both read, the platform action that popped and re-adopted it, the GUI hook that fired that action, and the pop call at the pan→zoom edge of `sync_nav_drag_mode`. The ctrl-UP handover is untouched and so is `GuiPlatform::set_notional_pointer_x`. A SECONDARY DIFFERENCE, recorded once and not engineered around: the stem clamps into the WAVEFORM and the cursor restore into the WINDOW, so the two can differ by the ≤15px inert right gutter when the cursor is out there — and nothing now promises they agree, the stem being simply where the cursor was rather than a prediction of where it will go. That gutter is ZERO PIXELS at every window width that is a multiple of 16, which is every width either host runs (1920 and 1024, and 2560/3840 besides), so it exists only under a hand resize to an odd width; the restore path deliberately takes no waveform clamp, the gutter being a real place on the window even though it is not a place on the waveform. Since the wrap it is also unreachable BY TRAVEL — a captured pointer folds back at the waveform's own right bound and so never accumulates into it; it can only be there because an absolute delivery left it there before the capture opened, and then the capture's very first event folds it in (it is already past the bound, so either direction wraps it), which is a one-event oddity on a window width neither host runs and is recorded rather than guarded. A PERSISTENT pivot (seated once per gesture, kept across every toggle) was built and WITHDRAWN the same day, and the reason is recorded so it is not re-derived: it was a workaround for THE CAPTURE'S UNBOUNDED POSITION ACCUMULATOR, which made "where the pointer is" unanswerable after a long drag — a pointer 3000 px past the left edge clamped to column 0 and STAYED there through a 750 px reversal, thousands of pixels of debt to unwind before it moved at all. THE ROOT CAUSE IS FIXED INSTEAD (architect 2026-08-14: "if I reverse direction and it's clamped, it should drift back into the middle no matter how far in the other direction we've travelled. And the same should be true of the zoom stem"), so the pivot can honestly follow the pointer again — which is the architect's own first instinct, and what the touch pinch did at the time this was written (the pinch has since SEATED its own pivot too, later the same day: it re-derived its anchor from the live centroid every frame then, and holds a song frame for the phase now — the ruling is a few paragraphs above and touch.md owns it). THE NOTIONAL POSITION IS A SECOND QUANTITY, NOT A CLAMPED FIRST ONE, and that is the whole design: the captured accumulator MUST stay unbounded because every captured gesture derives its per-event delta by DIFFERENCING the delivered positions, so clamping it in place would stall the pan the instant the hand passed the window edge and would eat the promised unlimited travel. So a notional position is kept BESIDE the travel ledger, advanced by the same deltas and CLAMPED AT EVERY STEP (which is what leaves no debt) — and THERE IS EXACTLY ONE OF IT IN THE PRODUCT, `GuiPlatform::notional_pointer_x_`, owned where the raw events are and live for the whole process rather than only under a capture (uncaptured nothing is virtual, so it simply IS the pointer's delivered position — every delivery that carries a real one writes it, the touch translation's included; captured, the raw relative stream accumulates it per event, WRAPPING it into the waveform's span and clamping into the window behind that; the release moves it to the restore hint). Its consumers are the capture's RESTORE — the cursor now reappears where the pointer notionally is rather than at the edge the raw travel's one-shot clamp produced — and this seat, which PROJECTS it into waveform columns and re-clamps in the bounds that layer owns (`nav_notional_col`, input_pointer.cpp: a projection, held nowhere). BUT THE CURSOR WRAPS EDGE TO EDGE INSTEAD OF PINNING AT A BOUND, and that is the other half of the restore rather than a second arm of it (architect 2026-08-14, from the rig: "I want to undo that idea... what if instead we had the cursor, every time that it touches the bounds, teleport back to the centre of the waveform? So much of this workflow is centred and centre-oriented" — and then "whatever the virtual point is is where the cursor shows back up", which is what makes the whole homecoming apparatus unnecessary — and then, having DRIVEN that centre form, "make the wraparound a full screen wraparound, not just the half width wraparound", the modular fold he had named as the alternative when he chose the centre one commit earlier). THE RULE: under a capture the notional position no longer clamps at a bound and sits there accumulating a verdict; an accumulation that would push it PAST the RIGHT bound re-enters at the LEFT one and one past the left re-enters at the right, CARRYING ITS OVERSHOOT, and it goes on travelling. THE FOLD IS TWICE AS LONG AS THE CENTRE FORM'S, WHICH IS THE WHOLE OF THAT REVISION: each crossing now buys the waveform's FULL width of travel instead of half of it, so a pan of several screens folds half as often and the cursor spends its time spread across the whole surface rather than clustered around the middle. Everything else about the mechanism is untouched and stays true, item for item, below. A pan of several screens therefore leaves the cursor somewhere ordinary rather than stranded on the last pixel, so the release simply restores where the virtual pointer is — always, with no runaway case to detect and no home to remember. CROSSING WRAPS, LANDING DOES NOT: a value exactly on either bound is inside and stays, so a hand that comes honestly to rest on the last pixel rests there. ONE APPLICATION SUFFICES and there is deliberately no loop — a single event's overshoot cannot realistically exceed the span, and the window clamp in `note_notional_pointer_x` is the backstop for a pathological delta; a degenerate or unsupplied span is skipped and the clamp answers alone. THE BOUNDS ARE THE WAVEFORM'S, NOT THE WINDOW'S, on the architect's own reasoning that this makes the behaviour identical at every resolution. The platform is TOLD both bounds (`GuiPlatform::set_capture_wrap_span`, the told-not-inferred family's sixth member, capture-guarded like the rest and cleared to a degenerate span at every capture begin), because this class knows nothing about a waveform; the GUI supplies them from `waveform_area` immediately after `begin_strip_pointer_capture` at BOTH capture sites through one owner (`tell_capture_wrap_span`, input_pointer.cpp) — uniform by design, the wrap being a property of THE CAPTURED POINTER rather than of a gesture. THERE IS NO THIRD VALUE TO PASS, AND THE EVEN-WIDTH ROUNDING QUESTION IS RETIRED WITH THE CENTRE RATHER THAN SETTLED: the centre form had to be handed a column it could not derive exactly (the waveform's width is the window width floored to a multiple of 16 and so always even, leaving the true midpoint between two columns), and an edge-to-edge fold needs no middle at all, so the question stops existing. THE WRAP LIVES AT THE CAPTURED ACCUMULATION (`on_relative_pointer_motion`) and NOT inside `note_notional_pointer_x`, which must remain the ONE clamp body for every writer: an absolute delivery from the compositor is a real position and must be taken as given, never folded — the round-17 record at the field says why, and it still holds. THE ZOOM PHASE CANNOT WRAP, because the lateral freeze suppresses the very accumulation the wrap rides: a zoom writes no position at all and so can never reach a bound, its travel being spent on the level. IT CHANGES NO VIEW: the pan's dx comes from the TRAVEL LEDGER, which is unbounded and untouched, so the scroll is exactly as smooth as it was and only where the cursor reappears is different. THE WRAP IS FREE BECAUSE THE CURSOR IS HIDDEN: Wayland gives a client no pointer-warp request at all, so a VISIBLE cursor can never be moved by us — the only position we may ever state is the locked pointer's release hint — but while captured the position is our own bookkeeping entirely, so wrapping it costs nothing, needs no protocol, and cannot be seen. A WINDOW RESIZE MID-CAPTURE LEAVES THE SPAN STALE, ACCEPTED: the values are re-supplied at the next capture, the drift is bounded by the resize itself, and a live push would need a producer that does not exist. THE TELEPORT-ON-CLAMP SPLIT IS SUPERSEDED AT ITS PREMISE and is history rather than current behaviour: it read "a small pan moves the cursor a little, a runaway pan brings it home", and the wrap removes the runaway case ITSELF — the cursor is never stranded, so there is nothing to bring home. A reader who wants the old rule has git. AND THE RECENTRING DELAY IS CONSIDERED AND DECLINED, in the architect's own words — "that would make it feel less responsive" — the third refusal of a preventative backstop on this surface, recorded so it is not proposed a fourth time. THE Y HAS NO HALF OF THIS: the restore's y is the press row unconditionally. THE SECOND CLAMPED POSITION IS DELETED AND MUST NOT COME BACK (codex round 17): the GUI briefly accumulated its own column, advanced once per DELIVERED motion by the net travel of a whole coalesced pointer frame, and CLAMP-PER-RAW-STEP AND CLAMP-ONCE-ON-THE-NET-DELTA ARE NOT EQUIVALENT AT A WALL — raw `+20` then `-8` at the right edge leaves the platform at `max-8` and the column pinned at `max`, after which interior motion preserves the eight-pixel offset for the rest of the gesture, so the zoom pivot seated at the edge while the pan release restored the cursor inward. Two clamped positions advanced on different cadences cannot be made to agree by matching their clamps; the fix is that only one exists. THERE IS NO NOTIONAL Y: the restore's y is frozen at the press row and no gesture reads a vertical POSITION — since the rotation the nav drag discards dy in both phases, and the overview lane's still-vertical zoom consumes dy as a per-event DELTA off the ledger — so a clamped y would have no reader (it would cost that zoom no travel — a notional position is a SECOND quantity beside the ledger, exactly as the x one is; the missing reader is the whole reason). THE FROZEN RESTORE Y IS NOT THE LATERAL FREEZE'S OTHER HALF, which is why the 2026-08-14 fix grew no y twin: the x defect was an ACCUMULATOR silently diverging from the pointer and feeding TWO consumers, and there is neither a y accumulator to diverge nor a second y consumer — what is left is a restore-position preference, a drag that wandered 300 px down returning the cursor 300 px up, and the press row is the ergonomic answer there because vertical travel is unbounded and would restore into another lane or off the surface, whereas the notional x always lands back on the waveform (since the rotation the nav drag has no vertical term at all, so what the press row answers is the hand's own drift and nothing else), which spans the full width. Architect's ruling, unchanged. ONE CAPTURE serves the whole gesture, begun at the 8px crossing whatever the mode (a ctrl click never blinks the cursor either — superseding the retired zoom drag's capture-at-press) and untouched by every mode edge; only the release's restore x and restore kind ride the switches (set_capture_restore_kind / clear_capture_restore_x, platform_wayland.cpp). THE ACT STAYS PRESS-TIME: a ctrl-armed press runs NO deferred click act at its motionless release (a ctrl click was never the placement) while a plain-armed press runs its act even with ctrl down at the release — press-time modifiers arm the ACT, live modifiers steer the CONTINUOUS gesture. THE SCOPING ARGUMENT, architect-accepted, recorded at the modifiers-at-press rule's own site (AppState::ChromePress::shift): that rule lives on the chrome buttons' act and the modal release, and exists so an ARMED ACT cannot change under the user's finger between press and commit — a continuous viewport gesture arms no act, nothing commits, nothing can surprise, and every part of it is undone by moving the other way, so a live modifier on a navigation gesture is OUTSIDE that rule's scope, not an exception to it; the chrome and modal rules stand exactly as written. WHAT THE RULING DELETED: the whole hard per-segment axis lock — the classification, the 60-degree angle and its knob, the 8px classify distance, the 75ms pause reset, the segment state and the two-surface comparison — because the question the ladder answered, "which axis did this drag mean?", STOPS BEING ASKED: the modifier answers it directly and reversibly. THE CALIBRATION SUCCESSION, recorded whole so it is revivable and not re-run: knee 48 → 200 → 600 (the cumulative off-axis curve f(D) = D·|D| / (|D| + knee), field-killed by the wall diagnosis — a saturated wall drag is where the hand keeps sweeping, so D accumulated and the arc dumped into zoom) → flat 0.08 damping (held the wall, still let a long arc drift the other axis) → THE HARD PER-SEGMENT LOCK at 45 then 60 degrees (classified once at 8px Chebyshev, the off axis a literal zero for the segment's life, a 75ms pause re-aiming it; the architect RATIFIED it in the field — "zoom now works as expected") → DELETED, made unnecessary by the live modifier. IT WAS NOT A FAILURE — it answered a question that no longer exists, and it is the shape to revive if a classifier is ever wanted again. Damping and hysteresis stay rejected on their own recorded verdicts. AND THE SIMULTANEOUS DUAL-AXIS MODEL THE LADDER GREW OUT OF IS OFF BUT NOT CLOSED (architect 2026-08-14, across two sittings — first "if we can't get something close to what Ableton does, then we should just stick with what we have now... the horizontal pan must be absolutely smooth, and it is right now", then, having driven the current model: it is "technically correct" and "basically makes incorrect movement impossible" but "feels quite awkward", and "if I had to choose between dual axis and the current solution, I think dual axis might be better even if it's a little buggy. I'm not sure"). THE CURRENT MODEL SHIPS AND THE QUESTION STAYS OPEN, with the dy reservoir below as the next experiment on his explicit endorsement. NOTE ALSO THAT THE ONE-MODEL RULING'S FRAMING IS CORRECTED: the touch panel is NOT the primary surface and the desk is NOT its translation — the two are CO-EQUAL in the warp / phase-reset sense, the LAPTOP going first ("way more precise and much faster") while the panel is the more natural and the one available on the go, and ASYMMETRY BETWEEN THEM IS ACCEPTED WHERE IT IS GENUINE, exactly as warp markers carry information where phase resets carry only placement. The architect expects THIS GESTURE to be the biggest such asymmetry and says that is fine, so cross-surface symmetry is not a constraint on its mechanics. THE DIAGNOSIS IS ANATOMICAL RATHER THAN NUMERICAL, and it is why every rung of the ladder missed: a wrist-driven horizontal drag is an ARC — the wrist pivots, so a pan is never pure dx but dx with an OSCILLATING dy riding on it — while zoom comes from the SHOULDER and is nearly straight. Under dual axes that dy became zoom and the view breathed (his "accordion": the waveform subtly squeezing together and apart while dragging one way). TWO PHENOMENA WERE CONFLATED THROUGHOUT: the ACCUMULATION, the drift walking away over time, which was the non-invertible re-projected song pivot and is FIXED — zero-mean jitter now cancels rather than ratcheting — and the INSTANTANEOUS BREATHING, which is the arc itself and would survive any such fix. EVERY MODEL TRIED WORKED IN THE AMPLITUDE DOMAIN (damping and the curve scaled jitter AND intent together; the lock forbade diagonal outright), BUT ARC-JITTER IS NOT DISTINGUISHED FROM INTENT BY SIZE — it is distinguished by SIGN PERSISTENCE, jitter oscillating about zero while deliberate zoom holds one direction, and no rung used that discriminator. THE ONE UNTRIED MECHANISM, recorded because it is the only one that matches the physics: accumulate dy into a small reservoir and apply zoom only when it crosses a threshold, then drain what was spent — oscillation cancels inside the reservoir and never fires, sustained motion accumulates in one direction and fires promptly, with no timer, no classification, no mode, and diagonal surviving. IT IS THE NEXT THING TO TRY ON THIS SURFACE — cheapest on the ctrl-held zoom, where it is reversible — and if the breathing it removes is the breathing that killed dual-axis, that is the evidence for reopening dual-axis, which is the architect's call and not a foregone one. CONSEQUENCES SETTLED WITH THE RULING: kStripSegmentClassifyPx does NOT survive — the 8px drag threshold is kDragMovedThresholdPx, a DIFFERENT job (press-becomes-drag, not classification), and it stands untouched; CTRL+WHEEL IS UNTOUCHED and still wanted (the zoom step, the ctrl drag's detent-sized sibling with keyboard-zoom semantics); and the OVERVIEW LANE's ctrl drag — the strip-drag machinery's ONE remaining entry when this was written — kept its dual-axis simultaneous form with BOTH AXES PLAIN, the pre-lock model, its deliberate ctrl press never having produced the wrist-arc complaint; THAT GESTURE IS DELETED WHOLE ON 2026-08-15 (the lane's own section below), so the strip-drag machinery has no entry left at all and the lane's separately ruled horizontal inversion, never built, went subject-less with it. THE CURSOR under the model: the hover map still answers Pan plain and Zoom with ctrl over the whole surface, and the cue now changes at a ctrl EDGE with no motion under it (the loop-tail owner reads live modifiers); MID-DRAG the capture hides the cursor for the gesture's whole life, so a mid-gesture switch shows nothing until the release — whose restored kind the switches re-stamp, so the cursor comes back as the phase the gesture ENDED in — and the live-gesture-keeps-its-cue exception (trim / overview / marker, all visible-cursor drags) needed no revision.

THE ZOOM ROTATES ONTO THE HORIZONTAL AXIS, AND RIGHT MEANS IN (architect 2026-08-14, from the rig, later the same day than everything above — the ROTATION and the SIGN are two rulings hours apart, and the quote here is the rotation's, whose sign half is superseded by the flip recorded below: "we should rotate the axis of zoom, because on the touchpad zoom is also a horizontal pinch motion — it just happens to have two fingers... if the user is holding control and they move the mouse to the left it zooms in, and if they move to the right it zooms out; moving the mouse to the left advances forward in the timeline, and so the forward motion would mean zoom in, because full zoom out is how we start"). THE AXIS: `apply_nav_zoom_at` reads dx where it read dy and discards dy where it discarded dx, so DY IS DISCARDED IN BOTH PHASES NOW and the vertical axis has left the nav drag entirely. THE SIGN, FLIPPED HOURS AFTER THE ROTATION LANDED ON THE DOMINANT HAND'S OWN EVIDENCE (architect 2026-08-14, from the rig, once he had driven the first one: "I got the direction reversed... what it should really do is imitate what the right hand does, because I'm right handed — on the touch screen, for zoom, if I move my fingers apart, the right finger is moving to the right, and that zooms in; so control drag right is zoom in"): `new_level = zoom_level - dx / kNavZoomPxPerLevel` — RIGHT (dx positive) lowers the level and zooms IN, LEFT raises it and zooms OUT. THE DERIVATION IS THE PINCH THE DESK IS IMITATING, taking the DOMINANT HAND'S finger as the one the mouse stands in for: spreading the fingers apart moves that finger RIGHT and zooms in, bringing them together moves it left and zooms out. THE SUPERSEDED DERIVATION IS RECORDED RATHER THAN DELETED, because it is the plausible-sounding wrong answer someone will re-derive: it reasoned from the PAN — dragging left is what advances the view FORWARD through the piece, and a piece OPENS at full zoom out, so forward motion ought to mean in. IT IS OUTRANKED BECAUSE THE ROTATION ITSELF CAME FROM THE PINCH: the axis moved onto the horizontal to speak the sentence the glass already speaks, and a sign taken from the pan would have had the two surfaces disagree on the one thing the rotation existed to make agree. NOTHING ELSE MOVED WITH THE FLIP — the axis, the rate constant, the song-anchored pivot and its edge rebind, the screen seat, the notional-x freeze, the ctrl-up handover, the release restore and the pan phase are all untouched, and the touch pinch needed no sign to begin with, being a distance ratio. THE CROSS-SURFACE ARGUMENT IS THE WHOLE POINT AND IT IS A GENUINE CONVERGENCE: the modifier now changes WHAT HORIZONTAL TRAVEL MEANS rather than WHICH AXIS IS LIVE, so one finger sliding sideways pans and two fingers sliding sideways pinch, and the desk's plain-drag / ctrl-drag pair speaks the same sentence the glass already speaks. That is not symmetry chased for its own sake against the standing "asymmetry accepted where genuine" ruling above — the asymmetry simply stopped being real, the same hand motion meaning the same thing on both surfaces, so there is nothing left to except. THE RATE SPLIT INTO ITS OWN CONSTANT with the rotation: `kNavZoomPxPerLevel` (app_state.h) serves the nav drag's horizontal zoom, and it served it BESIDE `kZoomStripPxPerLevel`, the overview lane's still-vertical one, for exactly one day — that lane lost its drag-zoom on 2026-08-15 and the vertical constant was deleted with the subsystem (the overview paragraph at the end of this file), so `kNavZoomPxPerLevel` IS THE PRODUCT'S ONE DRAG-ZOOM RATE now; THE RATE IS 200.0 AND IT IS SET TO MATCH THE PINCH (architect 2026-08-14, from the rig, having driven the two surfaces one after the other: "ninety pixels is still a little too fast compared to the touch screen; the touch screen rate of motion is very natural and the mouse is too fast... the waveform is clamped at five hundred, so let's try two hundred and forty, for a multiple of the sixty"), and THE DERIVATION IS WHAT MATTERS RATHER THAN THE LADDER OF NUMBERS BEHIND IT (60.0 the vertical constant carried over as a first guess, then 90.0 on the first drive — "the zoom is a little too fast now... it's not horribly too fast, it's just slightly too fast" — then 240.0, which reached the band but sat at its SLOW END, the architect having driven it and found it "a little too slow", so 200.0 is that same derivation with the overshoot taken off): THE GLASS IS THE REFERENCE AND THE DESK IS TUNED TO IT. A pinch's level is `log2` of the FINGER-GAP RATIO, so its px-per-level is not a constant at all — it depends where the gap starts — but at a comfortable gap one level costs roughly 150-250 px of single-finger travel, and 200 places the mouse hand inside that band; that band is what "the touch screen rate is very natural" was measuring, so the two surfaces now move the view at about the same rate for the same hand motion, which is the whole point of the rotation that put the desk's zoom on the horizontal to begin with. WHAT IT COSTS AND WHY THAT IS AFFORDABLE: the whole [kMinZoom, ceiling] span is now about 3200 px of travel, over a screen and a half at the deployment size — fine BECAUSE OF THE CAPTURE, the notional-x freeze below being exactly what makes the zoom phase's sideways travel unlimited, so the span is reachable inside one gesture whatever the screen is. The two constants are no longer the same quantity — a px-per-level across ~1920 px of horizontal room is a different ergonomic from one across ~500 px of vertical — and sharing one would have made a retune of either gesture silently move the other. Both stay architect-tunable on the rig. THE LATERAL FREEZE STAYS AND ITS REASON GETS STRONGER, NOT WEAKER: it was asserted because the zoom DISCARDED its lateral travel and accumulating it invisibly was a defect (the paragraph above, kept as history), and it is asserted now because the zoom SPENDS that travel on the LEVEL — letting the notional position spend it a second time would double-count it, and would CAP A ZOOM AT THE WINDOW'S WIDTH, since the notional position clamps into the surface where the travel ledger does not. Frozen, the zoom's travel stays unlimited exactly as the vertical travel was unlimited by having no notional coordinate at all. Nothing else moved: the song-anchored pivot and its edge rebind, the screen SEAT rule, the ctrl-up handover, `nav_stem_column_x`, the release restore, the pan phase's arithmetic, the overview lane's own drags, the wheel, the `=`/`-` keys and the touch pinch (distance-based and direction-agnostic, so it needs nothing here) are all untouched. `ScrollDragState::last_y` IS DELETED (2026-08-15). The rotation left it written by both phases and READ BY NOTHING, and it was HELD in that state — the producer-less rule's one open exception — only while the rotation itself was unverified on the rig; the rate is settled at 200.0 after four retunes from the rig, so the exception had no subject left and the field, its three writes and its own comment went together. The vertical axis has left this gesture entirely, and `apply_nav_zoom_at`'s `y` parameter is deliberately unread for that reason (silenced at the site, the parameter kept because its callers hand the motion event's pair straight through). A future vertical term here is a NEW design question and not a restoration: it would have to re-derive its own rebase rather than inherit one kept warm for it.

THE OPEN QUESTION IS ANSWERED BY REMOVAL — THE OVERVIEW LANE DOES NOT ROTATE ITS ZOOM, IT LOSES IT (architect 2026-08-15, superseding the open question this paragraph recorded: "zoom is not what I'm looking for in the overview strip, because the overview strip can indirectly control zoom by directly controlling the outline box"). THE QUESTION WAS REAL and is worth keeping visible, because it is what the removal answers: the standing expectation was that the lane follows whatever the main zoom becomes (the lane's zoom-only item was withdrawn on exactly that reasoning), but its ctrl drag was DUAL-AXIS — dx panning and dy zooming, both live at once — so BOTH of its axes were already spoken for and there was no free axis for a rotated zoom to move onto; rotating it would have meant deciding what happened to its pan, a design fork rather than a mechanical follow-through, and the lane's separately ruled HORIZONTAL INVERSION sat on the same axis and would have had to be decided with it. THE FORK IS GONE WITH THE GESTURE: there is no lane zoom drag to rotate and no lane pan axis to trade away. THE HORIZONTAL INVERSION RULING (still unbuilt) IS LEFT WITHOUT A SUBJECT rather than repealed here — it was ruled on the deleted ctrl drag's PAN axis, and the box drags that replace it are ABSOLUTE PLACEMENTS rather than deltas (the box goes where the pointer is), so there is no sense to invert; if it is meant to say something about the box, that is an architect ruling and not a follow-through. The lane still CHANGES the zoom, through the box: dragging a bound is a zoom anchored at the far bound, and that is the whole of it (the lane's section at the end of this file is authoritative).

THE ONE MODEL SUPERSEDES THE FRICTION ASYMMETRY (architect 2026-08-14, the same day's later ruling; touch.md's two-finger section is authoritative for the glass gesture, this paragraph owns the cross-surface statement). ON GLASS: one finger pans, two fingers zoom — "on the touch panel, two fingers is zoom, one finger is pan... the two-finger gesture will never scroll on the touch panel" — and a finger lifting from the pair DOWNGRADES the gesture to the survivor's pan (touch.md). ON THE DESK: the plain drag pans, and CTRL — held, pressed or released at any point of the drag — is the zoom, the second finger's part played by a key. So the mental model transfers, "for speed purposes, for motor reflex": PAN BY DEFAULT, ADD THE ZOOM MODIFIER AT ANY TIME, DROP IT AT ANY TIME. The FRICTION ARGUMENT this paragraph used to record — the mouse keeping a pan axis inside its ctrl drag because "dropping it would make every pan-then-zoom mean releasing ctrl mid-gesture and re-pressing... clicks are naturally more frictional than touching the screen" — is ANSWERED rather than violated: the modifier release mid-gesture IS the pan now, costing no click, which is what dissolved the asymmetry, the classifier and the shared retunables all at once (the deletion paragraph above).

THE TWO-FINGER SUCCESSION, recorded rather than erased so the middle rung can be revived if the feel ever wants two-finger panning back. (1) THE ACCORDION, the shipped model until 2026-08-14: the gesture applied centroid pan and log2-distance zoom SIMULTANEOUSLY every frame, and two fingers moving together never hold their separation exactly, so the drift fed the zoom term and the view breathed — "if I move two fingers in the same direction on the touchpad, I get the old unrestricted zoom motion... during the drag it squeezes in and out". (2) THE FINGER-AGREEMENT SEGMENT LOCK, 2026-08-14, SUPERSEDED THE SAME DAY: this lock's model brought to glass, classifying BETWEEN THE TWO FINGERS' own travel vectors rather than between two axes — pan or zoom rather than horizontal or vertical — over the mouse lock's own three retunables, then shared and since deleted with it (the 8 px classify distance read per finger, the 60° angle as each finger's pan cone, the 75 ms pause as the same lazy rest). A segment classified ONCE, when the FARTHER finger's Chebyshev travel from the segment origin reached the classify distance: PAN iff (a) the fingers' x components shared a strict sign (a pinch's fingers move toward or away from EACH OTHER, so along a roughly-horizontal pinch axis they OPPOSE), (b) each finger's vector lay within the angle's cone off horizontal, ties pan (a steep pinch failed here, and a VERTICAL same-direction drag failed here too and classified ZOOM, under which a rigid vertical slide barely changes the finger gap and so did approximately nothing — the same answer zoom-only gives today), and (c) the LESSER finger travelled at least HALF the classify distance, the ANCHORED-FINGER FLOOR, derived not knobbed: a planted-thumb pinch's near-motionless finger could satisfy (a) and (b) out of jitter, while a panning hand moves as one rigid unit so both fingers carry essentially the hand's own travel and half tolerates landing/roll asynchrony. The classified segment's OFF MODE was A LITERAL ZERO — the binary, not damping, this surface inheriting the mouse ladder's closed calibration verdict (curve → flat factor → the lock) rather than re-running it. The vectors were CUMULATIVE from the segment origin, so per-frame delta size — the panel's cadence — never entered the test and the verdict could not flap. ITS ONE DELIBERATE DIVERGENCE from the mouse's shape, worth keeping if it is ever revived: THE UNCLASSIFIED WINDOW HELD AND FOLDED instead of responding both-axes-plain, because "both plain" is exactly the accordion in miniature re-opened at every pause reset — nothing applied until classification, the frames' dx sum and ratio product accumulating, the classifying frame applying the ON mode's accumulator whole and DISCARDING the off mode's unapplied; no motion was lost, it arrived in one placement at the cost of up to the classify distance of latency per segment. It answered the accordion and was dropped because zoom-only makes it unnecessary, not because it failed. (3) ZOOM ONLY, the current model. THE ENTRY POINTS NEVER MOVED ACROSS ANY OF THE THREE: the gesture reaches the viewport through `Viewport::apply_strip_drag_zoom` and the nav update/end hooks, never the pointer-press arm; every refusal arrives through `wheel_context` per frame; and the latch, the disambiguation window and the hard ends are untouched (the 2026-08-14 one-model ruling then gave the pair its DOWNGRADE — a finger lifting from two continues the survivor's pan — recorded in touch.md's two-finger section).

THE OVERVIEW STRIP (architect-ratified 2026-08-12, his pick from the offered fillers for the row unification's freed space: "the whole song overview strip, yes, that's the best one... that's perfect"; the Ableton model per his own reference, ableton.png in the redesign folder — "a Zoom strip right underneath the transport buttons... if you hover it, it shows you a magnifying lens, and you can do the usual Zoom to move around... it draws a box around the area that you currently view"): a lane INSIDE THE CENTERED BLOCK, between the ICON ROW and the TRIM BAR — TOP lane 3 since the relayout's COMMIT B, which moved it up out of the bottom strip the evening it landed (it was bottom lane 0 under the unified row, with the blank foot below it, for that afternoon) — showing the WHOLE SONG as per-column min/max bars with THE VIEWPORT BOX and THE PLAYHEAD TICK, and carrying — since the LANE REWORK later the same day (architect-ruled post-relayout: box endcaps, plain drag = pan only, ctrl = the zoom drag, click-teleport, contrast, cursors) and as REDESIGNED 2026-08-15 (THE BOX IS THE SUBJECT; the ctrl zoom drag deleted) — the BOX-ENDCAP EDGE DRAGS, the CLICK-TELEPORT and the BOX-FOLLOWS-POINTER PAN, all of them on its PLAIN press. It WAS the ZOOM-STRIP CONCEPT'S THIRD HOME and is its LAST: the dedicated zoom LANE (deleted 2026-07-31), the RULER ("the zoom strip reborn" with row 5, its entry dead for good since the sixth glass ruling), then this lane — and the concept has now been withdrawn from all three, the deleted gesture's succession recorded above `ScrollDragState` in app_state.h. GEOMETRY: ONE FIXED TINY HEIGHT ON EVERY HOST — `kOverviewHeightPx` = 24 CONTENT plus a 1px border at EACH EDGE, a 26px lane at 100% (architect live at commit B: "always the same height, very tiny height in both the laptop and the touch screen"). THE TOP BORDER LANDED 2026-08-13 (architect: the lane "gains an almost-black top border, the same colour as the bottom one"), and the lane GREW BY THAT ROW rather than eating one of its own 24 — "gains" is what he said, and the miniature waveform stays exactly as tall as it was. The row is deliberately ADJACENT to the ICON ROW'S OWN border-bottom, two 1px lines at that seam: the lane reads as its own framed object instead of as ground the icon row happens to end above, which supersedes commit B's reasoning that a top line would merely double the icon row's. THE HEIGHT ARITHMETIC IS RE-DERIVED, NOT ADJUSTED: the lane's +1 raises the top lanes to 196 and the block above the waveform to 161, which moves GAP 1 (1080p 95 → 94) and the Pi's unclamped waveform (354 → 353) and leaves the 1080p waveform and GAP 2 untouched — the stacks are in this file's lane roster above and at main.cpp's vertical block, the one owner. THE ONE-DAY PAIR IT SUPERSEDES: `kOverviewMinHeightPx` = 24 / `kOverviewMaxHeightPx` = 96 with height = clamp(the leftover past the waveform clamp, min, max), the minimum reserved AHEAD of the waveform's natural height so the Pi kept a 24px sliver (waveform ~310 at the ratification, ~355 after the relayout's toolbar-row deletion) while 1080p rested at 96 overview / 213 blank — the clamp, the reserve arithmetic and the blank-foot remainder are all DELETED producer-less, the waveform's height being the plain leftover the two flex gaps share now. The surviving 24 is that pair's own minimum, the sliver the Pi already read well; the one application point is still main.cpp's vertical block. PAINT (paint_overview_strip, paint_handler.cpp): the lane wears the WAVEFORM'S OWN GROUND under TWO 1px BORDER ROWS, one at each edge (kWaveformCanvas + kWaveformBorder, no new sample; the painter spells the fill and both lines itself instead of calling render_canvas, whose rows are the waveform's own thicker chrome, and `overview_content_rect` — SYMMETRIC since the top row landed — is the band the bars and the box clip to, the bar cache carrying that band's y offset into both channel rects) on every frame class; a lane too short to carry both rows draws NEITHER, render_canvas's own shape; THE DATA IS THE SOURCE DOMAIN, ALWAYS (the ruled choice, recorded at the painter): the whole-song TARGET domain does not exist as audio (the preview is trim-scoped), so the lane shows the PIECE itself in every view — the bars are the plate renderer's own column writer (render_waveform, two channel bands, kWaveformInk, aliased binary alpha) over the whole piece, the pyramid rung chosen per column by the ONE level owner (GuiAudio::level_for_span at total/width frames per column — the coarse rungs exist for exactly this span, ≤5 pairs per column unconditionally) — and THE BOX DOES THE DOMAIN WORK: in source view it spans the viewport's source span directly; in TARGET view the viewport's target span maps back to source columns through the memoized inverse map, so the box may BREATHE NONLINEARLY across a switch or a tempo edit — correct, recorded. The box is a 1px outline in kOverviewBoxLine (BRIGHTENED off kRedesignLine at the rework — "increase contrast on the outline", the outline being a GRAB SURFACE now; the value is kRulerLabel's own #c2c2c2 spelled as its own constant, derivation at the constant — still grey, deliberately below the paper white so the lane's one WHITE vertical stays the playhead's), inside the content band, its columns from the ONE span owner the hit geometry shares (overview_box_span, hoisted out of the painter at the rework); THE TICK is one kPlayheadStem column at the playhead's source position (the scanner's while live, the resting cursor's otherwise), full lane height across the borders like every position line. THE BARS ARE CACHED (OverviewBarCache, paint_handler.h): rasterized once per (lane width, lane height) — the WHOLE invalidation key, the audio being process-immortal (loaded once at launch, no in-session source load; `'` replaces sidecars, never samples) — so a per-frame tick repaint never re-reads the pyramid. The key's SHAPE is unchanged by commit B's fixed height: the height still rides gui_scale, so it stays a key field rather than a constant the cache could drop, and the width was always the one a resize moves. DAMAGE: the lane is INSIDE Viewport::invalidate_waveform_area's one rect (window top through the waveform's bottom) since commit B put it in the block, so every viewport/zoom change and every discrete playhead write behind that owner repaints it by construction — the DEDICATED RIDER IS DELETED: it was a second rect that skipped the unified bottom row between the waveform and the lane (to spare pan/zoom frames that row's HarfBuzz shaping), and both the rect and the skip are producer-less now, its two inline copies at waveform_cache.cpp's publish sites going with it; the flag-cache rebuild's own rect contains the lane too and owes it nothing on the merits (nothing on the strip keys on the marker stores). The TICK AT SCANNER CADENCE keeps its narrow per-frame pair — its own 1px lane columns at the pre-paint advance, through the shared column owner `overview_tick_column` (the playheads' cadence rule at playhead_pixel_x, app_state.h, extended). INTERACTION (the lane rework, 2026-08-12 — architect-ruled post-relayout, RESOLVING the ratification's recorded later phase: the BOX-DRAG PAN and the TRIM-STYLE ZOOM BRACKETS are BUILT, and this section is that phase's record; OverviewDragState in app_state.h is the code contract): THE BOX ENDCAPS — the outline's LEFT and RIGHT edges as grab handles, hit-tested like the trim endcaps (hit_test_overview_endcap: the 1px edge columns off overview_box_span inflated by trim_endcap_grab_px() per side, nearest-edge arbitration with Begin winning the exact tie; the endcap claim OUTRANKS everything else on the lane, plain only — and the grab went 4 → 10 per side on 2026-08-14, 10 → 15 on 2026-08-15 — both the architect's touch-panel widenings — and 15 → 5 on 2026-08-18, his own narrowing after driving the unified region/trim; the constant is SHARED because the box edge, the trim endcap and (since that day) the waveform trim overlay's two bounds ARE THE SAME GESTURE ON THE SAME SHAPE and so need the same fingertip tolerance by construction, THREE readers moving together. Every retune leaves the arbitration untouched (nearest-edge is grab-independent, so both edges stay reachable at any box width) and moves THE BOX'S INTERIOR: a box drawn narrower than 2 + 2·grab + 1 px — 23 at 10, against 13 at 5, 33 at 15 and 11 at 4 — is all edge handle, so at a WIDE band a press that would pan such a box drags an edge instead, and a narrowing gives that back while a widening spends it: less of the lane resolves as an edge, so more of it resolves as the OUTSIDE press that teleports and then pans. The click-teleport faces the same ring, 10px now; the cost record is at kTrimEndcapGrabPx, render.h) — drag ONE viewport bound: the dragged edge's whole-song position follows the pointer column and the OPPOSITE bound stays fixed, a zoom anchored at the far edge, applied per event through Viewport::apply_strip_drag_zoom with the FIXED bound as the anchor at its own window column (area.w for a dragged left edge, 0 for a dragged right one) — the span-to-level fit formula (level = 1 + log2(span·1000/(0.625·sr·w))) pre-clamped into [kMinZoom, effective ceiling], the kMinZoom floor doubling as the INCLUSIVE CANNOT-CROSS CLAMP at the partner (the trim-drag convention in level space), clamp_viewport_start owning both clamps downstream, every event the synchronous full-render route. PLAIN PRESS INSIDE THE BOX = THE BOX-FOLLOWS-POINTER PAN: the press grabs the box with its grab-point OFFSET preserved, and per motion event the viewport centers on (pointer's whole-song position − grab offset), X ONLY, STRUCTURALLY — apply_overview_drag_at takes no y at all, so vertical motion is ignored entirely ("no cross axis allowance for up/down": this pan has no zoom axis at all); a motionless release inside the box is a consumed nothing (the v1 rule standing). PLAIN PRESS OUTSIDE THE BOX TELEPORTS AT THE PRESS AND THEN ARMS THE PAN (the press-time act architect-ruled 2026-08-17 under CONTENT ACTS THE MOMENT ITS IDENTITY IS CERTAIN — `conventions.md`'s third clause — and the arm added at his live drive on 2026-08-18, "so a finger that keeps moving keeps panning"): the viewport CENTERS on the PRESS column's whole-song position (run_overview_teleport: position = overview_anchor_sample_at_x at the lane-clamped column, start = nearbyint(position) − samples_visible/2, the delta through scroll_viewport's funnel — clamped, zoom level unchanged, a PURE VIEWPORT MOVE: no playhead, no region/selection touch, follow suppressed for the session exactly as a pan, a pan-class producer in the follow_overridden_for_session inventory). AN OUTSIDE PRESS CAN ONLY MEAN THE TELEPORT, so there is nothing for a lift to disambiguate — and SINCE 2026-08-18 IT FALLS THROUGH INTO THE BOX PAN on exactly the terms an inside press does, so a pointer or a finger that keeps moving keeps panning and a motionless release still commits nothing. IT IS ONE ARM, NOT TWO: the teleport has already centred the box on the press column, so the press column IS inside the box by the time the arm below it runs, which is what makes the single seat correct for both entries — no `return`, no second seat and no kind of its own (`OverviewDragKind::Pan`). **THIS IS NOT A BREACH OF THE THIRD CLAUSE BUT THE MARKER FLAG'S OWN SHAPE** — act at the press, then arm the drag the same press may become — because the press means TELEPORT either way and the drag CONTINUES it rather than replacing it; `conventions.md`'s certainty audit carries the classification. THE GRAB OFFSET IS MEASURED, NOT ASSUMED ZERO, and measuring it is load-bearing: it is read AFTER the teleport, and the teleport rounds the position, halves an integer span and then takes `clamp_viewport_start`'s grid snap and wall clamp, so what rests in `grab_offset` is that residue — sub-half-pixel in the interior, and AT EITHER WALL, where the teleport saturated, the whole leftover. Assuming zero would jump the box on the first motion event. DO NOT CONFLATE THIS WITH THE OUTSIDE-DRAG EXTENSION: what came back is the PAN, and a BOUND is still dragged by its own grab band and nowhere else (below). THE TWO-DAY DEFERRAL IS DELETED WITH ITS PENDING KIND (2026-08-15..17, `OverviewDragKind::Pending` — the enum is `{Pan, EdgeBegin, EdgeEnd}` again and `OverviewDragState::moved` means only that a real drag has begun applying): it was taken to close "the last press-time act on the navigation surfaces", a framing that was already wrong when written (the marker flag click and four others outlived it by a day), and its ONE real argument was the touch consequence — two fingers can never land on the same frame, so a press-time teleport necessarily fires on the FIRST finger. THAT ARGUMENT IS CARRIED BY THE DISAMBIGUATION WINDOW AND NOT BY THE LIFT, which is what makes the press-time act safe here where the lane's 2026-08-15 landing model was not: the synthesized press is delivered only when the window RESOLVES to one finger, and a pair landing inside it goes straight to Nav with NO PRESS EVER DELIVERED (the platform's Pending arm), so a fast two-finger landing cannot fire the teleport at all. A force-end (resize, WM close, Ctrl+Q) reaches nothing here either — there is no armed record for it to end. THE OUTSIDE-DRAG EXTENSION IS DELETED (architect 2026-08-15, the same day it landed: "we can remove that, because the threshold for the bounds is fine, the ten pixels on either side works, it's a large enough threshold") — it had resolved the crossing into A BOUND DRAG ON WHICHEVER BOUND IS NEARER THE PRESS COLUMN, decided once and never re-derived, and its resolver went with it; A BOUND IS DRAGGED BY ITS OWN GRAB BAND AND NOWHERE ELSE, `kTrimEndcapGrabPx`'s slack on either side being the whole target (ten pixels when he said it, fifteen from later the same day, five on 2026-08-18, and ten again — settled — since 2026-08-19). `seat_overview_edge_drag` keeps its other caller, the endcap press, and stays the one writer of an edge drag's kind and fixed partner — WHICH IS THE PAINTED EDGE since codex round 22 (2026-08-15): it reads `overview_box_edge_samples`, the owner the box painter reads, because the ruled right-wall grid rest may sit up to one waveform pixel PAST the song end and the box is drawn from the clamped value, so seating the raw `viewport_end_sample` pivoted a begin-edge drag on an invisible past-EOF endpoint — the span biased long, the level a hair low, and the wall clamp able to settle the grabbed edge a painted column left of the finger. ABSOLUTE-POSITION DRAGS, the trim endcap model: NO pointer capture, NO zoom stem, the generic 8px press-becomes-drag gate ahead of the first apply. CTRL PRESS BINDS NOTHING — it carried the DUAL-AXIS STRIP DRAG from the rework until the redesign deleted that gesture whole (state, arm, per-event apply, the anchor stem's third producer, the lane's Zoom cursor arm, the term in `any_pointer_gesture_active` and the vertical rate constant `kZoomStripPxPerLevel`), and a ctrl press on the lane is now a consumed nothing like shift, alt and the right button. THE ZOOM DID NOT LEAVE THE LANE, ONLY THE DRAG DID: the box's own bound drags ARE the lane's zoom, which is the ruling's whole point — "the overview strip can indirectly control zoom by directly controlling the outline box". CURSOR: the endcaps wear the trim pair — TrimBoundBegin (left_side) on the left edge, TrimBoundEnd (right_side) on the right — and the BOX'S INTERIOR wears TrimResize (ew-resize) on the plain hover ("left/right arrows like on plain trim hover"), honest there because the box pan is an x-only slide of the whole span — the trim bridge's own shape. OUTSIDE THE BOX THE LANE ANSWERS TrimResize TOO SINCE 2026-08-18, so the whole plain lane off the endcaps wears one cue — "left/right arrows like on plain trim hover", the architect's words for the band. THE PREMISE OF THE ARROW DIED WITH THE ARM: the band-wide resize cue had been true until 2026-08-15 because an outside drag pulled the nearer BOUND, went to the Arrow at codex round 19 under the map's standing rule that a point ARMING NOTHING shows the Arrow, and comes back now that the outside point arms the PAN. Two standing rulings decide it: THE CUE NAMES THE DRAG, NOT THE CLICK (the marker flag box's own rule — it wears TrimResize while its plain press also runs a click act), and keeping the Arrow would have flipped the cue at the drag's own crossing, the live-drag arm answering TrimResize for a Pan, which is exactly the mid-slide flip the architect closed on 2026-08-13. **THIS IS AN OPEN ITEM AWAITING THE ARCHITECT** — a one-line revert plus its comment restores the Arrow. The inside/outside question no longer reaches the cue at all, so the hover map does not ask `overview_box_span`; degenerate geometry (no box) gives the same answer, its press being an outside press that teleports and pans like any other. EVERY LIVE LANE DRAG KEEPS ITS CUE for the gesture's life, read from `overview_drag.kind` (the trim exception's rule — the two EDGE drags took it at the rework and THE BOX PAN JOINED 2026-08-13 at the architect's ruling, closing the one live lane drag that used to fall back to the uniform Arrow mid-slide); an OUTSIDE press is no longer the exception it was: since 2026-08-18 it seats a Pan like an inside press, so it takes the live-drag arm's TrimResize for the gesture's whole life and the lane has no press left that answers the hover map mid-hold. (Until then it kept the Arrow for the whole hold with no arm of its own, the teleport having run at the press and the hover map answering the point the pointer rested on.) CTRL ANYWHERE ON THE LANE NOW ANSWERS THE ARROW, its Zoom magnifier having gone with the strip drag — the map's standing rule that a point arming nothing shows the Arrow, cue and gesture agreeing by construction; the ctrl+wheel zoom step is still live there and is deliberately uncued, the map answering what a PRESS would do. Shift/alt/mixed presses bind nothing and answer Arrow as before. WHEEL: unchanged — the stepped pan, and `Ctrl`+wheel the zoom step, exactly as over the waveform (wheel_context's context 3, the routing predicate reading no modifier). THE `h` VIEW: the lane stays LIVE — every arm is a navigation-class viewport move the mode admits, the press claim sits ABOVE the mode's pointer gate deliberately (the band-claims precedent; recorded at the claim), and the box reflects the mode's viewport by construction. TOUCH (the lane stays OFF the pan zone, the 60ms/8px window): THE VOCABULARY IS IDENTICAL ON BOTH SURFACES BY CONSTRUCTION, which is what the redesign's absolute capture-free drags buy — everything inherits through the pointer translation with no touch code of its own. A TAP delivers its press+release burst at the LIFT, so a motionless tap outside the box is the TELEPORT — fired by the burst's PRESS, which is where the mouse fires it too since 2026-08-17, the two surfaces agreeing on the act and the window (not the lift) being what keeps a two-finger landing from reaching it; a QUICK DRAG delivers its press at the slop-crossing (the lane is off the pan zone, so the crossing resolves to POINTER and the press lands at the DOWN point), so a drag inside the box slides it and a drag STARTED OUTSIDE it teleports to its down point AND THEN PANS FROM THERE — the mouse's own answer exactly, which is the whole point of the 2026-08-18 arm ("a finger that keeps moving keeps panning"); NOTHING happens at bare finger-down — the window holds everything until tap-lift or slop-crossing (the architect's own question, answered in touch.md's lane paragraph). THE LANE'S VOCABULARY IS THREE GESTURES AND NOTHING ELSE — DRAG THE OUTLINE, CLICK TO TELEPORT, DRAG THE BOUNDS — and THE LANE IS INERT TO NAV GESTURES ENTIRELY, ONE FINGER DOWN MAKING THE SECOND COMPLETELY IGNORED (architect 2026-08-15, from the rig — two rulings the same day: first "let's just make two-finger gestures no-op on the overview strip, it's tiny anyways", after driving the lane's own two-finger BOUNDS gesture through two rounds of fixes, then the generalization — "get rid of all two-finger gestures on the overview strip and on the trim bar; once one finger is down, the second finger is completely ignored, which is what we do with three-finger gestures on the waveform — which makes sense, because the waveform is large and the overview and trim are small"). THE LANE IS THEREFORE ONE OF TWO MEMBERS OF THE THIN-LANE CLASS, with the TRIM BAR — thin, precise, absolute, with nothing a nav gesture could mean there — and touch.md's thin-lane paragraphs are authoritative for the rule and both of its doors. In outline: THE REFUSAL IS AN ACT rather than an omission — the lane's positive wheel context admits nav frames, so a gesture begun here would otherwise fall through to the WAVEFORM's pinch and zoom the view from a strip the user was touching for another reason — and it lives in `apply_touch_nav_update` rather than in `wheel_context`, that predicate being the WHEEL's routing owner and the wheel staying live here. It reads `GuiTouchNavFrame::down_on_thin_lane` ALONE, with no finger-count term, THE FIRST FINGER'S DOWN POINT and never the live centroid: this lane is 26 px tall and its drags are x-only, so a finger dragging a bound wanders off the strip and a centroid test would change the answer under the fingers; and the SECOND DOOR is the platform's own second-finger fork, which ignores a second contact here whether the first has moved or not, so a live lane drag simply continues. THE MIRROR IS WANTED TOO — a pinch that BEGINS on the waveform keeps the pinch wherever its centroid wanders. The one-day PINCH REFUSAL of the redesign is therefore back in force, and the defect it retired stays dead by construction: a pinch centred here computed its anchor with the WAVEFORM's mapping (`viewport_start + centroid·spp`) where a lane column means a WHOLE-SONG position, so it had been anchoring somewhere meaningless the whole time. SO THE LANE HAS NO GLASS ZOOM GESTURE OF ITS OWN, and the piece's zoom is the pinch ON THE WAVEFORM; the lane reaches a zoom through the box's bound drags, one finger or one mouse.

DELETED AND RECORDED SO IT IS NOT RE-PROPOSED ON A HUNCH — THE TWO-FINGER BOUNDS GESTURE (built and deleted the same day: per frame the BEGIN bound went to the LEFTMOST contact's whole-song position and the END bound to the RIGHTMOST, absolute, so spreading the fingers widened the box and zoomed OUT while pinching narrowed it and zoomed IN, mapping through `overview_anchor_sample_at_x` into the edge drags' own clamps; no stem by ruling, no finger-crossing support by ruling). It went with everything built to reach it — the raw contact pair on `GuiTouchNavFrame`, the moved-latch exception that admitted a second finger on this lane, the gesture's own body, and the span application hoisted out of the edge arm for it, which is INLINED BACK where its one caller is. THE LESSON: it was reachable only through the touch layer's one-finger translation, its surface had to be decided at the down point because a 26 px lane cannot hold a centroid, and even then the refusal remained centroid-based — three couplings for a gesture whose whole job the box's own three motions already do. A SECOND CONTACT DURING A LIVE LANE DRAG IS A NO-OP AGAIN and nothing claims it (the architect's own justification, restored with the deletion: it matches what a THIRD finger does on the waveform), so the drag a second finger interrupts simply continues; THE SIMULTANEOUS TWO-BOUND STRETCH is no longer deferred but DECIDED AGAINST, having been built and driven. The capture-from-glass oddity stays CLOSED and is now closed by construction rather than by a modifier no finger can synthesize: the lane has no capturing gesture left at all.

