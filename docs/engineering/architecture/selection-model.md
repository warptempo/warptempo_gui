# Selection, Playhead Re-Coupling, Group Drag, Undo/Redo Visual Language

Moved verbatim from CLAUDE.md in the 2026-07-25 slim-down (project feature-complete). This file is now the authoritative home for this detail; CLAUDE.md carries only a digest and a pointer here. Keep it current with landed rulings.
Continues from pointer-hit-testing.md; continues in marker-ui.md.

SELECTION AND PLAYHEAD ARE RE-COUPLED (Ableton-parity arc step 1, architect-designed 2026-07-23). GENEALOGY: coupled originally → DECOUPLED 2026-07-20 (Ableton-shift phase 3, for the lead-in workflow: parking the playhead upstream of a marker to audition the approach into it) → RE-COUPLED 2026-07-23 — a reversal-of-a-reversal that is legitimate because the coming Ableton-style SCRUB AREA supplies the upstream-audition workflow the decoupling existed for. THE INVARIANT: clicking a marker ALWAYS lands the playhead on it — the PLAIN marker click (flag shape or lane run, both views/columns, read-only allowed) single-selects AND seats the playhead on the marker with NO viewport move (direct cursor write, not move_playhead_to — the sole difference from Tab, which recenters, and `c`, which re-zooms; the two-step placement basis `source_frame_to_active_domain` then `clamp_playhead_to_live_domain`; runs on EVERY plain marker click, the double-click-consume path included, and clears the region like every playhead-jump) — and the playhead FOLLOWS the focused/grabbed marker through nudges and drags UNCONDITIONALLY (the coincident-ride at-grab tests and the `playhead_rides` fields are DELETED; most focus routes land the playhead already, and a focus set WITHOUT a land — a Ctrl+click toggle-remove's focus repair, undo/redo's touched-set selection — is towed onto the marker by the first nudge). The satellite family died with the tests: the 2026-07-22 ALT-EXACT FLAG CLICK land route is RETIRED (the plain click does it; alt-exact over a marker is inert), and the accepted costs #alt-click-offscreen and #nudge-ride-next-press dissolved. THE SELECTION MODEL (arc step 2, same ruling): Shift+click on a marker is an INCLUSIVE RANGE select, file-manager style — the first shift-click of a continuous shift-held interaction ANCHORS (`app.shift_range_anchor`, session scratch: seeded by ADOPTING THE FOCUS when no anchor is live — so plain-click A then shift+click B ranges A..B, the file-manager gesture, and any platform-side anchor loss self-heals from the focus (the labwc round-2 root fix; an anchorless click with nothing focused anchors on itself); its release owner is the PLATFORM's shift falling edge — an installed `set_shift_released_hook` fired at the xkb modifiers callback's held→up transition, keyboard leave, and keyboard-capability loss, since bare modifier traffic never reaches dispatch and next-command polling could not see a release+re-press — and every other Selection mutator clears it too as the index-invalidation owner, so the Ctrl+Shift+Z redo, which holds shift through its store mutation, closes via sanitize_selection_after_restore's clear), and each successive shift-click REPLACES the selection with the inclusive index range between the anchor and the clicked marker (stores are time-sorted, so index range == time range; clicked = range end = FOCUS, while the click LANDS the playhead at the range's EARLIEST member — land and focus DIVERGE by ruling 2026-07-23, a Space then auditioning from the range's start and the first nudge towing the playhead onto the focused marker); Ctrl+click on a marker is the individual membership TOGGLE (the former shift behavior — a ctrl-exact top-strip marker press, AMENDING the "Ctrl keeps only the letter chords" rule for this one surface; the ctrl-exact WAVEFORM press keeps the zoom-strip drag, different surface, no collision; whether it adds or removes, the toggle lands at the EARLIEST selected marker afterward — an emptied selection lands nothing and just drops the region); neither shift nor ctrl clicks arm drags, seed/consume double-clicks, or open editors; the shared land is `land_playhead_on_marker` (input_pointer.cpp), used by plain, shift-range, and ctrl-add clicks alike. Mechanics: waveform plain/Shift presses stay marker-blind (above); the follow moves the RESTING cursor playhead only (nudge still stops playback first; the drag never stops playback and a live scanner is untouched — move_playhead_to's scanner-inactive guard owns that), all target mapping through the LIVE memoized cache (no per-drag map copy); the drag's commit lands the playhead against the POST-commit map (after store write + reorder, before kick_waveform_sync) and runs regardless of net change so a wander-back drag lands exactly back; the drag's Esc-cancel restores the SelectionSnapshot AND the grab playhead (`pre_ride_playhead_sample`, always applied; trim cancel still touches no playhead); THE GROUP DRAG (architect 2026-07-23, reversing the recorded single-marker-only drag rationale): dragging a member of a 2+ selection moves the WHOLE selection rigidly — the plain flag press on an already-selected member DEFERS the click's single-select+land to a motionless release/lost-button (`PendingMarkerDrag::deferred_click`, file-manager convention; Esc ABANDONS the deferred click, the multi-selection stands; the deferral requires the press to be one that would arm a drag, so read-only, off-home, and lane-run presses keep the press-time collapse — and since 2026-07-23 the W+target TEMPO surface defers too when the grabbed marker is tempo-drag-ELIGIBLE (an ineligible grab arms nothing and keeps the press-time collapse)), begin_drag seeds every selected marker with the grabbed one as the delta/follow/land reference (`DragState::grabbed_k`, a vector POSITION never remapped — hit_marker stays the remapped store index), the walls intersect in the ACTIVE domain (architect 2026-07-23, retiring the briefly-recorded target-view squash): each member contributes [fwd(0)−fwd(orig_k), fwd(eof_wall)−fwd(orig_k)] through the displayed map and apply_drag_motion clamps the POINTER delta once against the intersection, so the group stops AS A UNIT the instant the FIRST member's image reaches its wall in BOTH views — source view is the fwd-identity special case, bit-for-bit the prior behavior; the grabbed member's viewport clamp folds in the same domain (viewport_marker_bounds is already active-domain) and is WIDENED TO CONTAIN ZERO (codex round 5: flags cull only when FULLY offscreen, so a half-offscreen flag is a legal grab whose center sits outside the strip — the naive pair would exclude 0 and the first motion would jump the group inward against the pointer; the clamp only forbids moving FURTHER out), and the per-member [0, eof_wall] source clamp survives as fp-safety only, the viewport clamp stays GRABBED-ONLY (blind motion of a group held by its on-screen handle, the trim-pair precedent — the offscreen ruling forbids blind grabs, not blind motion), the THRESHOLD CROSSING (begin_drag, after every pre-drag capture) FOCUSES the grabbed marker without collapsing membership (`Selection::focus_without_collapse` — anchor dissolved, no size crossing; moved off the first-moved-motion edge in codex round 2: a WALL-SATURATED drag clamps every proposal to a no-op and would never fire an any_changed-gated transfer, stranding focus on the pre-drag marker while commit's unconditional land parks the playhead on the grabbed one — a crossed drag is real whether or not the walls let anything move; the lane run tracks the grabbed member via the DragOverlay membership lookup that replaced the moveable_times[0] substitution), the COMMIT is rigid too — the grabbed member column-snaps and the group rides its uniform active-domain delta through snap_authored_frame (see the pixel-anchoring section; per-member column snapping was the codex round-3 spacing defect), an ACTIVE extent region live-tracks the moving group's clamped displayed-basis extent per motion and re-derives from the POST-commit store via set_region_to_selection_extent (now externally linked; a third caller MAINTAINING an existing highlight through the store mutation, regardless of net change so a wander-back drag snaps the region home — a drag never CREATES a region: an inactive region stays untouched, preserving "programmatic selections define nothing"), and Esc-cancel restores selection snapshot + grab playhead + the pre-drag region (`DragState::pre_drag_region`; all three no-ops for a single drag whose press landed and cleared); the TEMPO gestures went GROUP 2026-07-23 post-labwc (see the group-tempo passage at the home-view binding's tempo exception) and the POSITION NUDGES went GROUP too (session #4, retiring their collapse_to_focused): Alt+Left/Right with 2+ selected moves the whole selection rigidly — the phase nudge in its target home view, the WARP nudge in its SOURCE home view only (the 2026-07-24 both-views "third exception" was re-ruled away the same day — W+target Alt+Left/Right is the TEMPO-IMAGE STEP, see the home-view binding) — the FOCUSED marker is the pixel-anchored member (its painted column steps one, the group riding its uniform active-domain delta D as computed positions, never per-member re-snapped), warp-in-SOURCE keeping the clamp regime group-intersected (D clamps ONCE into the member wall-headroom intersection, unit stop at the first member's wall, the clamped anchor plain-rounding — walls win over the grid, D never recomputed) while the MAPPED-domain path — the phase-reset nudge, its ONLY remaining consumer after the warp-target branch's 79b73d3 harvest — refuses all-or-nothing group-wide (any member's snapped proposal outside [0, total−1] refuses the whole press; riders go through `unclamped_target_to_source` in warp_frame_map_view.h — phase-only now, the recorded asymmetry — which extends segment 0's slope backward below the map's first breakpoint so the exact integer wall belt sees an out-of-range proposal instead of the standard inverse's clamp hiding it at 0 — the refusal lives on snapped integers against exact integer walls, never a float pre-filter); one coalescing-eligible undo entry per press with the group's touched hints (touched_snapshot fixed at the first press's pre-burst coordinates, touched_live REFRESHED by continuation presses via Undo::refresh_coalesced_touched_live since a later press in the burst can reorder further), the playhead following the FOCUSED member's committed frame, and an ACTIVE SelectionExtent region re-deriving post-commit via set_region_to_selection_extent (MAINTAIN only — a nudge never creates a region; Free/TrimWindow untouched, position gestures never re-sync TrimWindow); undo/redo ADOPTS THE GROUP VISUAL LANGUAGE (architect 2026-07-25, fourth formulation — superseding the 2026-07-20 shows-its-target-without-the-playhead ruling; the group-action principle decided it: group selection uses the region highlight, so group undo/redo does too — the region IS the group's playhead stem): the touched set is SELECTED (a removal clears instead) — identified by EXPLICIT IDENTITY HINTS for the reposition drags (`UndoEntry::touched_snapshot`/`touched_live`, index sets in the entry-snapshot and op-after-state coordinate spaces that commit_drag fills and the counter-entry SWAPS; the diff-based row matcher stays the hint-less fallback for every other producer, being structurally blind to a moved row landing field-identical on another — a translated group or a column-snapped single drag onto a row-identical marker; codex round 1) — with focus defaulting to the EARLIEST touched marker (the stored focus hint is RETIRED 2026-07-25 — all members are equal, a singleton's earliest IS the touched marker; the counter-entry survives as the redo stack entry, carrying the swapped identity hints and the op's lateral bit). The entry's A/B tab is switched in when foreign (the Ctrl+Tab-equivalent band restore — `UndoEntry::tab` and the target-tab read-only peek are load-bearing for it), and the restore's VISUAL TAIL (undo.cpp, after sanitize — the region write must follow sanitize's provenance demote, the demote-then-derive order — gated off settings-only entries, branching on the POST-sanitize selection size) splits: a SINGLETON restore LANDS the playhead on the touched marker through land_playhead_on_marker (dissolving any resting region, exactly the plain marker-click land; offscreen → plain recenter at the CURRENT zoom) (the singleton's always-on stem then follows from the selection itself — the UndoEntry::lateral bit and the restore-tail stamp were harvested in the 2026-07-25 blue-focus pivot; for a tempo drag / tempo-image step the touched row is the PREDECESSOR — the cents receiver — so the restore's land and stem sit there, deliberately); a GROUP restore LANDS the playhead on the EARLIEST touched member and THEN sets the SelectionExtent REGION to the touched set's extent through the one owner set_region_to_selection_extent (architect 2026-07-25 second ruling, superseding the same-day cursor-untouched form: the extent's split half-triangles ARE the dissolved cursor playhead, its left half ON the earliest member — the spot the double-Esc collapse parks at — so the land makes the bottom-strip timestamp, the Esc-collapse park, and Space's left-bound launch agree by construction; land-then-extent is the multi-select clicks' earliest-land order, the land's region dissolve overwritten by the extent write; region.active still suppresses the cursor line, so the only visible delta is the timestamp readout — an all-coincident touched set rests an ACTIVE zero-width region, the split-playhead's single-mask case, matching the clicks bit-for-bit; undo/redo JOINS the extent-region writers), and when any touched member is offscreen PREFERS A PLAIN SCROLL at the current zoom (extent strictly narrower than the visible span → center it, the singleton recenter's group sibling; architect refinement 2026-07-25 post-labwc), ZOOMING only when the group cannot fit at the current level (the shared framer as the cannot-fit fallback — fit level + 2.5%-per-side margin, centered, no playhead recenter; it then only ever zooms OUT; the zoom-row double-click keeps its unconditional zoom-to-span) — fully visible means no viewport write. The teardown deleted `Selection::sync_playhead_to_last_selected` and `Selection::jump_playhead_to` (undo was their last caller) and shed `Selection`'s now-dead `GuiPlayback` dependency. KEPT playhead-onto-position writes that are drop CONSEQUENCES, not selection syncs: `s`/Alt+S seat the playhead on the marker they create (Alt+S in target view lands it at `playhead − N/2`, the lead-in). BPM-editor open (`m`) and the flag editor's in-edit retarget click no longer move the playhead either (shared `enter_text_edit` chokepoint). 