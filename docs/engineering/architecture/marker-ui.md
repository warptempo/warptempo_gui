# Marker UI (flags, lane text occlusion, stems, focus model, editors, key repeat)

Moved verbatim from CLAUDE.md in the 2026-07-25 slim-down (project feature-complete). This file is now the authoritative home for this detail; CLAUDE.md carries only a digest and a pointer here. Keep it current with landed rulings.
Continues from selection-model.md; continues in region-scrub-esc.md.

THE MARKER REDESIGN, FINAL FORM (third labwc pass, 2026-07-20 — the earlier text-chip and half-triangle models are RETIRED): every flag is a TEXTLESS UNIFIED SHAPE — a `kFlagWidthPx` × `kFlagHeightPx` rectangle (flag lane; 15 × 17 at the default font since the 2026-07-27 asset shrink, and every figure below is that default — the painted sizes are the AUTHORED constants scaled by gui_font_scale(), so read the accessors, not these numbers, when the font size moves) fused with a tip-down triangle beneath it whose slopes leave the rect's OWN bottom corners (triangle lane; widest row = kFlagWidthPx, H = (kFlagWidthPx+1)/2 = 8, odd columns so the tip centers — the corner-inset model's 90° seam jog is extinct: a top row with 45° corner chamfers is collinear with, hence identical to, this shape), CENTERED on the authored frame with the tip marking it, painted by ONE `paint_flag_shape` (fill + a single continuous outside-only outline; THE ALIASING RULE: perfect vertical/horizontal 1-px lines are hard at +0.5, DIAGONALS anti-alias — the relaxed 2026-07-20 convention; the WAVEFORM PLATE is EXEMPT from this rule since the 2026-07-26 draw arc: render_waveform is a direct ARGB32 coverage writer — per-column raw min/max interiors (opaque, REPLACE-written) plus Wu-style antialiased TIP POLYLINES connecting adjacent columns' top tips and bottom tips (max-composited; a thin column draws NO interior — its two tip segments are the whole rendering, painting the subpixel extent as a soft partial-coverage band (raw-tip bands, the ruled reading; the segments reduce to one only at exactly zero extent) with a >=1-pixel-equivalent never-fade floor per column) — this IS the once-parked Ableton-style hand-rolled per-column coverage write, proven free of the 2026-07-21 revert's cost because the general cairo AA rasterizer never runs (the revert's actual culprit); the 2026-07-21 aliased-but-fast ruling is SUPERSEDED for the plate (the flag/chip shapes keep the hard-vertical/AA-diagonal convention above); disabled is an OPAQUE COLOR CLASS (the kMarkerDisabled/kMarkerDisabledOutline pair, Breeze ContrastFade 0.65 derivations — the cairo-group alpha dim died with every compositing alpha in the 2026-07-26 recolor arc); the color class is DISABLED > NORMALIZATION-RED > default, THREE classes and SELECTION IS NOT ONE OF THEM (architect 2026-07-27, deleting the kSelected/kSelectedOutline keys) — a flag whose marker renders as the 1.00 fallback paints kAccent fill + kAccentOutline ring whether or not it is selected (the earlier selected pair masked that cue), and a disabled marker paints BOTH halves of the disabled pair whatever its red status and whatever its selection (the earlier disabled+selected composition, which kept the disabled fill under the selected ring, is gone — once the selected pair compiled to the marker pair that ring read as switched ON; a disabled marker's lane text paints kTextDisabled). A selected marker of any class paints exactly the pair it would paint unselected; selection's ONLY paint on the flag is the TRIANGLE INTERIOR, filled kWaveform (architect 2026-07-27) — the rectangle fill and the WHOLE outline, triangle slopes included, stay exactly what the class resolved, so the guarantee above is unweakened: it is layered AFTER the ladder and is therefore not a fourth class. The cue inventory is that ink triangle plus kSelectedStem, the z-order lift, and the playhead land — see the normalization-red rule in the load-lenient section above). The playhead triangle is the IDENTICAL anti-aliased shape (15×8 at the default font) (its cached A8 mask AA-baked; the mask width is `2·playhead_triangle_h_px() − 1`, so the SCALED flag width is forced ODD at `flag_lane_w_px` — an even round would leave the mask one pixel narrower than the flag and break the marker/playhead identity at non-default font sizes). DURING A STRIP DRAG an ANCHOR STEM (`kStripAnchorStem`, its default the kLine value since the 2026-07-26 line unification (current defaults live in the canonical block, docs/engineering/color-configuration.md) — a muted structural value, dimmed by choice of value not by alpha, its own config key so it can be tuned off the line color; hard 1-px vertical, painted straight over any ink it crosses — the ink-notch overdraw was retired with the 2026-07-26 polarity inversion) paints at the pivot's clamped column — the Ableton affordance that makes the edge clamp visible. Flags may hang HALF OFFSCREEN (cull only when fully out); the hit area is the rectangle PLUS the fused triangle — a point in the triangle lane hits when it lies inside the row-dependent taper, computed from the ONE shared owner `flag_triangle_half_width_at` (render.h) that `paint_flag_shape` fills from too, so the clickable slope and the painted slope cannot drift (the playhead triangle paints in the same lane but is not a hit target — it is not in the emitted rect list, so a marker wins where they overlap). The PLAYHEAD TRIANGLE is the same mask drawn in the triangle lane, its width the flag width by construction (2·H−1 = flag_lane_w_px, 15 at the default font; tip at the waveform's top edge). Trim chips reuse the shape WITHOUT the triangle and at their OWN aspect — SQUARE by construction since 2026-07-27, height = flag_lane_w_px = the chip's own width (15 at the default), so a chip is shorter than a marker's rect rather than matching it (the calm kTrimChip/kTrimChipOutline pair since 2026-07-26 — the bright pair moved to the bridge bar; textless) and EDGE-ANCHOR on their bounds — begin's LEFT edge on the column, end's RIGHT edge (FlagHAnchor; fully visible at frame 0/EOF, never half-offscreen — bounds are edges, the recorded asymmetry vs centered marker flags), with TWO-SEGMENT stems joined through the lanes (both segments in the ONE live paint_trim pass since 2026-07-25 — no cross-cache join) and the bridge bar/press being the true inter-chip gap. The bound's rounded column is CLAMPED into `[0, W−1]` at every mapping site through the ONE column owner `trim_bound_column` (render.h — the audit-C1 collapse of the former hand-copied trio in render_trim_flags, render_trim_stems, and hit_test_trim_chip) — so the inclusive END wall `total−1`, which rounds to column W (one past the surface) at full zoom-out, lands on the last visible column instead of losing the chip's bound-edge pixel and both stems to the cache clip; one clamp owner keeps paint and hit column-coherent by construction. The bridge BAR's gap interval, by contrast, is SIDE-AWARE for an OFFSCREEN bound (TrimBoundColumn carries the side from the SAME unrounded compare that decides in_viewport — col_raw's rounding cannot tell the side within half a pixel of an edge): an offscreen bound's gap edge is a side-specific sentinel STRICTLY past the visible range, so the bar fills flush to the edge with no chip-width gap — a BARELY-offscreen bound included — the offscreen-side border goes offscreen with the chip, and a window wholly offscreen on either side yields an EMPTY gap (an in-viewport bound, EOF included, still bounds the gap at its clamped chip's inner edge); the painter clips the drawn interval to the effective width, so the inert right gutter never paints; the gap interval has ONE shared owner (`trim_bridge_gap`, render.h) consumed by the painter and route_trim_chip_press's pair-drag between test alike, so the clickable bridge equals the painted bar exactly, with the router additionally gating clicks into [0, W) so the inert right gutter can never arm the pair drag. TRIM PAINTS LIVE, BELOW THE PLAYHEADS (architect 2026-07-25 — the trim-stem-over-playhead-triangle fix): every trim pixel (chips, bridge bar, both stem segments) paints per frame in GuiPaintHandler::paint_trim, in the old stem-cache slot — after the phase overlay, before the selected stem and every playhead element, the flag blit still after — so the order is STRUCTURAL: trim < selected stem < playheads < marker flags/triangles/text (markers over trim by pass order, the playhead triangle over trim, the marker-flags-above-the-cursor z-order flip untouched); the pass rides the FREE displayed item basis + the shared trim geometry owners (paint == hit), runs whenever the exposed rect touches the top strip OR the waveform (render_background erases exposed strip pixels, so unrelated strip damage must repaint live trim), and paints trim on cold pre-first-publish frames the old null-cache skip left blank (intentional improvement). The trim-only STEM CACHE and the flag cache's trim pass/fp_trim_* fields DIED with this — the flag-cache rebuild is now the SOLE stager of the displayed item basis (verified single-writer). MARKER TEXT lives in the TEXT LANE and the bottom strip, under THE OCCLUSION RULE (architect 2026-07-24, superseding the 2026-07-20 one-run model): the lane shows EVERY onscreen marker's text ambiently IFF the whole visible set — the active column's markers whose flags paint (the flags' half-offscreen cull), DragOverlay proposed positions mid-drag — fits with no run overlapping another, all-or-nothing; each ambient run is capped at 9 GLYPHS (a longer composed text displays as its first 8 bytes + `…` U+2026 — DISPLAY-ONLY, the store/sidecars/editor seed/hover fields/copy payload never see it; ASCII-only input makes `…` a reserved truncation marker, and ALL width math runs on the run's GLYPH count, never display-byte size — a truncated run is 11 bytes/9 glyphs), the verdict compares clamped rects on the shared placement owner with abutting legal (half-open compare; coincident stacks and same-edge clamp collisions fail by construction, and the verdict can flip mid-drag — live truth like the flags); when the verdict FAILS the lane falls back to EXACTLY the one-run arbitration — the HOVERED marker's own value (both columns, through the ONE flag-text composer the editor seeds from — `flag_text_iter`; phase resets show the literal `p.r.` (4 glyphs since 2026-07-27 — the 1-glyph `p` sat dense reset clusters right at the lane fit verdict's threshold, blinking all-texts/fallback under zoom; the wider token fails the verdict decisively and rests stably in fallback)), else the LAST-SELECTED marker's value CULLED by its painted column (a fully-offscreen marker paints no flag, so it shows no run; the bottom readout is the position-free surface; during an active drag whose grabbed marker is the last-selected one the run centers on the LIVE proposed position from `app.drag.moveable_times` — drag-only, nudges mutate the store so their runs already follow), CAPPED like the ambient runs (TRUNCATION IS PERMANENT, architect labwc feedback 2026-07-24 — the fallback run shows the same 8+ellipsis form, so a dragged truncated marker never spells out on a verdict flip; the editor seed/readout/copy payload keep full text). THE HOVER EXPANSION (same feedback — the ONE sanctioned text occlusion beside the editor): hovering a truncated marker's rendered TEXT RUN — the text part, never its flag (hover_popup carries the MarkerHit part bit; the recompute short-circuit compares index AND part) — expands that one run to the FULL text in place, painted last over the ambient runs, which keep painting occluded beneath it; hit-tested FIRST with the expanded rect, which provably CONTAINS the capped rect under every clamp case, making the hover a stable hysteresis latch (the convergence loop settles by pass 2 — an applied hover's expanded rect has precedence); the expansion never enters the occlusion VERDICT (capped widths only) and applies in both modes. ONE resolver owns the run set (`current_marker_lane_runs` returning LaneRunSet — paint and `marker_hit_at` read the same runs, so painted and clickable cannot drift; all-visible runs hit with HALF-OPEN x intervals so abutting runs cannot double-hit, the fallback run keeps the closed compare byte-identical), and ALL lane geometry rides the DISPLAYED VIEWPORT BASIS beside the displayed map (`item_viewport_basis`, app_state.h — renamed from `displayed_viewport_basis` 2026-07-30, the epoch split — the vp_start/spp the committed flag item cache baked, staged by the flag-cache rebuild (the sole stager since 2026-07-25) and promoted in LOCKSTEP with the displayed map in the one staged_displayed_valid block, cleared at the same two clear sites — load/adopt and the S/T toggle — cold falling back live; DELIBERATELY distinct from GuiPaintHandler::plate_viewport_basis (its own 2026-07-30 name; the two accessors shared one `displayed_viewport_basis` spelling before, which C++ scoping resolved silently), which locks the PLATE-registered overlays to the live wf_cache.fp_* — plate vs item caches, TWO OWNERS as a mechanism/lifecycle split: direct fp read vs the staged-then-promoted mirror; the one-frame divergence window that originally motivated the split CLOSED 2026-07-25 when the worker publish joined the pan and sync paths in rebuilding the flag item cache INLINE before its committing damage (the stem cache died 2026-07-25), so the two owners are EQUAL at the three plate-writer commits (worker publish, sync rebuild, pan) by construction — but NOT unconditionally: a RESIZE can promote an item-only rebuild (old fp_vp span over the NEW effective width) while the old plate still displays at its old fp_area_w, the accepted item-only-promotion window (stated consumer-side at paint_trim; 2026-07-25) — kept separate as real, if rare, divergence, not merely lifecycle clarity), the flag pixels' own basis, so runs cannot jump off their flags during an async publish window. THE EDITOR IS AN OVERLAY (superseding "one occupant, editor wins"): the open flag editor suppresses ITS OWN marker's ambient run and paints its full-text box LAST over the remaining ambient runs, which keep painting per the verdict (the verdict always runs on the capped widths — the editor's full-width box never participates, its marker's capped run does) — while the pass/ref RESOLVED readout (the parser-composed, notice-free `compute_hover_popup_text`) renders in the bottom strip's restored hover slot under the old `popup_eligible_marker` gate — hover wins, else the LAST-SELECTED eligible marker's readout shows — two surfaces, one hover state (`lane_text`/`readout_text`/`any_visible`), and NO GESTURE HINTS anywhere (standing ruling: the user reads HELP). The Enter flag editor (kMarker fill + kMarkerOutline ring — the one live marker pairing, which is what a selected chip paints too — the editor box is a rectangle with no triangle, so selection's ink-triangle fill has nothing to act on here and the pairing is the same either way, so the kAccent flash on an invalid commit is a color change on an already-visible box, ruled 2026-07-22; caret + click/drag in the lane's rendered run via the one `lane_text_left_x` origin; ANY non-caret click closes it guard-free — see the marker-unification rules below) — both centered over the marker and clamped fully onscreen, editor beats popup; the iter bracket surfaces through them. STEMS live in the WAVEFORM ONLY (top at waveform.y — the flag+triangle is the connection above; the overhang, the dimmed group composite, and the see-through class died by geometry) and are ALWAYS-ON FOR THE SINGLETON SELECTION (architect 2026-07-25 — superseding the round-4 working-with-it conditional model and the whole apparatus that served it) with NO exception at all (the active-region suppression died 2026-07-30 with the span form — the region is trim scratch, not a playhead, and outranks nothing): the one selected marker's stem paints kSelectedStem — its OWN palette key since 2026-07-27 (it briefly rode kSelectedOutline; separated because a full-height line reads far louder than the same value as a 1px flag ring, so ring and stem tune independently) as a per-frame live overlay (paint_selected_stem; render_playhead's line-only form, painting straight over the plate ink — the ink-notch overdraw retired 2026-07-26 with the polarity inversion) whenever the selection is exactly one marker — hover-independent, playback-independent (it persists through scrubs and auditions), both columns and both audio views — at the store frame through the displayed map, or at the LIVE proposed position via the DragOverlay during a position drag (the one surviving override, and the position drag is now the ONLY marker pointer gesture — the tempo drag, which needed no arm because its per-step sync re-warps slid the store frame's image under the stem, is deleted, contortion ruling 8). HARVESTED WITH THE PIVOT, assumptions included: the hover arm and the 2026-07-24/25 hover-latch history, the LATERAL-GESTURE PIN (stem_pin_marker/stem_pin_command_seq, its five stamp sites, the on_tick reaper, StemPinPreserveGuard and the platform damage_seq counter — damage-quiescence died as a concept), UndoEntry::lateral, and the click-site stem damages. STEM-TRANSITION DAMAGE HAS ONE OWNER: Selection::stem_subject() (the singleton's active-column source frame, else none — frame-based, remap-stable, the phase_overlay_subject sibling) captured before every membership mutation, and damage_stem_on_subject_change(old) FULL-damaging the waveform area (since 2026-07-30 — the narrow item-basis invalidator `invalidate_stem_column` is DELETED: the stem paints on the PLATE basis, so its item-basis columns were the wrong coordinate epoch inside the resize item-only-promotion window; a stem subject change is a rare discrete selection event, so the widening is the ownership-window-proof shape) — wired at the same ten mutator/wholesale sites as the overlay owner; frame/image-moving gestures (nudges, drags, re-warps) already full-damage, so the subject owner covers exactly the selection-driven appear/move/disappear set. THE FOCUS MODEL (one form since 2026-07-30, the region simplification): the cursor playhead ALWAYS PAINTS — the kPlayheadCursor line + triangle at the resting cursor, whatever the selection and whatever the region; the focus cues layer beside it: the singleton's always-on STEM (kSelectedStem, no suppression of any kind) and, for a group, the members' kWaveform ink triangles with the cursor standing on the landed focus (the extent-region ground, the split half-triangles, the cursor suppression, and the spanless-group analysis all died with the span form; the per-member focus triangles and kPlayheadCursorFocusGrey were removed 2026-07-25 — ONE focus family). THE Z-ORDER FLIP (same ruling): the cursor playhead paints BEFORE the flag blit, so a marker flag occludes it — a cursor resting on a marker tucks behind that marker's flag (identical triangle geometry at the same column); no selection gating, pure z-order (a deselected-but-present marker still hides a coincident cursor triangle — accepted, the cursor's full-height line below the strip conveys the position); the scanner, Space's lead-in audition, and the marker-anchored phase-reset overlay are all unaffected; region.active still outranks both with the split halves). THE EMPTY FLAG/TRIANGLE-LANE press has waveform parity (R6): plain click = deselect + playhead placement + region-drag arm (the shared place_playhead_and_arm_region body; playback stops are CLAIM-SITED since 2026-07-27 — there is no blanket top-strip stop any more — and this claim carries no stop call, so a live session reseeks instead of stopping), plain DOUBLE-CLICK creates a marker at the clicked position — the AUGMENTED drop (warp copy-previous / phase reset N/2 lead-in), which since 2026-07-28 is the only drop bare `s` performs and therefore the only one this surface performs, reached through the standing _at_playhead chokepoints (DoubleClickSurface::EmptyLane, position-keyed) — home-view + read-only refusing silently. The SHIFT+double-click augmented variant and its seed-only first press are RETIRED with the plain/augmented convention: a shift press on these lanes now claims nothing at all and falls to the inert top-strip return, alongside the SHIFT-exact chip-row press. GESTURES: plain flag CLICK = select (both views); plain flag DRAG = move — the press single-selects and arms a `PendingMarkerDrag` (a first-class armed gesture: any_pointer_gesture_active, the key gate, and the force-end finalizer `finalize_active_drags` — which replaced `cancel_active_drags` 2026-07-29 — all include it, so the viewport cannot shift between press and crossing), and crossing the one generic `kDragMovedThresholdPx` (= 8 px since the 2026-07-24 unification — markers keep their grab slop, and every other surface rose to it) calls `marker_drag.begin_drag(marker, press_x)` — anchored at the PRESS x so the first apply folds the whole catch-up — then rides the existing drag machinery unchanged; Shift never arms; read-only selects but never arms. ENTER (bare Return/KpEnter, no editor open) opens the flag editor on the FOCUSED marker (repair_last_selected; W view only — phase resets have no per-flag editor; read-only refuses at the allowlist; opens with the payload selected (open-selected)); a plain DOUBLE-CLICK on a warp flag opens the SAME editor on that marker (`enter_top_flag_edit`, the first click having single-selected it and armed no drag; P view and read-only refuse silently, matching Enter). All double-clicks are hand-rolled (Wayland delivers none) and arbitrated by ONE surface-tagged `DoubleClickCandidate` (surface ∈ {ZoomRow, Marker, EditorText, EmptyLane — the R6 create-drop}, `kDoubleClickMs` = 500 / `kDoubleClickSlackPx` = 8, Marker additionally keyed on the marker index — the marker is ONE pointer item, flag shape ∪ lane run, see the marker-unification rules in the redesign paragraph below; the run resolved by the shared `current_marker_lane_run`, render.cpp, the ONE hover-else-last-selected arbitration paint also consumes, so paint and hit cannot drift) so the surfaces cannot cross-fire; the region drag additionally dissolves a resting highlight narrower than `kDragMovedThresholdPx` at gesture end (`end_region_drag_min_size_check`, both end points — the latch-once arm gate could otherwise rest a jitter sliver), and the text-editor double-click selects the maximal run of the clicked character CLASS (word `[A-Za-z0-9_]`+bytes≥0x80 / whitespace / punctuation, never crossing a class change — the Qt/Kate convention; `select_word_at`'s own classifier, deliberately distinct from the Ctrl+Left/Right motion scanners; non-ASCII input is UNSUPPORTED — the ≥0x80 word-classing is graceful degradation, a selection never splits a multibyte character, nothing more); a text-editor double-click selects via `select_word_at` (since 45550ee a class-run selector — see the LaneText sentence below — no longer the Ctrl+Left/Right scanners; the editor core already carried full selection-range/replace/erase). THE MARKER IS ONE POINTER ITEM AND THE EDITOR LIFECYCLE IS GUARD-FREE (architect 2026-07-22, ending the two-surface guard class the chip/text split had spawned — the own-flag swallow, the alt carve-out, and the discard bool are DELETED): a marker is hit by its FLAG SHAPE or its RENDERED LANE RUN (one file-local `marker_hit_at` resolver over `hit_test_flag` + `current_marker_lane_run`); a plain click on EITHER part single-selects and lands the playhead (the flag alone stays the drag handle — the run never arms), Shift range-selects from the FOCUSED marker (the anchor adopts the focus when none is live — the labwc round-2 root fix) and Ctrl toggles membership, both landing the playhead on the FOCUS they set (R1; land-on-the-focus since 2026-07-28) and both defining the multi-select highlight at 2+, and a double-click on either part opens the editor (surface `Marker`, target = index, ONE seed timing — the PRESS — with the threshold-crossing clear covering moved drags; the release-time seeding is retired). While the editor is open, a click in ITS rendered text places the caret (F2.1); ANY other click CLOSES it without committing (exactly Esc's teardown, `exit_top_flag_edit_no_commit`, Enter stays the sole commit route, placed ahead of every band claim below it so the close is unconditional) and the press then acts normally — a double-click on the open editor's own marker is therefore close-then-reopen, which IS the documented double-click-opens rule, no special case. EVERY editor opens with its SEEDED content fully selected (open-selected, one universal rule at the `enter_text_edit` chokepoint — type replaces wholesale, bare Left/Right collapse to the extremes; the BPM editor's seeded bracket opens selected through the same chokepoint; the blank-seeded settings/commit editors degenerate to caret 0 with no carve-out; the click_x caret-seat is retired — a specific caret spot is a click inside the open editor). SPACE IS THE SOLE PLAYBACK TOGGLE (`is_play_pause_key` = Space, bare-exact at its one shared owner; architect: "i never use enter for playback"), which also drops Return/KpEnter off the read-only allowlist — the desired silent Enter refusal there. Space carries a SECOND binding since 2026-07-28: the phase-reset LEAD-IN AUDITION, moved off Alt+Space when alt left the keyboard and gated on the reset being FOCUSED (`Selection::phase_overlay_subject`) rather than on the overlay being painted — the `N/2` geometry and the full ruling live in render-pipeline.md, which owns it. Key REPEAT is ELIGIBILITY-GATED at press time (`repeat_eligible`, an application probe the platform consults BEFORE arming): globally only the continuous step gestures repeat — ALL FOUR BARE ARROWS (2026-07-28: Left/Right are the two-lane playhead step — the waveform-lane step with an empty selection, the marker-lane position nudge with one, and in W+target a consumed refusal since the tempo-image step's deletion — and Up/Down are the tempo cent step, formerly unbound; the lane split is decided per fire at dispatch, so the arrows repeat as one family and NO modified arrow repeats at all), PageUp/PageDown, `=`/`-` zoom (`=`/`-` remains THE zoom binding), the marker-focus cycle (bare Tab / Shift+Tab / IsoLeftTab; the cycle ALWAYS recenters on the focused marker — architect 2026-07-19, reversing the earlier follow-only recenter), and the THREE repeating Ctrl chords — the Ctrl+Shift+Tab march plus Ctrl+Z / Ctrl+Shift+Z undo/redo (architect 2026-07-23: history stepping is a continuous step gesture; each repeat fires the full command, and a shift pressed mid-hold flips undo to redo under the live-modifier rule; Ctrl+Tab stays one-shot) — and inside an open text editor the typing/cursor/edit keys (never Enter/Esc/Tab); every one-shot command, toggle, opener, and other Ctrl / Ctrl+Alt chord is one-shot, and a press that OPENS an editor is judged under the pre-open context so it never repeats into what it opened. Repeat fires recompute modifiers AND codepoint live, and any pointer-button press or completed wheel emission disarms the hold.
