# Pointer Reach, Trim Router, Hit Geometry and the Displayed Basis

Moved verbatim from CLAUDE.md in the 2026-07-25 slim-down (project feature-complete). This file is now the authoritative home for this detail; CLAUDE.md carries only a digest and a pointer here. Keep it current with landed rulings.
This text is the opening third of the former CLAUDE.md "GUI surfacing" paragraph; it continues in selection-model.md.

GUI surfacing: render dispatch validates NOTHING (`warp_render_preflight` is removed — every failure it could surface was unreachable from the live store; the worker's own resolve→build chain and its stderr are the tripwire surface). Target-view entry (`validate_target_view_entry`, shared by the `t` toggle and load's active_audio_view=T restore) is likewise resolve-then-build only — marker arrangements always enter, trim plays no part, and there is no kick-back tick gate (an edit made in target view just re-previews through the same normalizing resolver). Mouse reach: the `kMarkerHitHalfPx` (4 px) halo is the hit rule around VISIBLE painted columns only — a stem or chip whose column lies outside the visible strip is not a hit candidate (the offscreen-halo reach and its blind-grab pull-in machinery were removed 2026-07-18 as a non-use-case; the drags' viewport clamp survives as a live tracking clamp). ALT IS POINTER-ONLY VOCABULARY (architect 2026-07-28, RETIRING the 2026-07-17 gesture pivot's "alt is the pointer/arrow authoring modifier"): on the pointer alt is PAN and nothing else — the alt+drag captured grab-pan and the alt+wheel stepped pan, a matched pair against ctrl's zoom — and on the KEYBOARD alt survives only in the five Ctrl+Alt render/propagate chords (R / Shift+R / I / P / Shift+P). Every alt-modified keyboard gesture it used to name became BARE: the augmented drop is now the only `s` drop, the lead-in audition rides Space's own overlay-state gate, and the tempo step and the position/tempo-image steps became the bare arrows under the LANE MODEL (see the selection-model and tempo-and-home-view rules). Ctrl still keeps only the letter chords — S/Q/Z/C/N/D/P — the tab chords, those Ctrl+Alt chords, and the editors' internal keys — AMENDED 2026-07-23 with one pointer surface: the ctrl-exact TOP-STRIP MARKER press is the membership toggle, see the selection-model rule in the re-coupling paragraph; the ctrl-exact waveform press stays the zoom-strip drag. STRICT MODIFIER VALIDATION IS PROJECT-WIDE (architect 2026-07-28): a modifier combination with no explicit binding is a no-op everywhere, keyboard and pointer alike — see the conventions rule for the full statement and the two predicates where shift is a real binding rather than laxity. THERE IS NO ALT POINTER GESTURE (second labwc pass, 2026-07-20: the Alt marker arm retired with phase 4 — plain flag drag moves markers, `hit_test_marker_line` deleted — and the trim drags then went PLAIN too, architect Q&A "chips-only": the b/e chip and inter-chip bridge drags arm on a plain chip-row press through a `PendingTrimDrag` + the shared threshold, the WAVEFORM trim-stem grab and `hit_test_trim_boundary` are deleted — bounds are grabbed by their chips, the chip the ruled unambiguous handle — and an Alt-exact press arms no marker or trim gesture and performs NO action on a marker at all (the 2026-07-22 alt-exact flag-click land is RETIRED 2026-07-23 — the PLAIN marker click lands now, see the re-coupling paragraph; alt+anything from a flag is inert); the Alt+drag waveform PAN is navigation, separate — see the strip-row paragraph). Trim geometry, via `route_trim_chip_press` (renamed from route_trim_alt_press; returns whether it CLAIMED the press): on a set bound's chip rect → that bound's single reposition; in the CHIP ROW (`top_upper_row_area`, the exact band `hit_test_trim_chip` y-gates on; the other lanes are NOT trim's) inside the shared trim_bridge_gap interval with both set (the one bar-gap owner paint and hit consume — see the bridge-bar sentence below) → the pair drag (the WAVEFORM between-region deliberately does NOT pair-drag — a trim span can cover the whole view; the pair's handle is the chip-row inter-chip span, visually marked by a block painted in `render_trim_flags` when both bounds are set — the chip-row band between the chips, the BRIDGE BAR — opaque kTrimBar fill with a 1px kTrimBarOutline ring, the trim family's bright pair and the sole inside-the-trim-window signal since the out-of-trim dim retired, 2026-07-26); EVERY trim pointer gesture (both drags) requires the FULL pair — a lone bound (reachable only via the settings editor's per-bound `=-1` clear or a load) is gesture-inert, transparent to the press, still painted, cleared by `Shift+X` or the editor, and renders its COMPLETED window (missing begin → 0, missing end → total, completed at the render boundary — see the trim fallbacks below); a claimed press never falls through, and read-only claims WITHOUT arming; (3) anywhere else: NOTHING for trim — the Alt+drag waveform pan (navigation, now captured) and, on a CTRL-exact waveform press, the dual-axis zoom strip live there instead (see the strip-row paragraph in the pixel-anchoring section). The marker and trim arms refuse read-only with a silent return. Trim bounds are TRANSPARENT to plain/Shift presses (behave as if the bound were absent). A Ctrl+Alt-exact press is a strict-guard no-op. The wheel: plain zoom; Alt pan everywhere over the waveform and top strip (the chip-row trim-end move is RETIRED 2026-07-20, and `wheel_context`'s chip-row region collapsed with it — the split existed solely to keep sub-detent remainder attribution from bridging the two formerly-diverging Alt routes); Ctrl+Alt is not a wheel chord; a CTRL-exact waveform DRAG runs the dual-axis zoom strip (2026-07-21, navigation), but there is no Ctrl-exact WHEEL gesture, no tempo wheel (bare Up/Down is the only tempo step), and no font-size chord. A trim press with no motion commits nothing. TRIM GESTURES NEVER TOUCH THE PLAYHEAD (architect 2026-07-18): the drags move bounds only — no tracking, no pin, no pre-drag capture/restore (a recorded difference from the marker drag, which tracks its grabbed marker and restores playhead + SelectionSnapshot at Esc-cancel; trim cancel restores bounds only). The pair drag has no grabbed-bound notion and no viewport clamp — both bounds ride the rigid delta, walls only, blind partner motion deliberate (the offscreen ruling forbids blind grabs, not blind motion of a pair held by its middle). HIT GEOMETRY IS EVENT-SYNCHRONIZED (architect 2026-07-18; the VIEWPORT half joined 2026-07-24 with the occlusion arc): the item hit tests (flag, trim chip, the trim router's bridge columns, the lane runs) read `displayed_or_live_target_map` AND `displayed_viewport_basis` — the map and the vp_start/vp_end/effective-width the committed flag item cache painted with (both halves staged by the flag-cache rebuild — the SOLE stager since the stem cache died 2026-07-25 and promoted in the one staged_displayed_valid block; the four painted-pixel deciders — hit_test_flag, hit_test_trim_chip, marker_hit_at's lane half, route_trim_chip_press's bridge — all ride it, while gesture COMMITS, drag mechanics, and navigation stay live-basis by ruling, and DAMAGE follows the basis of the pixels it erases — the stem-column invalidator (invalidate_stem_column, the subject-change owner's low-level half) rides the displayed basis since the stem paints there, playhead-column and other live-painted damage stays live) — the map the last completed waveform job painted with, copied to `app.displayed_target_warp_frame_map` at the TOP of the committing frame's paint (the promote runs BEFORE any painting, so the frame's blitted caches and its live overlays — lane text, hover — land on the SAME map; each promotion bumps `displayed_map_gen`, which joins the hover identity and the tick-side stationary-cursor repair so a silent promotion cannot strand a stale hover or copy payload) — so what you grab is what you see even inside the worker's publish window; AUTHORING-GESTURE MECHANICS RUN ON THE DISPLAYED PAINT BASIS TOO (2026-07-22, the slippery-drag fix + its codex round — superseding the earlier mechanics-stay-live rule): the MARKER DRAG's delta model anchors in the DISPLAYED target domain (per motion `proposed_src = inv_disp(fwd_disp(orig) + active-domain delta)` against `displayed_or_live_target_map`, with an exact zero-EFFECTIVE-motion short-circuit to orig — tested on the CLAMPED delta, not the raw pointer delta (codex round 6) — preserving the bit-exact untouched contract, so the flag tracks the pointer 1:1 BY CONSTRUCTION under any map — the former live-map inverse slipped at the slope ratio wherever displayed ≠ live, or across the press-offset straddle at a fresh breakpoint), its begin-time viewport clamp and commit column-snap read the same displayed map (stored equals SHOWN, literally); the TRIM drags (single-bound anchor/motion/clamp, pair motion, release snap) and BOTH NUDGES' pixel anchoring (painted_column/authored_frame maps; the wall policy is one regime per column under the home-view binding — warp clamps in source, phase reset refuses all-or-nothing in target) moved to the same displayed basis, as did the phase-reset overlay's map; the freeze premise is made TRUE by two halves — maybe_enqueue_waveform_render FULLY freezes dispatch while a marker/trim drag is active (whole-enqueue early return, replacing the original hash-only exclusion whose vp/area fields could sustain a drop-rewind-redispatch loop) and on_waveform_render_done DROPS the at-most-one job in flight at the grab (pending_fp_* rewound to fp_*, supersede cleared; marker/trim drags only — strip/pan/region keep dispatching and publishing) — so the displayed basis cannot swap mid-gesture; kick_waveform_sync itself reclamps through clamp_viewport_start before rendering (final clamped geometry in ONE sync render per edit — the tick's live-total backstop owns only asynchronous total changes like a preview completion), and the flag-editor's sync is gated on canonical_changed (a bracket-only commit skips the pixel-identical rebuild); STILL LIVE-basis by ruling: the click-playhead placement family (column-based, incl. drops at the playhead and the `x` region→trim conversion) and the land/follow-placement family (source_frame_to_active_domain — the Tab basis, post-commit truth), while walls stay integer source frames everywhere; and EVERY DISCRETE WARP-MAP EDIT REACHABLE IN TARGET VIEW RE-WARPS SYNCHRONOUSLY (febe117 established the discipline for the then-six stragglers; the home-view binding, 2026-07-22, then made the placement edits source-view-only and their sync tails were harvested as dead code at ac0ae2c — the surviving target-view re-warp sites are the bare Up/Down tempo step, the bare Left/Right tempo-image step (2026-07-24 second pass — replacing the re-ruled-away warp position nudge's target press), the settings engine-scale commit, undo/redo, and adopt — plus the tempo DRAG's per-cent-step and Esc-cancel-restore synchronous kicks, the live-gesture siblings of these discrete sites; the ONE authoritative grep-derived caller inventory lives at viewport.h's kick_waveform_sync declaration), so displayed == live at every command boundary: the divergence window closes at its source, the live-basis `x` conversion coincides with the displayed one by construction, and the displayed-basis mechanics + completion-drop gate remain as belt-and-braces for the one residual case (a viewport-dispatched worker job in flight across a gesture boundary, always carrying the then-current map); cold states (pre-first-publish, post-load) fall back to the live map, and the column-based playhead-placement clicks stay out of scope by ruling.
