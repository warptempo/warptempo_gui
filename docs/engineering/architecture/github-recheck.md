# The GitHub Recheck (history mode)

The arc landed 2026-08-03..04 — dd4757e (the diff model), 3e671a8 (the mode), 3efc64e (load-in-place-from-a-commit), 2324f62 (docs), 3b698b4 (the commit act and the two buttons), 7bc2af9 (docs), 58a449d (the first codex round's nine conversions), plus the live-pass round (view switching; projects_repo required) — the commit messages carry the full per-round narratives; this file is the standing ruling record. The feature: bare `h` (also the icon row's twelfth button, Icon::VcsDiff behind a separator gap, selected while the mode stands) opens a read-only view of how the live in-memory authoring state differs from the piece's committed checkpoints, `,`/`.` walk the checkpoints, the marker lane paints the delta as green/red diff flags, `'` loads a viewed (or pasted) commit's whole sidecar state in place in memory, and Ctrl+Alt+R — the Render button, wearing "Save and Commit"/Icon::VcsCommit while the mode stands — SAVES the piece and publishes the current state as a new checkpoint. The architect's purpose: commit checkpoints to GitHub with confidence instead of keeping local file copies, and catch slips of the hand as projects grow.

## Transport and trust

LOCAL GIT, THE READ/WRITE SPLIT AS TWO ENTRY POINTS. The module (`src/gui/history_diff.{h,cpp}`) shells to git with an argv exec (no shell anywhere — corpus paths carry spaces) against the constexpr repo root. `run_git_capture` passes ONLY reads — `log`, `show`, `ls-tree`, `rev-parse`, `rev-list`, `diff-tree`, `status`, `remote get-url` — and `run_git_mutate` (round 5, the commit act's fenced writer and its ONLY site) passes ONLY `add`/`commit`/`push`; both inventory comments are authoritative, and the fence is which function a call site names. No fetch, no credential in the program: every read is against the LOCAL branch (`HEAD` since round 5 — the product is a commit producer, and a checkpoint whose push failed must still be visible history; `origin/main` would hide it until the next successful push), and the push authenticates through the machine's own SSH setup. main.cpp's `SIG_IGN` SIGCHLD auto-reap (the audio-player precedent) makes `waitpid` return ECHILD, so success is INFERRED, and since the codex round the inference is honest per class: captures are a TRI-STATE (a CLOEXEC self-pipe distinguishes could-not-exec from ran; the load-in-place path additionally cross-checks each captured blob against `ls-tree -l`'s own byte count, so a failed or truncated `show` can never stage a sidecar state the commit never held), and mutations are judged by REPOSITORY OBSERVATIONS THAT OUTRANK THE TRANSPORT (codex round 2): the commit verdict FINDS THE COMMIT CARRYING THE CHECKPOINT BY CONTENT SIGNATURE — over a bounded `rev-list before..refs/heads/<captured branch>` walk (cap 10; the range names the captured branch's own ref, never the symbolic HEAD — round 4), the commit whose message is the act's exact title, whose own diff touches a non-empty SUBSET of the three literal paths and nothing else, and whose resulting three blobs equal the bytes the act wrote (subset not equality: a checkpoint that changes two of three sidecars is the ordinary case; the bytes leg proves completeness) — so a concurrent unscoped commit that consumed the staged sidecars is never attributed or pushed, the checkpoint is found even under another commit landed on top, and the verdict runs REGARDLESS of the transport result (git moves HEAD before post-commit hooks, so a deadline-killed hung hook can sit over a checkpoint that landed — it is found and pushed, the anomaly noted). THE IDENTITY IS CONTENT, NOT AUTHORSHIP (codex round 3, the contract stated honestly rather than overclaimed): every leg is a fact about what a commit CARRIES, none about who ran the git, and content-equivalent commits are DELIBERATELY INTERCHANGEABLE — publishing one publishes the intended state, and the byte legs are what make that safe (nothing carrying different sidecar state can be substituted, no foreign path can ride along). An act-unique message trailer was considered and DECLINED: mechanism plus a permanent oddity in every checkpoint message, to distinguish commits whose content is identical, against a producer (the architect's wrapper) retired for checkpoints as of 2026-08-04. The pre-flight's Clean arm forks on the remote-tracking ref: caught-up → NothingToCommit; behind with the captured branch tip's bytes confirmed → push the existing checkpoint (the retry-after-timeout route, and the correct answer to a terminal-committed unpushed branch). The push verdict is the remote ref catching up, transport discarded — and CONTAINMENT MUST BE OBSERVED, never inferred from silence (round 3): the walk is `rev-list --count`, whose successful run ALWAYS prints a number ("0" is containment), so an empty or non-numeric answer is a FAILURE and the two verdict sites take it as such — the push reports the checkpoint unconfirmed (never `Committed`), the clean arm treats the branch tip as unpublished and falls through to the push with its own stderr line. THE TRI-STATE SURVIVES THE REF READ TOO (round 4): `rev-parse --verify` answers "" for a ref that is ABSENT and for a read that could not be MADE, so an empty first read is followed by a POSITIVE-WITNESS probe of `refs/heads/<captured branch>` — a ref the act cannot be running without — and the witness printing means the repository answers queries (so the target ref is genuinely absent → Missing), while a witness that is also empty means UNAVAILABLE; without it an unreadable repository routed as an ordinary "the remote does not carry it", omitting the clean arm's "could not read" line and printing `Push failed` where the transport had not failed. AND THE WITNESS ADJUDICATES ONLY A READ THAT RAN (round 5): the target probe returns the capture's own verdict beside the object name, so a probe that COULD NOT EXECUTE is Unavailable on the spot with no witness probe run at all — a witness proves the repository answers NOW and can never reach backwards to prove an earlier invocation executed, and letting it convert a could-not-exec target read into "absent" manufactured exactly the false negative the tri-state exists to prevent (`Push failed` reported over a tracking ref that carries the checkpoint, and "committed locally and still unpushed" printed without an observed Missing). The one false unavailable, recorded: a captured branch deleted mid-act, which costs a fall-through push — and that push is NOT refused by the branch's absence (round 5, correcting the recorded consequence): the refspec sends an object name the act already holds, so git resolves no local branch to send it and can create or update the remote branch with the local one gone (verified live). The conservative routing is safe because a redundant push moves nothing at the remote, not because the missing branch would stop it. WHAT THAT FALL-THROUGH PUSH ACTUALLY DOES at the remote (round 4, correcting "a no-op if the remote did carry it"): it is a no-op only at TIP-EQUALITY (`Everything up-to-date`); a DESCENDANT remote tip — which containment also calls carrying it — makes the explicit `<sha>:refs/heads/<branch>` refspec a backward move and git REJECTS it as a non-fast-forward, safely, since nothing forces, and a rejected push does not update the remote-tracking ref either (both verified live), so the verdict reads what that ref already said. Nothing at the remote is lost or rewound in either case, which is the claim the fall-through rests on. And the local-checkpoint line no longer claims "still unpushed" over an unobservable containment: the clause is printed only after an observed Missing. THE PUSH CONSUMES THE VALIDATED DESTINATION (round 3, the real TOCTOU close): the guard runs at init for the mode and again at the mutating boundary for the act, and the URL that second asking validates is PINNED into the push child's own config — a `-c remote.origin.pushurl=` clear followed by `-c remote.origin.pushurl=<validated>`, both halves needed because the key is multi-valued (a lone `-c` ADDS a destination; the empty value clears the list), which in the multi-pushurl case replaces the whole validated set with one confirmed member. The named remote stays in the argv so the remote-tracking ref still updates. The other two terms of the publication are bound with it: the attributed commit is pushed BY SHA (`<sha>:refs/heads/<branch>`, no force anywhere — a remote ahead is an ordinary fast-forward refusal; the recorded trade is that a commit sitting on top of the checkpoint stays unpublished, the user's own to push), and the branch is read ONCE at act start — the act's ONLY reading of the symbolic HEAD for a VALUE — and serves the refspec, the tracking-ref observation AND every source-side observation the act makes (the `before` read, the attribution walk's tip, the clean arm's byte confirmation all name `refs/heads/<captured>`; round 4 closed the gap where they still followed HEAD, which let a mid-act checkout have the walk attribute a commit made on ANOTHER branch and the pinned push then publish that branch's ancestry onto the captured one — verified live). THE ONE OBSERVATION THAT CANNOT BE SPELLED IS CHECKED INSTEAD (round 5, completing that binding): `git status` takes no ref and always answers for whatever HEAD points at, so the pre-flight compares the `##` header it already demanded as its ran-witness against the captured name and a MISMATCH FAILS THE ACT CLOSED with one line naming both branches — never Clean, never Dirty, nothing committed or pushed, the written sidecars left in the working tree as after any other failure here. Without it the clean arm could end the act on another branch's answer (reproduced live: captured `main` carries the old bytes, a topic branch already carries the new ones, a checkout lands between capture and probe, the freshly written bytes match the topic tree exactly → `status` says CLEAN, captured `refs/heads/main` resolves with `origin/main` caught up to it, and NothingToCommit returns before the byte confirmation ever runs — the user told the checkpoint already carries bytes the captured branch does not have). Header grammar verified live in every shape (`## main`, `## main...origin/main [ahead 1, behind 2]`, `## HEAD (no branch)` = detached and compares as the captured detached "", `## No commits yet on main`): the cut is on `...` then on a space, both unambiguous because a refname may contain neither two consecutive dots nor a space, so `feat/a.b.c` parses whole; any shape the parse has not seen simply mismatches and fails closed. The commit itself necessarily lands wherever git is checked out, and needs no pinning: if a checkout intervened the walk over the captured ref's range finds nothing and the act returns CommitFailed having published nothing, which is the honest outcome. A detached HEAD keeps the HEAD spelling (there is no branch ref to name) and publishes nothing, saying so. The one destination rewrite the guard cannot see, recorded at its site: `url.<base>.insteadOf` redirects a URL when git USES it while `remote get-url` reports the raw config value — pre-existing, and unclosable by pinning. `run_git_mutate` carries a 30s deadline whose expiry kills the child's whole process group (setpgid; git's own ssh/hook children die with it) under a pre-fork non-interactive environment (GIT_TERMINAL_PROMPT=0, ssh BatchMode, stdin /dev/null), and shares the capture's exec self-pipe so could-not-start is a real verdict on both entry points. Tree listings are NUL-separated (`ls-tree -z`) so a UTF-8 base name survives `core.quotePath`.

`projects_repo` (settings.md owns the key's full record; a REQUIRED key like every other since 2026-08-04, the one-day optional exception retired at the architect's live pass) names WHICH repository is the projects home; the local clone is the transport, and init's guard normalizes the setting against the clone's FETCH URL AND EVERY EFFECTIVE PUSH URL (`remote get-url` + `--push --all` — a configured pushurl must match too, or the checkpoint act could publish to an unconfirmed repository; scheme, scp `git@host:path`, trailing `.git` all compare equal), refusing a mismatch with one stderr line, and — at the pre-push asking — handing the first validated push URL back as the destination that asking's caller pins. An empty value never matches — the feature's opt-out. Which repository, not how fresh: the guard never checks staleness.

## The match and the walk

THE MATCH IS BY COMMITTED FILE NAME UNDER `projects/` (architect 2026-08-03; narrowed from the whole tree 2026-08-04 — the repo-layout convention is `<repo>/projects/`, one folder per piece, and a theoretical other user builds their own repo with a `projects/` folder): the loaded source's sidecar base name (the loader's own `stem()` derivation, mirrored — it strips only the last extension, load-bearing for period-heavy titles) is resolved against HEAD's tree filtered to `projects/`; exactly one directory may carry the sidecars. The walk is `git log -n 20` over `:(glob)projects/**/<base>.<ext>` pathspecs with the basename's wildmatch metacharacters ESCAPED, every mutation/status pathspec wears `:(literal)` magic (a filename carrying `*?[` can never widen a checkpoint), and each commit's snapshot paths resolve from THAT commit's own tree — preferring HEAD's own matched directory, taking a sole other, and otherwise REFUSING as ambiguous (through `read_commit_sidecars`) rather than silently showing one piece's history as another's. THE WALK IS LOAD-GATED (architect 2026-08-04): membership is THE LOAD-IN-PLACE GATE ITSELF — `load_commit_sidecars_strict`, the exact resolution + scratch staging + three strict frozen loaders the `'` act runs, ONE predicate with no relaxed variant — applied eagerly to every candidate at init, so every checkpoint the mode can step to is one `'` can load. A candidate that refuses (a missing sidecar, a parse refusal, an ambiguous per-commit path resolution) leaves the walk at entry, ONE stderr line counting the hidden ones; the corner's `n/N` counts the ELIGIBLE list, and the gate keeps each member's three sidecar snapshots as its then side, so no delta ever runs git. The per-commit `Ambiguous` display machinery is RETIRED with the gate — an ambiguous commit is never shown at all, and the ambiguity refusal survives only as the `'` act's own arm for a PASTED spelling naming a commit outside the walk. ACCEPTED TRADES, recorded: basename matching cannot see history across a SOURCE RENAME, and every legacy-format checkpoint leaves the walk under the gate exactly as the `examples/`-era commits left it under the round-5 narrowing (the walk heads at the first load-clean commit until the first commit act). A piece with NO committed history — or none whose sidecars pass the strict load — cannot open the mode at all; its bootstrap stays a manual act.

## The diff model

The NOW side is the live in-memory state serialized through the save writers' own string halves (`format_warpmarkers_text` / `format_phaseresetmarkers_text` / `format_settings_text` — the round-1 split; byte-identical to a Ctrl+S, no disk touched) and is FROZEN AT init(); the mode's mutation gate is what keeps the frozen snapshot honest (authoring is blocked while the mode stands, so it cannot go stale by construction). Then-vs-now is an in-tree line LCS (DP with prefix/suffix peeling and a cell cap) typed into per-commit deltas: warp added/removed/changed carrying each line's VERBATIM tempo token (the flag shows the sidecar's own spelling, never a re-derivation), phase resets added/removed/changed (their line is frame plus disable prefix, so a `#` toggle pairs as a genuine change, symmetric with warp), and the `scale=` tokens from both sides (THE ONLY SETTINGS KEY THE MODE DISPLAYS — a recorded asymmetry: every other settings delta is deliberately invisible). A same-frame remove+add pair IS the changed entry — it feeds the double flag. WALK MEMBERSHIP IS LOAD-GATED (2026-08-04, superseding the old two-tier "diff is parse-free / load-in-place is parse-gated" answer): everything visible is loadable, so both sides of every diff are loader-clean text with all three files present, and the diff mechanism stays line-based with NO leniency arm left — the drop-unparseable-lines arm with its counters (`GuiHistoryLineCounts`), the missing-file-reads-as-empty-side arm, and the per-commit `Ambiguous` state are all DELETED as dead code (the architect's no-legacy rule: the program never imports leniently; an old-format checkpoint is hand-edited, never tolerated). The extraction's boolean is the parse's own optional shape, not a tolerance. The one degraded arm kept is an ALLOCATION guard, not a format leniency: a loader-clean sidecar past the DP cell caps diffs coarsely as replaced whole. Deltas compute lazily against the snapshots the gate kept and cache per commit; stepping revisits are free and no delta runs git at all.

## The mode

`h` (bare-exact; `GuiKeys::H`) toggles — REBOUND FROM BARE `/` on 2026-08-05, the architect dropping `/` entirely, so it is an unbound no-op everywhere again and in-mode drops at the allowlist like any other unclaimed key; `,` older / `.` newer, clamped walls as consumed no-ops; the `i`-mode toggle shape, NO Esc binding (the Esc inventory stays six). The on_key gate sits directly under the drag-modal gate and above the read-only gate — every entry refusal (prompt, dropdown, loading, editors, live gesture) has already run above it, `h` typed in any editor is CLAIMED as a printable, and the read-only tab bit is structurally irrelevant to the mode's own keys. Exits: `h`, a source load, either load-in-place, quit; resize survives and re-lays-out through the flag-cache fingerprint miss. The fingerprint carries a SESSION GENERATION (bumped at the one entry owner, surviving the close reset) so the checkpoint act's in-place re-init rebuilds the lane — the empty-diff confirmation cannot be a stale blit.

THE MODE'S OWN KEYBOARD VOCABULARY grew three families on 2026-08-05 (architect), all CLAIMED by `handle_history_mode_key` one line above the allowlist rather than admitted by it, and all MODE-LOCAL — each reads the diff-flag list and the mode's own focus, never the live stores the ordinary arm walks, which is what makes them fit a view that shows no live marker. (1) THE DIFF-FLAG CYCLE: bare Tab steps the mode focus to the next flag, bare Shift+Tab and IsoLeftTab (shift-agnostic) to the previous, in `flags`' own order — `rebuild_history_diff_flags` leaves it sorted ascending by `time_frame`, so list order IS reading order and the hit stash's indices stay the same indices. Each step lands the playhead on the flag through `land_playhead_on_source_frame` and recenters at the current zoom, exactly as the live cycle does; NO WRAP (the live cycle does not wrap either — nothing ahead is a consumed no-op), with no focus standing Tab takes the FIRST flag and Shift+Tab the LAST, and an empty list (empty delta, or an active column whose half is empty) is a consumed no-op. Repeat eligibility is inherited unchanged from the live cycle's own three shapes, so a held Tab walks the flags. (2) BARE HOME / END jump ABSOLUTE — frame 0 and the last frame of the active domain, deliberately NOT the trim bounds the live arms take: the view reviews the whole piece, and an End stopping at a trim bound would hide the flags past it (with a full trim window the two coincide, `trim_window_is_full`, so the difference shows only under a set trim). They clear the mode focus, the analog of the live arms' selection clear — the playhead is leaving the flag. (3) BARE `c` is the live arm's recipe with the mode's focus in place of the live marker's: region clear up front, re-land on the focused flag when one stands, then working zoom and centre on the playhead; `repair_last_selected` / `jump_playhead_to_focused_marker` deliberately do not run. ALL THREE KEEP THE LIVE ARMS' PLAYBACK AND REGION REGIMES (stop a live audition, dissolve a resting region — the keyboard stop rule's cursor-moving navigation class), which is where they part from the mode's diff-flag CLICK, a pointer route whose recorded minimalism touches neither. The face partition did not move: no roster chord is any of these shapes (the table's only Tab entries are the tabs' Ctrl+Tab, excluded by the predicate's ctrl test).

READ-ONLY WHILE STANDING — two allowlists plus one lockout. Keyboard admits exactly: Space, `=`/`-`, `0`, PageUp/PageDown, Ctrl+S, Ctrl+Q, `'` (since round 3), Ctrl+Alt+R with its in-mode meaning (since round 5), BARE ESC (2026-08-04 — admitted for its EXISTING bindings and no binding of its own: the bare-Esc inventory stays at six places, and the two reachable in here are the region clear, over a span formed before `h` since the pointer allowlist admits no region former, and the render/batch cancel; neither touches authored state, and Esc CANNOT close the view because the toggle is `handle_history_mode_key`'s `h` alone — with no region and no render it is a consumed nothing), and — the architect's live-pass ruling — the VIEW SWITCHES, bare `t`/`p`/`1`/`2`/`3` with the view bar and the S/T + W/P radios riding the same chords: the diff DELTA is line-shaped and view-independent, only the painted subset is column-scoped, so switching views is exactly how both halves of a checkpoint's delta get reviewed (the switches run their normal handlers whole; the mode-local focus clears on every switch as on every step; the A/B TAB SWITCHES stay consumed — views yes, tabs no). Pointer admits: the right-button scrub, alt-pan, ctrl-strip-drag, the ruler drag, the lower-half scrub; everything else is consumed, including both trim-bar ctrl writes — except the mode's OWN three plain acts, the lane's focus click and, since 2026-08-05 (architect), the waveform upper half's two: A DIFF FLAG'S STEM IS THAT FLAG'S CLICK (`hit_test_marker_stem` unchanged, reading the stash the mode's own lane pass publishes, so the ordinals and the "grabbable = drawn" rule both come free — the changed pair's single red stem resolves to its one ordinal exactly as its double-width box does; asked BEFORE the placement, the live press's own order between the two surfaces of one item), and EVERY OTHER PLAIN UPPER-HALF PRESS LANDS THE PLAYHEAD at its column — the live placement press's recipe and playback regime through one shared body (`place_playhead_at_click_column`, hoisted for this), but ARMING NOTHING: the region former stays out of the mode, which is what keeps "the pointer allowlist admits no region former" true for the bare-Esc reasoning above and leaves the mode with no drag of its own. It clears the mode focus ahead of its own gutter return (the analog of the live body's deselect) and dissolves a resting region where the live arm does; there is no waveform double-click surface to inherit, so a second click is another placement press. The cursor map needed nothing: the live upper-half plain zone is the unnamed Arrow and stays it. All twenty-two chord buttons drop through `dispatch_redesign_chord` → on_key — no second membership (the history and load buttons' in-mode meanings arrive through the same dispatch). AND THE ROSTER WEARS THE REFUSALS (architect 2026-08-04, the principle asked about the tabs and applied to the whole row set): while the view stands, every button whose act it consumes takes its row's DISABLED face and ignores the pointer — Undo, Redo, BOTH TABS with their lock slots, copy, paste, the BPM opener, iteration, follow, listen, and the two menu anchors — while Quit, the view bar's three, Save, the Save-and-Commit-faced Render, the S/T + W/P radios, the `'` load opener and the history button stay lit; the ones that work are exactly the ones that light. It is the mode-scoped EXCEPTION to the icon row's never-grey rule and to rows 1 and 3 having no disabled face, taken on the view bar's own precedent (a whole surface wearing a state that is not the enabled bit), and the face is built from the product's one disabled blend `kRedesignDisabledMix` — every ink and edge retaining that fraction of itself over its row ground, so a dead button dims as one object and a dead SELECTED toggle keeps its state muted rather than hidden. THE PARTITION IS DERIVED, NEVER LISTED: `history_mode_disables_button` (input_pointer.cpp, which carries the authoritative inventory) walks the chord table and asks `history_mode_key_blocked` about each button's own chord — asking `history_mode_owns_key` first, since the mode's own vocabulary is consumed one line above the allowlist and the history button's bare `h` would otherwise grey out the very button that leaves — so admitting a chord lights its button on the next frame with nothing to remember. The two anchors are the one hand entry, having no chord at all: their lockout is `toggle_dropdown`'s first line. Read live from `history_mode.active` at `redesign_button_enabled`, so leaving restores every face with nothing latched; hoverability, the press claim and the tooltip all read that one predicate, so a dead button neither lights, dispatches nor advertises. The greyed anchors also stop the armed-row HOVER OPEN twice over: the row cannot even be armed in here (the arm's one producer is `toggle_dropdown`'s open path, refused, and on_key/on_button_press disarm at their tops — so the `h` that opened the view disarmed it), and the toggle would refuse anyway. `toggle_dropdown` refuses whole while the mode stands, closing the one dispatch route that reaches no key gate (`finish_dropdown_release`'s Settings half opens the prefilled editor directly) — a popup and the history mode are mutually exclusive in both directions. Two structural guards ride the mode edge: `drop_lane_stash_across_history_edge` clears the flag hit rects at entry/exit/step (the stash's `marker_index` CHANGES DOMAIN across the edge — store index vs diff ordinal — and the stash is tick-cadence), and `pointer_cursor_kind` empties the trim-bar band in-mode so no cue promises a refused gesture (per-zone, the read-only model exactly).

## The lane display

While the mode stands the lane suppresses all live marker flags/stems (the playhead head still paints) and paints the current delta through the SAME `iterate_visible_flags_impl` → `frame_to_paint_sample` mapper live markers ride — identical placement in both views by construction, the current session's warp map doing the target-view mapping for both sides. Text: `[+] <token>` / `[-] <token>`, the `#` disable spelling prefixed when set, applied UNIFORMLY to phase resets (`[+]` / `[+] #`) since their same-frame change IS a disable toggle. The changed pair is ONE double-width flag: red left `[-] then`, green right `[+] now`, one outer border, the fill boundary its only seam, RED stem (deference to the old — architect's ruling). Stems always paint the class's UNSELECTED fill. Diff labels are deliberately NOT nine-glyph-truncated: the token is the flag's whole point (the shared cull's width bound generalized to a parameter for this). Colors are the eight crop-exact `kHistoryAdded*`/`kHistoryRemoved*` constants (render.h palette block, provenance = the four row_5_lane_3 crops, modally sampled — the sampling also established that the LIVE red class already matches its crops exactly: `kMarkerFlagFillRed`/`kMarkerFlagEdgeRed` are the selected red crop's pair, `kMarkerStemRed` the unselected fill).

MODE-LOCAL FOCUS: a plain flag click swaps that flag (both halves of a double) to the selected faces and lands the playhead through `land_playhead_on_source_frame` (hoisted from `land_playhead_on_marker` — one landing recipe); the live store selection is untouched. THE FLAG HAS TWO CLICKABLE SURFACES since 2026-08-05, its lane box and its STEM in the waveform's upper half, one shared body (`focus_history_diff_flag`) so they cannot answer differently, and the recorded MINIMALISM covers both — no playback stop, no region clear, it is the same click at a different pixel. Empty-lane clicks clear it; modified lane clicks are consumed; it clears on step and exit, and on the waveform PLACEMENT press (the plain upper-half press that hits no stem), the playhead having left the flag. The bottom-left corner (the modal editors' rect — never contended, by gate) shows `<n>/<count> <shortsha>` then `Scale: <token>`, or `Scale: [-] <then> [+] <now>` when changed, tokens verbatim, the segment omitted when both sides are empty.

## Load in place from a commit

In the mode, `'` opens the load editor PREFILLED (open-selected, the flag editor's convention — the product's one prefilling precedent) with the viewed commit's full SHA under a `Load: ` prefix; any spelling `rev-parse --verify <spelling>^{commit}` resolves is accepted, so a SHA pasted from GitHub's web UI works. THE WHOLE VALIDATION IS `load_commit_sidecars_strict` — the same ONE predicate that is walk membership since 2026-08-04: the commit's three sidecars resolve by base name from ITS OWN tree and reach the STRICT WHOLE-FILE LOADERS through an RAII per-commit scratch directory under the system temp root (all three parse entry points are path-only and frozen; a GUI-side string scanner would be the second grammar the strict gate exists to prevent; the directory dies on every exit including refusals). A PARTIAL COMMIT IS A REFUSAL — a load-in-place is a whole-state replace, and two files from a commit plus one from nowhere composes a state no checkpoint ever was. A prefilled spelling (a walk member's own SHA) always passes by construction; every refusal arm stays live for a PASTED spelling naming a commit outside the walk — unresolvable, partial, ambiguous, or legacy-format, the last failing BY DESIGN. Every refusal is the editor's red-flash with typed text preserved plus one stderr line, first-error-only, nothing touched.

The apply mirrors `load_render_entry_in_place` step for step (settings.md owns that sequence) and drops exactly: the WAV existence check (the corpus stores no audio — the loaded source IS the source), and the `renders/` wipe with its running-batch guard (the guard protects the wipe's race; this path wipes nothing, and a render live from before `h` renders its request snapshot untouched). One cross-file undo entry, dirty set — auditioning then Ctrl+Z back is the point. The mode closes as part of the act (the frozen now side no longer matches the live state). Autocomplete Tab is a consumed no-op in the mode (render-entry identifiers are the wrong vocabulary). Outside the mode, `'` and the load editor are byte-identical to before.

## The commit act (round 5; SAVE AND COMMIT since 2026-08-04)

Ctrl+Alt+R while the mode stands IS the checkpoint commit — the iteration-mode precedent exactly, the mode bit selecting the command inside the one render route; outside the mode the chord is byte-identical to before, and Ctrl+Alt+Shift+R stays consumed in the mode. The Render button wears the act (label "Save and Commit", Icon::VcsCommit — the row's second stateful string, after "Render Iterations", and the second architect-named exception to the sentence-case label convention: capital S, lowercase "and", capital C) and dispatches through its existing chord. The product's FOURTH PROMPT confirms (case-sensitive `[Y]es`, Esc through the prompts' existing binding, the prompt class's playback stop; the title is never asked — always `Update <id>`), and its question names both halves in prose — `Save and commit "<title>" to <repo>?`, sentence-case, the label's title case being the button's alone.

THE ACT SAVES FIRST (architect 2026-08-04), and the ORDER IS SAVE → WRITE → COMMIT → PUSH. On the prompt's `y`, before any checkpoint work, `run_history_commit` runs the FULL Ctrl+S act through its one owner `GuiSaveOps::save` — the same three atomic writes beside the source, the same per-write stderr, the same `mark_saved` + `recompute_dirty` tail — never a second writer or a partial imitation. The purpose is one sentence for the mental model (the checkpoint is what you see, saved and published) and the death of a real incoherence: in the architect's own workflow the loaded source LIVES inside the matched `projects/<id>/`, so the act's repo write already WAS the working sidecar set, yet the session still carried the dirty dot over bytes a save would have written verbatim. A FAILED SAVE REFUSES THE WHOLE ACT — the save returns bool and every arm that fails has already named its path on stderr; the refusal adds one line of its own and returns ABOVE `commit_history_checkpoint`, which is the only call in that body that writes a byte or spawns a git child, so "no checkpoint-side write, no git subprocess, nothing committed" is a property of the ordering rather than of a check (the prompt is already down by then, matching every other failure's shape here) — narrower than "no repo write": the save's own three writes are sequential, not cross-file transactional (`save_ops.cpp`), so a save that fails partway through can leave earlier atomic renames on disk exactly as any ordinary Ctrl+S failure can, and in the coincident `projects/<id>/` workflow those are repository working-tree paths; the ordinary save's documented partial-write-on-I/O-failure contract is unchanged by this act. THE DOUBLE WRITE IS DELIBERATE: in the coincident workflow the save and the act's own write hit the SAME three paths with BYTE-IDENTICAL content (`build_history_now_side` mirrors `refresh_active_tab_view_from_app` onto local copies, so a save running first changes none of its bytes) and both are the atomic tmp+fsync+rename, so the second rename replaces a file with its own contents; it is NOT deduped, since recognizing the coincidence would mean canonicalizing three absolute paths against the repo root and carrying a skip only one layout ever takes, to save three renames of bytes already held. When the source lives elsewhere (the older workflow) both writes are wanted and distinct — the save publishes beside the source, the act publishes the repo's copies. THE SAVE BUTTON STAYS (architect's explicit reasoning): saving to disk is its own act and the common one; two buttons because one is to disk and one is to disk and the remote.

The act proper: the three sidecar byte strings are REBUILT FRESH via `build_history_now_side` (the session's frozen now side provably drifts on the settings file — the per-tab view band moves under the mode's own allowed navigation — and the fresh rebuild is also what makes the after-commit empty diff true), written into the matched `projects/<id>/` directory through the atomic writer, then a pathspec-scoped `git commit -m "Update <id>" -- <the three paths>` (foreign staged work can never ride along) and `git push`. VERDICTS ARE REPOSITORY OBSERVATIONS, never child status (the SIGCHLD regime), and the transport paragraph above owns their current shape whole: the commit verdict is the CONTENT-SIGNATURE WALK over `before..refs/heads/<captured branch>` (not the old `rev-parse HEAD` before/after), the pre-flight `status --porcelain --branch` names the NothingToCommit non-failure distinctly, VERIFIES ITS `##` HEADER STILL NAMES THE CAPTURED BRANCH (a mismatch ends the act — the probe takes no ref, so checking is what pins it), and FORKS on the remote-tracking ref when it comes back clean (already published → NothingToCommit; committed-but-unpushed with the bytes confirmed → push it), the push verdict is that ref observably CARRYING the attributed commit, and every one of those observations reports "could not ask" as itself rather than as a yes. The push is bound to the destination the guard validated, sends the attributed commit by sha, and reads the branch captured at act start — to which every source-side observation is bound, by naming its ref or by verifying it is still checked out, so observation and publication cannot come apart under a checkout. Push hardening: stdin /dev/null + ssh BatchMode, so a credential prompt fails in one line instead of freezing the GUI. A push failure after a landed commit says so distinctly — the checkpoint exists locally and the mode's own HEAD-based history walk shows it (that walk is the DISPLAY's, distinct from the act's attribution walk, which reads the captured branch ref). Success re-inits the session in place (`open_history_mode_fresh`, the one mode-entry owner): the walk heads at the new checkpoint, index 0, and the lane reads an EMPTY diff — the visual confirmation. THE MODE STAYS OPEN (architect). The program embeds no identity and no credential: author and transport are the machine's git config and SSH setup.

## Accepted costs and open items

- A render launched before `h` hides its progress line (the mode's corner line outranks it). ITS ESC CANCEL IS BACK, 2026-08-04: the architect took the one term, bare Esc joined the keyboard allowlist, and the cancel plus the region clear run inside the view (the allowlist paragraph above owns the ruling). The hidden progress line stands as the accepted cost.
- A delta's first computation runs no git at all since the load gate (each member's snapshots are kept at init), so `paint_bottom_strip`'s `delta_at` costs only the in-memory diff on a commit's first visit — the old "can in principle run git from the paint path" cost is retired with the lazy snapshot reads.
- Basename matching cannot see across a source rename (above).
- A READ-ONLY ACTIVE TAB blocks `'` (the load-in-place) AND Ctrl+Alt+R (the commit act) in the mode as it does outside it (neither is on the read-only allowlist, and that gate sits below the mode gate). RATIFIED AS STANDS (architect 2026-08-04): the lock means hands off everything, exactly as it blocks the save, and unlock is one `o` away — the view is lock-agnostic for VIEWING only. It follows that a locked tab greys Save and the Save-and-Commit-faced Render in the view as outside it (the read-only gate is row 2's own enabled term), while the `'` button stays lit on row 4 under the never-grey rule — the lock's refusal is not the VIEW consuming the act, so the mode-scoped disabled-face partition says nothing about it.
- THE TAB SWITCHES STAY CONSUMED — Ctrl+Tab and the Ctrl+Shift+Tab paired march — ratified 2026-08-04 and now with the faces saying so: views yes, tabs no (a view switch re-reads the same piece; a tab switch swaps the per-tab band the session was measured with), and both tab surfaces wear the disabled face while the view stands rather than swallowing clicks silently. THE BARE HALF OF THAT RATIFICATION WAS SUPERSEDED 2026-08-05: bare Tab / Shift+Tab / IsoLeftTab became the mode's OWN diff-flag cycle (and `c` the mode's own centre), which the ratification's premise — Tab and `c` navigate by live markers — no longer describes. The tab-band argument was never about markers and stands untouched, so the two tab CHORDS and their faces are unchanged.
- Checkpoints predating the current schema refuse the strict load (a `font_size=` era file is an unknown-key refusal; a pre-2026-08-04 one lacks the now-required `projects_repo`), and under the load gate they LEAVE THE WALK TOO — the mode shows only checkpoints whose sidecars carry the full current schema, and a corpus whose every commit refuses cannot open the mode at all: the strict gate working as ruled, hidden count on stderr. Concretely at the gate's landing: the `550 - 1` walk's three candidates gate to ONE eligible (58a449d, whose ride-along settings refresh carries `projects_repo` and `gui_scale`; 37560ee lacks `projects_repo`, f762f52 still carries `font_size`), so the corner reads `1/1` with two hidden.
- This clone's `git config user.name` is literally the string `user.email` (a config slip, found round 5): commits the product makes carry it until the architect fixes the config; the act uses the machine's identity by ruling and touches no config.
