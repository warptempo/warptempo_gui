# The GitHub Recheck (history mode)

The arc landed 2026-08-03..04 — dd4757e (the diff model), 3e671a8 (the mode), 3efc64e (load-in-place-from-a-commit), 2324f62 (docs), 3b698b4 (the commit act and the two buttons), 7bc2af9 (docs), 58a449d (the first codex round's nine conversions), plus the live-pass round (view switching; projects_repo required) — the commit messages carry the full per-round narratives; this file is the standing ruling record. The feature: bare `h` (also the icon row's twelfth button, Icon::VcsDiff behind a separator gap, selected while the mode stands) opens a read-only view of how the live in-memory authoring state differs from the piece's committed checkpoints, `,`/`.` walk the checkpoints (and are the icon row's fourteenth and fifteenth buttons since 2026-08-05, Icon::GoPrevious / Icon::GoNext in that same group — the walk's pointer surface, greyed and pointer-dead outside the view), Ctrl+H REVERTS the selected differences into the live state (the row's thirteenth button, Icon::DocumentRevert, between the history button and the walk pair — its own section below), the marker lane paints the delta as green/red diff flags, `'` loads a viewed (or pasted) commit's whole sidecar state in place in memory, and Ctrl+Alt+R — the Render button, wearing "Save and Commit"/Icon::VcsCommit while the mode stands — SAVES the piece and publishes the current state as a new checkpoint. The architect's purpose: commit checkpoints to GitHub with confidence instead of keeping local file copies, and catch slips of the hand as projects grow.

## Transport and trust

LOCAL GIT, THE READ/WRITE SPLIT AS TWO ENTRY POINTS. The module (`src/gui/history_diff.{h,cpp}`) shells to git with an argv exec (no shell anywhere — corpus paths carry spaces) against the constexpr repo root. `run_git_capture` passes ONLY reads — `log`, `show`, `ls-tree`, `rev-parse`, `rev-list`, `diff-tree`, `status`, `remote get-url` — and `run_git_mutate` (round 5, the commit act's fenced writer and its ONLY site) passes ONLY `add`/`commit`/`push`; both inventory comments are authoritative, and the fence is which function a call site names. No fetch, no credential in the program: every read is against the LOCAL branch (`HEAD` since round 5 — the product is a commit producer, and a checkpoint whose push failed must still be visible history; `origin/main` would hide it until the next successful push), and the push authenticates through the machine's own SSH setup. main.cpp's `SIG_IGN` SIGCHLD auto-reap (the audio-player precedent) makes `waitpid` return ECHILD, so success is INFERRED, and since the codex round the inference is honest per class: captures are a TRI-STATE (a CLOEXEC self-pipe distinguishes could-not-exec from ran; the load-in-place path additionally cross-checks each captured blob against `ls-tree -l`'s own byte count, so a failed or truncated `show` can never stage a sidecar state the commit never held), and mutations are judged by REPOSITORY OBSERVATIONS THAT OUTRANK THE TRANSPORT (codex round 2): the commit verdict FINDS THE COMMIT CARRYING THE CHECKPOINT BY CONTENT SIGNATURE — over a bounded `rev-list before..refs/heads/<captured branch>` walk (cap 10; the range names the captured branch's own ref, never the symbolic HEAD — round 4), the commit whose message is the act's exact title, whose own diff touches a non-empty SUBSET of the three literal paths and nothing else, and whose resulting three blobs equal the bytes the act wrote (subset not equality: a checkpoint that changes two of three sidecars is the ordinary case; the bytes leg proves completeness) — so a concurrent unscoped commit that consumed the staged sidecars is never attributed or pushed, the checkpoint is found even under another commit landed on top, and the verdict runs REGARDLESS of the transport result (git moves HEAD before post-commit hooks, so a deadline-killed hung hook can sit over a checkpoint that landed — it is found and pushed, the anomaly noted). THE IDENTITY IS CONTENT, NOT AUTHORSHIP (codex round 3, the contract stated honestly rather than overclaimed): every leg is a fact about what a commit CARRIES, none about who ran the git, and content-equivalent commits are DELIBERATELY INTERCHANGEABLE — publishing one publishes the intended state, and the byte legs are what make that safe (nothing carrying different sidecar state can be substituted, no foreign path can ride along). An act-unique message trailer was considered and DECLINED: mechanism plus a permanent oddity in every checkpoint message, to distinguish commits whose content is identical, against a producer (the architect's wrapper) retired for checkpoints as of 2026-08-04. The pre-flight's Clean arm forks on the remote-tracking ref: caught-up → NothingToCommit; behind with the captured branch tip's bytes confirmed → push the existing checkpoint (the retry-after-timeout route, and the correct answer to a terminal-committed unpushed branch). The push verdict is the remote ref catching up, transport discarded — and CONTAINMENT MUST BE OBSERVED, never inferred from silence (round 3): the walk is `rev-list --count`, whose successful run ALWAYS prints a number ("0" is containment), so an empty or non-numeric answer is a FAILURE and the two verdict sites take it as such — the push reports the checkpoint unconfirmed (never `Committed`), the clean arm treats the branch tip as unpublished and falls through to the push with its own stderr line. THE TRI-STATE SURVIVES THE REF READ TOO (round 4): `rev-parse --verify` answers "" for a ref that is ABSENT and for a read that could not be MADE, so an empty first read is followed by a POSITIVE-WITNESS probe of `refs/heads/<captured branch>` — a ref the act cannot be running without — and the witness printing means the repository answers queries (so the target ref is genuinely absent → Missing), while a witness that is also empty means UNAVAILABLE; without it an unreadable repository routed as an ordinary "the remote does not carry it", omitting the clean arm's "could not read" line and printing `Push failed` where the transport had not failed. AND THE WITNESS ADJUDICATES ONLY A READ THAT RAN (round 5): the target probe returns the capture's own verdict beside the object name, so a probe that COULD NOT EXECUTE is Unavailable on the spot with no witness probe run at all — a witness proves the repository answers NOW and can never reach backwards to prove an earlier invocation executed, and letting it convert a could-not-exec target read into "absent" manufactured exactly the false negative the tri-state exists to prevent (`Push failed` reported over a tracking ref that carries the checkpoint, and "committed locally and still unpushed" printed without an observed Missing). The one false unavailable, recorded: a captured branch deleted mid-act, which costs a fall-through push — and that push is NOT refused by the branch's absence (round 5, correcting the recorded consequence): the refspec sends an object name the act already holds, so git resolves no local branch to send it and can create or update the remote branch with the local one gone (verified live). The conservative routing is safe because a redundant push moves nothing at the remote, not because the missing branch would stop it. WHAT THAT FALL-THROUGH PUSH ACTUALLY DOES at the remote (round 4, correcting "a no-op if the remote did carry it"): it is a no-op only at TIP-EQUALITY (`Everything up-to-date`); a DESCENDANT remote tip — which containment also calls carrying it — makes the explicit `<sha>:refs/heads/<branch>` refspec a backward move and git REJECTS it as a non-fast-forward, safely, since nothing forces, and a rejected push does not update the remote-tracking ref either (both verified live), so the verdict reads what that ref already said. Nothing at the remote is lost or rewound in either case, which is the claim the fall-through rests on. And the local-checkpoint line no longer claims "still unpushed" over an unobservable containment: the clause is printed only after an observed Missing. THE PUSH CONSUMES THE VALIDATED DESTINATION (round 3, the real TOCTOU close): the guard runs at init for the mode and again at the mutating boundary for the act, and the URL that second asking validates is PINNED into the push child's own config — a `-c remote.origin.pushurl=` clear followed by `-c remote.origin.pushurl=<validated>`, both halves needed because the key is multi-valued (a lone `-c` ADDS a destination; the empty value clears the list), which in the multi-pushurl case replaces the whole validated set with one confirmed member. The named remote stays in the argv so the remote-tracking ref still updates. The other two terms of the publication are bound with it: the attributed commit is pushed BY SHA (`<sha>:refs/heads/<branch>`, no force anywhere — a remote ahead is an ordinary fast-forward refusal; the recorded trade is that a commit sitting on top of the checkpoint stays unpublished, the user's own to push), and the branch is read ONCE at act start — the act's ONLY reading of the symbolic HEAD for a VALUE — and serves the refspec, the tracking-ref observation AND every source-side observation the act makes (the `before` read, the attribution walk's tip, the clean arm's byte confirmation all name `refs/heads/<captured>`; round 4 closed the gap where they still followed HEAD, which let a mid-act checkout have the walk attribute a commit made on ANOTHER branch and the pinned push then publish that branch's ancestry onto the captured one — verified live). THE ONE OBSERVATION THAT CANNOT BE SPELLED IS CHECKED INSTEAD (round 5, completing that binding): `git status` takes no ref and always answers for whatever HEAD points at, so the pre-flight compares the `##` header it already demanded as its ran-witness against the captured name and a MISMATCH FAILS THE ACT CLOSED with one line naming both branches — never Clean, never Dirty, nothing committed or pushed, the written sidecars left in the working tree as after any other failure here. Without it the clean arm could end the act on another branch's answer (reproduced live: captured `main` carries the old bytes, a topic branch already carries the new ones, a checkout lands between capture and probe, the freshly written bytes match the topic tree exactly → `status` says CLEAN, captured `refs/heads/main` resolves with `origin/main` caught up to it, and NothingToCommit returns before the byte confirmation ever runs — the user told the checkpoint already carries bytes the captured branch does not have). Header grammar verified live in every shape (`## main`, `## main...origin/main [ahead 1, behind 2]`, `## HEAD (no branch)` = detached and compares as the captured detached "", `## No commits yet on main`): the cut is on `...` then on a space, both unambiguous because a refname may contain neither two consecutive dots nor a space, so `feat/a.b.c` parses whole; any shape the parse has not seen simply mismatches and fails closed. The commit itself necessarily lands wherever git is checked out, and needs no pinning: if a checkout intervened the walk over the captured ref's range finds nothing and the act returns CommitFailed having published nothing, which is the honest outcome. A detached HEAD keeps the HEAD spelling (there is no branch ref to name) and publishes nothing, saying so. The one destination rewrite the guard cannot see, recorded at its site: `url.<base>.insteadOf` redirects a URL when git USES it while `remote get-url` reports the raw config value — pre-existing, and unclosable by pinning. `run_git_mutate` carries a 30s deadline whose expiry kills the child's whole process group (setpgid; git's own ssh/hook children die with it) under a pre-fork non-interactive environment (GIT_TERMINAL_PROMPT=0, ssh BatchMode, stdin /dev/null), and shares the capture's exec self-pipe so could-not-start is a real verdict on both entry points. Tree listings are NUL-separated (`ls-tree -z`) so a UTF-8 base name survives `core.quotePath`.

`projects_repo` (settings.md owns the key's full record; a REQUIRED key like every other since 2026-08-04, the one-day optional exception retired at the architect's live pass) names WHICH repository is the projects home; the local clone is the transport, and init's guard normalizes the setting against the clone's FETCH URL AND EVERY EFFECTIVE PUSH URL (`remote get-url` + `--push --all` — a configured pushurl must match too, or the checkpoint act could publish to an unconfirmed repository; scheme, scp `git@host:path`, trailing `.git` all compare equal), refusing a mismatch with one stderr line, and — at the pre-push asking — handing the first validated push URL back as the destination that asking's caller pins. An empty value never matches — the feature's opt-out. Which repository, not how fresh: the guard never checks staleness.

## The match and the walk

THE MATCH IS BY COMMITTED FILE NAME UNDER `projects/` (architect 2026-08-03; narrowed from the whole tree 2026-08-04 — the repo-layout convention is `<repo>/projects/`, one folder per piece, and a theoretical other user builds their own repo with a `projects/` folder): the loaded source's sidecar base name (the loader's own `stem()` derivation, mirrored — it strips only the last extension, load-bearing for period-heavy titles) is resolved against HEAD's tree filtered to `projects/`; exactly one directory may carry the sidecars. The walk is `git log -n 20` over `:(glob)projects/**/<base>.<ext>` pathspecs with the basename's wildmatch metacharacters ESCAPED, every mutation/status pathspec wears `:(literal)` magic (a filename carrying `*?[` can never widen a checkpoint), and each commit's snapshot paths resolve from THAT commit's own tree — preferring HEAD's own matched directory, taking a sole other, and otherwise REFUSING as ambiguous (through `read_commit_sidecars`) rather than silently showing one piece's history as another's. THE WALK IS LOAD-GATED (architect 2026-08-04): membership is THE LOAD-IN-PLACE GATE ITSELF — `load_commit_sidecars_strict`, the exact resolution + scratch staging + three strict frozen loaders the `'` act runs, ONE predicate with no relaxed variant — applied eagerly to every candidate at init, so every checkpoint the mode can step to is one `'` can load. A candidate that refuses (a missing sidecar, a parse refusal, an ambiguous per-commit path resolution) leaves the walk at entry, ONE stderr line counting the hidden ones; the corner's `n/N` counts the ELIGIBLE list, and the gate keeps each member's three sidecar snapshots as its then side, so no delta ever runs git. The per-commit `Ambiguous` display machinery is RETIRED with the gate — an ambiguous commit is never shown at all, and the ambiguity refusal survives only as the `'` act's own arm for a PASTED spelling naming a commit outside the walk. ACCEPTED TRADES, recorded: basename matching cannot see history across a SOURCE RENAME, and every legacy-format checkpoint leaves the walk under the gate exactly as the `examples/`-era commits left it under the round-5 narrowing (the walk heads at the first load-clean commit until the first commit act). A piece with NO committed history — or none whose sidecars pass the strict load — cannot open the mode at all; its bootstrap stays a manual act.

## The diff model

WHICH TWO SIDES A DELTA COMPARES IS THE COMPARE MODE'S ANSWER (the section below; the cumulative reading is described here, and the iterative one substitutes the NEXT-NEWER ITEM for the now side — a commit, or this same frozen now side at the newest index — through the same mechanism). The CUMULATIVE NOW side is the live in-memory state serialized through the save writers' own string halves (`format_warpmarkers_text` / `format_phaseresetmarkers_text` / `format_settings_text` — the round-1 split; byte-identical to a Ctrl+S, no disk touched) and is FROZEN AT init(); the mode's mutation gate is what keeps the frozen snapshot honest (authoring is blocked while the mode stands, so it cannot go stale by construction). Then-vs-now is an in-tree line LCS (DP with prefix/suffix peeling and a cell cap) typed into per-commit deltas: warp added/removed/changed carrying each line's VERBATIM tempo token (the flag shows the sidecar's own spelling, never a re-derivation), phase resets added/removed/changed (their line is frame plus disable prefix, so a `#` toggle pairs as a genuine change, symmetric with warp), and the `scale=` tokens from both sides (THE ONLY SETTINGS KEY THE MODE DISPLAYS — a recorded asymmetry: every other settings delta is deliberately invisible). A same-frame remove+add pair IS the changed entry — it feeds the double flag. WALK MEMBERSHIP IS LOAD-GATED (2026-08-04, superseding the old two-tier "diff is parse-free / load-in-place is parse-gated" answer): everything visible is loadable, so both sides of every diff are loader-clean text with all three files present, and the diff mechanism stays line-based with NO leniency arm left — the drop-unparseable-lines arm with its counters (`GuiHistoryLineCounts`), the missing-file-reads-as-empty-side arm, and the per-commit `Ambiguous` state are all DELETED as dead code (the architect's no-legacy rule: the program never imports leniently; an old-format checkpoint is hand-edited, never tolerated). The extraction's boolean is the parse's own optional shape, not a tolerance. The one degraded arm kept is an ALLOCATION guard, not a format leniency: a loader-clean sidecar past the DP cell caps diffs coarsely as replaced whole. Deltas compute lazily against the snapshots the gate kept and cache per (commit, compare reading); stepping revisits and reading switches alike are free, and no delta runs git at all.

## The two compare modes (2026-08-05)

A CHECKPOINT HAS TWO INTERESTING OTHER SIDES, and the architect ruled that the view offer both rather than choose. The reading the mode shipped with is named CUMULATIVE — the viewed checkpoint against the FROZEN LIVE NOW SIDE, which answers "how does what I have now differ from that checkpoint" and is the reading a restore or a load-in-place is about. The new one is ITERATIVE, and it COMPARES FORWARD, TOWARD NOW (architect 2026-08-05, superseding his walk-parent pairing of earlier that day): the viewed checkpoint is the OLD side and THE NEXT-NEWER ITEM the new one — the next-newer ELIGIBLE WALK MEMBER at every index but the newest, and THE FROZEN LIVE NOW SIDE at the newest, which has no committed successor. It answers "what happened AFTER this checkpoint", one step at a time, which is what a walk backwards through the history is asking at each stop. ITERATIVE IS THE DEFAULT AT EVERY ENTRY — RATIFIED at the architect's live pass (2026-08-05), the reading he kept, and the bit is a plain member initializer on `AppState::HistoryMode::compare`, so the whole-struct reset at both edges applies it and no visit inherits the last one's reading.

THE COLOR GRAMMAR IS ONE RULE ACROSS BOTH, which is the pair's whole reason for fitting: the NEWER side is always the diff's now side, so green `[+]` is what the newer side has and red `[-]` what the older side had and the newer dropped. In cumulative the newer side is the session; in iterative it is the next step forward — the newer checkpoint, or the session itself at the newest index. Nothing in the lane, the flag texts, the double-flag pairing or the colors knows which reading it is drawing — `rebuild_history_diff_flags` reads whatever delta it is handed.

THE MECHANISM IS THE SAME TYPED LINE DIFF, taken off the walk position: `compute_commit_delta` (history_diff.cpp) runs the identical LCS + extraction + same-frame pairing over whichever pair of texts the reading names, and `delta_at` picks the pair — cumulative = the member's kept snapshots against `now_`, iterative = the member's kept snapshots against the NEXT-NEWER member's (or against `now_` at index 0). Both sides are LOADER-CLEAN in both readings, since walk membership is the strict whole-set load and the gate kept every member's three sidecar texts; so a delta still runs NO GIT AT ALL, in either reading. THE `scale` PAIR RIDES THE SAME SUBSTITUTION (then = the older side's token, now = the newer's), so the corner line says the same kind of thing in both. CACHED PER (INDEX, COMPARE) — one vector of optionals per reading, both sized once at init — so a switch back is as free as a step back and no delta is ever recomputed.

THE TWO READINGS COINCIDE AT THE NEWEST INDEX, deliberately and by construction: both are the newest checkpoint against the live now side there, so A SESSION FRESHLY LOADED RIGHT AFTER A COMMIT SHOWS A BLANK LANE IN BOTH READINGS — the architect's stated purpose for turning the direction forward. Nothing has to know: the two are simply the same delta, computed and cached twice in their own slots rather than aliased, since the coincidence is a property of the pairing and not a rule any reader should carry. The visible cost is that a switch at `1/N` redraws the same lane and moves only the lit tab; accepted, and it is the honest face of the pairing.

EVERY INDEX HAS A FORWARD PARTNER, so THE EMPTY-DELTA SPECIAL CASE IS DELETED — the walk-parent pairing's oldest-member arm, its structural no-diff-run and the reasoning that justified it are gone from the model, the header and the docs alike. The walk's OLDEST end is now an ordinary index whose partner is the member one newer, and its NEWEST end is where the live state is. AND THE NEXT-NEWER MEMBER MAY SPAN COMMITS THE LOAD GATE HID: that is the walk's own honesty rather than a defect of the pair, since an ineligible commit is not steppable, not loadable and not shown, and the nearest checkpoint the mode CAN show is the only one whose delta has two reachable sides. It applies BETWEEN COMMITTED NEIGHBOURS only — the newest index's partner is the live state, which nothing can hide.

THE TABS SELECT THE MODE, and it is the Render-button hijack applied to a whole row (kdenlive-redesign.md's row-3 record): while the view stands row 3 stops being the A/B tabs and becomes this bit's radio — tab A reads "Iterative", tab B "Cumulative" (sentence-case ordinary labels, not the Render row's two named title-case exceptions), the selected face marks the live reading, a press on the other switches and a press on the lit one is a consumed nothing. THE LOCK SLOTS ARE GONE WHOLE in there — not drawn, not reserved in the width, no rect published — because a padlock reports a TAB's read-only bit and these are not tabs; which also makes the press path's lock branch unreachable in the mode without testing for it. ITS HOTKEY IS CTRL+TAB — THE TABS' OWN CHORD, FOLLOWING THE SURFACE (architect 2026-08-05, SUPERSEDING his same-day "there is no hotkey, the surface is repurposed while the keys are not"): in the mode that chord TOGGLES the reading through the one switch owner instead of switching tabs, which is the mode-bit-selects-the-command shape Ctrl+Alt+R already has, and outside the mode it is byte-identical to before. It is claimed by `history_mode_owns_key` — the mode's own vocabulary, one line above the allowlist — as that predicate's ONE ctrl-carrying shape, ctrl-exact, so Ctrl+Shift+Tab's paired march stays the allowlist's consumed no-op. AND THE HAND EXCEPTION THE PAIR NEEDED FOR ONE DAY WENT WITH IT: `history_mode_disables_button` had to answer LIVE for two buttons whose chord was consumed, and the claim now says that by derivation — the walk asks `history_mode_owns_key` first, which owns Ctrl+Tab — so the two menu anchors are again the ONE hand entry. The row's mode-scoped DISABLED FACE, added 2026-08-04 for exactly these two buttons, is RETIRED with the arc: row 3 has no disabled face again, and the dim machinery went with its producer rather than sitting in the painter unreachable.

THE READ-ONLY LOCK DOES NOT REACH THE SWITCH, deliberately: the gate that refuses on a locked tab is on_key's, and the switch dispatches no key. A lock means hands off the piece's authored state; the compare mode is neither authored nor per-tab, and refusing it would stop a locked session from READING its own history, which is the one thing the view is for.

A SWITCH IS A MODE EDGE, with the `,` / `.` step's shape exactly and ONE owner for the tabs' presses AND the Ctrl+Tab toggle alike (`GuiInputHandler::set_history_compare`): clear the mode focus (it indexes the painted list, and the two readings are two different lists), `drop_lane_stash_across_history_edge` (the fourth edge it now covers), `frame_history_view_whole_song` (the edges' full-zoom-out reset, below), full-window damage. The owner is IDEMPOTENT at its top, which is where the live tabs' radio rule comes from — no call site tests for "already selected", and it is what lets the keyboard toggle be a plain "the other one". EVERY READER OF THE DISPLAYED DELTA PASSES THE BIT, re-derived by grep on `delta_at`: the flag cache's rebuild, `frame_viewed_commit_diff_span`, `paint_trim`'s diff-span substitution and the bottom strip's corner line. And THE FLAG-CACHE FINGERPRINT CARRIES IT as its eighteenth field and the history block's fifth — one commit has two deltas and a switch moves no other fingerprint input (same session, same index, focus cleared to the same -1), so without it the lane would blit the reading the user just switched away from and the two would cross-blit freely.

THE ONE READER THAT NAMES CUMULATIVE EXPLICITLY IS `head_delta_empty`, the Save-and-Commit grey: the act commits the LIVE state, so "is there anything to checkpoint" is live-vs-newest whatever the lane displays. Under the FORWARD pairing that name now picks the same delta the session's bit would — index 0 coincides — and it is KEPT explicit anyway, because what this reader asks is live-vs-newest BY DEFINITION and spelling it is what keeps that true if the pairing ever moves again.


## The mode

`h` (bare-exact; `GuiKeys::H`) toggles — REBOUND FROM BARE `/` on 2026-08-05, the architect dropping `/` entirely, so it is an unbound no-op everywhere again and in-mode drops at the allowlist like any other unclaimed key; `,` older / `.` newer, clamped walls as consumed no-ops; the `i`-mode toggle shape, NO Esc binding (the Esc inventory stays six). The on_key gate sits directly under the drag-modal gate and above the read-only gate — every entry refusal (prompt, dropdown, loading, editors, live gesture) has already run above it, `h` typed in any editor is CLAIMED as a printable, and the read-only tab bit is structurally irrelevant to the mode's own keys. Exits, re-derived from `close_history_mode`'s callers: `h`, either load-in-place, THE COMMIT ACT when the checkpoint reached the repository (2026-08-05, the act's own partition below), and quit; resize survives and re-lays-out through the flag-cache fingerprint miss. The fingerprint carries a SESSION GENERATION (bumped at the one entry owner, surviving the close reset) because two visits open in the same shape — index 0, no focus, `active` true — and a close plus a reopen can reach the paint as ONE edge, so without it the new session's lane would blit the old session's flags.

THE MODE'S OWN KEYBOARD VOCABULARY grew FOUR families on 2026-08-05 (architect) — the three below plus CTRL+TAB, the compare toggle, whose ruling lives in the compare-modes section above — all CLAIMED by `handle_history_mode_key` one line above the allowlist rather than admitted by it, and all MODE-LOCAL — each reads the diff-flag list and the mode's own focus, never the live stores the ordinary arm walks, which is what makes them fit a view that shows no live marker. (1) THE DIFF-FLAG CYCLE: bare Tab steps the mode focus to the next flag, bare Shift+Tab and IsoLeftTab (shift-agnostic) to the previous, in `flags`' own order — `rebuild_history_diff_flags` leaves it sorted ascending by `time_frame`, so list order IS reading order and the hit stash's indices stay the same indices. Each step lands the playhead on the flag through `land_playhead_on_source_frame` and recenters AT THE CURRENT ZOOM, exactly as the live cycle does (the working-zoom snap both families carried for one commit was REVERTED the same day it landed — architect 2026-08-05, "no zoom on Tab": a walk must not re-frame the view under the reader, and the mode mirrors the live landing rather than owning a rule of its own); NO WRAP (the live cycle does not wrap either — nothing ahead is a consumed no-op), with no focus standing Tab takes the FIRST flag and Shift+Tab the LAST, and an empty list is a consumed no-op in all three of its shapes — an empty delta, an active column whose half is empty, and THE ONE-TICK GAP AFTER A `,` / `.` STEP, where the list has been dropped and not yet rebuilt (2026-08-05: `flags` is paint-cache output, so a step and a Tab arriving in one key batch would otherwise cycle the LEAVING commit's flags and leave focus and playhead describing different items; the drop is the pointer stashes' own cold-answer rule widened to the list those indices name — nothing is navigable that is not drawn). Repeat eligibility is inherited unchanged from the live cycle's own three shapes, so a held Tab walks the flags — and CTRL+TAB is excluded from it at the predicate, ctrl-exactly, so the compare toggle is not a Tab step with a modifier on it. (2) BARE HOME / END jump ABSOLUTE — frame 0 and the last frame of the active domain, deliberately NOT the trim bounds the live arms take: the view reviews the whole piece, and an End stopping at a trim bound would hide the flags past it (with a full trim window the two coincide, `trim_window_is_full`, so the difference shows only under a set trim). They clear the mode focus, the analog of the live arms' selection clear — the playhead is leaving the flag. (3) BARE `c` is the live arm's recipe with the mode's focus in place of the live marker's: region clear up front, re-land on the focused flag when one stands, then working zoom and centre on the playhead; `repair_last_selected` / `jump_playhead_to_focused_marker` deliberately do not run. ALL THREE KEEP THE LIVE ARMS' PLAYBACK AND REGION REGIMES (stop a live audition, dissolve a resting region — the keyboard stop rule's cursor-moving navigation class; the stop half is kept for the regime's shape rather than for a producer, no audition being reachable in the view since playback left it), which is where they part from the mode's diff-flag CLICK, a pointer route whose recorded minimalism touches neither. The face partition did not move, for either growth: no roster chord is any of the BARE shapes, and the one roster chord the vocabulary did take — the tabs' Ctrl+Tab — was already answered LIVE by the hand exception the claim replaced.

PLAYBACK IS REMOVED FROM THE VIEW, WHOLE (architect 2026-08-05, superseding every "playback stays live in here" clause above and below): it was SLOW — the mode's full-song trim forces a full target preview render — and buggy besides, so THE VIEW IS SILENT BY RULING. Bare SPACE left the keyboard allowlist and is a consumed no-op in here; BOTH SCRUB ENTRIES left the pointer allowlist (the bare RIGHT press over the whole waveform and the plain LOWER-HALF left press), so no press in the view starts audio at all; the ONE ENTRY OWNER (`open_history_mode_fresh`) STOPS A LIVE AUDITION as it opens, refusal-gated below init()'s unavailable return — with Space consumed a session playing at `h` would otherwise be unstoppable from inside; and the SCRUB CURSOR goes with the gesture, `pointer_cursor_kind` answering Arrow for the waveform's lower half while the view stands (the right-button scrub was always unmarked). Target-view PREVIEW renders are untouched — they are display, and the view still shows the target waveform — and so is Esc's render/batch cancel. WHAT THE WAVEFORM DOES INSTEAD is the regular views' placement press, below.

READ-ONLY WHILE STANDING — two allowlists plus one lockout. Keyboard admits exactly: `=`/`-`, `0` (full zoom out, or, once already there, THE MODE'S OWN `c` — no longer a toggle, 2026-08-05; the mode's edges already land at full out, so in here `0` is mostly that second arm, and it reads the diff-flag focus because `run_center_command` forks on the mode bit, which also means it is NOT a pure viewport move: it is `c`, region clear, stop and land included), PageUp/PageDown, Ctrl+S, Ctrl+Q, `'` (since round 3), Ctrl+Alt+R with its in-mode meaning (since round 5, and since 2026-08-05 only while the head delta is non-empty — the act's own grey, below), Ctrl+H (2026-08-05, the REVERT act, and only while a diff flag is selected or focused — its own section below; admitted rather than claimed, so the read-only gate reaches it), BARE ESC (2026-08-04 — admitted for its EXISTING bindings and no binding of its own: the bare-Esc inventory stays at six places, and the two reachable in here are the region clear — over a span formed before `h`, or, since 2026-08-05, one formed INSIDE the view by its own placement press and the drag it arms — and the render/batch cancel; neither touches authored state, and Esc CANNOT close the view because the toggle is `handle_history_mode_key`'s `h` alone — with no region and no render it is a consumed nothing), and — the architect's live-pass ruling — the VIEW SWITCHES, bare `t`/`p`/`1`/`2`/`3` with the view bar and the S/T + W/P radios riding the same chords: the diff DELTA is line-shaped and view-independent, only the painted subset is column-scoped, so switching views is exactly how both halves of a checkpoint's delta get reviewed (the switches run their normal handlers whole; the mode-local focus clears on every switch as on every step; NO A/B TAB SWITCH is admitted — views yes, tabs no, with Ctrl+Shift+Tab's march consumed here and Ctrl+Tab never reaching this gate since 2026-08-05, the mode's own compare toggle having claimed it one line above). Pointer admits: alt-pan, ctrl-strip-drag and the ruler drag — that is the whole pass-through list since playback left the view; everything else is consumed, the RIGHT BUTTON whole and both trim-bar ctrl writes included — except the mode's OWN plain acts: the TRIM BAR'S FRAMING DOUBLE-CLICK (the section below), the lane's focus click and, since 2026-08-05 (architect), the waveform's two: A DIFF FLAG'S STEM IS THAT FLAG'S CLICK (`hit_test_marker_stem` unchanged, reading the stash the mode's own lane pass publishes, so the ordinals and the "grabbable = drawn" rule both come free — the changed pair's single red stem resolves to its one ordinal exactly as its double-width box does; asked BEFORE the placement, the live press's own order between the two surfaces of one item, and upper-half-only by that owner's own half test), and EVERY OTHER PRESS ON THE WAVEFORM — PLAIN AT EITHER HEIGHT, OR SHIFT-EXACT AT EITHER HEIGHT — IS THE REGULAR VIEWS' PLACEMENT PRESS (architect 2026-08-05, the second half of the playback removal: with the lower half no longer the scrub, the whole waveform takes one act). It lands the playhead at its column AND ARMS THE REGION DRAG, through the two shared bodies (`place_playhead_at_click_column` then `arm_region_drag_at`) — so a motionless release is just the placement, the arm having dissolved any resting span at mouse-down, and motion past the shared gate grows a region with the playhead riding the moving endpoint, ended by the shared release and motion bodies with no mode arm. SHIFT ADDS NOTHING BUT REACHING IT, exactly as outside, where the former's anchor moved to the clicked column the same day (region-scrub-esc.md owns that reshape); in the UPPER half the two modified flag claims have first refusal, so a shift press on a diff stem is still that flag's range select and only a press that misses falls through. The press clears the mode focus and its selection ahead of its own gutter return (the analog of the live body's deselect — the playhead is leaving the flag, which is also why the shift arm is not the "deliberately independent" former it was for one day). THAT CLEAR IS RATIFIED (architect 2026-08-05, promoting the day-old coder's reading into the ruling): EVERY REGION FORMER DROPS THE SELECTION ITS SURFACE OWNS — the live entries the store selection, this one the mode's focus-plus-selection pair — so no former leaves a selection standing beside the span it draws, and the surface decides only WHICH selection that is (the family rule is stated in full at `RegionState`, app_state.h). The press touches NO store selection (the view's standing rule) and seeds no double-click surface, so a second click is another placement press. THE VIEW'S REGIONS ARE VIEW-LOCAL (architect 2026-08-05): the exit and every `,`/`.` step and compare switch clear a resting span — a span drawn in here is a reading mark on the checkpoint it was drawn against — which is also what restores "a region rests only beside an empty selection" for the EDITOR, nothing formed in here being able to rest out there; the surviving exception is scoped INSIDE the view, where the mode's flag selection and a span may coexist (the inventory is at `RegionState`, app_state.h). The ENTRY clears nothing, a span drawn before `h` being the user's. `x` is consumed, so nothing in the view can turn a span into a trim, and no scrub remains to preview one — it is a reading mark, not listening scratch. `pointer_cursor_kind` needed nothing for shift: it is deliberately unnamed over the waveform, in the view as outside. The cursor map needed nothing for those two: the live upper-half plain zone is the unnamed Arrow and stays it. All twenty-five chord buttons drop through `dispatch_redesign_chord` → on_key — no second membership (the history, load and walk buttons' in-mode meanings arrive through the same dispatch). AND THE ROSTER WEARS THE REFUSALS (architect 2026-08-04, the principle asked about the tabs and applied to the whole row set): while the view stands, every button whose act it consumes takes its row's DISABLED face and ignores the pointer — Undo, Redo, copy, paste, the BPM opener, iteration, follow, listen, and the two menu anchors — while Quit, the view bar's three, Save, the Save-and-Commit-faced Render, the S/T + W/P radios, the `'` load opener, the history button, the revert button (with a subject standing) and the walk's two arrows stay lit; the ones that work are exactly the ones that light. THE WALK PAIR IS THE RULE RUN BACKWARDS (2026-08-05): Older and Newer REST disabled and light only in here, because bare `,` and bare `.` are bound in exactly one place in the product — `handle_history_mode_key` — so outside the view there is no act for a live face to promise. That inversion is `redesign_button_enabled`'s own arm rather than this partition's: the derivation answers LIVE for the pair on its own (their chords are the mode's own vocabulary, which `history_mode_owns_key` admits), and the enabled predicate then reads `history_mode.active` directly. They do NOT grey at the walk's walls — a step past the oldest or newest checkpoint is a consumed no-op by key and by click alike, and the `n/N` corner readout is what says where the walk stands. BOTH TABS LEFT THE DEAD SIDE on 2026-08-05, with their lock slots: the view REPURPOSES the pair as its compare selector rather than refusing it, so they are live with a mode-local press route (the compare-modes section owns the ruling; they needed a hand exception in `history_mode_disables_button` only for the day the pair had no hotkey, and Ctrl+Tab becoming the compare toggle made the derivation answer LIVE for them on its own). Render is the ONE session-dependent entry (2026-08-05): it greys with the rest when the head delta is empty, since the allowlist stops admitting its chord there — the derivation carries that for free, which is why `history_mode_disables_button` takes the mode. It is the mode-scoped EXCEPTION to the icon row's never-grey rule and to rows 1 and 3 having no disabled face, taken on the view bar's own precedent (a whole surface wearing a state that is not the enabled bit), and the face is built from the product's one disabled blend `kRedesignDisabledMix` — every ink and edge retaining that fraction of itself over its row ground, so a dead button dims as one object and a dead SELECTED toggle keeps its state muted rather than hidden. THE PARTITION IS DERIVED, NEVER LISTED: `history_mode_disables_button` (input_pointer.cpp, which carries the authoritative inventory) walks the chord table and asks `history_mode_key_blocked` about each button's own chord — asking `history_mode_owns_key` first, since the mode's own vocabulary is consumed one line above the allowlist and the history button's bare `h` would otherwise grey out the very button that leaves — so admitting a chord lights its button on the next frame with nothing to remember. The two anchors are the one hand entry, having no chord at all: their lockout is `toggle_dropdown`'s first line. Read live from `history_mode.active` at `redesign_button_enabled`, so leaving restores every face with nothing latched; hoverability, the press claim and the tooltip all read that one predicate, so a dead button neither lights, dispatches nor advertises. The greyed anchors also stop the armed-row HOVER OPEN twice over: the row cannot even be armed in here (the arm's one producer is `toggle_dropdown`'s open path, refused, and on_key/on_button_press disarm at their tops — so the `h` that opened the view disarmed it), and the toggle would refuse anyway. `toggle_dropdown` refuses whole while the mode stands, closing the one dispatch route that reaches no key gate (`finish_dropdown_release`'s Settings half opens the prefilled editor directly) — a popup and the history mode are mutually exclusive in both directions. Two structural guards ride the mode edge: `drop_lane_stash_across_history_edge` clears the lane's whole published content at entry/exit/step — the flag hit rects, the stems and, since 2026-08-05, the diff-flag LIST those indices name (the stash's `marker_index` CHANGES DOMAIN across the edge — store index vs diff ordinal — and all three are tick-cadence, so the keyboard cycle needed the same protection the pointer already had), and `pointer_cursor_kind` is MODE-SCOPED PER ZONE (the read-only model exactly) IN TWO PLACES — the trim bar and, since playback left the view, THE WAVEFORM'S LOWER HALF, whose Scrub cue goes with the gesture it named. On the TRIM BAR the endcap, bridge and two ctrl cues go, since the mode consumes those gestures, and the PLAIN zone shows the ARROW with them — the locked tab's own answer for the same band (2026-08-05, superseding that morning's ZOOM cue, which had followed the framing act while it was a SINGLE click; the act is a DOUBLE-click now, and a double-click carries no cursor promise anywhere in the product, the live band's own span framing included). Cue and gesture agree by construction in here as outside.

## The view is a viewer (2026-08-05)

THE MODE STOPS MUTATING THE EDITOR'S NAVIGATION STATE (architect). Until this ruling the view navigated the LIVE per-tab band, so whatever the review did — every pan, every zoom, every playhead landing — was what the editor came back to. Three parts, all display/navigation and none of them touching authored state.

THE ENTRY SNAPSHOT AND THE EXIT RESTORE. The ONE entry owner parks the trio the mode's own vocabulary moves — `viewport_start_sample`, `zoom_level`, `playhead_cursor_sample` — on the session (`AppState::HistoryMode::entry_*`, cleared by the whole-struct reset like every other field but the generation), and the ONE exit owner puts it back through the family's own clamp chokepoints: `Viewport::move_playhead_to` (clamp_playhead_to_live_domain) then `Viewport::apply_zoom_to_start` (clamp_zoom_level + clamp_viewport_start), in that order, because the playhead move scrolls when its destination is offscreen and the explicit start write then lands over the top of it. Going through the clamps rather than writing the fields raw is what makes a RESIZE DURING THE VISIT — or a `t` into another total — leave a clamped-valid rest instead of a stale raw one. The read happens BEFORE the whole-struct reset and the apply AFTER it, so the synchronous rebuild those two calls kick sees the mode already down and republishes the LIVE lane rather than the leaving session's diff flags. ONE RESTORE SITE FOR ALL FOUR CLOSERS, and the two load-in-places need no exemption: each calls the closer on the first line past its last refusal and applies the LOADED file's own band some seventy lines later (the `tab_a`/`tab_b` replace, the clamps, the live-band pull), so the restore is simply overwritten by the state the user asked for.

WHAT IS NOT IN THE SNAPSHOT, deliberately: the ACTIVE VIEWS (the user keeps the S/T and W/P he switched to — those switches are how both halves of a delta get read, and undoing them would undo the reading), the A/B TAB (which cannot move at all in here — both switches are consumed, and since 2026-08-05 the tab buttons do not even show the A/B pair, being the compare selector — so the snapshot's tab cannot change under it), and trim, `read_only` and the selection, none of which the mode touches. THE TRIO IS ACTIVE-DOMAIN, NOT AUTHORED, STATE — in `T` the three fields carry TARGET frames — so the snapshot carries THE AUDIO VIEW IT WAS TAKEN IN beside them: the restore is bit-exact whenever that view has not moved (every visit that switches nothing, and every visit whose switches cancel), and when it has, the two frame-shaped values translate through the live warp map in the flip's own direction, the zoom LEVEL riding untranslated exactly as the `t` toggle carries it across.

THE EDGES LAND AT FULL ZOOM OUT AND THE FRAMING IS A CLICK (architect 2026-08-05, SUPERSEDING his same-day per-diff framing AT THE EDGES; the framing act itself survives whole, rehomed). Entry, each `,` / `.` step and each COMPARE SWITCH now reset the viewport to FULL ZOOM OUT — the whole song in the window — at the same three edges that drop the lane stash and synchronously in the same press (`frame_history_view_whole_song`, input_key_dispatch.cpp). That is the reading position a checkpoint review starts from: the flags are laid out across the piece and WHERE they sit is as much of the answer as what they say, and a user can still look around freely on one commit because stepping away and back resets it. Full zoom out is spelled as the span framer's own whole-song arm, `[0, total]` with no margin, which its centering plus the wall clamp degenerate to the per-file effective ceiling at start 0 — the rest bare `0` reaches, arrived at through the mode's one framing route rather than a second recipe.

THE FRAMING IS THE TRIM BAR'S PLAIN DOUBLE-CLICK (`frame_viewed_commit_diff_span`, ONE caller now): a plain left DOUBLE-click ANYWHERE on the trim-bar band zooms to the viewed checkpoint's whole diff span — the regular views' span-framing gesture exactly, on the same band and through the same machinery, with this act as its command (architect 2026-08-05, SUPERSEDING the single click it shipped with that morning: the band behaves like a read-only tab's, where the trim drags refuse and the framing double-click still navigates, so a SINGLE click in here is the consumed nothing it is in a locked tab). THE MACHINERY IS THE LIVE BAND'S UNCHANGED — the consume-before-arm test is shared (`trim_bar_double_click_at`, over the candidate snapshot `on_button_press` takes before clearing the field, which is why `handle_history_mode_press` is handed it), and the mode's press records the same `TrimBarPressSeed` for the release to resolve; that release-side seeder needs no mode arm of its own, seeding on "the pointer never left the slack and no trim drag went live" while no trim drag can go live in here at all. The band is already DISPLAYING that span (below), which is what makes the gesture read as "zoom to what the bar shows", and the whole band including the endcaps is the surface, those endcaps being painted geometry with no gesture behind them in here. Read-only does not refuse it — framing is navigation, exactly as the pan and the zoom are. THE RECIPE IS THE TRIM-BAR DOUBLE-CLICK'S — its framing core `frame_span_into_view`, the deterministic zoom-TO-span with the 2.5%-per-side margin, NOT the undo/redo restore's scroll-if-it-fits-else-frame variant (that wrapper lives in undo.cpp's group tail and only frames as its cannot-fit fallback) — and the EMPTY-DELTA arm falls through to the whole-song owner above, harmless where the edges have already left the view. A one-frame span is not a special case: the framer clamps the degenerate span to 1.0 before its log2 and saturates at `kMinZoom`, resting at the deepest zoom centred on that frame. Both routes move the VIEWPORT and nothing else — no playhead, no focus, no selection. Inherited from the double-click unchanged: `apply_zoom_to_start` is deliberately not a follow-suppression producer, so outside the view a framing made while playback chases with follow on is snatched back by the chase on the next tick — unreachable in here since 2026-08-05, the view auditioning nothing to chase. The act is IDEMPOTENT (the framer's `apply_zoom_to_start` no-ops when nothing moves), so repeated double-clicking rests.

THE DIFF SPAN ITSELF is `GuiHistoryCommitDelta::frame_span` — min/max over ALL SIX entry vectors in SOURCE frames, false when no entry carries a frame (the empty delta and the `scale`-only one alike; a scale change has no frame). It is THE WHOLE DELTA, never the painted half, because its readers describe the CHECKPOINT rather than the lane: a span that shrank when the user pressed `p` would make a view switch — whose purpose is reviewing the other half of that same delta — move the viewport out from under him. It reads the delta directly through `session.delta_at(index, compare)` and never `AppState::HistoryMode::flags`, which is paint-cache output on a once-per-tick cadence and empty for a frame after every mode edge. The framer converts to the active domain through `source_frame_to_active_domain`, whose target arm is `nearbyint(map_source_to_target(...))` — `frame_to_paint_sample`'s own formula — so the framed span is exactly the span the lane paints its flags across in either view.

THE TRIM BAR DISPLAYS THE DIFF SPAN while the view stands (empty delta → the full window), as ONE display-only substitution at the ONE paint site: `GuiPaintHandler::paint_trim` swaps the source-frame pair ABOVE the `displayed_trim_ms` mapping, so the substituted frames ride the target view's display map exactly as the authored pair does. `app.trim` is not read, not written and not shadowed. Nothing consumes the painted pair for BEHAVIOR in here — the bar's three WRITING press routes are consumed by `handle_history_mode_press` (which is also why the band's trim cues go, the Arrow standing over the whole band), its span-framing double-click is REPLACED there by the diff-span framing rather than consumed, and the endcap/bridge hit tests are reachable only from those consumed presses — so the standing "paint == hit by shared owners" coherence is moot in this one state rather than broken. The band's ONE live gesture, the plain framing double-click above, reads the DELTA rather than the painted pair, so it adds no hit test of its own. THE RECORDED INCOHERENCE THIS SUBSTITUTION CARRIED — an audition inside the view starting and stopping at the AUTHORED window while the bar above it showed the diff span — IS GONE WITH PLAYBACK ITSELF (2026-08-05): the view auditions nothing, so the borrowed bar has no behavioural reader left at all, and the substitution is display and only display.

## The lane display

While the mode stands the lane suppresses all live marker flags/stems (the playhead head still paints), AND SO DOES THE PHASE-RESET LEAD-IN RING, the waveform's own live-marker surface (architect 2026-08-05 — the 1px purple ring reading `kMarkerFlagFill` is triggered by `app.last_selected_marker`, the LIVE focus, which the view neither reads nor clears, so it went on painting over the delta until the gate joined this suppressed inventory at `phase_reset_overlay_band`, the visibility owner; `Selection::phase_overlay_subject` deliberately does not mirror it, that mirror being SELECTION state while this is a display fact — a divergence with nothing observable behind it since playback left the view the same day, the mirror's other reader being Space's lead-in launch), and paints the current delta IN THE SESSION'S COMPARE READING (the pair's section above; the lane is reading-agnostic by construction — green is the newer side in both) through the SAME `iterate_visible_flags_impl` → `frame_to_paint_sample` mapper live markers ride — identical placement in both views by construction, the current session's warp map doing the target-view mapping for both sides. Text: `[+]<token>` / `[-]<token>`, the payload DIRECTLY AGAINST the bracket with NO SPACE (architect 2026-08-05, superseding the arc's original `[+] <token>`), the `#` disable spelling prefixed when set, applied UNIFORMLY to phase resets (`[+]` / `[+]#`) since their same-frame change IS a disable toggle. ONE OWNER FOR THE SPELLING, `history_diff_label` (declared in paint_handler.h since the corner line became its second caller, defined in waveform_cache.cpp), so no surface can bracket differently. The changed pair is ONE double-width flag: red left `[-]then`, green right `[+]now`, one outer border, the fill boundary its only seam, RED stem (deference to the old — architect's ruling). Stems always paint the class's UNSELECTED fill. Diff labels are deliberately NOT nine-glyph-truncated: the token is the flag's whole point (the shared cull's width bound generalized to a parameter for this). Colors are the eight crop-exact `kHistoryAdded*`/`kHistoryRemoved*` constants (render.h palette block, provenance = the four row_5_lane_3 crops, modally sampled — the sampling also established that the LIVE red class already matches its crops exactly: `kMarkerFlagFillRed`/`kMarkerFlagEdgeRed` are the selected red crop's pair, `kMarkerStemRed` the unselected fill).

MODE-LOCAL FOCUS: a plain flag click swaps that flag (both halves of a double) to the selected faces and lands the playhead through `land_playhead_on_source_frame` (hoisted from `land_playhead_on_marker` — one landing recipe); the live store selection is untouched. THE FLAG HAS TWO CLICKABLE SURFACES since 2026-08-05, its lane box and its STEM in the waveform's upper half, one shared body (`focus_history_diff_flag`) so they cannot answer differently, and the recorded MINIMALISM covers both — no playback stop, no region clear, it is the same click at a different pixel. Empty-lane clicks clear it; a MODIFIED lane click that hits no flag is still consumed; it clears on step, on each compare switch, on each view switch, on exit, on bare Home / End and on the waveform PLACEMENT press (any plain or shift-exact waveform press that hits no stem, either half), the playhead having left the flag — and since 2026-08-05 the multi-selection below clears with it at every one of those, through the ONE clearer that takes the pair (`clear_history_mode_focus`, app_state.h). The bottom-left corner (the modal editors' rect — never contended, by gate) shows `<n>/<count> <shortsha>`, then `Scale: [-]<then> [+]<now>` — through the lane's own spelling owner, so brackets and spacing match the flags — ONLY WHEN THE SCALE CHANGED (architect 2026-08-05, superseding the arc's unchanged-token report: a value both sides agree on is not a difference, and this view shows differences), tokens verbatim. An empty side renders as its bracket with nothing after it, unreachable while `scale` is a required key and every walk member is strict-load clean.

## The multi-selection and the revert act (2026-08-05)

THE VIEW GAINED A WAY TO PUT DIFFERENCES BACK (architect, the arc's last feature): a MODE-LOCAL MULTI-SELECTION over the diff flags, and CTRL+H, which applies each selected flag's INVERSE to the live store and closes the view. It is FULLY MANUAL AND IT ALWAYS FORCES — the architect's explicit ruling and the whole shape of the feature: there is not one coherence check anywhere in it, the user may select any subset of any commit's delta in either compare reading, and an "erroneous" state is a state he asked for. The mitigation is the product's ordinary one, ONE undo entry for the whole act.

THE SELECTION IS THE LIVE MODEL RE-EXPRESSED MODE-LOCALLY (`AppState::HistoryMode::selection`, ordinals into `flags` beside the focus; the STORE selection stays as untouched as the focus click leaves it). A PLAIN click on either flag surface keeps its exact behaviour — focus + land — and CLEARS the set, the focus then being a selection of one; SHIFT-click takes the contiguous ordinal RANGE from the focus (the list is frame-sorted, so an ordinal range is the visible span) or the clicked flag alone with no focus standing; CTRL-click TOGGLES one flag's membership. Both modified arms then set the focus to the clicked flag and LAND the playhead on it, one body over both surfaces (`select_history_diff_flags_modified`). RECORDED CONSEQUENCE: a ctrl press removing the last member rests on the focus alone, that flag still lit and still the subject — the same state a plain click leaves, the live column's focus repair having no analog here. THE PRECEDENCE at the press: a flag surface claims only where a flag is ACTUALLY hit, so a modified LANE press that hits nothing stays the consumed nothing it always was, while over the waveform the claim is the STEM's alone — a ctrl press missing every stem still arms the dual-axis strip drag, and a shift press missing every stem falls through to the PLACEMENT PRESS (which shift reaches at either height since 2026-08-05, the stem claim being upper-half-only by its owner's own test). PAINT: a selected flag wears the focus's own brightened faces, ONE face for both by ruling, and the flag cache gained its SIXTH history fingerprint field for the membership (`fp_history_selection_hash`, the live lane's own hash owner). CLEARERS: the set clears wherever the focus clears and through the same line of code — step, compare switch, both view switches, Home/End, the placement press, the empty-lane and plain clicks, entry and exit — which is what covers the ordinal-domain hazard the focus already had; the Tab cycle sets the focus and clears the set, the live cycle's replace-with-a-singleton shape.

THE ACT (`GuiInputHandler::run_history_revert`). THE SUBJECT is the selected ordinals, else the focused one alone; with neither there is no subject and the chord is not admitted at all. The COLUMN is the active one by construction (the lane paints only that half of a delta), so the store is chosen once. THE PER-CLASS INVERSE: an ADDED flag DELETES the live marker at its exact frame (none there = nothing happens for that flag, never a refusal); a REMOVED flag PUTS THE THEN SIDE BACK, replacing any live occupant; a CHANGED double flag SETS the live marker at that frame to the THEN side — the same insert-or-replace primitive as the removed arm, so the two share one body. Blind in BOTH compare readings: the then side is whatever the displayed delta's then side is. THE THEN VALUE TRAVELS AS TEXT AND THE FROZEN PARSER JUDGES IT — the flag carries the removed line's payload token verbatim (`HistoryDiffFlag::then_token` / `then_disabled`, filled from the same delta entry the label is built from), the act reconstitutes the sidecar LINE and hands it to `parse_single_canonical_line`, so pass markers, label definitions, references and typed scales all restore with no vocabulary of their own here and there is no second grammar to drift. The phase reset column needs no trip: frame plus the disable bit IS its line. A restored warp marker arrives as a FRESH `GuiWarpMarker`, session-only iter/bpm scratch at its defaults, exactly as a load delivers it. It STOPS A LIVE AUDITION (the load-in-place's regime — a store rewrite under playback), CLEARS THE LIVE SELECTION (store indices, which it inserts and removes under), pushes ONE undo entry over the one store it touched and marks dirty — all four only when something actually changed, so a subject that found nothing to do leaves no entry behind.

THEN THE VIEW CLOSES, after the mutations land: the act rewrites the very state the frozen now side was measured against, which is the load-in-place's own argument applied to a narrower write, and the exit owner's entry-band restore then runs over the finished state. It closes even when nothing changed — the act ran and answered. THE MODE'S MUTATOR COUNT IS THREE: `'`, Ctrl+Alt+R and Ctrl+H.

ITS CHORD IS ADMITTED, NOT CLAIMED. Ctrl+H is on the keyboard allowlist (`history_mode_key_blocked`) rather than in `handle_history_mode_key`'s vocabulary, and that is what puts the dispatch in on_key's ordinary body BELOW the read-only gate: a locked tab refuses the act exactly as it refuses `'` and Ctrl+Alt+R. The admission is CONDITIONAL on a subject standing — the head-delta precedent exactly, one decision (`history_mode_revert_subject_standing`) serving both the key's refusal and the button's grey — and it differs from the head delta's in cadence, moving with every click instead of being measured once at entry. Outside the mode Ctrl+H is unbound.

THE BUTTON is row 4's, immediately right of the history button and left of the walk pair (Icon::DocumentRevert, tooltip "Revert (Ctrl+H)", no selected bit — it is an act, not a toggle). It RESTS DISABLED like Older and Newer, for their reason (its chord is bound only inside the view), and inside the view it greys again whenever no flag is selected — that second face falling out of the derived partition with no hand entry, since the allowlist has stopped admitting the chord. Like `'`, it stays LIT on a locked tab while the key refuses: row 4's never-grey rule, the lock's refusal not being the VIEW consuming the act.

## Load in place from a commit

In the mode, `'` opens the load editor PREFILLED (open-selected, the flag editor's convention — the product's one prefilling precedent) with the viewed commit's full SHA under a `Load: ` prefix; any spelling `rev-parse --verify <spelling>^{commit}` resolves is accepted, so a SHA pasted from GitHub's web UI works. THE WHOLE VALIDATION IS `load_commit_sidecars_strict` — the same ONE predicate that is walk membership since 2026-08-04: the commit's three sidecars resolve by base name from ITS OWN tree and reach the STRICT WHOLE-FILE LOADERS through an RAII per-commit scratch directory under the system temp root (all three parse entry points are path-only and frozen; a GUI-side string scanner would be the second grammar the strict gate exists to prevent; the directory dies on every exit including refusals). A PARTIAL COMMIT IS A REFUSAL — a load-in-place is a whole-state replace, and two files from a commit plus one from nowhere composes a state no checkpoint ever was. A prefilled spelling (a walk member's own SHA) always passes by construction; every refusal arm stays live for a PASTED spelling naming a commit outside the walk — unresolvable, partial, ambiguous, or legacy-format, the last failing BY DESIGN. Every refusal is the editor's red-flash with typed text preserved plus one stderr line, first-error-only, nothing touched.

The apply mirrors `load_render_entry_in_place` step for step (settings.md owns that sequence) and drops exactly: the WAV existence check (the corpus stores no audio — the loaded source IS the source), and the `renders/` wipe with its running-batch guard (the guard protects the wipe's race; this path wipes nothing, and a render live from before `h` renders its request snapshot untouched). One cross-file undo entry, dirty set — auditioning then Ctrl+Z back is the point. The mode closes as part of the act (the frozen now side no longer matches the live state). Autocomplete Tab is a consumed no-op in the mode (render-entry identifiers are the wrong vocabulary). Outside the mode, `'` and the load editor are byte-identical to before.

## The commit act (round 5; SAVE AND COMMIT since 2026-08-04)

Ctrl+Alt+R while the mode stands IS the checkpoint commit — the iteration-mode precedent exactly, the mode bit selecting the command inside the one render route; outside the mode the chord is byte-identical to before, and Ctrl+Alt+Shift+R stays consumed in the mode. The Render button wears the act (label "Save and Commit", Icon::VcsCommit — the row's second stateful string, after "Render Iterations", and the second architect-named exception to the sentence-case label convention: capital S, lowercase "and", capital C) and dispatches through its existing chord. The product's FOURTH PROMPT confirms (case-sensitive `[Y]es`, Esc through the prompts' existing binding, the prompt class's playback stop; the title is never asked — always `Update <id>`), and its question names both halves in prose — `Save and commit "<title>" to <repo>?`, sentence-case, the label's title case being the button's alone.

THE ACT SAVES FIRST (architect 2026-08-04), and the ORDER IS SAVE → WRITE → COMMIT → PUSH. On the prompt's `y`, before any checkpoint work, `run_history_commit` runs the FULL Ctrl+S act through its one owner `GuiSaveOps::save` — the same three atomic writes beside the source, the same per-write stderr, the same `mark_saved` + `recompute_dirty` tail — never a second writer or a partial imitation. The purpose is one sentence for the mental model (the checkpoint is what you see, saved and published) and the death of a real incoherence: in the architect's own workflow the loaded source LIVES inside the matched `projects/<id>/`, so the act's repo write already WAS the working sidecar set, yet the session still carried the dirty dot over bytes a save would have written verbatim. A FAILED SAVE REFUSES THE WHOLE ACT — the save returns bool and every arm that fails has already named its path on stderr; the refusal adds one line of its own and returns ABOVE `commit_history_checkpoint`, which is the only call in that body that writes a byte or spawns a git child, so "no checkpoint-side write, no git subprocess, nothing committed" is a property of the ordering rather than of a check (the prompt is already down by then, matching every other failure's shape here) — narrower than "no repo write": the save's own three writes are sequential, not cross-file transactional (`save_ops.cpp`), so a save that fails partway through can leave earlier atomic renames on disk exactly as any ordinary Ctrl+S failure can, and in the coincident `projects/<id>/` workflow those are repository working-tree paths; the ordinary save's documented partial-write-on-I/O-failure contract is unchanged by this act. THE DOUBLE WRITE IS DELIBERATE: in the coincident workflow the save and the act's own write hit the SAME three paths with BYTE-IDENTICAL content (`build_history_now_side` mirrors `refresh_active_tab_view_from_app` onto local copies, so a save running first changes none of its bytes) and both are the atomic tmp+fsync+rename, so the second rename replaces a file with its own contents; it is NOT deduped, since recognizing the coincidence would mean canonicalizing three absolute paths against the repo root and carrying a skip only one layout ever takes, to save three renames of bytes already held. When the source lives elsewhere (the older workflow) both writes are wanted and distinct — the save publishes beside the source, the act publishes the repo's copies. THE SAVE BUTTON STAYS (architect's explicit reasoning): saving to disk is its own act and the common one; two buttons because one is to disk and one is to disk and the remote.

The act proper: the three sidecar byte strings are REBUILT FRESH via `build_history_now_side` (the session's frozen now side provably drifts on the settings file — the per-tab view band moves under the mode's own allowed navigation — and the fresh rebuild is also what makes the after-commit empty diff true), written into the matched `projects/<id>/` directory through the atomic writer, then a pathspec-scoped `git commit -m "Update <id>" -- <the three paths>` (foreign staged work can never ride along) and `git push`. VERDICTS ARE REPOSITORY OBSERVATIONS, never child status (the SIGCHLD regime), and the transport paragraph above owns their current shape whole: the commit verdict is the CONTENT-SIGNATURE WALK over `before..refs/heads/<captured branch>` (not the old `rev-parse HEAD` before/after), the pre-flight `status --porcelain --branch` names the NothingToCommit non-failure distinctly, VERIFIES ITS `##` HEADER STILL NAMES THE CAPTURED BRANCH (a mismatch ends the act — the probe takes no ref, so checking is what pins it), and FORKS on the remote-tracking ref when it comes back clean (already published → NothingToCommit; committed-but-unpushed with the bytes confirmed → push it), the push verdict is that ref observably CARRYING the attributed commit, and every one of those observations reports "could not ask" as itself rather than as a yes. The push is bound to the destination the guard validated, sends the attributed commit by sha, and reads the branch captured at act start — to which every source-side observation is bound, by naming its ref or by verifying it is still checked out, so observation and publication cannot come apart under a checkout. Push hardening: stdin /dev/null + ssh BatchMode, so a credential prompt fails in one line instead of freezing the GUI. A push failure after a landed commit says so distinctly — the checkpoint exists locally and the mode's own HEAD-based history walk shows it (that walk is the DISPLAY's, distinct from the act's attribution walk, which reads the captured branch ref). The program embeds no identity and no credential: author and transport are the machine's git config and SSH setup.

THE ACT CLOSES THE VIEW (architect 2026-08-05, superseding his own 2026-08-04 "THE MODE STAYS OPEN" and the in-place re-entry that showed an empty diff as the confirmation — `open_history_mode_fresh` is back to ONE caller, bare `h`). The view asks one question, what differs between this session and a checkpoint, and an act that has just made the answer "nothing" has answered it. THE PARTITION IS THE PRINCIPLE, read against the act's five verdicts and stated at `run_history_commit`: THE VIEW CLOSES IFF THE ACT ENDS WITH THE CHECKPOINT IN THE REPOSITORY — `Committed`, `CommittedNotPushed` (the commit exists locally, and the act's own stderr line says exactly that, so leaving hides nothing) and `NothingToCommit` (the newest checkpoint already carries these bytes) — and it STAYS OPEN on every ending where nothing landed: `WriteFailed`, `CommitFailed` (which is also where an unreadable repository lands, the pre-flight `status` arm), the failed save prelude and the two silent guards. Nothing landed means nothing is finished, so the view is left exactly as it was, which is every refusal's shape in this product, and the act is one Ctrl+Alt+R away from a retry against the state still on screen. The session's generation counter is untouched by the change and now serves the act's own close-then-user-reopens pair.

AND THE ACT GREYS OUT WHEN THERE IS NOTHING TO CHECKPOINT (architect 2026-08-05). THE HEAD DELTA is the session's delta of WALK INDEX 0 — the newest eligible checkpoint — against the frozen now side, IN THE CUMULATIVE READING ALWAYS (2026-08-05: the act commits the LIVE state, so the question is live-vs-newest whatever the lane displays; it is the one reader of a delta that names a reading instead of passing the session's bit), and it is EMPTY when all six of the delta's entry vectors are empty and `scale` is unchanged (`GuiHistoryCommitDelta::is_empty`, the one predicate, on the type). It is measured ONCE at the one entry owner and stored on the session (`AppState::HistoryMode::head_delta_empty`, cleared by the whole-struct reset at close); it is STATIC FOR THE VISIT BY CONSTRUCTION, both sides being fixed — the now side frozen at init, and the allowlists admitting no authoring — and it reads INDEX 0 whatever the walk position, since the act always commits on top of the newest checkpoint, and index 0 CUMULATIVELY whatever the compare selector says. ONE MEMBERSHIP DECISION SERVES BOTH READERS: `history_mode_key_blocked`'s Ctrl+Alt+R admission is conditional on the bit, so the chord is a consumed no-op AND the Render button takes its row's disabled face from the same line (the predicate now takes the mode beside key+mods; neither caller restates a term of it, and the on_key gate sits above the render route, so nothing can raise the confirmation prompt on an empty head delta). The label stays "Save and Commit" while greyed — the stateful string reads the MODE bit, not the enabled bit — and the greyed button neither lights, dispatches nor shows its tooltip, all three off the one enabled predicate. RECORDED ASYMMETRY: "no changes" means no AUTHORING changes, in the delta's own vocabulary — the two marker columns plus `scale`. A session whose only drift is the invisible settings bookkeeping (the per-tab view band the mode's own navigation moves) greys too, even though a byte-level commit would land; deliberate, the same reasoning that makes `scale` the only settings key the mode displays. Ctrl+S stays live for the disk save, and the act's own `NothingToCommit` arm remains the byte-level backstop. The user-visible point: the button's face answers "does this session differ from the newest checkpoint" at a glance, and the delta is view-independent, so no S+W/T+P/T+W toggling is needed to know. The read-only tab's own grey on row 2 is independent and unchanged.

## Accepted costs and open items

- A render launched before `h` hides its progress line (the mode's corner line outranks it). ITS ESC CANCEL IS BACK, 2026-08-04: the architect took the one term, bare Esc joined the keyboard allowlist, and the cancel plus the region clear run inside the view (the allowlist paragraph above owns the ruling). The hidden progress line stands as the accepted cost.
- A delta's first computation runs no git at all since the load gate (each member's snapshots are kept at init), so `paint_bottom_strip`'s `delta_at` costs only the in-memory diff on a commit's first visit — the old "can in principle run git from the paint path" cost is retired with the lazy snapshot reads.
- Basename matching cannot see across a source rename (above).
- A READ-ONLY ACTIVE TAB blocks `'` (the load-in-place), Ctrl+Alt+R (the commit act) AND Ctrl+H (the revert act, 2026-08-05) in the mode as it does outside it (none is on the read-only allowlist, and that gate sits below the mode gate). RATIFIED AS STANDS (architect 2026-08-04): the lock means hands off everything, exactly as it blocks the save, and unlock is one `o` away — the view is lock-agnostic for VIEWING only. It follows that a locked tab greys Save and the Save-and-Commit-faced Render in the view as outside it (the read-only gate is row 2's own enabled term), while the `'` and Revert buttons stay lit on row 4 under the never-grey rule — the lock's refusal is not the VIEW consuming the act, so the mode-scoped disabled-face partition says nothing about it.
- NO A/B TAB SWITCH HAPPENS IN THE VIEW — ratified 2026-08-04 as "the tab switches stay consumed": views yes, tabs no (a view switch re-reads the same piece; a tab switch swaps the per-tab band the session was measured with). The tab-band argument stands untouched; both OTHER halves of that answer were SUPERSEDED on 2026-08-05. THE FACES stopped saying so when the tab SURFACE became the compare selector, live and mode-local, so it neither switches tabs nor advertises that it might. And THE CHORD stopped being merely consumed when Ctrl+Tab became the compare TOGGLE — the mode's own key, claimed above the allowlist, doing the mode's own work; Ctrl+Shift+Tab's paired march is still the plain consumed no-op. THE BARE HALF OF THAT RATIFICATION WAS SUPERSEDED 2026-08-05: bare Tab / Shift+Tab / IsoLeftTab became the mode's OWN diff-flag cycle (and `c` the mode's own centre), which the ratification's premise — Tab and `c` navigate by live markers — no longer describes. The tab-band argument was never about markers and stands untouched, so the two tab CHORDS and their faces are unchanged.
- Checkpoints predating the current schema refuse the strict load (a `font_size=` era file is an unknown-key refusal; a pre-2026-08-04 one lacks the now-required `projects_repo`), and under the load gate they LEAVE THE WALK TOO — the mode shows only checkpoints whose sidecars carry the full current schema, and a corpus whose every commit refuses cannot open the mode at all: the strict gate working as ruled, hidden count on stderr. Concretely at the gate's landing: the `550 - 1` walk's three candidates gate to ONE eligible (58a449d, whose ride-along settings refresh carries `projects_repo` and `gui_scale`; 37560ee lacks `projects_repo`, f762f52 still carries `font_size`), so the corner reads `1/1` with two hidden.
- This clone's `git config user.name` is literally the string `user.email` (a config slip, found round 5): commits the product makes carry it until the architect fixes the config; the act uses the machine's identity by ruling and touches no config.
