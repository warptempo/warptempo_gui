# Region, Scrub, Provenance, Esc Ladder, x / Shift+X

Moved verbatim from CLAUDE.md in the 2026-07-25 slim-down (project feature-complete). This file is now the authoritative home for this detail; CLAUDE.md carries only a digest and a pointer here. Keep it current with landed rulings.
Continues from marker-ui.md.

THE PLAIN WAVEFORM DRAG SELECTS A REGION (2026-07-21, opus session — the plain-drag SCRUB of the prior pass is superseded and the plain-drag region reinstated as a VERIFIED REVERT of the 61126db pivot against its f2bdaab parent; ScrubDragState / arm_scrub_drag_at and every scrub reference are gone; the shift-exact press's former strict no-op became the one-shot region former / demote 2026-07-23 — see below). The plain waveform PRESS splits by HALF (the scrub area, arc step 4, architect-designed 2026-07-23): the UPPER half keeps the placement press — clears the selection, drops the playhead at the clicked column (`playhead_frame_at_click_column` clamped through the live-domain clamp; marker-BLIND — the invisible stem is not a grab target and there is no 3px marker-snap magnet), reseeks a live scanner to it, overrides follow for the session, and arms the region drag (`arm_region_drag_at`, receiving the same clamped frame) — which DISSOLVES any resting highlight at mouse-down after snapshotting `pre_region` for an Esc-mid-drag restore (the drag never touches the selection anywhere — the press's deselect/demote was the committed act, and downward-only is structural: no selection write exists in the drag, its end points, or its Esc cancel), so the wash vanishes on press whether the gesture stays a click or becomes a fresh drag; the LOWER half is the SCRUB surface — the press launches the SCANNER from the clicked frame without moving the cursor (stopped → `scrub_launch_at` over the shared `launch_playback_from` body factored from toggle_playback, every Space gate identical incl. the target-view is_updating outer gate; playing → KILL-AND-REVIVE: the session stops and relaunches fresh at the clicked frame (architect 2026-07-23 — every scrub is a fresh audition re-capturing the loop verdict, never a positional seek; the upper-half click reseek stays keep-alive, that being its point)), the act is ONE-SHOT PER CLICK, Ableton-style (architect 2026-07-23 — the drag re-scrub was removed with its per-column quiescence-fence cost; a held press does nothing further and playback continues until stopped), and NOTHING else is touched (no selection, region, cursor, or follow — the upstream-audition gesture the re-coupling was built around). Past the shared press-becomes-drag gate (`kDragMovedThresholdPx` = 8 px latched Chebyshev since the 2026-07-24 unification — the one gate for every press-becomes-drag surface) the drag extends a REGION — a span of ACTIVE-DOMAIN frames (`RegionState`, session-only, never serialized, outside selection AND undo — a SPAN, never markers; the sweep-select machinery stays dead; the drag itself never moves the playhead, which rests at the press-column anchor = one region bound), painted as a full-height wash (`kRegionWashAlpha` = 0.10 over `kSelectedLight` #A8DBF5, the lightened Breeze blue — same 55%-toward-white derivation off kSelected; blue since the 2026-07-25 blue-focus pivot, the wash being the group's spread-stem focus cue — the green kPlayheadCursorLight and the older white kRegionWash COLOR are retired, only the alpha constant survives; the region's split half-triangles are FULL-OPACITY BREEZE BLUE (kSelected — architect veto 2026-07-25: the halves join the region's blue focus family, marking the bounds where the cursor's split form sits); a direct per-frame overlay over plate+dim on the displayed-viewport fp_* recipe, no cache involvement). A SHIFT-exact waveform press FORMS A REGION (arc step 3, architect-designed 2026-07-23 — replacing the reserved strict no-op; the LIVE DRAG added 2026-07-24 second pass): with NO markers selected, from the PLAYHEAD to the clicked column; with markers selected it DEMOTES — the selection clears and the region spans from the selected marker FURTHEST from the click (argmax |pos − click| in active-domain frames, one rule that covers the between-the-series case as the longest side) to the click — moving no playhead, stopping no playback, seeding no double-click, read-only allowed, with sub-threshold spans refused under the standing sliver rule; the press ALSO ARMS a region drag anchored at the FAR endpoint (`arm_region_drag_preserving` — no dissolve, so a motionless release keeps the formed region, or on the sliver path the pre-press region, bit-for-bit one-shot), and motion past the shared gate drags the CLICK-side endpoint live through the untouched region-drag motion path, Esc-mid-drag restoring the PRE-press region; gutter presses arm nothing (region endpoints hold PLAYABLE live-domain frames only — EVERY former clamps through clamp_playhead_to_live_domain: the demote/delete captures and the shift-click frame since the unclamped forward map can round an EOF marker's image one past the wall, and — codex round 4, disproving the earlier viewport-bounded provenance claim — the plain-drag former's anchor and far endpoints too, since a fractional flush-right zoom's painter-quantized wall can put the last visible column's frame at domain_total); DELETE of 2+ selected markers likewise demotes DOWN to the region spanning the deleted positions (positive span required, both columns, session scratch outside undo — undoing the delete restores the markers, and the group restore then re-derives the region to the restored set's extent, 2026-07-25), and the ctrl toggle that EMPTIES the selection clears the region. The coupling is DOWNWARD-ONLY (architect 2026-07-23 late session, after labwc use — the reverse coupling, a region selecting its contents, was tried and retired; do not re-propose): SELECTION → REGION → TRIM, nothing flows upward. The two multi-select CLICKS define the highlight (a shift range or ctrl toggle resting at 2+ selected sets the region to the selection's clamped position extent via `set_region_to_selection_extent`, AFTER the land's clear — so the highlight, the R1 earliest-landed playhead, and Space's left-bound launch agree by construction), the position group drag live-tracks it and the image-moving group TEMPO gestures re-derive it per step (see those passages), and `x` carries the region into the trim; but the plain-drag region SELECTS NOTHING (the press's deselect-all stands and the drag rests with an EMPTY selection — the R2 live-select and `Selection::select_contained_in_span` are DELETED; a marker press inside a drag-formed region single-selects and its land dissolves the region, the architect's "clicking a marker collapses the highlight" flow) and the trim-window sync writes the REGION only (see the trim paragraph). PROVENANCE (codex group-tempo rounds 3-4, completing the model — a plain ownership bool proved insufficient): `RegionProvenance { Free, SelectionExtent, TrimWindow }` on RegionState. SelectionExtent is set in exactly one place (set_region_to_selection_extent, the downward selection-to-extent owner every legitimate extent re-derive routes through) and DEMOTES TO FREE the instant the selection membership is REPLACED (`demote_region_provenance` at every membership-replace site — the Selection mutators, sanitize/prune, restore_selection_snapshot, undo's touched-set write and W/P swap, the propagate paste; NOT the reorder remap or focus_without_collapse, membership unchanged — closing the cross-column stale-retarget: a surviving region beside a swapped-in different selection stops following). TrimWindow is set by the trim sync (sync_region_to_trim_window, the extracted free function) and is selection-independent. The image-follow gestures gate on provenance: the position group drag's live-track + commit re-derive and the tempo follows act on SelectionExtent only, and the two tempo MUTATION sites (group step, drag per-event) CAPTURE the follow/re-sync decision BEFORE kick_waveform_sync and act UNCONDITIONALLY after (drag CANCEL instead restores the whole-struct pre_drag_region verbatim — codex round 7, see the coincident-policy passage) (the kick's live-domain reclamp wholesale-clears a region whose old endpoint fell outside a shrunken target total — the follow must re-activate it, not gate on post-kick active), with a TrimWindow region RE-SYNCED from app.trim's source-frame bounds through the NEW map at the same sites (the wash tracks the chips; `x` can never inverse-map a stale span). Free regions are untouched display scratch. RECORDED BOUNDARY (retold codex round 5): only the group tempo gestures (the Alt+Left/Right tempo-image step included — 2026-07-24 second pass, replacing the re-ruled-away warp position nudge's target press; it capture-before-kicks and re-syncs identically) and the settings editor's active-tab trim commit for an already-TrimWindow region RE-DERIVE or RE-SYNC provenance — joined 2026-07-25 by the GROUP undo/redo restore, which DEFINES a fresh SelectionExtent region from the touched set (a creator, not a maintainer of pre-existing provenance); the settings-scale commit and adopt never do (the kick validator's out-of-domain clear and load/adopt's unconditional region clear still apply to any region — the boundary is about re-derivation, not numeric immutability). The trim re-sync's COINCIDENT-SPAN policy: a window whose two mapped bounds round to the SAME target frame (16x compression is bracket-legal) takes the CLEAR arm instead of publishing a zero-width highlight — the trim rests untouched, only the highlight clears (the standing sliver rule: degenerate spans never rest), so `x` sees no highlight and silently no-ops (set-only since 2026-07-25 — bare x can no longer destroy the pair through auto_clear at all; Shift+X is the deliberate unset) (so `has_begin && has_end` does NOT imply an active TrimWindow region — the two clears are distinct: crossed AUTHORED bounds destroy window AND highlight, coincident MAPPED IMAGES clear the highlight only). The coincident arm erases live provenance BY DESIGN mid-gesture, so the tempo DRAG's within-gesture re-sync decision reads a GRAB-TIME capture instead (`TempoDragState::grab_trim_highlight`, codex round 6, per-event-only scope — a mid-drag coincident collapse re-syncs BACK into view when further motion re-separates the images, while CANCEL restores a whole-struct `TempoDragState::pre_drag_region` VERBATIM, last, after the kick and the selection-snapshot demote (codex round 7: the recorded staleness boundary means a scale commit or undo can leave a region stale — even coincident — BEFORE the press, falsifying both the separated-by-construction claim and the pure-function re-derive argument; the restored map IS the grab-time map since nothing else can change mid-gesture, so the captured region is exactly the pre-drag display, stale or fresh, any provenance — no geometry assumptions, no coincident-arm involvement); a drag RELEASED while coincident rests cleared, and the per-press group STEP keeps the live decision — the chip-row re-click is the recorded recovery for both; follow_extent stays per-event live, a SelectionExtent region never resting cleared mid-drag). INVARIANT: 2+ selected with an active SelectionExtent region ⇒ region == the current selection's extent. The Esc ladder, Space's left-bound launch, `x`, and the navigation clears stay provenance-blind. The shift-click waveform former/DEMOTE and the delete demotion still DROP selection by explicit ruling, programmatic selections (paste, drops, Tab/`c`) define nothing — undo/redo LEFT this list 2026-07-25, a group restore defining the touched set's extent region — and a plain SINGLE-select marker click still dissolves the highlight through its land. A motionless plain press-release still starts no region (the plain-drag region machinery acts only on a real drag past the gate). AN ACTIVE REGION DISSOLVES THE CURSOR PLAYHEAD into two half-triangles at its bounds (`render_split_playhead`): while `app.region.active` the cursor playhead (line + triangle) is SUPPRESSED and the ONE AA-baked tip-down mask is blitted as a LEFT half (center/tip column ON the left bound, slope flaring OUTSIDE the region) and a RIGHT half (center column on the right bound, slope flaring right), reusing the one mask by clipping each half — the degenerate `left_col == right_col` case stamps the full mask ONCE (no double-composited AA tip). SPACE PLAYS FROM THE REGION'S LEFT BOUND: on the play-start edge with `app.region.active` the plain Space toggle first `move_playhead_to(min(a_frame, b_frame))` — the highlight's start, drag-direction-agnostic — then launches; the region sets ONLY the start, the trim loop verdict still captured from `app.trim` inside toggle_playback (region + trim loops the trim window; region + no trim plays through with no loop). THE MOVING SCANNER PLAYHEAD STAYS VISIBLE during region playback — the cursor-suppression rule applies ONLY to the CURSOR playhead (which becomes the two half-triangles), not the scanner, so Space auditioning from the region's left bound shows the scanner line tracking playback alongside the static half-triangles and the wash. Region clears: a plain waveform press, THE ESC LADDER, region-first and DOWN-ONLY (after drag cancel / editor close / render cancel: with 2+ selected under an ACTIVE region the first Esc clears the SELECTION ONLY — the region rests, demoted to Free by clear_selection's demote, playhead untouched — and the NEXT Esc collapses the region to its start (the two-step multimarker collapse, architect 2026-07-23 session #4, re-ruling the earlier one-Esc collapse); with 0/1 selected an ACTIVE region collapses IMMEDIATELY to its start — cleared with the selection, playhead parked at min(a,b); a region NEVER collapses into the selected markers' subregion — the ladder walks one rung per press; the drop-to-extent-region rung serves NO-REGION programmatic multi-selects only, and a no-region SINGLETON deselects and lands on the marker; Esc during a live region drag instead restores the `pre_region` snapshot captured at arm), file load, Ctrl+Tab, the S/T switch (the domain flips), AND every playhead-jump navigation command (2026-07-22, one `clear_region_highlight` helper): the Tab family + `c` (via the one jump tail), bare `0`, the Left/Right scrub, Home/End, and the plain marker-click land — while the region SPACE LAUNCH, the nudge/drag playhead follow, marker drops, and pure viewport moves deliberately keep it (recorded at the helper; undo/redo LEFT this list 2026-07-25 — a singleton restore dissolves the region via its land, a group restore re-defines it to the touched extent); W/P deliberately does NOT clear. HOVER identity is the unified marker hit too (`marker_hit_at`, shared with the press chain — flag shape OR a rendered lane run from the occlusion resolver's set, homed in render.h/.cpp beside `current_marker_lane_runs`), so the hover persists when the pointer travels from a flag into its lane text, stable in both lane modes: in ALL-VISIBLE mode the run set is hover-independent (store/viewport/map/drag/iteration inputs only), so a resolve is a fixed point by construction; in FALLBACK mode the single run's rect derives from the marker it currently shows and the hover/last-selected tiers compose identical text, so the rect never oscillates (the full two-mode convergence argument lives at the hover recompute, viewport.cpp). Read-only tabs may plain-drag a region (transient visual, authors nothing) and place the playhead (navigation-class). `x` BRANCHES ON THE HIGHLIGHT (architect 2026-07-20, final after two intermediate models): a LIVE region → ALWAYS set trim to it, overwriting any existing bounds (active-domain lo/hi through `active_domain_to_source_frame` — one snap, monotone so order survives — then the shared walls and the `auto_clear_crossed_trim` commit tail), KEEPING the highlight (architect 2026-07-23, reversing the earlier consume — the tail runs sync_highlight_to_trim_window so region and window rest coupled (region only — downward-only, the selection untouched)); NO region → SILENT NO-OP — x is SET-ONLY (2026-07-25, reversing this ruling's clear arm): the trim UNSET moved to the shift-exact `Shift+X` binding (`handle_trim_shift_x` — one-shot, not repeat-eligible, read-only-refused at both the dispatch gate and in-handler exactly like x, delegating to `handle_trim_clear_both`, whose has-guard makes no-trim a natural no-op, then running a TrimWindow-provenance-GATED coupling sync so the trim's own highlight dissolves with the destroyed window while a Free/SelectionExtent region rests untouched — the ungated sync's no-window arm would wipe any region, which is not trim's to clear). The positional context rules (playhead-on-bound clear, inside-pair clear) and the begin-at-playhead AUTOSET are RETIRED; read-only refuses both branches silently, region untouched.

