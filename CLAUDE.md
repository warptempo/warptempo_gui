# Warptempo GUI

Custom C++23 in-process PGHI phase-vocoder GUI (Arch / labwc / Wayland / JACK) for time-warping audio toward historically-informed tempos. Single developer/user (the architect); one target laptop. PROJECT STATUS: FEATURE COMPLETE (architect, 2026-07-25). The detailed architecture rulings formerly held in this file now live in `docs/engineering/architecture/` — see the docs map below. This file carries the build/process rules, the per-topic digests, and the pointers; the topic files are the authoritative detail. When a ruling lands, update the topic file (committed) and this file's digest only if the headline changes.

## Build Commands

```bash
# Configure (from project root, first time or after CMakeLists changes)
cmake -B build -S . -DWARPTEMPO_BUILD_CLI=ON

# Build (Release is the project default; -O3 -march=native is always on)
cmake --build build -j$(nproc)

# Debug build into a separate dir; never reconfigure `build/` itself with -O0
cmake -B build-debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug -j$(nproc)
```

`build/warptempo_gui` is the GUI binary and the default build's only product. No library archives: CMake carries four per-directory source-list variables (`WARPTEMPO_AUDIO_IO_SOURCES`, `WARPTEMPO_PARSER_SOURCES`, `WARPTEMPO_ENGINE_SOURCES`, `WARPTEMPO_PREPOST_SOURCES`) each binary compiles directly — object-code duplication accepted deliberately. One opt-in binary, default OFF: `warptempo_cli` (`-DWARPTEMPO_BUILD_CLI=ON`, the headless insurance render path — exactly the terminal Ctrl+Alt+R, byte-identical to the GUI by shared stages). A headless host builds the CLI with `-DWARPTEMPO_BUILD_GUI=OFF`. `tools/` holds standalone one-shot utilities (own CMakeLists, no link path from product targets): currently `migrate_sidecar_to_frames`, the sole conversion route from the legacy MM:SS.mmm sidecar format (grid details in `docs/engineering/architecture/data-model.md`). Binaries are not portable (`-march=native`); rebuild on the target host.

## Dependencies

Checked via pkg-config: `fftw3` (+ `fftw3_threads`), `wayland-client`, `wayland-cursor`, `wayland-protocols`, `xkbcommon`, `cairo`, `jack`. Plus `wayland-scanner` (build-time, generates protocol stubs into `${CMAKE_BINARY_DIR}/wayland-generated`). All audio container I/O is in-tree and WAV-only, so the non-GUI sources need only `fftw3`.

C++23 (the parser uses `std::expected`). `-Wall -Wextra -O3 -march=native` on GCC/Clang. Do not introduce SDL, GTK, GLFW, or wlroots-specific protocols — the GUI is libwayland-client only (`wayland-cursor` is the canonical exception). Generated protocol stubs cover four standard wayland-protocols XMLs: xdg-shell, xdg-decoration, pointer-constraints-unstable-v1, and relative-pointer-unstable-v1 (the last two are the strip-drag pointer capture, OPTIONAL at runtime — absence degrades to clamped absolute motion with one stderr line).

## Running

```bash
./build/warptempo_gui <source.wav>
```

Target environment: labwc on Arch Linux, single 1920x1080 monitor at 60 Hz. No portability shims, no X11 fallback.

## Testing / Verification

There is no automated test suite, and that is a CONSTRAINT rather than a preference: exercising this product means driving a GUI, which needs synthetic input into a live compositor — and Wayland exposes no such facility (there is no portable equivalent of X11's XTEST, and labwc offers no injection path), so a GUI harness is not buildable here at all. What CAN be verified is verified: the CLI shares the parser/engine/prepost/audio-io stages with the GUI byte-identically, and the architect runs cmp-null and audio verification against it. Verification is otherwise brief-driven: the coder (implementation subagent) is the ONLY writer of source code — it edits files only, and source-only checks requested by a brief are okay. The external reviewer (codex) is REPORT-ONLY: it finds and describes defects and residue, changes no source and no docs, runs read-only git freely (status/diff/log/show — never anything mutating; architect 2026-07-22, state it in every codex brief), and reports no per-change "byte effect" (it changes no bytes); the planner turns its findings into coder briefs. THE CODEX ROUTINE (architect-codified 2026-07-22; every `tmp/` path named below is a LOCAL-ONLY working file — `tmp/` is gitignored, so none of them appears in a clone and the protocol is reconstructed from this description, not from checked-in artifacts): every round is a FRESH codex session, and the architect's "initiate a codex review" / "update the codex brief" means the whole sequence — rewrite tmp/codex_brief.md fully SELF-CONTAINED (project line, discipline block, arc + commit hashes, and a prior-round RECAP: "the previous round found X, Y, Z — fixed in <commits>"; the brief instructs codex to write tmp/codex_review.md then touch tmp/codex_done), delete any stale codex_review.md, touch tmp/codex_start, arm a Monitor on tmp/codex_done (zero-cost wait), and on the event delete both trigger files, read the review, and implement findings JUDICIOUSLY — stopping to present anything that needs an architect ruling (freeze touches, design forks, contested accepted costs) rather than deciding solo. Build/compile checks are not part of a brief unless the architect explicitly asks. Do not propose automated tests, regression harnesses, or commercial-tool comparisons.

The coder (subagent) runs no MUTATING git commands (no add/mv/rm/commit/push/restore — read-only git like status/diff/log/show is fine and encouraged for self-checks), does not stage or commit, and does not launch labwc or drive the GUI. The `examples/` folder is ARCHITECT-ONLY: neither planner nor coder modifies it; architect edits there ride along with the next commit (the warptempo_git wrapper stages everything — expected content, not planner residue). The planner commits via `warptempo_git` (a local wrapper; its invocation lives in the gitignored tmp/dispatch.md, not in the repo), and does NOT hold or bundle commits waiting for the architect — commit as work completes and verifies. The architect owns labwc verification and cmp-null/audio verification.

Coder briefs should point at the relevant `docs/engineering/architecture/` topic files instead of restating their content.

## Docs Map (read per task, not wholesale)

All in `docs/engineering/architecture/` unless noted. Each file holds the full hardened ruling text — read the one(s) your task touches before editing.

| Task touches | Read |
|---|---|
| Authored positions/frames, tempo cents, value brackets, playback_speed, sidecar grammar, the section rule | `data-model.md` |
| Settings keys/schema/editor, render-entry sidecars, adopt (`'`), audio_player, font_size | `settings.md` |
| Zoom, viewport, strip lanes, strip/alt/ctrl drags, pointer capture, zoom-row double-click, playback scanner | `zoom-viewport-strip.md` |
| Resolver normalization, red flags, marker walls/stores, trim commit rules + loop audition, undo coalescing, adversarial load taxonomy, tripwires | `normalization-and-boundaries.md` |
| Pointer reach/modifiers, trim chip router, hit geometry, displayed paint basis, damage ownership | `pointer-hit-testing.md` |
| Selection model, marker click land, the marker drag, undo/redo restore visuals, shift/ctrl clicks, the never-parked rule + coincidence auto-select, the retired never-span-less invariant | `selection-model.md` |
| Flag shapes, lane text occlusion, stems, focus model, flag/BPM/settings editors, double-click, key repeat, Space | `marker-ui.md` |
| Region highlight, scrub area, provenance, the Esc bindings, `x`/`Shift+X` | `region-scrub-esc.md` |
| Home-view binding, the bare Up/Down tempo cent step (singleton + group), propagate paste | `tempo-and-home-view.md` |
| Parser phase-reset compilation, engine geometry, trim prepost stage, render dispatch/cancel/reuse | `render-pipeline.md` |
| Modality (all four editors), strict modifier validation, the alt vocabulary, the retired plain/augmented convention, bare-`e` mouse key, type names, warp/phase-reset naming symmetry | `conventions.md` |
| Any color, ground recolors, disabled/trim/line color classes | `src/gui/render.h` palette block + `src/gui/color_config.cpp` (code comments are authoritative) |
| The colors.conf grammar + the canonical key block | `docs/engineering/color-configuration.md` |
| Any guard/validator (add/move/remove) | `docs/engineering/validation_topology.md` |
| Engine performance (closed campaign; retired candidates need new data) | `docs/engineering/perf_campaign_2026_07.md` |
| User-facing behavior reference | `docs/HELP.md` |

Trim spans several files by nature: store/commit rules in `normalization-and-boundaries.md`, chips/router/geometry in `pointer-hit-testing.md` and `marker-ui.md`, the prepost render window in `render-pipeline.md`, the highlight sync in `region-scrub-esc.md`.

## Architecture Rules (MUST follow — digests; the topic files are authoritative)

### Freeze Status (architect-declared 2026-07-12)
`src/engine/`, `src/parser/`, `src/audio_io/`, and `src/prepost/` are under PERMANENT HARD FREEZE, declared after the combined maximum-effort external audit came back near-clean: surgical changes only, each requiring explicit architect approval to reopen. `src/cli/cli_main.cpp` is freeze-adjacent (pure plumbing over the frozen stages; same discipline). Only GUI work remains: quality-of-life improvements, ad hoc bugfixes, and refactors as needed. Engine performance history and the retired-candidates list live in docs/engineering/perf_campaign_2026_07.md — a retired candidate needs new measured data to re-propose. The planner is allowed to update this file; it is COMMITTED (un-ignored 2026-07-27), so an edit here lands in history like any other and a revert restores it.

### Authored Domain (→ data-model.md)
Authored positions (marker times, trim bounds) are whole source frames in `int64_t` everywhere at rest; `snap_authored_frame` (app_state.h) is the ONLY double→authored conversion route. Authored tempo is 100-based integer CENTS in [25, 400]; every other value is a bracketed double via `value_format.h`. One serialization domain (`frame_format.h`): canonical integer spelling only — anything else is adversarial load-fatal. Walls are exact integer compares on the shared inclusive [0, total−1] domain. Sources: WAV-only, 16/24-bit PCM, ≥44100 Hz, stereo-only.

### Pixel Anchoring, Viewport, Zoom (→ zoom-viewport-strip.md)
Position gestures anchor to the painted column grid and commit through `authored_frame_at_column` → `snap_authored_frame`; walls win over the grid everywhere. Waveform effective width is floored to a multiple of 16 px; the viewport snaps to grid points at `clamp_viewport_start` (the one chokepoint, which also owns the zoom-level clamp). Zoom is a continuous double in [1.0, 17.0] with `kWorkingZoomLevel` = 2.0 ("working zoom"); strip drags are dual-axis, song-anchored, pointer-captured, synchronous per pointer frame. THE TOP STRIP IS FIVE LANES (zoom strip / trim chip / marker text / flag / triangle) and the zoom-row double-click frames region-else-trim-else-whole-song; a removal arc that deleted the lane, rehomed the framer onto the chip row trim-only, and dropped the marker-text-lane scrub was built and then ROLLED BACK WHOLESALE at 80a265e (architect 2026-07-27 — the surface is used and missed; do not re-propose the lane deletion or the framer rehoming; the scrub deletion alone WAS then re-executed deliberately at 42743dd, the marker-text lane ruled playback-inert). ROW HEIGHTS were retuned 2026-07-27 at the default font: zoom row 15 on its own kZoomRowHeightPx, trim chip row = flag_lane_w_px so chips are SQUARE by construction, marker-text and both bottom lanes 24 (text BOX 20 + kTextBoxMarginPx 2 per side, the css split from kTextBoxPadPx 1 inside the ring); the raw text-row slot is accessor-less render.cpp file-scope state, purely the box's ingredient (the monospace_row_h accessor pair was deleted with zero callers). Flag rect 15x17, triangle 8 (the assets took their font-10 size). THE 2026-07-26 DRAW ARC (topic files marker-ui + zoom-viewport-strip hold the detail): the plate is a direct ARGB32 coverage renderer — per-column raw min/max interiors + Wu tip polylines (raw-tip bands for subpixel material; never-fade >=1px floor), column edges on the authoring lattice `g(k0+c)` in BOTH views (bit-exact pan invariance), symmetric left/right halos, a 13-rung peaks pyramid (.peaks v7, <=5-pair reads unconditional), and ONE synchronous full-render route for every user-driven pan/zoom frame (the incremental pan path is retired; the async worker serves only undriven load/resize/follow/drift). The ink-notch rule is retired PERMANENTLY (architect). The scanner renders at a sub-frame position; its value fields are meaningful only while active. EVERY PAN SUPPRESSES FOLLOW for the playback session (architect 2026-07-30): one line at the `scroll_viewport` funnel (paging, alt+wheel, touchpad, alt+drag grab-pan) plus the strip drag's bypass viewport write; keyboard zoom is a deliberate non-producer (it centers ON the scanner); the chase resumes at the next launch or an explicit `f` re-enable (producer inventory at the flag's declaration, app_state.h). ALL THREE VIEW SWITCHES (`t`, Ctrl+Tab, `p`) take the SYNCHRONOUS rebuild — `p` joined 2026-07-30, closing the one view switch that blitted a stale flag frame (the run loop paints before the tick's fingerprint check; inventory at viewport.h's kick_waveform_sync declaration).

### Render Pipeline (→ render-pipeline.md)
The parser is the sole authored→engine compiler (phase resets emit `S − N/2`, no render-time offset anywhere); geometry `kN`/`kRs` lives in `engine_geometry.h`. Trim is a prepost stage: full maps → `plan_trim` (refusal = render untrimmed, never a failed render) → trim-ignorant engine → `post_trim` → unconditional limiter → encode. No subprocesses in the render path. A render dispatch kills the running render unconditionally; killed sessions can never publish Success; target-view previews may reuse identical completed renders through the two reuse rungs (load-bearing for undo/redo A→B→A).

### Colors (→ render.h palette block + color_config.cpp, 2026-07-26)
Every GUI color is a runtime-tunable global loaded ONCE at startup from `~/.config/warptempo_gui/colors.conf` — strict whole-file grammar (23 keys, canonical order, `key=#rrggbb` lowercase, no spaces), any deviation = one stderr line + compiled defaults wholesale, missing file = silent defaults, restart-only, GUI binary only. The palette is FULLY OPAQUE (no compositing alphas): the region highlight recolors the CANVAS GROUND under the ink (painted before the plate blit — the Ableton model); the phase-reset overlay is a 1px RING ONLY over the plate (its ground recolor removed, architect 2026-07-27); the out-of-trim dim is RETIRED (the bright kTrimBar/kTrimBarOutline bridge bar is the sole inside-the-trim signal; chips/stems are the calm pair); disabled markers are an opaque color class (disabled > red > default — THREE classes, and SELECTION IS NOT ONE: the kSelected/kSelectedOutline keys were deleted 2026-07-27 because the outline arm outranked disabled, lighting up a disabled+selected flag; a selected marker's RECTANGLE FILL and WHOLE OUTLINE are exactly what they would be unselected, so a disabled or red pair is never masked; selection's only paint is the flag's TRIANGLE INTERIOR, filled kWaveform (architect 2026-07-27), layered after the ladder resolves and therefore still not a class — the cue inventory is that ink triangle plus kSelectedStem, the z-order lift and the playhead land); one structural kLine color paints the strip-row ring and the waveform area's 1px top/bottom border (taken from the area); the selected stem has its OWN key (kSelectedStem, 2026-07-27 — a full-height line tunes differently than the 1px ring). The key→global table in color_config.cpp is the single palette enumeration (23 keys). Codex is SKIPPED for simple updates like key additions/tuning (architect 2026-07-27); the routine stays for substantive arcs.

### Settings (→ settings.md)
`app.engine_settings` is typed truth; all three boundaries are strict (whole-file schema load shared verbatim by GUI and CLI — a sidecar set loads in both products or neither; editor red-flash; typed dispatch). `kSettingsOrder` owns write order, `kEngineKeys` membership; adding an engine key = six edits, four files. GUI-kind keys commit through each key's own gesture chokepoint, history-less. Numeric values are pinned to one canonical spelling each. Render entries carry the same sidecar set a source does, written once at dispatch; `'` adopts an entry 1:1 in memory.

### Load-Lenient, Render-Normalizing Boundaries (→ normalization-and-boundaries.md)
Gestures move freely and NOTHING POPS: ambiguous authored states normalize at the render boundary (`resolve_warp_markers_for_render` — every ambiguous tempo → 1.00, one stderr line per timestamp per resolve; normalization-red flags as the display cue), never refused or force-resolved. The two-category rule: a state the GUI can commit always loads and renders; a state the GUI can never produce is adversarial and hard-fails the load, first error only, identically in both binaries. Marker walls are 0 and total−1 both columns; crossed trim pairs auto-clear at commit and at load; a lone trim bound completes to its extreme at the render boundary; a set trim loops the audition. EVERY TrimWindow SETTER CLEARS THE SELECTION (2026-07-29, contortion ruling 2 — the plain chip click in all three arms, both bound-set clicks, `x`, and the drags' live sync, each deselecting PAST ITS OWN REFUSALS through one setter's-publish helper; the settings trim keys JOINED 2026-07-30, "a typed commit is a commit" — active-tab arms only, and the setter class is defined by what a route does: it commits the live tab's trim window, writing a bound or claiming the resting pair; the one non-setter is `Shift+X`, the clearer), so a TrimWindow region rests only beside an EMPTY selection and the sync is TWO-ARMED with the coincident clear arm deleted (a compressed window rests active at one column). THE CLAIM IS ONE-WAY (narrowed 2026-07-30): the sync publishes a region for every full pair it sees, but a RESTING full pair implies nothing — the entry/restore routes (load, the Ctrl+Tab band pull, `t`, `p`, adopt) rest pairs bare. `x` gained a DEGENERATE-RESULT REFUSAL as the one consequence of that (ARCHITECT-CONFIRMED 2026-07-29: a pair inverse-mapping to `end <= begin` writes nothing, since the crossed-commit auto-clear would silently destroy it and trim has no undo). A held key's synthesized repeats coalesce into one undo entry BY REPEAT IDENTITY (2026-07-29, contortion ruling 7: the platform stamps repeats via `GuiInputState::synthesized_repeat`, the physical press pushes, repeats never push — no wall clock, no command counter, so the compositor's repeat delay no longer participates in undo semantics; rapid manual taps are separately undoable by design; adjacency survives structurally via the repeat contract's event-edge disarms plus TWO stamp rules — every stack-top change clears the stamp, and an eligible PHYSICAL press invalidates it ON ARRIVAL inside `coalesce_gesture`, which is what covers a press that REFUSES and makes "separate presses are separate entries" exactly true with no accepted delta left; detail in normalization-and-boundaries.md).

### Interaction Model (→ selection-model.md, marker-ui.md, region-scrub-esc.md, pointer-hit-testing.md)
Selection and playhead are RE-COUPLED: every plain marker click lands the playhead; shift = inclusive range from the anchor/focus, ctrl = membership toggle, both landing on the FOCUS they set (2026-07-28; audition-from-range-start is Space's left-bound region launch, not the land — and since 2026-07-29 that launch is CURSOR-NEUTRAL, riding the audition scrub's own entry so the cursor never separates from the focus under a standing span). HORIZONTAL MOVEMENT IS A FOCUS ACT — GROUPS ARE NEVER MOVED (architect 2026-07-29, contortion ruling 9, full symmetry): one marker moves per gesture, the pointer press on a member single-selecting and landing IMMEDIATELY (an ordinary singleton drag follows) and the keyboard nudge COLLAPSING to the focus in the twins' shared prologue before stepping it. Both group-move forms are deleted — the rigid drag (intersection walls, shared delta, grabbed-anchored commit, live-track) and the per-member nudge (proposal loops, whole-press wall refusal, same-column merge) — along with the pointer DEFERRAL that existed only to tell click-on-member from group-drag arm. THE GROUP-VERB DOCTRINE (at position_nudge.h's head): group-capable where members are INDEPENDENT or the span is READ (Ctrl+D, the Up/Down cent step, delete, m, Ctrl+P, Space-from-extent, x, zoom framing, undo/paste restores); focus-collapse where members are COUPLED or the act is POSITIONAL (Ctrl+N and all horizontal movement). Coupling is DOWNWARD-ONLY: selection → region → trim, nothing upward; region provenance is {Free, SelectionExtent, TrimWindow}, and a membership REPLACE clears a SelectionExtent region OUTRIGHT (2026-07-29, retiring demote-on-replace — a demoted-but-visible span is a lie). Hit geometry and painted-pixel deciders ride the DISPLAYED basis (map + viewport staged/promoted in lockstep); gesture commits and navigation stay live by ruling; damage follows the basis of the pixels it erases — the playheads' pixels are PLATE-registered, so their narrow damage resolves on the plate basis where the site can reach it and WIDENS to full-area where it cannot (the rule and per-site table at `playhead_pixel_x`, app_state.h; the stem's narrow damage died by widening). THE TWO COORDINATE EPOCHS TOOK DISTINCT NAMES (2026-07-30): `GuiPaintHandler::plate_viewport_basis` (plate-registered overlays) and the free `item_viewport_basis` (item hit geometry/lane runs) — formerly one colliding `displayed_viewport_basis` spelling that C++ scoping resolved silently. Focus is ONE family, no hue asserted (every palette key is colors.conf-tunable): singleton = the always-on stem (kSelectedStem), group = the extent region's ground recolor (kRegionCanvas) WHEN THE GROUP HAS A SPAN; an active region dissolves the cursor playhead into split half-triangles AND suppresses the singleton stem (2026-07-29 — the region is the playhead's SPAN form and outranks every point cue; the selection persists beneath, the flag keeping its ink triangle). Undo/redo restores adopt the group visual language (singleton lands; group lands at earliest + extent region). THE LANE MODEL (architect 2026-07-28) is why the bare horizontal arrows split on selection emptiness: with a selection the cursor stops painting and the focused flag's ink triangle IS the playhead, so the arrows move THAT (a position nudge in either column's home view — and NOTHING in W+target, a consumed refusal since ruling 8) and the playhead rides along because every such route re-lands it on the committed focus; with none they are the waveform-lane playhead step. ONE predicate owns the split (`playhead_in_marker_lane`) with two readers — the dispatcher and `read_only_key_blocked`'s `is_playhead_step` entry, the SOLE read-only defense for both surviving marker-lane routes. The lane is left by ANY DESELECTING route (2026-07-29 — Esc is unbound outside the editors, the prompts, the drag swallow and the render cancel); there are still no gesture fallbacks, so a refused marker-lane press is a consumed no-op. THE MARKER LANE OWNS THE PLAYHEAD (architect 2026-07-28): any route that changes WHICH marker is focused while the lane is active LANDS the playhead on the new focus — every flag/BPM editor open and retarget (REVERSING "opening is a selection act, not a playhead move"), the Ctrl+N collapse, the two position nudges' prologue collapse, the `t` audio-view switch (on a surviving singleton OR group), and the propagate paste's created-set arm (focus moved from the LAST created reset to the FIRST, and since 2026-07-29 it sets the created set's extent region — a mass-marker event reads like undo/redo's group restore); any route that LEAVES the lane leaves the playhead where it lands, which is why Home/End clear the selection instead — and since contortion ruling 1 the `p` swap and Ctrl+Tab are lane-LEAVERS too, handing the lane nothing (the coincidence auto-select may then re-enter it, on a marker the playhead is already standing on). Both arms of the cent step re-land on the focused marker's post-step image in target view (the singleton for the label-definition repricing that can move even its own image) — the ONLY map-change re-lands left, the two settings ones having died with ruling 6. THE SELECTION IS NEVER PARKED (contortion ruling 1, 2026-07-29): it lives in AppState alone, and A COLUMN OR TAB SWITCH CLEARS IT WHILE THE AUDIO-VIEW SWITCH (`t`) CARRIES IT — `p` and Ctrl+Tab clear outright and then re-acquire by COINCIDENCE AUTO-SELECT at three entry chokepoints (file load, `p` entry, Ctrl+Tab entry): the first marker in the active column whose land value is EXACTLY the resting playhead single-selects (the land's own formula reused, exact int64, first-in-store on ties, the adjacency land a provable no-op) — one owner, auto_select_marker_at_playhead. So the tabs' stored cursor is honored VERBATIM and both pending-architect flags retired BY DELETION. The generalization: VALUE-SHAPED SESSION STATE PARKS SAFELY; INDEX-SHAPED SESSION STATE DOES NOT — the per-tab band keeps viewport/zoom/playhead/trim/read_only and nothing index-shaped, ONE PLAYHEAD PER TAB. A 2+ SELECTION ALWAYS HAS A DRAWN PLAYHEAD FORM, BUT THE ENFORCEMENT IS GONE — the guarantee lives in the VERBS now (contortion ruling 6 plus the same-day rejection that finished it, architect 2026-07-29). Ruling 6 deleted the distributed collapse protocol (nine sites, six after ruling 1), its enumeration, and `sync_region_to_trim_window`'s injected `Selection&`. Of the six: TWO closed symmetrically (the ENGINE-key settings commit and the settings-only ('S') undo/redo restore now clear selection AND region at their own tail, which killed both #19 map-changer re-lands); ONE was unreachable dead code (the trim-sync no-window arm, its non-setter callers gating on TrimWindow provenance, which rests only beside an empty selection); and THREE — the S/T flip (bare `t` AND the settings `active_audio_view=` twin), bare `0`, `c` — briefly produced a spanless group, which THE ARCHITECT REJECTED as "a hybrid third option that i did not ask for or ratify". All three collapsed at first — then BARE `0` WAS RE-RULED A PURE VIEWPORT COMMAND (architect 2026-07-30, "overscoped"): its region clear died WITH its collapse+land (a clear alone would resurrect the rejected spanless state — the coupling is the do-not-reintroduce guard), so a group and its span survive the overview toggle. The form stands at FOUR SITES (the nudges' prologue, Ctrl+N, `t`, `c`). THE ARCHITECT'S GENERAL RULE, verbatim and durable, at the group-verb doctrine (position_nudge.h): if group is relatively cheap to implement, implement it; otherwise collapse to last selected — no hybrid third form. So a spanless 2+ selection has NO PRODUCER (re-verified across all 26 region-clearing routes), by the producers' own form rather than by anything policing the clears. THE PLAYHEAD HAS TWO FORMS, POINT AND SPAN, AND EXACTLY ONE IS EVER VISIBLE (architect 2026-07-29): the land is a PURE playhead write; every playhead-moving POINT command owns its own region collapse at its own site, UNCONDITIONALLY (the plain click, multi-select clicks at a result under two, `enter_text_edit`, BOTH position nudges, the navigation jumps, and the marker drops); the non-moving membership ops (Ctrl+N, sanitize, clear_selection) rely on the membership clear alone, a TrimWindow region resting through them, while the THREE MEMBERSHIP-WHOLESALE routes — the undo/redo restore, the `p` swap, the propagate paste — clear ANY resting region (the maximally greedy collapse). The SPAN-PRESERVING exceptions are the GROUP CENT STEP's unconditional re-derive (the last member of that class), Space, `x`, and pure viewport moves; `m` re-derives its span extent after the open's chokepoint clear (clear-then-extent). The rule and its landing-site enumeration live at `land_playhead_on_marker` (input_pointer.cpp). THE KEYBOARD STOP RULE (architect 2026-07-30; authoritative at `stop_playback_if_playing`'s declaration, playback_lifecycle.h): collapse-to-point commands stop a live audition (the nudges, `c`; `t` on its own domain-flip ruling); group-preserving value steps (the cent step) and pure viewport moves (`0`, paging, zoom) do NOT; trim mutations (`x`, `Shift+X`) stop in BOTH views, refusal-gated like every stop — the rule classifies only the verbs it names. TWO GESTURES WERE CALLED "SCRUB"; only the waveform LOWER-HALF audition press keeps the name (`scrub_act_at` and friends, untouched) — the arrow gesture is the playhead step everywhere now. The authoritative kick_waveform_sync caller inventory lives at viewport.h's declaration. POINTER GESTURES HAVE NO CANCEL (architect 2026-07-29, contortion rulings 3+4): Esc mid-gesture is a CONSUMED NO-OP, release ends the gesture and WHAT STANDS STANDS, button-lost ends it the same way, Ctrl+Q / resize / WM-close end it through the same release bodies (`finalize_active_drags`, which replaced `cancel_active_drags`), and UNDO IS THE MITIGATION — "a drag implies I'm ready to commit"; every bystander capture that fed a restore is deleted, trim's force-ended bounds being unrecoverable by its standing history-less ruling rather than by a new gap; the rule is stated once at the drag-modal gate (input_handler.cpp). BARE ESC IS BOUND IN FOUR PLACES AND NOWHERE ELSE (architect 2026-07-29, contortion ruling 5): the four editors, the prompts, the drag-modal swallow, and the render/batch cancel — that last one ARCHITECT-CONFIRMED 2026-07-29 as its own binding class. The whole selection/region Esc ladder is deleted — the two-step demote, the collapse rung, the drop-to-span rung, the singleton rung, and the down-only doctrine — and with it the DEMOTE AS A CONCEPT: no route re-forms an extent's pixels as Free after a deselect, so Free has FOUR producers (the plain drag, the shift-click former, both delete demotions), and lane exit is any deselecting route. Detail in region-scrub-esc.md's Esc paragraph.

### The Home-View Binding (→ tempo-and-home-view.md)
Each marker column authors in its HOME view only (warp = source, phase reset = target) via `active_column_authoring_allowed`; off home, columns are display/navigation-only with silent refusals. Two ruled exceptions, down from a three-flavor tempo family (contortion ruling 8, 2026-07-29): the bare UP/DOWN TEMPO CENT STEP in W+target (owner-only there, singleton + group, the group arm all-or-nothing over a view-independent wall set) and the phase-reset propagate (starts in source, lands in target). The pointer TEMPO DRAG and the bare Left/Right TEMPO-IMAGE STEP are DELETED — so W+target has NO pointer authoring gesture (matching P+source) and bare Left/Right there with a selection is a consumed refusal.

### Modality and Input (→ conventions.md)
Modal surfaces live on the bottom strip, and since 2026-07-28 the top-strip flag editor is KEYBOARD-modal too (architect, reversing the old "commands punch through" ruling): all four editors admit exactly the editor's own keys plus bare Esc, Ctrl+S and Ctrl+Q through ONE `route_modal_editor_key` contract, so an unbound chord is structurally inert rather than a command that discards the edit — which is what closed the flag editor's destructive fall-through tail, and with it the `StrayModifierKey` class that tail alone had justified. Modality is CHORDS ONLY: the flag editor stays pointer- and wheel-transparent and still does not stop playback (`modal_bottom_strip_editor_active` has exactly ONE caller, `wheel_context`; the playback stop has ONE owner since 2026-07-28, `GuiPlaybackLifecycle::stop_playback_for_modal_open` — refusal-gated, three editors + three prompts, the flag editor's exemption explicit at its declaration — the keyboard gate is `keyboard_modal_editor_active`). Accepted cost: no in-editor undo, the gap the bottom-strip editors always had. BPM mode is exactly its editor session. The GUI never writes a load-invalid state. All program-authored text is all-lowercase; never a double space. Bare `e` is the left mouse button, translated at the platform boundary (never bind it in dispatch); bare `2` is unbound. STRICT MODIFIER VALIDATION (architect 2026-07-28): a modifier combination with no explicit binding is a no-op everywhere — Space and Escape bare-exact at every reader, the editors' session keys bare-exact and their clipboard chords ctrl-exact, the prompt case-sensitive; THREE families are deliberately untightened because the modifier is a real binding there — `is_tab_cycle` (Shift+Tab reverse-cycles), Ctrl+Z (Shift selects redo), and the editors' MOTION arm, which keeps ctrl and shift in every combination but excludes ALT (they carry real meaning on all six keys and degrade to the plain gesture where they add nothing, exactly as an ordinary one-line field does, so exactness would break them; alt binds nothing in the family). A stray modifier on an owned key shape is simply NotEditorKey and drops at the gate; the classifier stays the single keymap owner and no caller enumerates editor keys. SUPER IS DROPPED AT THE PLATFORM BOUNDARY (architect 2026-07-30 — super belongs to labwc): the modifier mask never modelled LOGO, so every bare-exact predicate accepted a held Super (Super+Escape discarded an open edit); the platform now tracks `mod_super_` and delivers NO key event while it is held — one gate at `deliver_key` covering physical presses and synthesized repeats, reset on keyboard-leave and capability loss, no fourth bool in any predicate. ALT IS NOW POINTER-ONLY (same ruling): alt+drag pan and alt+wheel pan, plus the five Ctrl+Alt render/propagate chords (R / Shift+R / I / P / Shift+P), and nothing else — the plain/augmented modifier convention died with `Alt+S` (bare `s` and the bare empty-lane double-click both drop the AUGMENTED marker, render-neutral except onto an occupied frame — where the exact-frame collapse to 1.00 is loud, the markers going red — and inside a label definition, whose span its references reproduce; both ruled non-problems 2026-07-28).

### Validation Topology (architect-blessed 2026-07-17)
Every guard classifies under docs/engineering/validation_topology.md — five classes, one owner each; a duplicate predicate needs an (a)/(b)/(c) justification; an error arm exists iff a producer exists. A new guard that fits no class is a design smell to resolve, not a row to force.

### Type Names
Marker and related GUI types carry the `Gui` prefix (`GuiWarpMarker`, `GuiPhaseResetMarker`, `GuiAudio`, `GuiPlayback`, etc.). New marker-side types follow the same convention.

### Warp / Phase Reset Naming Symmetry (→ conventions.md)
Warp and phase reset are co-equal axes; new pipeline surfaces ship both columns or record the asymmetry in a comment at the site. "Phase reset" is one concept token, never shortened to "reset" in names; warp is never the unmarked default. The recorded-asymmetry inventory lives in conventions.md.

### Rounding
Use `std::nearbyint` for banker's rounding wherever fractional values convert to integer-domain values (playhead placement, pixel mapping). No epsilon nudge. For authored positions specifically, `snap_authored_frame` (`app_state.h`) is the single conversion chokepoint; no other site may round or cast an authored position.

### Comments
Comments describe current behavior and rationale only. No dead-process citations (Brief/chunk/Stage/spec tags or "per the brief" prose).

### Grep Before Asserting an Invariant
Before claiming "every site that does X" or "no other caller does Y" inside a brief or a code change, grep it. The repo is small enough that this is cheap. AND AT RETELLS, NOT ONLY AT CREATION (architect + codex, 2026-07-24): an inventory retell RE-GREPS membership, never edits the inherited list in place — five consecutive retell rounds preserved a pre-existing omission in the kick_waveform_sync inventories because each round edited the copied membership instead of re-deriving it. Structural fix preferred over discipline: an exhaustive enumeration lives at exactly ONE authoritative site per concept (e.g. viewport.h's kick_waveform_sync declaration), and every other site states only its own class plus a pointer.
