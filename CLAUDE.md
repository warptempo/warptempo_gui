# Warptempo GUI

Custom C++23 in-process PGHI phase-vocoder GUI (Arch / labwc / Wayland / JACK) for time-warping audio toward historically-informed tempos. Single developer/user (the architect); one target laptop. PROJECT STATUS: FEATURE COMPLETE (architect, 2026-07-25). The detailed architecture rulings formerly held in this file now live in `docs/engineering/architecture/` — see the docs map below. This file carries the build/process rules, the per-topic digests, and the pointers; the topic files are the authoritative detail. When a ruling lands, update the topic file (committed) and this file's digest only if the headline changes.

## Build Commands

```bash
# Configure (from project root, first time or after CMakeLists changes)
cmake -B build -S . -DWARPTEMPO_BUILD_CLI=ON

# Build (Release is the project default; -O3 -march=native is always on)
cmake --build build -j$(nproc)

# Debug build into a separate dir; never reconfigure `build/` itself with -O0
cmake -B build-debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug -j$(nproc)
```

`build/warptempo_gui` is the GUI binary and the default build's only product. No library archives: CMake carries four per-directory source-list variables (`WARPTEMPO_AUDIO_IO_SOURCES`, `WARPTEMPO_PARSER_SOURCES`, `WARPTEMPO_ENGINE_SOURCES`, `WARPTEMPO_PREPOST_SOURCES`) each binary compiles directly — object-code duplication accepted deliberately. One opt-in binary, default OFF: `warptempo_cli` (`-DWARPTEMPO_BUILD_CLI=ON`, the headless insurance render path — exactly the terminal Ctrl+Alt+R, byte-identical to the GUI by shared stages). A headless host builds the CLI with `-DWARPTEMPO_BUILD_GUI=OFF`. `tools/` holds standalone one-shot utilities (own CMakeLists, no link path from product targets): currently `migrate_sidecar_to_frames`, the sole conversion route from the legacy MM:SS.mmm sidecar format (grid details in `docs/engineering/architecture/data-model.md`). Binaries are not portable (`-march=native`); rebuild on the target host.

## Dependencies

Checked via pkg-config: `fftw3` (+ `fftw3_threads`), `wayland-client`, `wayland-cursor`, `wayland-protocols`, `xkbcommon`, `cairo`, `cairo-ft`, `harfbuzz`, `jack`. Plus `wayland-scanner` (build-time, generates protocol stubs into `${CMAKE_BINARY_DIR}/wayland-generated`). All audio container I/O is in-tree and WAV-only, so the non-GUI sources need only `fftw3`. HARFBUZZ IS THE RULED TEXT-SHAPING PATH (architect 2026-07-30, resolving the parked UTF-8/text-shaping question): proportional text measures AND paints through the ONE shaping chokepoint `src/gui/text_shape.{h,cpp}` (contract authoritative in its header comment — the shaped run is the single width truth, the monospace glyph-count arithmetic's successor), GUI-only (deliberately absent from the four shared source lists); THE CONVERSION IS COMPLETE (row 7, 2026-08-01): every text surface in the product shapes and paints through the chokepoint on the ONE sans face (fontconfig "sans" → Liberation Sans, 12pt scaled by gui_scale) — the toy-API monospace path is DELETED WHOLE, and the font_size settings key with it. Do not add Pango (font fallback/bidi is deliberately out of scope; one wide-coverage face is the model). TEXT IS UTF-8 IN FREE TEXT AND ASCII IN GRAMMARS (architect 2026-08-02, retiring the old "non-ASCII unsupported" project line as a fossil of the deleted monospace path): the five editors' buffers and the free-text settings values (title/bpm/notes/url/cover/audio_player) take UTF-8 verbatim and round-trip byte-identically through load, display, edit, recall and save, while every STRUCTURAL grammar stays ASCII, unrelaxed — marker labels, numeric spellings, settings keys and value vocabularies, the canonical-line payload (so flag text stays ASCII by construction, which is what lets cap_marker_label walk it by byte); render-entry ids are filesystem paths, bytes not text, untouched. Three exclusions accepted: no font fallback (an uncovered codepoint draws .notdef), no bidi/complex shaping, and grapheme clusters out of scope — the editors move by CODEPOINT, so a combining mark carets on its own. One incoming filter (text_editor::replace_selection: printable ASCII plus well-formed multi-byte UTF-8; ASCII controls and malformed sequences drop one byte at a time), no platform fix needed (xkb_state_key_get_utf32 already delivers whole codepoints, which the editor UTF-8 encodes), and editor internals that index by BYTE but step by codepoint — contract authoritative at the head of text_editor.h, full ruling in conventions.md. A hand-edited file's malformed bytes still load verbatim: no new refusal, no migration.

C++23 (the parser uses `std::expected`). `-Wall -Wextra -O3 -march=native` on GCC/Clang. Do not introduce SDL, GTK, GLFW, or wlroots-specific protocols — the GUI is libwayland-client only (`wayland-cursor` is the canonical exception). Generated protocol stubs cover four standard wayland-protocols XMLs: xdg-shell, xdg-decoration, pointer-constraints-unstable-v1, and relative-pointer-unstable-v1 (the last two are the strip-drag pointer capture, the ONLY optional protocols — absence degrades to clamped absolute motion with one stderr line; xdg-decoration is REQUIRED at startup since 2026-07-30, labwc always advertising it). THE REQUIRED-GLOBALS SET IS FIVE since 2026-08-02, when `wl_data_device_manager` joined it with the system clipboard (architect): it is CORE protocol — the same wayland.xml as wl_seat, so no library, no pkg-config entry and no fifth generated stub — bound at v2..v3 and required on the xdg-decoration reasoning, a compositor without it being a broken environment rather than a degraded one. The two optionals above are still the whole optional list.

## Running

```bash
./build/warptempo_gui <source.wav>
```

Target environment: labwc on Arch Linux, single 1920x1080 monitor at 60 Hz. No portability shims, no X11 fallback.

## Testing / Verification

There is no automated test suite, and that is a CONSTRAINT rather than a preference: exercising this product means driving a GUI, which needs synthetic input into a live compositor — and Wayland exposes no such facility (there is no portable equivalent of X11's XTEST, and labwc offers no injection path), so a GUI harness is not buildable here at all. What CAN be verified is verified: the CLI shares the parser/engine/prepost/audio-io stages with the GUI byte-identically, and the architect runs cmp-null and audio verification against it. Verification is otherwise brief-driven: the coder (implementation subagent) is the ONLY writer of source code — it edits files only, and source-only checks requested by a brief are okay. The external reviewer (codex) is REPORT-ONLY: it finds and describes defects and residue, changes no source and no docs, runs read-only git freely (status/diff/log/show — never anything mutating; architect 2026-07-22, state it in every codex brief), and reports no per-change "byte effect" (it changes no bytes); the planner turns its findings into coder briefs. THE CODEX ROUTINE (architect-codified 2026-07-22; every `tmp/` path named below is a LOCAL-ONLY working file — `tmp/` is gitignored, so none of them appears in a clone and the protocol is reconstructed from this description, not from checked-in artifacts): every round is a FRESH codex session, and the architect's "initiate a codex review" / "update the codex brief" means the whole sequence — rewrite tmp/codex_brief.md fully SELF-CONTAINED (project line, discipline block, arc + commit hashes, and a prior-round RECAP: "the previous round found X, Y, Z — fixed in <commits>"; the brief instructs codex to write tmp/codex_review.md then touch tmp/codex_done), delete any stale codex_review.md, touch tmp/codex_start, arm a Monitor on tmp/codex_done (zero-cost wait), and on the event delete both trigger files, read the review, and implement findings JUDICIOUSLY — stopping to present anything that needs an architect ruling (freeze touches, design forks, contested accepted costs) rather than deciding solo. Build/compile checks are not part of a brief unless the architect explicitly asks. Do not propose automated tests, regression harnesses, or commercial-tool comparisons.

The coder (subagent) runs no MUTATING git commands (no add/mv/rm/commit/push/restore — read-only git like status/diff/log/show is fine and encouraged for self-checks), does not stage or commit, and does not launch labwc or drive the GUI. The `projects/` folder is ARCHITECT-ONLY: neither planner nor coder modifies it, and since 2026-08-05 the warptempo_git wrapper EXCLUDES it from staging (`git add -- . ':(exclude)projects'`) — checkpoint commits are the app's own Save-and-Commit act, code commits are the wrapper's, and the two never collide (the old ride-along convention is retired; architect edits under projects/ sit dirty until his next in-app checkpoint). The planner commits via `warptempo_git` (a local wrapper; its invocation lives in the gitignored tmp/dispatch.md, not in the repo), and does NOT hold or bundle commits waiting for the architect — commit as work completes and verifies. The architect owns labwc verification and cmp-null/audio verification.

Coder briefs should point at the relevant `docs/engineering/architecture/` topic files instead of restating their content.

## Docs Map (read per task, not wholesale)

All in `docs/engineering/architecture/` unless noted. Each file holds the full hardened ruling text — read the one(s) your task touches before editing.

| Task touches | Read |
|---|---|
| Authored positions/frames, tempo cents, value brackets, playback_speed, sidecar grammar, the section rule | `data-model.md` |
| Settings keys/schema/editor, render-entry sidecars, load in place (`'`), audio_player, gui_scale | `settings.md` |
| Zoom, viewport, strip lanes, strip/alt/ctrl drags, pointer capture, trim-bar span-framing double-click, playback scanner | `zoom-viewport-strip.md` |
| The kdenlive redesign (row-by-row protocol, per-row records, the hard-coded-color/sans/text-shape/gui_scale standing rulings, the menu row + quit button) | `kdenlive-redesign.md` |
| Resolver normalization, red flags, marker walls/stores, always-set trim + commit rules, undo coalescing, adversarial load taxonomy, tripwires | `normalization-and-boundaries.md` |
| Pointer reach/modifiers, trim bar router, hit geometry, displayed paint basis, damage ownership | `pointer-hit-testing.md` |
| Selection model, marker click land, the marker drag, undo/redo restore visuals, shift/ctrl clicks, the never-parked rule + coincidence auto-select, the retired span form | `selection-model.md` |
| Flag shapes, lane text occlusion, stems, focus model, flag/BPM/settings editors, double-click, key repeat, Space | `marker-ui.md` |
| Region (trim scratch), scrub area, the Esc bindings, `x`/`Shift+X` | `region-scrub-esc.md` |
| The GitHub recheck (`h` history mode, the diff lane, `,`/`.` walk, `projects_repo`, load-in-place-from-a-commit) | `github-recheck.md` |
| Home-view binding, the bare Up/Down tempo cent step (singleton + group), propagate paste | `tempo-and-home-view.md` |
| Parser phase-reset compilation, engine geometry, trim prepost stage, render dispatch/cancel/reuse | `render-pipeline.md` |
| Modality (all five editors), strict modifier validation, the alt vocabulary, the retired plain/augmented convention, bare-`e` mouse key, type names, warp/phase-reset naming symmetry | `conventions.md` |
| Any color, ground recolors, disabled/trim/line color classes | `src/gui/render.h` palette block (code comments are authoritative) |
| Any guard/validator (add/move/remove) | `docs/engineering/validation_topology.md` |
| Engine performance (closed campaign; retired candidates need new data) | `docs/engineering/perf_campaign_2026_07.md` |
| The retired waveform antialiasing (technique record + reinstatement seed) | `docs/engineering/waveform_antialiasing_retired.md` |
| User-facing behavior reference | `docs/HELP.md` |

Trim spans several files by nature: store/commit rules in `normalization-and-boundaries.md`, endcaps/router/geometry in `pointer-hit-testing.md` and `marker-ui.md`, the prepost render window in `render-pipeline.md`, the highlight sync in `region-scrub-esc.md`.

## Architecture Rules (MUST follow — digests; the topic files are authoritative)

### Freeze Status (architect-declared 2026-07-12)
`src/engine/`, `src/parser/`, `src/audio_io/`, and `src/prepost/` are under PERMANENT HARD FREEZE, declared after the combined maximum-effort external audit came back near-clean: surgical changes only, each requiring explicit architect approval to reopen. `src/cli/cli_main.cpp` is freeze-adjacent (pure plumbing over the frozen stages; same discipline). Only GUI work remains: quality-of-life improvements, ad hoc bugfixes, and refactors as needed. Engine performance history and the retired-candidates list live in docs/engineering/perf_campaign_2026_07.md — a retired candidate needs new measured data to re-propose. The planner is allowed to update this file; it is COMMITTED (un-ignored 2026-07-27), so an edit here lands in history like any other and a revert restores it. EVERY GRANTED FROZEN TOUCH RECORDS ITS GRANT at the site and in the commit message ("(architect approval YYYY-MM-DD)") — the #22 audit found the record could not distinguish granted from ungranted touches, and this line is what makes it answerable next time.

### Authored Domain (→ data-model.md)
Authored positions (marker times, trim bounds) are whole source frames in `int64_t` everywhere at rest; `snap_authored_frame` (app_state.h) is the ONLY double→authored conversion route. Authored tempo is 100-based integer CENTS in [25, 400]; every other value is a bracketed double via `value_format.h`. One serialization domain (`frame_format.h`): canonical integer spelling only — anything else is adversarial load-fatal. Walls are exact integer compares on the shared inclusive [0, total−1] domain. Sources: WAV-only, 16/24-bit PCM, ≥44100 Hz, stereo-only.

### Pixel Anchoring, Viewport, Zoom (→ zoom-viewport-strip.md)
Position gestures anchor to the painted column grid and commit through `authored_frame_at_column` → `snap_authored_frame`; walls win over the grid everywhere. Waveform effective width is floored to a multiple of 16 px; the viewport snaps to grid points at `clamp_viewport_start` (the one chokepoint, which also owns the zoom-level clamp). Zoom is a continuous double in [1.0, 17.0] with `kWorkingZoomLevel` = 2.0 ("working zoom"), where `c` lands and THE TAB FAMILY DELIBERATELY DOES NOT (2026-08-05: the cycle set it for one commit, in both the live family and the history view's diff-flag mirror, and the architect reverted it the same day — "no zoom on Tab", a walk lands and recentres at the level it was pressed at), while bare `0` IS NO LONGER A TOGGLE (same day): below the effective ceiling it goes to FULL ZOOM OUT, at the ceiling it RUNS THE `c` COMMAND (run_center_command, the hoisted owner both key arms and `0`'s second arm share, forking live-vs-mode inside itself — so `0` `0` is overview then working zoom centred with `c`'s focus semantics); strip drags are dual-axis, song-anchored, pointer-captured, synchronous per pointer frame. THE TOP STRIP IS SEVEN LANES, ALL REDESIGNED (menu row / toolbar / tab row / icon row / trim bar / ruler / markers — rows 1-4 joined 2026-07-31, ROW 5 landed 2026-08-01 replacing the four legacy lanes (trim chip / marker text / flag / triangle) with three: the endcap trim bar, the Reaper-model ruler that is also the zoom strip reborn, and the kdenlive text-on-flag marker lane with always-on class-colored stems (which also hosts the aliased playhead head — it moved out of the ruler at the row-5 live test); the hover popup and the whole occlusion resolver are DELETED, the readout is selection-only, Ctrl+C copies the focus — kdenlive-redesign.md's row-5 record is authoritative, all on the gui_scale axis, wheel-inert; Quit + Navigation + Settings (that painted order since 2026-08-03, when the architect moved Settings last) plus the RIGHT-FLOATING VIEW BAR (both 2026-08-02 — kdenlive's workspace switcher as three buttons S+W/T+P/T+W that ARE bare 1/2/3, selected = the live combination, none lit in keyless S+P; its "disabled" face is the UNFOCUSED WINDOW on the existing activation edge, never the enabled bit; row 1 grew 30→34 content + 1px margin-bottom for it), Save/Undo/Redo/Render with click + gate-mirroring disabled faces, the A/B Breeze tabs, and the SIXTEEN-button icon row — S/T + W/P radios, copy/paste phase with Shift-paste, bpm `m` / iter `i` / follow `f`, listen `l` + load in place `'` (the zoom out/in pair joined 2026-08-01 and LEFT 2026-08-02 under the architect's no-duplicate-commands ruling, deleted whole with its glyphs; the `-`/`=` keys untouched), and since 2026-08-04 the HISTORY GROUP behind a separator gap — the history button (Icon::VcsDiff, selected while the history mode stands; its chord is bare `h` since 2026-08-05), joined 2026-08-05 by THE WALK'S OLDER/NEWER ARROWS (Icon::GoPrevious / Icon::GoNext, bare `,` / `.`, an ordinary 2px gap and no sixth group; a pure append, the walk having no right float and no total-width term) and by THE REVERT BUTTON between them and the history button (Icon::DocumentRevert, Ctrl+H), and 2026-08-08 by THE CUMULATIVE TOGGLE immediately right of the history button (Icon::OfficeChartLineForecast, bare `u`, kIconCount 18->19 — Breeze's OWN office-chart-line-forecast, verbatim from breeze-dark like every icon in the set since the same-day authored-glyph episode was reverted at the architect's use-Breeze's-own ruling; the rejected candidates stay recorded at the table row. ICON THEMES ARE AN AUTHORING-TIME MATTER ONLY: Breeze/breeze-dark is the ruled glyph SOURCE, consulted when an icon is TRANSCRIBED on the authoring machine — the committed SVGs are provenance records, read by neither the build nor the runtime, so no installed theme is needed to build or run; the architect's rationale for Breeze, recorded 2026-08-08: the one theme addressing Qt and GTK both fully, with a commitment to dual support), so the group reads HISTORY | CUMULATIVE | REVERT | OLDER | NEWER — the architect's order, the view, how it READS, what you can DO, then where you can step; neither insertion is a pure append and neither costs anything, the row being one left-to-right accumulation recomputed each paint — THE FOUR BEING THE ROSTER'S RESTING-DISABLED BUTTONS: their keys are bound only inside the history view, so `redesign_button_enabled` reads `history_mode.active` for them and the icon row's never-grey rule has a SECOND exception — the first inverted, same dead face, same one predicate; the arrows do NOT grey at the walk's walls, a wall step being a consumed no-op by key and by click alike, while REVERT ALSO GREYS INSIDE the view with nothing selected, which is the FIRST exception's own derivation rather than a third rule, and THE CUMULATIVE TOGGLE IS THE FAMILY'S FIRST SELECTABLE-AT-REST MEMBER (its lamp reads the session bit `history_cumulative`, which outlives every visit, so outside the view it wears the DIMMED SELECTED fill whenever the reading is cumulative — the one disabled blend over whatever the button was going to wear, no new arm) — with five-state faces and live selected bits, plus the tabs' permanent two-state LOCK slots (the active tab's click toggles read-only; absent in the history view, where the pair is the WALK selector); ALL TWENTY-SIX chord buttons (28-button roster = 26 chords + the 2 anchors since 2026-08-08, when row 3's compare-only pair was deleted and row 4 gained the toggle) — Quit included since 2026-07-31, the view bar's three since 2026-08-02, the history button since 2026-08-04, the walk's two and Revert's Ctrl+H since 2026-08-05, the Cumulative toggle's bare `u` since 2026-08-08 (the compare selector's two extra tab slots lived here 2026-08-07..08 — history-view-only surfaces whose table rows never dispatched, kept for the roster's totality — and were deleted with the four-tab row) — dispatch through ONE table + dispatch_redesign_chord body via on_key; SETTINGS AND NAVIGATION ARE THE TWO NON-CHORD BUTTONS, each click TOGGLING ITS DROPDOWN over ONE popup state (AppState::Dropdown, menu-or-None — so two menus are never open together by shape, and the other button's press — or, since 2026-08-03, merely the HOVER crossing onto it — switches through the one toggle; that same day the HOVER CROSSING ONTO A NON-ANCHOR ROW-1 BUTTON — Quit or the view bar's three — CLOSES the open menu through close_dropdown, ahead of the roster recompute so the closing frame is the lighting frame, because only one button in that row is lit at a time; the brief row-1 hover EXEMPTION died with it, redesign_button_hoverable back to refusing the whole roster under an open popup and redesign_button_in_menu_row's one consumer now the close rule; and THE ROW IS A MODE ONCE A MENU HAS BEEN OPENED FROM IT, that same day's completion of the close rule — ONE ARMED BIT INSIDE THE POPUP STATE (AppState::Dropdown::menu_row_armed), set by toggle_dropdown's OPEN, the one route that opens either menu, so an open menu is always armed, and cleared by close_dropdown — which clears it ABOVE its own "nothing is open" return (2026-08-03, the real dismissal: the mode's defining state is menu CLOSED and row ARMED, the state a row-1 hover close leaves behind, and riding the whole-struct reset alone made Esc, the wheel, Ctrl+Q and a resize no-ops in exactly that state), so EVERY dismissal disarms through the one close owner (Esc, an item, the anchor's own click, a press anywhere else, the wheel, Ctrl+Q, the WM close, a resize, — the checkpoint notice's opener was a fourth such closer from 2026-08-08 and died 2026-08-09 with the modal it raised, so the WM close and the resize are again the whole asynchronous list and every prompt is raised by a key or a gesture) while the ONE close that KEEPS it, the row-1 hover close, re-arms explicitly at its site; BEYOND THE CLOSE OWNER the mode ends on ANY pointer press (on_button_press's top) and ANY key press (on_key's top) — blanket rules needing no exception list, since the one press that must keep the mode re-arms through toggle_dropdown's open a few lines later and no chord opens a dropdown at all — plus the pointer leaving row 1's band, and the pointer leaving the WINDOW OUTSIDE that band (2026-08-08: row 1 abuts the titlebar, so sliding one pixel up off the row is a step onto the titlebar rather than out of the visit — the leave hook asks the platform's LEAVE REASON first (GuiPointerLeaveReason, OrdinaryLeave vs CapabilityLoss, each fire site passing its own: a capability loss is the hard end of the stream, with no return motion to re-derive anything, so it clears and disarms unconditionally) and then point_in_menu_row_band of the REMEMBERED position and, on an ORDINARY leave through the band with the mode armed, skips BOTH the disarm and the roster hover clear, so the mode survives and the hovered row-1 button stays lit, exactly as an open menu already survives that edge; recompute_redesign_button_hover's new pointer_in_window early return is what stops the tick clearing the kept face, a resize still ends the mode, and any other leave is unchanged; AND THE HARD BRANCH IS HANDLED TO NOT-BROKEN, NOT TO POLISHED (architect 2026-08-09, slimming it): ONE hook fire leading the branch — its claim drop is what stops a held menu item firing on a hardware edge — then both hold ends, then the capture teardown (the holds first, so the release reads the drag's true last position rather than the cursor restore hint), while a staged-motion flush may put `pointer_in_window` back and leave a hover face stale until the pointer next enters, an ACCEPTED GLITCH that self-heals on entry and is not to be re-guarded; the second hook fire and the proof walls that justified it are DELETED), all four through disarm_menu_row, which carries the no-menu-open gate the rule can only go wrong at (a standing menu is still the mode, and while one is up the popup's own routes own the mode); armed with NO menu open, the pointer merely ENTERING either anchor's rect opens that menu through the same toggle, and THE TWO MOTION HALVES ARE SPLIT BY THEIR GUARD LISTS — the OPEN half lives in on_motion's no-gesture tail beside the roster recompute, so its guard list IS the branches that already return above it and its reachability is exactly the anchors' own press claim's, while the EXIT half ("the pointer left row 1") runs at the TOP of on_motion, above every branch, because a modal owning the pointer is a reason not to open a menu and no reason to forget where the pointer went): the SETTINGS menu is six recall-prefilled settings-editor openers in two categories, the NAVIGATION menu (2026-08-02) seven COMMAND items in two — zoom in/out, overview `0`, center `c` | Tab, Shift+Tab, Ctrl+Shift+Tab — each dispatched through on_key like a button's chord, hotkeys shown in a right-aligned accelerator column (architect-ordered, sampled from dropdown_full_hotkeys.png: 57px label indent, 13px column gap, 30px right margin, ink #b8b9ba) and never greying out WITH ONE RULED EXCEPTION (architect 2026-08-08, the menus' first per-item disabled state): "Walk both tabs" inside the `h` view, where Ctrl+Shift+Tab is the mode's own reverse walk-source cycle rather than the A/B walk the label promises — a difference in KIND from the never-grey rule's ordinary "nothing to act on" refusals, one predicate (dropdown_item_enabled) read by the painter, the press claim, the hover walk (which resolves a greyed row to NO ITEM) and the release, geometry deliberately outside it (the row keeps its slot; a press on it arms nothing, dismisses nothing and leaves the menu up) and two sampled inks off dropdown_disabled.png (label #686a6c, hotkey #515356, derivations recorded, neither shipping as a factor) — and since 2026-08-03 BOTH menus take those popup-box metrics (the settings menu's 12px item pad retired at the architect's harmonize ruling), the width one expression whose accelerator terms are optional on whether the item table carries hotkeys; one painter, one gate, ONE bare-Esc binding for both (still the sixth, no seventh place), and the FOUR shift-admitting buttons (Render, paste, and since 2026-08-07 the walk's Older/Newer arrows, whose shift-click is the wall jump) carry the kdenlive two-line SHIFT TOOLTIP on a 700ms hover dwell — SHOWN ON DISABLED BUTTONS TOO since 2026-08-07 (kdenlive parity: the dwell and the hint ride redesign_button_hover_zone, the hover FACE alone keeping the enabled term, so the three resting-disabled history buttons read their hints outside the view) — Render's admission and its shift line are MODE-CONDITIONAL since 2026-08-02, and its label with them ("Render Iterations" in iteration mode, an architect-named exception to sentence-case labels; since 2026-08-04 the HISTORY MODE adds a second stateful string — "Save and Commit" with Icon::VcsCommit, the SECOND named title-case exception — which MOVED ONTO THE SAVE BUTTON 2026-08-08 (the act is save-first by definition, so the Render hijack died: Render wears its ordinary face over the mode's derived disabled one with both render chords consumed in the view, and SAVE has THREE states — "Save", "Save and Commit" in the view, and "Committing..." disabled in EVERY view while a checkpoint publishes, that third face the mirror of the global save lockout at GuiSaveOps::save) — all absorbed by the shaped-run layout walk, "Save and Commit" still the row's widest label at 7.948 em against 7.671); detail in kdenlive-redesign.md). THE ZOOM ROW IS DELETED (architect 2026-07-31, HIS OWN REVERSAL of the 80a265e rollback's do-not-re-propose — the 2026-07-27 clause is superseded; the marker-text-lane scrub deletion at 42743dd stands): the dual-axis strip drag survives WHOLE on TWO entries — the ctrl-waveform press and, since row 5, the ruler band's plain drag (one hoisted arm body, arm_strip_drag_at; since 2026-08-05 the ARM PAINTS THE HEADLESS ZOOM STEM FROM THE PRESS, every end of the gesture owing that stem's erase; the gesture is otherwise NAVIGATION-CLASS as it always was — the strip-drag PLAYHEAD JUMP of 2026-08-05..06, in all three of its shapes (the motionless-release click, the mousedown jump, the anchor-follow re-seat), was ROLLED BACK WHOLE 2026-08-06 as buggy under live audio, jump_playhead_to_strip_anchor deleted with it and the stem-at-press the one survivor; "move the playhead here" is SHIFT+CLICK) — and the span-framing double-click (region-else-proper-trim-else-whole-song, run_span_framing_command) REHOMED onto the TRIM BAR (the whole band, seed-at-motionless-release, consume-before-arm). THE PLATE RENDERER (the 2026-07-26 draw arc, ALIASED-ONLY since 2026-08-01 — the antialiasing deleted whole at the architect's side-by-side verdict, technique record in docs/engineering/waveform_antialiasing_retired.md): a direct ARGB32 writer with BINARY alpha — per-column raw min/max bars on the authoring lattice `g(k0+c)` in BOTH views (bit-exact pan invariance; a column is a pure function of its own span), the never-fade >=1px floor as integer geometry, a 13-rung peaks pyramid (.peaks v7, <=5-pair reads unconditional), and ONE synchronous full-render route for every user-driven pan/zoom frame (the incremental pan path is retired; the async worker serves only undriven load/resize/follow/drift). The ink-notch rule is retired PERMANENTLY (architect). The scanner renders at a sub-frame position; its value fields are meaningful only while active. EVERY PAN SUPPRESSES FOLLOW for the playback session (architect 2026-07-30): one line at the `scroll_viewport` funnel (paging, alt+wheel, touchpad, alt+drag grab-pan) plus the strip drag's bypass viewport write; keyboard zoom is a deliberate non-producer (it centers ON the scanner); the chase resumes at the next launch or an explicit `f` re-enable (producer inventory at the flag's declaration, app_state.h). ALL THREE VIEW SWITCHES (`t`, Ctrl+Tab, `p`) take the SYNCHRONOUS rebuild — and the bare 1/2/3 absolute selectors (2026-08-01; since 2026-08-02 also the view-bar buttons' chords) COMPOSE the `t`/`p` handlers rather than adding a fourth route — `p` joined 2026-07-30, closing the one view switch that blitted a stale flag frame (the run loop paints before the tick's fingerprint check; inventory at viewport.h's kick_waveform_sync declaration).

### Render Pipeline (→ render-pipeline.md)
The parser is the sole authored→engine compiler (phase resets emit `S − N/2`, no render-time offset anywhere); geometry `kN`/`kRs` lives in `engine_geometry.h`. Trim is a prepost stage: full maps → `plan_trim` (refusal = render untrimmed, never a failed render) → trim-ignorant engine → `post_trim` → unconditional limiter → encode. No subprocesses in the render path. CTRL+ALT+R IS TWO COMMANDS SELECTED BY THE ITERATION-MODE BIT (architect 2026-08-02): the single render beside the source with the mode off, the ITERATION SWEEP into renders/<N>_iterations with it on (the retired Ctrl+Alt+I's own body, refusals and all — the chord is unbound now, not refused), while Ctrl+Alt+Shift+R's miscellaneous cell is a CONSUMED NO-OP in iteration mode (archival is a plain-mode act, the refusal inside the one route so keyboard and button share it). A render dispatch kills the running render unconditionally; killed sessions can never publish Success; target-view previews may reuse identical completed renders through the two reuse rungs (load-bearing for undo/redo A→B→A; the fingerprint's phase-reset component is TRIM-AWARE since 2026-08-01 — the key serializes the reset set that actually reaches the engine, so out-of-window reset mutations hit the cache instead of spuriously re-rendering; version bumped 18→19 — codex proved a pre-fix trimmed sidecar could byte-collide with a post-fix key naming different engine input, so the re-domained reset field forced the namespace).

### Colors (→ render.h palette block)
THE PALETTE IS HARD-CODED WHOLE, NO USER-SETTABLE COLORS (rows 1-7 sampled every surface from the kdenlive crops, 2026-07-31..08-01; the architect retired the remaining runtime color system OUTRIGHT 2026-08-02): every painted surface, the waveform canvas/ink/borders and the marker classes included, is a constexpr from its sampled crop (or a recorded derivation, e.g. kWaveformRegionCanvas = canvas + the Breeze View→ViewAlternate lift); a retune is a recompile. colors.conf AND ITS MACHINERY ARE DELETED — the loader, the 23-key grammar, the ~/.config read; a conf left on disk is never read, no stderr, no migration (the retirement record and one provenance paragraph live at the render.h palette header; the deleted keys' values are git history). The palette is FULLY OPAQUE (no compositing alphas): the region highlight recolors the CANVAS GROUND under the ink (paint_region_ground before the plate blit — the Ableton model, opaque since the binary-alpha plate); the phase-reset overlay is a 1px RING on the borders' outermost pixels reading kMarkerFlagFill itself; the marker class ladder is DISABLED > RED > (selected-bright default) — selection is the class ladder's brighter pair — and since 2026-08-01 a SELECTED DISABLED marker takes the same lift too, the selected pair fed through the one disabled blend, fill and edge (red refuses the lift on both sides, its normalization cue unmasked); stems read the CLASS alone, disabled markers paint NO stem. Codex is SKIPPED for simple color tuning (architect 2026-07-27); the routine stays for substantive arcs.

### Settings (→ settings.md)
`app.engine_settings` is typed truth; all three boundaries are strict (whole-file schema load shared verbatim by GUI and CLI — a sidecar set loads in both products or neither; editor red-flash; typed dispatch). `kSettingsOrder` owns write order, `kEngineKeys` membership; adding an engine key = six edits, four files. `projects_repo` (the GitHub recheck's projects-home guard, default `github.com/warptempo/warptempo_gui`) joined the schema 2026-08-03 under grant and became REQUIRED LIKE EVERY OTHER KEY 2026-08-04 (the one-day optional exception retired at the architect's live pass, second grant recorded) — every key is required, no absent state, no load-side default (full record in settings.md). GUI-kind keys commit through each key's own gesture chokepoint, history-less. Numeric values are pinned to one canonical spelling each. `gui_scale` (2026-07-30, the kdenlive redesign's lever) is an integer percent in [100, 200], default 100, wired through the full schema under a granted frozen parser touch and LIVE since 2026-07-31: pushed via set_gui_scale_percent at the three application points (load, settings commit, load-in-place), applied live by the settings commit (apply_gui_scale, the resize-path rebuild), consumed by EVERY row since row 7 (`gui_scale_factor()` — the product's ONE scale axis; font_size and gui_font_scale are DELETED, 2026-08-01 under grant, no legacy: a sidecar still carrying font_size= is load-fatal as an unknown key). Render entries carry the same sidecar set a source does, written once at dispatch; `'` loads an entry in place, 1:1 in memory.

### Load-Lenient, Render-Normalizing Boundaries (→ normalization-and-boundaries.md)
Gestures move freely and NOTHING POPS: ambiguous authored states normalize at the render boundary (`resolve_warp_markers_for_render` — every ambiguous tempo → 1.00, one stderr line per timestamp per resolve; normalization-red flags as the display cue), never refused or force-resolved. The two-category rule: a state the GUI can commit always loads and renders; a state the GUI can never produce is adversarial and hard-fails the load, first error only, identically in both binaries. Marker walls are 0 and total−1 both columns. TRIM IS ALWAYS SET (2026-07-30): a full ordered pair always rests ([0, total−1] default, canonical [0,0] on a one-frame source); the unset state, lone bounds and the `-1` spelling are DELETED (the `-1` grammar died in the FROZEN parser under an explicit same-day grant, recorded at each site — a sidecar carrying it is adversarial load-fatal in both products, hand-edit the ruled recovery, no migration tool); crossed pairs RESET TO THE FULL WINDOW at commit and at load (full-window-first, so the one-frame [0,0] never self-resets; since 2026-08-02 the endcap drag itself CLAMPS INCLUSIVELY AT THE PARTNER — handles cannot move past each other, and a coincident release IS the reset, the drag's own trim-clear route — while typed/loaded values still cross and reset at commit; per-route detail at input_trim.cpp's header); THE FULL WINDOW IS SEMANTICALLY THE OLD UNSET STATE through ONE shared predicate, trim_window_is_full (settings_file.h — the kMinZoom cross-product precedent): no trim plan at the three render orchestrators (byte-parity with the pre-arc untrimmed render is load-bearing — the crop would fire otherwise), {0,total}/{0,live_total} at the two NAVIGATION range owners (they were the playback owners too until 2026-08-05), the old unset bytes at the render fingerprint (cache reuse holds, no version bump), recognition BEFORE the crossed/sub-sample diagnostics. NOTHING LOOPS (all audition looping removed 2026-07-30): every audition plays once to its end and stops — AND THAT END SPLITS BY AUDIO VIEW since 2026-08-05 (architect): the SONG'S END in source view, the trim window no longer bounding source playback (the gating served the target preview alone), the bound preview buffer's end in target; the split lives at launch_playback_from and Viewport::trim_range is the NAVIGATION range owner now (Home/End, the load-time playhead), nothing else. EVERY route that WRITES a trim bound CLEARS THE SELECTION (the setter deselect — the ctrl/ctrl+shift bound-set clicks (retired one day at 853c2c4, REINSTATED 2026-08-01 with a strictly-inside refusal: a click landing a bound on or past its partner is a consumed no-op — nothing writes, deselects, stops, or arms), `x`, the endcap/bridge drag at its first accepted bound change, the settings trim keys' active-tab arms) AND PARKS THE PLAYHEAD AT THE NEW TRIM START, clearing a resting region with it (architect 2026-08-05, generalizing `x`'s own 2026-07-30 land — one owner at the shared commit tail, the drag parking at its RELEASE only, `Shift+X` parking inside its identity guard though it is a maximizer and deselects nothing, and the crossed/coincident RESETS needing no arm since the committed begin is then frame 0; every refusal still moves nothing; membership at input_trim.cpp's header); the trim-window highlight sync is RETIRED (the window's permanent display is its always-painted bar/endcaps), the four highlight-only trim-lane routes retired outright, and `Shift+X` is the MAXIMIZER behind an already-full identity guard. `x` keeps its DEGENERATE-RESULT REFUSAL (a pair inverse-mapping to `end <= begin` writes nothing — trim has no undo). Undo coalescing is a HYBRID OF TWO RULES (architect 2026-08-01, superseding pure repeat identity's separate-presses clause): a HELD key's synthesized repeats coalesce BY REPEAT IDENTITY (the platform stamps repeats via `GuiInputState::synthesized_repeat`, the physical press pushes, repeats never push — no clock on this arm, so the compositor's repeat delay still never participates), AND rapid PHYSICAL taps of the same kind within `kTapCoalesceMs` = 500 of the last accepted event merge too, gated on the SUBJECT standing (the stamped selection + A/B tab — the tap arm's stand-in for the repeat contract's event-edge disarms); every stack-top change still clears the stamp, the physical-arrival invalidate survives verdict-first, and `record_gesture` stamps only on the accepted path, so a press that REFUSES, misses the window, or finds a changed subject opens its own entry (detail in normalization-and-boundaries.md).

### Interaction Model (→ selection-model.md, marker-ui.md, region-scrub-esc.md, pointer-hit-testing.md)
Selection and playhead are RE-COUPLED: every plain marker click lands the playhead; shift = inclusive range from the anchor/focus, ctrl = membership toggle, both landing on the FOCUS they set (2026-07-28) and both FLAG-ONLY since 2026-08-01 and UNIVERSALLY SO SINCE 2026-08-06 (the architect's SYMMETRY RULING: a modified press over a stem is the waveform's own shift/ctrl gesture — the placement press, the strip drag — in EVERY view, waveform modifiers being gesture vocabulary and selection lane vocabulary; plain clicks keep the stem-top hit everywhere, and the `h` view's stem-based multi-select, this line's one former exception, is deleted). THE PLAYHEAD HAS ONE FORM AND ALWAYS PAINTS (architect 2026-07-30, THE REGION SIMPLIFICATION — the two-forms model, the split half-triangles, the cursor/stem suppressions, SelectionExtent provenance, the membership-replace clear, the never-span-less analysis, and the trim-window highlight sync are ALL RETIRED): the playhead cursor (the white kPlayheadStem head + stem form since row 5, the head in the marker lane) draws unconditionally at the resting cursor; a selection's cue is its members' BRIGHTENED FLAGS (the class ladder's brighter pair — the pre-row-5 ink triangles and kSelectedStem singleton stem are retired forms), the cursor standing on the landed focus. THE REGION IS TRIM-ONLY FREE SCRATCH ("a temporary selection of what could potentially become a trim region"): THERE IS ONE FORMING GESTURE SINCE 2026-08-05 — THE WAVEFORM PLACEMENT PRESS AND THE DRAG IT ARMS — in three entries: the plain UPPER-HALF press, the SHIFT-exact press at EITHER HEIGHT (the architect's FORMER RESHAPE: the region ANCHORS AT THE CLICKED COLUMN, so shift IS the placement press and the playhead LANDS WHERE THE MOUSE RELEASES; a motionless shift click rests no region, and the playhead-anchor read, the furthest-selected-marker argmax, the one-act tail form_region_span_to_click, the non-dissolving arm arm_region_drag_preserving and the motion path's crossing bypass are all DELETED), and the `h` history view's own FULL-HEIGHT press (that view having no scrub). EVERY FORMER DROPS THE SELECTION ITS SURFACE OWNS (architect-RATIFIED 2026-08-05, promoting the mode arm's coder reading into the ruling; family rule at RegionState, app_state.h): the two live entries deselect the STORE selection at press, so A REGION DRAWN IN AN ORDINARY VIEW RESTS ONLY BESIDE AN EMPTY SELECTION (structural; it killed the phase-overlay region gates as dead code), while the view's entry clears the MODE'S focus and diff-flag selection instead and touches no store selection at all, costing that absolute nothing, THE VIEW'S REGIONS BEING VIEW-LOCAL (cleared at its exit and at every `,`/`.` step and compare switch, so nothing formed in there can rest in the editor — the surviving exception is scoped INSIDE the view, inventory at RegionState, app_state.h) — THE DRAG CARRYING THE PLAYHEAD on its moving endpoint (2026-07-30 refinement, every entry through the one motion path), previewed by either scrub press (the lower-half plain left press, or — since 2026-08-01 — the bare right-click anywhere on the waveform, full height, the right button's only binding; one shared body), consumed by `x` (which clears it after the write AND LANDS THE PLAYHEAD ON THE COMMITTED TRIM START), cleared by BARE ESC (the fifth Esc binding, 2026-07-30 — the one clear with no side effects, ranked above the render cancel, structurally unable to cancel a drag) and by every playhead-moving or selection-changing route (inventory at clear_region_highlight, input_handler.h; the keepers: both scrub presses, Space, pure viewport moves). EVERY CURSOR THE PRODUCT SHOWS IS A NAMED CURSOR FROM THE USER'S OWN XCURSOR THEME (architect 2026-08-03, revising his own ruling of earlier that day): the custom-drawn SPEAKER IS RETIRED WHOLE — its glyph, its ARGB32 wl_shm buffer and pool, build_speaker_cursor, the icons::Icon::PlayerVolume entry (kIconCount 14->13) and the committed SVG — and the product ships no cursor art at all. SEVEN KINDS, SEVEN NAMES (Arrow left_ptr, Scrub crosshair, Pan grab, Zoom zoom-in, TrimResize ew-resize, TrimBoundBegin left_side, TrimBoundEnd right_side; all present in Breeze_Light and Adwaita), each a theme lookup plus its own wl_surface with THE HOTSPOT THE FILE DECLARES (never a computed centre), loaded once through ONE per-kind loader driven by a {kind, name} table; A MISSING NAME DEGRADES TO THE ARROW PER KIND with one stderr line (the theme load's own failure shape — a poor environment, not a broken one), and the size still rides the THEME's axis (XCURSOR_SIZE, else 24), not gui_scale. THE CURSOR PROMISES THE GESTURE: ONE zone map DERIVED from on_button_press and the band routers (pointer_cursor_kind, input_handler.h) — waveform lower half plain = Scrub (MODE-SCOPED since 2026-08-05 — the `h` view consumes both scrub entries, so its lower half answers Arrow), waveform either half with ALT = Pan, waveform either half with CTRL = Zoom and the RULER band plain = Zoom too (one gesture, arm_strip_drag_at's two entries), everything else Arrow; the modifier arms outrank the plain zones and SHIFT is deliberately unnamed over the waveform (it is the placement press, which carries no cue on either half). THE TRIM BAR SPLITS THREE WAYS since 2026-08-03, and reads the ROUTER'S OWN OWNERS rather than the band: an ENDCAP extends ONE boundary and takes its bound's edge shape (begin left_side, end right_side) through hit_test_trim_endcap, the inter-cap BRIDGE keeps ew-resize because it moves BOTH bounds through point_in_trim_bridge_span (hoisted out of route_trim_bar_press for this second consumer), and CTRL / CTRL+SHIFT take the same pair matched to the bound each writes through trim_bound_click_frame (hoisted out of set_trim_bound_at_click, which keeps only the write) — so a point arming nothing, INCLUDING a ctrl click the strictly-inside guard would consume, shows the Arrow, and cue and gesture agree by construction. THAT BAND IS ALSO MODE-SCOPED PER ZONE (2026-08-05): while the `h` history view stands its three trim cues go, the mode consuming those gestures, and the PLAIN zone answers Zoom instead — the view's own diff-span framing click. EVERY refusal applies to EVERY kind — prompt / the three bottom-strip modal editors / an open dropdown / loading-or-empty audio / any live pointer gesture — which is what makes the cues HOVER-ONLY, with ONE NAMED EXCEPTION (2026-08-03): a live TRIM gesture keeps its own cue for its whole life, read from the drag's own record of what it grabbed, never re-derived from the pointer position. READ-ONLY IS OUT OF THE CURSOR MAP ENTIRELY since 2026-08-07 (the architect's band ruling: read-only protects the AUTHORED MUSICAL CONTENT — the marker stores and the engine settings — and nothing else; the per-tab BAND including TRIM, plus Ctrl+S and both render chords, are read-only-legal, so every trim gesture, `x`/`Shift+X`, and the typed trim keys work on a locked tab and their cursor cues stand; the ruling's home is read_only_key_blocked, and Save/Render's button faces follow while Undo/Redo keep their locked-tab grey; the SETTINGS EDITOR IS DISABLED on a locked active tab — one gate at its opener, closing the dropdown-opened engine-key hole, a silent consumed no-op because the menu items' never-grey rule stands, the modal playback stop moved past the gate so a refused open stops nothing; the sweep's locked-tab bracket wipe is RATIFIED, brackets being unsaved, impermanent, and self-documenting in renders/), the pointer-transparent FLAG editor refuses nothing, the full-height right-press scrub stays unmarked, and there is NO pressed-state cursor by ruling (the capture HIDES the cursor, which is what buys unlimited travel). THE CURSOR IS RESOLVED ONCE PER RUN-LOOP ITERATION BY ONE OWNER (architect 2026-08-03, replacing the per-site push model whole): the platform fires a settled-state hook at the tail of every run() iteration it is not leaving, main.cpp wires it to refresh_pointer_cursor (live modifiers handed in, remembered position, no-op while the pointer is outside the window), and EVERY other applier is deleted — the on_motion top push, the eighteen gesture-end calls, the two force-end tails, the wl_keyboard.modifiers hook and on_key's scope guard, twenty-three sites down to one — so the "run after the state has settled" ordering rule died with the sites; accepted staleness is one poll wakeup wide plus the capture's drop span (region-scrub-esc.md is authoritative). The platform keeps ONE cursor owner over a remembered kind (apply_cursor_kind, which also owns the per-kind fallback to the arrow; the capture's hide is the one set_cursor outside it, and the release restores the kind rather than the arrow). SPACE ALWAYS PLAYS FROM THE PLAYHEAD (the region left-bound launch is deleted; the scrub press is the span-preview gesture) and NOTHING LOOPS (all audition looping removed 2026-07-30 — verdict, wrap, resync counter, stash). HORIZONTAL MOVEMENT IS A FOCUS ACT — GROUPS ARE NEVER MOVED (2026-07-29, ruling 9); the group-verb doctrine and the wall policy (singleton steps CLAMP, group presses REFUSE whole) live at position_nudge.h. THE COLLAPSE FAMILY IS TWO SITES — the nudges' shared prologue and Ctrl+N; `t` and `c` CARRY a group whole since 2026-07-30 (`t` keeps its selection-gated land, the EXACT re-expression of a cursor already resting on the focus — it closes the T→S inverse double-round and keeps the coincidence compare true; the empty-selection generic translation is untouched, and the toggle's column anchoring is unaffected either way). THE LANE MODEL IS KEPT WITH THE CURSOR VISIBLE (architect 2026-07-30): bare arrows nudge the FOCUSED marker in its home view (one predicate, playhead_in_marker_lane, two readers; the read-only gate entry is the sole defense) and the playhead visibly rides the re-land; lane exit is any deselecting route. THE MARKER LANE OWNS THE PLAYHEAD: every focus-changing route lands (the rule, the caller inventory, and — since 2026-07-30 — the Tab/`c` jump all live at land_playhead_on_marker, input_pointer.cpp; the jump's hand-copied recipe is unified onto the owner, its narrow unmoved-branch damage superseded by the owner's full-area shape). THE SELECTION IS NEVER PARKED (ruling 1): `p`/Ctrl+Tab clear it outright; COINCIDENCE AUTO-SELECT at FOUR entry chokepoints (load, `p`, Ctrl+Tab, `'` load-in-place) re-acquires the marker the stored cursor stands on EXACTLY (the land's own formula, exact int64, first-in-store; owner auto_select_marker_at_playhead). Value-shaped session state parks safely; index-shaped does not (per-tab band: viewport/zoom/playhead/trim/read_only). Hit geometry and painted-pixel deciders ride the DISPLAYED basis (plate_viewport_basis / item_viewport_basis, distinctly named); DAMAGE FOLLOWS THE BASIS OF THE PIXELS IT ERASES — the playheads' damage rule (playhead_pixel_x, app_state.h) is CADENCE-scoped since 2026-07-30: narrow-on-plate for the two per-frame scanner sites alone, full waveform-area for every discrete command. POINTER GESTURES HAVE NO CANCEL (rulings 3+4): Esc mid-gesture is a consumed no-op, any end commits, undo is the mitigation. BARE ESC IS BOUND IN SIX PLACES AND NOWHERE ELSE (ruling 5 + the 2026-07-30 region clear + the 2026-07-31 settings popup; src enumeration at input_handler.cpp): the editor text-drag hatch, the five editors (one shared place — the commit-title editor of 2026-08-07 joined it and moved no count), the prompts, the DROPDOWN close — both menus through the one shared gate, which is why the second dropdown added no seventh place (under the prompts — a popup and an editor are structurally never open together, by TWO mechanisms since 2026-08-03: the four bottom-strip editors SWALLOW the press that would open a menu, and the pointer-transparent flag editor, which does not, is ENDED by the open in toggle_dropdown's open path), the region clear (above the render cancel; mid-drag the drag-modal gate swallows it — clear, never cancel), the render/batch cancel. THE KEYBOARD STOP RULE (authoritative at stop_playback_if_playing's declaration, playback_lifecycle.h): playhead-moving point commands stop a live audition; group-preserving value steps (the cent step) and pure viewport moves do not; trim mutations stop in BOTH views — the endcap/bridge drag at its FIRST ACCEPTED bound change (the press-time stop retired with the highlight-only trim-lane arms, 2026-07-30: a plain trim-bar click that never becomes a drag is a CONSUMED NOTHING), `x`/`Shift+X` refusal-gated as ever.

### The Home-View Binding (→ tempo-and-home-view.md)
Each marker column authors in its HOME view only (warp = source, phase reset = target) via `active_column_authoring_allowed`; off home, columns are display/navigation-only with silent refusals. Three ruled exceptions (contortion ruling 8, 2026-07-29, grew a third 2026-08-07): the bare UP/DOWN TEMPO CENT STEP in W+target (owner-only there, singleton + group, the group arm all-or-nothing over a view-independent wall set), the phase-reset propagate (starts in source, lands in target), and the ITERATION-BRACKET WIPE in W+target (the sweep's success tail and the `i` OFF toggle — granted with the 2026-08-07 ruling that ITERATION MODE IS TARGET-LEGAL: the S→T wipe of 2026-07-23 is deleted and superseded, the mode bit and every bracket persist across S↔T, `i` toggles on the warp column in EITHER audio view, the sweep dispatches from target, and bracket AUTHORING alone stays source-only at the flag editor's home gate; brackets are delta-relative, so they compose with any base-tempo change — inventory at active_column_authoring_allowed, app_state.h). The wipe's deletion also removed the `h` history view's one undo-stack producer, making the Local walk's frozen-stack premise EXCEPTIONLESS and the push-serial check deleted with it (producer-less mechanism, architect same day). The pointer TEMPO DRAG and the bare Left/Right TEMPO-IMAGE STEP are DELETED — so W+target has NO pointer authoring gesture (matching P+source) and bare Left/Right there with a selection is a consumed refusal.

### Modality and Input (→ conventions.md)
Modal surfaces live on the bottom strip, and since 2026-07-28 the top-strip flag editor is KEYBOARD-modal too (architect, reversing the old "commands punch through" ruling): all five editors (the history view's commit-title editor joined 2026-08-07) admit exactly the editor's own keys plus bare Esc, Ctrl+S and Ctrl+Q through ONE `route_modal_editor_key` contract, so an unbound chord is structurally inert rather than a command that discards the edit — which is what closed the flag editor's destructive fall-through tail, and with it the `StrayModifierKey` class that tail alone had justified. Modality is CHORDS ONLY: the flag editor stays pointer- and wheel-transparent and still does not stop playback (`modal_bottom_strip_editor_active` has exactly ONE caller, `wheel_context`; the playback stop has ONE owner since 2026-07-28, `GuiPlaybackLifecycle::stop_playback_for_modal_open` — refusal-gated, the bottom-strip editors + the prompts (the caller inventory at its declaration), the flag editor's exemption explicit there — the keyboard gate is `keyboard_modal_editor_active`). Accepted cost: no in-editor undo, the gap the bottom-strip editors always had. THE CLIPBOARD IS THE SYSTEM CLIPBOARD (architect 2026-08-02, resurrecting the Wayland half `e0c988c` removed as collateral of killing the in-session file load; DnD and that file load STAY DEAD, and primary selection is out of scope): the reach is unchanged — the editors' ctrl-exact `Ctrl+C`/`Ctrl+X`/`Ctrl+V` plus the readout's `Ctrl+C`, no new chord and no new gate — and only where the bytes travel moved, copies publishing to the compositor and a paste taking another application's text (our own payload short-circuits while we still own the selection). An empty answer is a CONSUMED NO-OP, which incidentally closed the pre-arc edge where `Ctrl+V` with nothing copied deleted the selection; `text_editor::replace_selection` stays the ONE incoming filter and now has a live producer for its malformed arm. Detail in conventions.md, the lifetime rules at platform_wayland.cpp's clipboard section header. BPM mode is exactly its editor session. The GUI never writes a load-invalid state. ALL text uses PROPER capitalization (GUI-facing 2026-08-01; terminal stderr/stdout 2026-08-02, THE TERMINAL CAPITALIZATION PASS — text-only, otherwise byte-identical output, the wide frozen touch recorded per file): sentence-initial capitals after the program-name prefix, prose acronyms capped (JACK/WAV/PCM/BPM), data/identifier tokens keep their spelling, identifier-headed messages start verbatim, the engine's progress readout is structured not prose — full rules and the ruled holds in conventions.md. Never a double space. Bare `e` is the left mouse button, translated at the platform boundary (never bind it in dispatch; its synthesized HOLD is ended by POINTER-capability loss too since 2026-08-08 — both sources, one delivered release on the logical 1->0 edge, keycode zeroed so the later keyup is a no-op — because the gesture it arms is a pointer gesture and a surviving hold would be inherited by the next stream, resuming at new coordinates with no click in between); bare 1/2/3 are the ABSOLUTE VIEW SELECTORS (2026-08-01: S+W / T+P / T+W — combinations, not toggles; composed from the `t` and `p` handlers, audio first then markers so the coincidence auto-select reads the finished combination; S+P deliberately keyless; digits 4..9 unbound). STRICT MODIFIER VALIDATION (architect 2026-07-28): a modifier combination with no explicit binding is a no-op everywhere — Space and Escape bare-exact at every reader, the editors' session keys bare-exact and their clipboard chords ctrl-exact, the prompt case-sensitive; THREE families are deliberately untightened because the modifier is a real binding there — `is_tab_cycle` (Shift+Tab reverse-cycles), Ctrl+Z (Shift selects redo — the one meaningful bit again since 2026-08-07, the stay-put alt pair's one-day life ending in the labwc-collision rollback), and the editors' MOTION arm, which keeps ctrl and shift in every combination but excludes ALT (they carry real meaning on all six keys and degrade to the plain gesture where they add nothing, exactly as an ordinary one-line field does, so exactness would break them; alt binds nothing in the family). A stray modifier on an owned key shape is simply NotEditorKey and drops at the gate; the classifier stays the single keymap owner and no caller enumerates editor keys. SUPER IS DROPPED AT THE PLATFORM BOUNDARY (architect 2026-07-30 — super belongs to labwc): the modifier mask never modelled LOGO, so every bare-exact predicate accepted a held Super (Super+Escape discarded an open edit); the platform now tracks `mod_super_` and delivers NO key event while it is held — one gate at `deliver_key` covering physical presses and synthesized repeats, reset on keyboard-leave and capability loss, no fourth bool in any predicate. ALT IS POINTER-PLUS-FOUR (2026-07-30's "pointer-only" as amended; the count went six→four 2026-08-07): alt+drag pan and alt+wheel pan, plus FOUR Ctrl+Alt chords — the render/propagate ones (R / Shift+R / P / Shift+P — down from five 2026-08-02, when Ctrl+Alt+R absorbed the iteration sweep and Ctrl+Alt+I retired unbound) — and nothing else; THE STAY-PUT UNDO PAIR (Ctrl+Alt+Z / Ctrl+Alt+Shift+Z, 2026-08-06) was ROLLED BACK WHOLE one day later (architect 2026-08-07: labwc grabs those chords on his desktop as his own hotkeys, so the GUI never received them — the feature was dead on arrival; `keep_viewport` deleted through the whole chain, undo.h/undo.cpp byte-identical to pre-feature, the repeating Ctrl chords back to THREE, the `h` mode's consumed family back to two shapes, do not re-propose on these chords; the rollback record in selection-model.md) — the plain/augmented modifier convention died with `Alt+S` (bare `s` and the bare empty-lane double-click both drop the AUGMENTED marker, render-neutral except onto an occupied frame — where the exact-frame collapse to 1.00 is loud, the markers going red — and inside a label definition, whose span its references reproduce; both ruled non-problems 2026-07-28).

### The GitHub Recheck (→ github-recheck.md)
Bare `h` toggles the READ-ONLY HISTORY MODE (2026-08-03..04, the architect-declared last feature; also the icon row's twelfth button, its group grown to four 2026-08-05 with Revert + the walk's two arrows and to FIVE 2026-08-08 with the Cumulative toggle; REBOUND from bare `/` 2026-08-05, `/` dropped entirely and unbound everywhere): the marker lane paints ONLY the delta of the viewed checkpoint — green added / red removed / one double-width red+green flag for a same-frame change, verbatim sidecar tokens, crop-exact kHistory* constants — while `,`/`.` walk the piece's WHOLE eligible history — UNCAPPED AND PREFETCHED since 2026-08-07 (the ruled depth of 20 retired): a background worker (GuiHistoryPrefetch, the eventfd pattern's fourth member) scans at STARTUP and STREAMS members one at a time, so `h` opens instantly, `n/N` grows live while the view stands, an entry before the header computes the cheap half synchronously, staleness (tip/source/projects_repo) re-kicks at entry, a kick while the view stands is DEFERRED to its exit, and a published checkpoint's completion re-warms; bare `,`/`.` KEY-REPEAT and their SHIFT-exact shapes JUMP TO THE WALLS (oldest/newest, one-shot, the step's own mode-edge shape, a wall landing a consumed no-op) since 2026-08-07 — AND ARE THE ICON ROW'S OLDER/NEWER ARROW BUTTONS since 2026-08-05 (shift-admitting with the two-line tooltip since the wall jumps), in the history button's own group, the roster's first RESTING-DISABLED pair (live iff the view stands, never greying at the walk's walls; the row-4 record above and kdenlive-redesign.md carry the face ruling) — matched BY COMMITTED FILE NAME under the repo's `projects/` folder (the repo/projects convention since round 5) against the LOCAL branch's own tree (HEAD, not origin/main — the product is a commit producer and a push-failed checkpoint must stay visible; basename match cannot see across a source rename, accepted), and LOAD-GATED since 2026-08-04: walk membership is the load-in-place gate itself (load_commit_sidecars_strict, the `'` act's own strict whole-set load, ONE predicate), ineligible commits hidden at init with one counted stderr line, `n/N` counting the eligible list. LOCAL GIT, no shell, no network beyond the push, no credentials in the program; the read/write split is TWO ENTRY POINTS — run_git_capture (log/show/ls-tree/rev-parse/status/rev-list/diff-tree/remote-get-url, reads only) and run_git_mutate (add/commit/push, the commit act's fenced writer, its ONLY site); `projects_repo` (a required key since 2026-08-04) names the projects home and the guard checks it against the clone's fetch AND push URLs (which repository, not how fresh) at TWO sites — init as the mode's gate, and again immediately before the push as the mutating boundary's own check, THE PUSH THEN CONSUMING THE URL THAT SECOND ASKING VALIDATED (pinned into the push child's own config through a `-c remote.origin.pushurl=` clear plus the validated value — the key is multi-valued, so both halves are needed — rather than re-resolving the mutable remote name; `url.*.insteadOf` rewriting is the one destination redirect the guard cannot see, recorded at the site). The mode is a viewer: two allowlists + the toggle_dropdown lockout — MENU-SCOPED since 2026-08-08, refusing the SETTINGS menu alone (its items open the settings editor by a DIRECT call, the mode's one pointer bypass), while THE NAVIGATION MENU OPENS IN THE VIEW, all seven rows being chords the two gates answer per item and one greying at the item — consume every mutation; PLAYBACK IS REMOVED FROM THE VIEW WHOLE (architect 2026-08-05: it was slow — the mode's full-song trim forces a full target preview render — and buggy besides, so THE VIEW IS SILENT BY RULING; bare Space left the keyboard allowlist, BOTH scrub entries left the pointer one, the entry owner STOPS a live audition as it opens since Space could no longer, and the Scrub CURSOR goes with the gesture — Arrow over the lower half while the view stands); pan/zoom/Ctrl+S/Ctrl+Q stay live; the VIEW SWITCHES are admitted (bare `t`/`p`/`1`/`2`/`3`, and the view bar + S/T + W/P radios riding those same chords — the delta is line-shaped and view-independent, so switching views is how both halves get reviewed; NO A/B TAB SWITCH happens in here — Ctrl+Tab was CLAIMED as the compare toggle 2026-08-05 and CYCLES THE WALK SOURCE since 2026-08-08 (the reading moved to bare `u`), Ctrl+Shift+Tab — consumed until 2026-08-07 — being that cycle's REVERSE, both IsoLeftTab spellings claimed with it); a PLAIN diff-flag click lands the playhead (store selection untouched) — ON EITHER OF THE FLAG'S TWO SURFACES since 2026-08-05, its lane box and its STEM in the waveform's upper half, one shared body over the painter's own stash — and EVERY OTHER WAVEFORM PRESS — plain or SHIFT-exact, EITHER HALF, STEMS INCLUDED FOR SHIFT — IS THE REGULAR VIEWS' PLACEMENT PRESS that same day (the playback removal's second half: with no scrub, the whole waveform takes one act), landing the playhead at its column AND ARMING THE REGION DRAG through the two shared bodies, clearing the mode focus and its selection as it goes and touching no store selection; the view's spans are VIEW-LOCAL (cleared at the exit and at every step and compare switch), a reading mark rather than listening scratch since `x` is consumed and nothing auditions; it makes Esc's region clear reachable from inside, which is all the bare-Esc reasoning had leaned on the other way; the LIVE MARKER SUPPRESSION covers the waveform too since that day — the phase-reset lead-in RING is gated at phase_reset_overlay_band, the live focus having leaked it into the view, while Selection::phase_overlay_subject deliberately does not mirror the gate, a divergence with nothing observable behind it now that Space is consumed in the view; a DOUBLE-CLICK ON THE TRIM BAR zooms to the viewed checkpoint's diff span (2026-08-05, revised same day from a single click at the architect's read-only-model ruling: the band behaves as in a locked tab — single click a consumed nothing, the existing seed-at-motionless-release double-click machinery carrying the mode's framing, and the band's cursor the plain ARROW, a double-click having no cursor promise anywhere in the product); and since 2026-08-05 the mode CLAIMS four more keyboard families — five since 2026-08-08, bare `u` joining as the reading's toggle — as MODE-LOCAL re-expressions reading the diff-flag list, its own focus or the session's reading bit instead of the live stores (superseding the bare-Tab half of the 2026-08-04 tab ratification, whose premise was that Tab and `c` navigate by live markers): bare Tab / Shift+Tab / IsoLeftTab is the DIFF-FLAG CYCLE (frame-ascending list order, land + recentre, no wrap, first/last from no focus, empty list a no-op), bare Home/End jump ABSOLUTE (0 / last frame, deliberately not the trim bounds — the view reviews the whole piece) and clear the mode focus, bare `c` is the live recipe on the mode's focus (no repair_last_selected / jump_playhead_to_focused_marker), and CTRL+TAB (ctrl-exact, one-shot) CYCLES THE ROW-3 TABS in row order (Ctrl+Shift+Tab the REVERSE cycle since 2026-08-07; ALL FOUR mode edges — entry, step/jump, reading switch, and the CLOSE below its band restore — REPUBLISH THE LANE SYNCHRONOUSLY inside the press since that day, so no edge paints a blank frame; the close publishes the LIVE lane, the mode being already down) through the one switch owner — the keyboard twin of the tab click, superseding the pair's same-day "there is no hotkey" ruling (a two-way toggle until the four-tab row of 2026-08-07); the three bare families keep the live arms' stop-and-clear-region regime, unlike the pointer click, and the face partition did not move (no bare shape is a roster chord, and Ctrl+Tab's two buttons were already hand-answered LIVE — the claim now derives that, retiring the hand entry so the two menu anchors were again the only one — and since 2026-08-08 they are hand-answered SEPARATELY, Settings dead on the lockout and Navigation LIT with its menu); the bottom-left corner (section C, so it renders to the RIGHT of a standing critical chip since 2026-08-09) shows `n/N shortsha` and, ONLY WHEN THE SCALE CHANGED, `Scale: [-]<then> [+]<now>` — the ONLY settings key the mode displays, in the lane's own bracket spelling, which since 2026-08-05 puts the payload DIRECTLY AGAINST the sign with no space (`[+]1.05`, `[+]#`) through ONE owner, history_diff_label. THE DELTA HAS TWO COMPARE READINGS since 2026-08-05 (architect): ITERATIVE, which COMPARES FORWARD, TOWARD NOW — the viewed checkpoint as the old side against THE NEXT-NEWER ITEM, the next eligible walk member or, at the newest index, the frozen live now side ("what happened after this checkpoint", one step at a time; the forward direction SUPERSEDED the same day's walk-parent pairing, which took the delta backwards) — THE DEFAULT AT EVERY ENTRY; and CUMULATIVE, the viewed checkpoint against the frozen live now side whatever the walk position ("how does my session differ"), the reading the mode shipped with. THE NEWER SIDE IS GREEN IN BOTH, so the color grammar is one rule and the lane is reading-agnostic; one mechanism (compute_commit_delta) over two pairs of loader-clean texts, still no git, CACHED PER (index, compare). TWO CONSEQUENCES RECORDED: the two readings COINCIDE AT THE NEWEST INDEX (both live-vs-newest, so a session freshly loaded right after a commit reads BLANK in both — the architect's stated purpose; the same delta computed and cached twice, never aliased, and a reading switch at 1/N moves only the row-4 Cumulative lamp), and EVERY INDEX HAS A FORWARD PARTNER, so the walk-parent era's oldest-member EMPTY-DELTA special case is DELETED whole (the next-newer pairing may still span load-gate-hidden commits between COMMITTED neighbors — the walk's own honesty, the newest index's partner being the live state, which nothing can hide). THE TABS SELECT IT — the Render-button hijack applied to row 3: while the view stands the row stops being the A/B tabs and is TWO TABS SELECTING THE WALK — "Remote" then "Local" (architect 2026-08-08, THE DAY'S THIRD SHAPE and the final one: the row was the compare bit's two-slot radio from 2026-08-05, the (WALK SOURCE, READING) product as FOUR SELF-LABELLED TABS on 2026-08-07 ("Iterative (Remote)" … "Cumulative (Local)"), then TWO LABELLED GROUPS earlier on 2026-08-08 (`[Iterative:] [Remote] [Local]    [Cumulative:] [Remote] [Local]`, the reading said once per group by a non-button text block), and the architect then SPLIT THE AXES — the READING left the row for row 4's Cumulative toggle and TabC/TabD were DELETED WHOLE with the group labels and their painter, so nothing publishes an empty rect in row 3 any more) — the selected face marking the live WALK, a press passing THE CURRENT READING through unchanged, the lock slots gone whole (not drawn, not reserved, no rect published — a padlock is TAB state), the read-only gate deliberately not reaching the switch, and CTRL+TAB AS ITS HOTKEY cycling the two SOURCES in row order (Ctrl+Shift+Tab its mirror, the two coinciding on a two-cycle and both still claimed so the shifted one cannot become the live paired march in here; the cycle's arithmetic left general). THE READING IS A SESSION PREFERENCE ON A BUTTON, not a tab: `AppState::history_cumulative` (off = iterative, on = cumulative) lives OUTSIDE HistoryMode because that struct is reset whole at both mode edges and remembering is the point — off at program start, KEPT ACROSS VISITS (superseding "Iterative is the default at every entry", 2026-08-05..08), forgotten at close, NEVER SERIALIZED (no key, no schema, no sidecar, no fingerprint term — it changes no rendered byte), while the SOURCE still resets to Commit at every entry, the deliberate asymmetry (a per-visit fact vs a preference); its ONE writer is set_history_reading, its readers AppState::history_compare() (the one mapping onto GuiHistoryCompare) and the toggle's lamp; BARE `u` is its key, mode-local, one-shot, claimed by history_mode_owns_key and bare-only. THE LOCAL TAB READS THE SESSION'S OWN UNDO/REDO TIMELINE AS STATES through the SAME machinery (GuiHistoryLocalWalk; architect 2026-08-08, superseding the undo-stack-only walk): with U and R the two stacks' sizes captured at init the walk carries N = U + R + 1 members newest-first — `k < R` the FUTURE state redo_stack[k], `k == R` THE LIVE MEMBER (the frozen now side's own texts, nothing serialized), `k > R` the PAST state undo_stack[U+R−k] — every member's texts serialized through the save writers' own string halves into the same compute_commit_delta — iterative = THAT EVENT'S OWN CHANGE (the commit walk's forward pairing verbatim, member k against k−1, with a SELF-PAIR at k = 0 answering the same blank the commit walk shows at its newest index — computed, not short-circuited), cumulative = that state vs THE LIVE MEMBER always, THE SIDES SWAPPING for a future member (then = live, now = the future state, the newer of the two) so "the newer side is green" stays one exceptionless rule and the live member is blank in both; the ENTRY lands on the LIVE member (local_index = live_index(), the captured redo count — the one place the two walks' entry positions differ, `.` walking into future states when redo entries exist), so a fresh file reads `1/1` (one state against itself, the architect's own words) and set-move-undo reads `2/3`; both stacks are FROZEN while the view stands, EXCEPTIONLESS since the S→T iteration wipe's deletion (2026-08-07, iteration mode target-legal — the walk's one admitted producer died with it); the push-serial tripwire that briefly verified it was DELETED same day at the architect's producer-less-mechanism ruling (the premise stands on its derivation alone; do not re-propose), each source keeps its OWN walk position, the corner drops the sha on Local (a timeline state has no name), `'` IS LIVE THERE TOO since 2026-08-08 — forked on the source, the editor taking a member NUMBER instead of a commit spelling (load_history_local_entry_in_place, its own line below) — REVERT STAYS LIVE, Save-and-Commit and its head-delta grey are untouched (commit-side by definition), the LOCAL walk is NEVER EMPTY (`0/0` there means an uninitialized walk, which a live tab cannot reach; the commit side's pre-first-member window is the one producer left), and THE REDO STACK'S EXCLUSION IS SUPERSEDED (2026-08-08: one index origin, the newest state, and the live state a member like any other). A switch is a MODE EDGE with the `,`/`.` step's shape through ONE owner for all FIVE routes — the two tabs, both Ctrl+Tab directions and bare `u` (set_history_reading over the pair, idempotent, which is where the radio rule comes from — set_history_compare's successor; since 2026-08-08 it WRITES THE PAIR ACROSS TWO HOMES, the source on HistoryMode and the reading on AppState, which is the reading's session scope and not a second owner), every displayed-delta reader goes through the ONE accessor (HistoryMode::displayed_delta, which TAKES the reading in — a nested struct cannot reach its enclosing object), and the flag-cache fingerprint carries source, reading, both indices and the commit count. THE ONE READER THAT NAMES CUMULATIVE EXPLICITLY is head_delta_empty, the Save-and-Commit grey — and since 2026-08-08 the session bit's ONE DELIBERATE NON-READER, a `u` press moving no face here: the act commits the LIVE state, so "nothing to checkpoint" is live-vs-newest whatever the lane shows — under the forward pairing that name picks the same delta the bit would (index 0 coincides), kept explicit because the question is live-vs-newest by definition. `'` in the mode prefills the load editor with THE VIEWED MEMBER IN THE ACTIVE WALK'S OWN VOCABULARY (the fork lives at the opener and at load_editor_commit): ON THE COMMIT TABS the viewed SHA (any resolvable spelling accepted), loading that commit's three sidecars in place, 1:1 in memory through the STRICT frozen loaders via an RAII scratch dir — partial commit and legacy format refuse; ON THE LOCAL TAB since 2026-08-08 (architect, superseding his own 2026-08-07 "the Local tab consumes it") the viewed member's displayed NUMBER, digits-only in [1, N] and anything else a red flash with one stderr line, applying THAT STATE's two marker columns and engine block — all an undo entry carries, so no tab bands, no trim, no prefs — taken as TYPED snapshots (GuiHistoryLocalWalk::member_state, copied before the first write so the identity load is ordinary) rather than through the member's diff texts; EITHER WAY one cross-file undo entry ON TOP of the current state rather than a rollback (Ctrl+Z returns; push_undo_both clearing the redo stack is correct — the load forks a timeline whose walk the close already ended), no renders wipe, no disk write, and the mode closes with the act. THE VIEW REVERTS DIFFERENCES INTO THE LIVE STATE (architect 2026-08-05, the arc's last feature): a MODE-LOCAL MULTI-SELECTION over the diff flags — plain click = focus and clear the set, SHIFT = the contiguous range from the focus, CTRL = toggle one, IN THE MARKER LANE ALONE (the two modified clicks lost the STEM surface 2026-08-06 at the architect's SYMMETRY RULING — the history view and the regular views answer a waveform click identically, a modifier over the waveform naming a GESTURE in both (ctrl the strip drag, shift the placement press plus its former) while SELECTION is lane vocabulary; plain clicks keep the stem hit in both), the set painted in the focus's own brightened faces and clearing wherever the focus clears through the one clearer (clear_history_mode_focus) — and CTRL+H, THE REVERT ACT: each selected flag's INVERSE applied to the live store of the active column (added = delete at that frame, removed/changed = put the THEN side back, insert-or-replace), FULLY MANUAL AND ALWAYS FORCING by ruling (no coherence check anywhere; an incoherent subset is the user's to ask for), the then value travelling as the sidecar's own LINE through the frozen parser so labels/pass/scale restore with no second grammar, ONE undo entry, dirty set, live selection cleared, playback stopped, AND THEN THE VIEW CLOSES (the act rewrites the state the frozen now side was measured against — the load-in-place's argument). Its chord is ADMITTED by the allowlist rather than claimed by the mode's vocabulary, so it dispatches BELOW the read-only gate and a locked tab refuses it like `'` (unlike Save-and-Commit, read-only-legal since 2026-08-07's band ruling and on Ctrl+S since 2026-08-08, saving and publishing being authoring-free); the admission is CONDITIONAL on a subject standing (selection or focus), the head-delta precedent — one decision refusing the key and greying the Revert button, which also rests disabled outside the view. The mode's admitted mutator CHORDS are THREE: `'`, Ctrl+S, Ctrl+H (FOUR mutating routes since 2026-08-08, `'` having forked into the commit load and the local one). CTRL+S IN THE MODE IS SAVE AND COMMIT (2026-08-04 on Ctrl+Alt+R, REHOMED onto the save chord 2026-08-08 — the act is save-first by definition, so the SAVE button wears "Save and Commit"/Icon::VcsCommit there, the Render hijack is gone whole, and the view has NO plain-save hotkey — a settings-only drift is saved by leaving the view first, architect-accepted; the iteration-mode bit-selects-the-command precedent): the COMMIT-TITLE EDITOR (2026-08-07, replacing the fourth [Y]es prompt WHOLE — a fifth modal text editor on the bottom strip, prefix "Commit: ", PREFILLED with `Update <id>` and open-selected so a bare Enter is the old `y`; the entered UTF-8 title IS the commit message and the attribution walk's identity, blank/whitespace-only a red-flash refusal, Esc an abandon; "derived, not chosen" superseded), then on Enter THE REAL Ctrl+S FIRST through its one owner GuiSaveOps::save (same writes beside the source, same dirty clear — so the checkpoint is what you see, saved and published, and the in-repo workflow's dirty-flag incoherence dies; a FAILED save refuses the whole act above the one call that writes or spawns git, so no checkpoint-side write or git subprocess happens by construction — narrower than "no repo write": the save's own three writes stay sequential rather than transactional, so a save failing partway through can leave earlier atomic renames on disk in the coincident projects/<id>/ workflow exactly as any ordinary Ctrl+S failure can — and the coincident double write of byte-identical content through two atomic renames is accepted rather than deduped), then the three sidecars rebuilt fresh and written into projects/<id>/, a pathspec-scoped `git commit` under the entered title + push, and EVERY VERDICT A REPOSITORY OBSERVATION, never a transport result — the commit verdict IDENTIFIES THE CHECKPOINT BY CONTENT (a bounded `before..refs/heads/<captured branch>` walk for the commit whose message is the act's own title, whose changed paths are a non-empty subset of the three, and whose tree carries the act's exact bytes at all three), so a checkpoint that landed under a hung post-commit hook is found and pushed rather than reported failed and DIFFERENT CONTENT IS NEVER BLESSED — the identity is content, not authorship, and content-equivalent commits are deliberately interchangeable (an act-unique trailer was considered and declined; the wrapper is retired for checkpoints); the attributed commit is then pushed BY SHA on the branch read once at act start (no force; a commit on top of it stays the user's own to publish) — and THE ACT READS THE SYMBOLIC HEAD EXACTLY ONCE FOR A VALUE, to learn that branch name, EVERY source-side observation after it BOUND to that branch — all but one by naming `refs/heads/<captured>`, and the pre-flight `git status` (which takes no ref, so it cannot be spelled) by VERIFYING its `##` header still names the captured branch and FAILING THE ACT CLOSED on a mismatch (2026-08-04: while the ref reads still followed HEAD, a mid-act checkout could have the walk attribute a commit made on another branch and the push publish that branch's ancestry onto the captured one; and while the status went unchecked, a checkout onto a branch already carrying the new bytes made the clean arm answer NothingToCommit about a captured branch that lacked them — reproduced live, closed same day; the commit itself lands wherever git is checked out and needs no pinning, since the captured range then finds nothing and the act fails having published nothing), and the push verdict is the remote-tracking ref OBSERVABLY CARRYING it — `rev-list --count`, whose success always prints a number, so an unanswerable containment is unconfirmed rather than a yes, with an empty `rev-parse` disambiguated by a POSITIVE-WITNESS probe of the captured branch's own ref so an ABSENT ref and an unreadable repository stop sharing an answer (the witness adjudicates only a target read that RAN — a could-not-exec probe is unavailable on the spot, since a witness proves the repository answers now and never that an earlier invocation executed); and the pre-flight's clean arm forks — already published = NothingToCommit, committed-but-unpushed (or unobservable) = push it (the retry route, whose fallback push is a no-op only at tip-equality and an ordinary non-fast-forward refusal against a descendant tip, and which a deleted local branch does not refuse at all — a sha refspec resolves no local branch). THE ACT CLOSES THE VIEW WHEN ITS SAVE LANDS (2026-08-07, superseding 2026-08-05's "closes iff the checkpoint ends up in the repository", which superseded "the mode stays open"): everything past the save runs ON A BACKGROUND WORKER (GuiHistoryCommitWorker — capture by value on the main thread, close, dispatch; the window never waits on the remote again), SINGLE IN FLIGHT (the chord refuses and the button greys off one AppState bit, bare `h` will not open a view over a repository mid-mutation, quit JOINS the act, and — 2026-08-08 — EVERY SAVE IS REFUSED at the one owner GuiSaveOps::save while the bit stands, the Save button reading "Committing..." disabled in every view: the worker writes the three sidecars into projects/<id>/ off the main thread, the very paths a coincident-workflow Ctrl+S writes through the same fixed temp name, so the lockout is what makes the act's writes race-free; the close prompt's [S]ave during the window lands on its existing Save-failed/[R]etry rung, accepted), Committed and NothingToCommit end silently on their own stderr lines, and WriteFailed / CommitFailed / CommittedNotPushed WRITE THE BOTTOM ROW'S PERMANENT CRITICAL SLOT (architect 2026-08-09, SUPERSEDING the acknowledge modal they raised from 2026-08-07 and deleting the whole park machinery with it — a critical failure must be impossible to MISS and impossible to HIJACK WITH, and a modal is both): AppState::critical_error_message, painted FIRST on the row as a chip in the product's one invalid red (kMarkerFlagFillRed/EdgeRed, the editors' flash anatomy called not copied), everything else rendering to its right, PAINT-ONLY (no key, no chord, no click, no rect) and PERMANENT, cleared ONLY by a later checkpoint ending Committed/NothingToCommit or by program close and surviving every view switch and both load-in-places; three short texts ("Checkpoint failed: nothing was committed" / "...files written but not committed" / "Checkpoint committed; push failed"), the detail staying on stderr unchanged, no retry key and nothing to acknowledge (the retry IS the act); NOTHING ASYNCHRONOUS RAISES A MODAL ANY MORE, so the parked notice, its opener's three park classes, the dropdown close, the release-owned scrap clears and the tick pump are all DELETED as producer-less, any_pointer_gesture_active is back to five INPUT-route consumers, and ERROR_NOTICE keeps its two SYNCHRONOUS callers (the target-view entry gate, the iteration cap refusal); the failed save prelude and the guards still leave the view exactly as it was, every refusal's shape. AND THE ACT GREYS OUT WITH NOTHING TO CHECKPOINT (same day): the HEAD DELTA — index 0's delta against the frozen now side, empty iff the six entry vectors are empty and `scale` is unchanged — is measured once member 0 exists (2026-08-07, the streaming walk: it RESTS TRUE — greyed — in the pre-arrival window, one measurement site, then static for the visit), and its bit makes history_mode_key_blocked's Ctrl+S admission conditional, so ONE decision both refuses the chord and greys the Save button, which keeps its "Save and Commit" label; the delta's vocabulary is authoring only, so a settings-only drift greys too (the "Ctrl+S remains the disk save" escape hatch died with the 2026-08-08 rehoming). AND THE VIEW IS A VIEWER, DOWN TO THE NAVIGATION STATE (architect 2026-08-05, three parts): (1) the ONE entry owner SNAPSHOTS the live viewport/zoom/playhead trio and the ONE exit owner RESTORES it through the family's clamp chokepoints (move_playhead_to then apply_zoom_to_start, so an offscreen playhead's scroll is overwritten by the explicit start; read before the whole-struct reset and applied after it, so the rebuild sees the mode down), ONE restore site for every closer with NO exemption (five reachable in-view since the LOCAL load-in-place joined, 2026-08-08; the caller inventory is close_history_mode's own) — the render-entry and commit load-in-places apply the loaded file's own band moments later and simply overwrite it, while the LOCAL load applies NO band (a timeline state carries none), so the restored band stands, which is the correct outcome; the active VIEWS are deliberately not restored (the user keeps what he switched to) and the A/B tab cannot move in here at all, while the trio being ACTIVE-DOMAIN state makes the snapshot carry its AUDIO VIEW too — bit-exact when that view has not moved, warp-map-translated in the flip's own direction when it has, the zoom level riding untranslated as `t` carries it; (2) THE ENTRY IS THE ONLY EDGE THAT FRAMES (architect 2026-08-08, SUPERSEDING his own 2026-08-05 "the three edges land at full zoom out": each `,`/`.` step and each compare switch called the framer and no longer do — THE VIEWPORT IS THE USER'S FOR THE WHOLE VISIT and UNIFIED ACROSS BOTH WALKS AND BOTH READINGS, so a pan and a zoom made once are read through every step, both tabs and both readings, and the only reset left is the exit's parked-band restore; a fresh visit still opens at FULL ZOOM OUT, the span framer's own whole-song arm, `[0, total]` with no margin, degenerating to the per-file effective ceiling at start 0, and frame_history_view_whole_song's callers are now the entry owner plus the framing act's empty-delta arm), and THE FRAMING SURVIVES WHOLE AS THE TRIM BAR'S DOUBLE-CLICK, the standing view's ONE framing gesture — the band's own machinery carrying the mode's command, over frame_span_into_view (deterministic zoom-to-span with margin, NOT the undo restore's scroll-else-frame wrapper; empty delta = the whole-song arm, a one-frame span saturating at kMinZoom) — both viewport only, no playhead, and the user's pans un-fought; the span is GuiHistoryCommitDelta::frame_span, min/max over ALL SIX vectors in source frames read from delta_at (never the tick-cadence flags list), the WHOLE delta so a `p` switch cannot move the viewport, converted through source_frame_to_active_domain (frame_to_paint_sample's own formula); (3) the TRIM BAR DISPLAYS THAT SPAN (empty delta = the full window) as one DISPLAY-ONLY substitution above paint_trim's displayed_trim_ms mapping — app.trim untouched, every trim gesture already consumed — with the recorded incoherence that PLAYBACK STILL USES THE REAL TRIM WINDOW. WALK MEMBERSHIP IS LOAD-GATED, so everything visible is loadable — the diff stays line-based but its legacy leniency arms (drop-unparseable, missing-file-as-empty-side, the per-commit Ambiguous display) are deleted dead code; the LOAD-IN-PLACE stays parse-gated, its refusal arms live for pasted SHAs outside the walk.

### Validation Topology (architect-blessed 2026-07-17)
Every guard classifies under docs/engineering/validation_topology.md — five classes, one owner each; a duplicate predicate needs an (a)/(b)/(c) justification; an error arm exists iff a producer exists. A new guard that fits no class is a design smell to resolve, not a row to force.

### Type Names
Marker and related GUI types carry the `Gui` prefix (`GuiWarpMarker`, `GuiPhaseResetMarker`, `GuiAudio`, `GuiPlayback`, etc.). New marker-side types follow the same convention.

### Warp / Phase Reset Naming Symmetry (→ conventions.md)
Warp and phase reset are co-equal axes; new pipeline surfaces ship both columns or record the asymmetry in a comment at the site. "Phase reset" is one concept token, never shortened to "reset" in names; warp is never the unmarked default. The recorded-asymmetry inventory lives in conventions.md.

### Rounding
Use `std::nearbyint` for banker's rounding wherever fractional values convert to integer-domain values (playhead placement, pixel mapping). No epsilon nudge. For authored positions specifically, `snap_authored_frame` (`app_state.h`) is the single conversion chokepoint; no other site may round or cast an authored position.

### Comments
Comments describe current behavior and rationale only. No dead-process citations (Brief/chunk/Stage/spec tags or "per the brief" prose).

### Grep Before Asserting an Invariant
Before claiming "every site that does X" or "no other caller does Y" inside a brief or a code change, grep it. The repo is small enough that this is cheap. AND AT RETELLS, NOT ONLY AT CREATION (architect + codex, 2026-07-24): an inventory retell RE-GREPS membership, never edits the inherited list in place — five consecutive retell rounds preserved a pre-existing omission in the kick_waveform_sync inventories because each round edited the copied membership instead of re-deriving it. Structural fix preferred over discipline: an exhaustive enumeration lives at exactly ONE authoritative site per concept (e.g. viewport.h's kick_waveform_sync declaration), and every other site states only its own class plus a pointer.
