# Warptempo GUI

Custom C++23 in-process PGHI phase-vocoder GUI (Arch / labwc / Wayland / JACK) for time-warping audio toward historically-informed tempos. Single developer/user (the architect); one authoring laptop. PROJECT STATUS: FEATURE COMPLETE (architect, 2026-07-25). The authoritative architecture rulings live in `docs/engineering/architecture/` — see the docs map below. This file carries the build/process rules, short per-topic digests, and pointers; the topic files are the authoritative detail. When a ruling lands, update the topic file (committed) and this file's digest ONLY if the headline changes — history, quotes and successions belong in the topic files and in git, never here. (This file was deliberately cut down from a ~190KB digest-of-everything on 2026-08-16; the full old text is in git history.)

## Build Commands

```bash
# Configure (from project root, first time or after CMakeLists changes)
cmake -B build -S . -DWARPTEMPO_BUILD_CLI=ON

# Build (Release is the project default; -O3 -march=native -ffp-contract=off is always on)
cmake --build build -j$(nproc)

# Debug build into a separate dir; never reconfigure `build/` itself with -O0
cmake -B build-debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug -j$(nproc)
```

```bash
# Android (the tablet): cross-build + APK, statically against android/prebuilt
bash android/app/build_apk.sh        # then: adb install -r android/app/build-android/warptempo.apk
```

`build/warptempo_gui` is the GUI binary and the default build's only product. No library archives: four per-directory source-list variables (`WARPTEMPO_AUDIO_IO_SOURCES`, `WARPTEMPO_PARSER_SOURCES`, `WARPTEMPO_ENGINE_SOURCES`, `WARPTEMPO_PREPOST_SOURCES`) each binary compiles directly — object-code duplication accepted deliberately. One opt-in binary, default OFF: `warptempo_cli` (`-DWARPTEMPO_BUILD_CLI=ON`, the headless insurance render — exactly the terminal Ctrl+Alt+R, byte-identical to the GUI by shared stages; a headless host builds it with `-DWARPTEMPO_BUILD_GUI=OFF`). `tools/` holds standalone one-shot utilities (own CMakeLists, no link path from product targets). `-ffp-contract=off` is load-bearing: renders are byte-reproducible across rebuilds and `-march` choices under the same toolchain (scope in `docs/engineering/perf_campaign_2026_07.md` §15).

THERE IS ONE BUILD HOST, the authoring laptop, and ONE GLASS HOST, the Android tablet (Galaxy Tab S10 FE; the Pi rig is returned — "from the rig" in a source comment is the PROVENANCE of a ruling taken on that panel). The laptop builds BOTH: the Wayland product and, since 2026-08-26, the Android APK over the same portable GUI (`docs/engineering/architecture/platform-seam.md`). A laptop build proves compilation and the pointer/keyboard path only; the tablet is adb-drivable (install, launch, screencap, single-touch injection, logcat), so a coder CAN verify paint and one-finger touch on the device — multi-touch and feel stay the architect's glass pass. Linux binaries are not portable (`-march=native`); the APK is.

## Dependencies

pkg-config: `fftw3` (+`fftw3_threads`), `wayland-client`, `wayland-cursor`, `wayland-protocols`, `xkbcommon`, `cairo`, `cairo-ft`, `harfbuzz`, `jack`; plus `wayland-scanner` at build time. All audio container I/O is in-tree and WAV-only, so the non-GUI sources need only `fftw3`. C++23 (`std::expected` in the parser), `-Wall -Wextra`. Do not introduce Pango, SDL, GTK, GLFW, or wlroots-specific protocols — the GUI is libwayland-client only (`wayland-cursor` the canonical exception). Generated protocol stubs: xdg-shell, xdg-decoration (REQUIRED at startup), pointer-constraints-unstable-v1 + relative-pointer-unstable-v1 (the strip-drag pointer capture — the ONLY optional protocols; absence degrades to clamped absolute motion). The required-globals set is five, `wl_data_device_manager` (system clipboard, core protocol) included.

Text: proportional text measures AND paints through the ONE shaping chokepoint `src/gui/text_shape.{h,cpp}` (contract in its header) on the one sans face (fontconfig "sans", 12pt × gui_scale); the row-8 CLOCK is the product's monospace surface (fontconfig "monospace", normal weight), joined 2026-08-28 by the render player's modal clock — one face, two cells. TEXT IS UTF-8 IN FREE TEXT AND ASCII IN GRAMMARS: editor buffers and free-text settings values take UTF-8 verbatim and round-trip byte-identically; every structural grammar (marker labels, numeric spellings, settings keys/vocabularies, canonical-line payload, the MARKER MEASURE suffix ` //<measure>` on both sidecars — `src/parser/marker_measure.h`, its one-day free-UTF-8 comment predecessor retired 2026-08-20) stays ASCII with one canonical spelling per value. Editors index by byte but step by CODEPOINT; no font fallback, no bidi, graphemes out of scope; `text_editor::replace_selection` is the one incoming filter. Contract at the head of text_editor.h; full ruling in conventions.md.

## Running

```bash
./build/warptempo_gui                      # opens last_project from the device config, else the first valid project folder
./build/warptempo_gui <project source.wav>  # the wav must be a project's SOURCE directly under projects_path (else a refusal, exit 1)
```

labwc on Arch; 1920x1080@60 laptop. No portability shims, no X11 fallback.

## Testing / Verification / Roles

There is NO automated test suite and that is a CONSTRAINT, not a preference: Wayland exposes no input-injection path, so a GUI harness is not buildable here. Do not propose automated tests, regression harnesses, or commercial-tool comparisons. The CLI shares the parser/engine/prepost/audio-io stages with the GUI byte-identically; the architect runs cmp-null and audio verification against it, and owns labwc verification.

Roles: the CODER (implementation subagent) is the ONLY writer of source code — it edits files only, runs no MUTATING git (read-only status/diff/log/show fine and encouraged), never stages or commits, never launches labwc or drives the GUI. Build/compile checks are not part of a brief unless the architect asks. The external reviewer (CODEX) is REPORT-ONLY: finds and describes defects and residue, changes no source and no docs, runs read-only git freely — state this in every codex brief. The PLANNER turns findings into coder briefs, may update this file (committed), and commits via `warptempo_git` (a local wrapper; invocation in gitignored tmp/dispatch.md) — commit as work completes, never bundle waiting for the architect. `projects/` is ARCHITECT-ONLY: neither planner nor coder touches it, and the wrapper excludes it from staging (checkpoint commits are the app's own Save-and-Commit act).

THE CODEX ROUTINE (architect-codified 2026-07-22; all tmp/ paths gitignored): every round is a FRESH codex session. "Initiate a codex review" means: rewrite tmp/codex_brief.md fully SELF-CONTAINED (project line, discipline block, arc + commit hashes, prior-round recap; the brief instructs codex to write tmp/codex_review.md then touch tmp/codex_done), delete any stale review, touch tmp/codex_start, arm a Monitor on tmp/codex_done, and on the event delete both triggers, read the review, and implement findings JUDICIOUSLY — stopping to present anything that needs an architect ruling (freeze touches, design forks, contested accepted costs). Codex is SKIPPED for simple mechanical changes (architect 2026-07-27); the routine stays for substantive arcs. Coder briefs point at topic files instead of restating their content. THE METER (new since the routine was written; observed 2026-08-26 and 2026-08-28): codex has a FIVE-HOUR usage meter beside the weekly one, and its failure has ONE SHAPE — THREE HEAVY ROUNDS, THEN ONE CUTOFF: the trigger is consumed (`codex_start` disappears) and no review ever lands; the Monitor's 60-minute watch (the tool's maximum; >30 minutes with nothing is already the verdict) expires, and THE WAIT SUCCEEDS — re-touch `codex_start` once the meter has reset and the same brief lands normally (round 17: died 07:55, re-armed 08:57, landed 09:20). Never wait for a review to land work — build and commit each item as it completes, and run the rounds in sequence as the window allows.

## Docs Map (read per task, not wholesale)

All in `docs/engineering/architecture/` unless noted. Each file holds the full hardened ruling text — read the one(s) your task touches before editing.

| Task touches | Read |
|---|---|
| Authored positions/frames, tempo cents, value brackets, sidecar grammar (the marker measure suffix included) | `data-model.md` |
| Settings keys/schema/editor, render-entry sidecars, load in place (`'`), the DEVICE CONFIG (its five keys, `sync_path` included), the retired playback_speed and audio_player | `settings.md` |
| Zoom, viewport, strip lanes, the overview strip, the grab-pan / the nav drag's ctrl zoom phase and the stepped wheel pan, pointer capture, trim-bar span-framing double-click, playback scanner | `zoom-viewport-strip.md` |
| The kdenlive redesign (row-by-row protocol, per-row records, the hard-coded-color/sans/text-shape/gui_scale standing rulings, the menu row + quit button) | `kdenlive-redesign.md` |
| Resolver normalization, red flags, marker walls/stores, always-set trim + commit rules, undo coalescing, adversarial load taxonomy, tripwires | `normalization-and-boundaries.md` |
| Pointer reach/modifiers, trim bar router, hit geometry, displayed paint basis, damage ownership | `pointer-hit-testing.md` |
| Selection model, marker click land, the marker drag, undo/redo restore visuals, shift/ctrl clicks, the never-parked rule + coincidence auto-select, the retired span form | `selection-model.md` |
| Flag shapes, lane text occlusion, stems, focus model, flag/BPM/settings editors, the MARKER MEASURE box and its editor, the score-video experiment's sunset record, double-click, key repeat, Space | `marker-ui.md` |
| The trim region overlay (the region IS the trim), the sweep and its retired minimum-width floor, scrub area, the Esc bindings, `[`/`Shift+[` | `region-scrub-esc.md` |
| The GitHub recheck (`h` history mode, the diff lane, `,`/`.` walk, `projects_repo`, load-in-place-from-a-commit) | `github-recheck.md` |
| The folder overlay (the ONE list panel in the on-screen keyboard's band and its TWO contents: the player mode over `render/` and `tmp/` with the modal transport row, the play-scrub, the load-by-button road and the retired `audio_player`; and the Open project picker), the click-activates ruling and the modal alignment rule | `render-player.md` |
| Touch (wl_touch binding, the one-finger pointer translation, the disambiguation window, the phone-model pan, the region hold, the two-finger pan/zoom, the hard-end contract, the out-of-scope list) | `touch.md` |
| The platform seam (`GuiPlatform` per backend over the ONE portable `GuiInputCore`; the loop contract; pixel containment's owner; the playback split; the font owner; `gui_main`; the Android stubs, the Java sliver, the APK build, the freeze posture, the tablet's facts) | `platform-seam.md` |
| Home-view binding, the bare Up/Down tempo cent step (singleton + group), propagate paste | `tempo-and-home-view.md` |
| Parser phase-reset compilation, engine geometry, trim prepost stage, render dispatch/cancel/reuse | `render-pipeline.md` |
| Modality (all six editor kinds, the field-less picker), the keyup retirement record + the third clause (content acts at the press when its identity is certain; the certainty audit), strict modifier validation, the alt vocabulary, bare-`e` mouse key, type names, warp/phase-reset naming symmetry | `conventions.md` |
| Messages: row 8's state cell (state), the notification cards (events), what is notified and what is silent | `messaging.md` |
| Any color, ground recolors, disabled/trim/line color classes | `src/gui/render.h` palette block (code comments are authoritative) |
| Any guard/validator (add/move/remove) | `docs/engineering/validation_topology.md` |
| Engine performance (closed campaign; retired candidates need new data) | `docs/engineering/perf_campaign_2026_07.md` |
| The retired waveform antialiasing (technique record + reinstatement seed) | `docs/engineering/waveform_antialiasing_retired.md` |
| User-facing behavior reference | `docs/HELP.md` |

Trim spans several files by nature: store/commit rules in `normalization-and-boundaries.md`, endcaps/router/geometry in `pointer-hit-testing.md` and `marker-ui.md`, the prepost render window in `render-pipeline.md`, the waveform overlay (which IS the trim, derived) and its show/hide in `region-scrub-esc.md`.

## Architecture Rules (MUST follow — digests; the topic files are authoritative)

### Freeze Status (architect-declared 2026-07-12)
`src/engine/`, `src/parser/`, `src/audio_io/`, and `src/prepost/` are under PERMANENT HARD FREEZE: surgical changes only, each requiring explicit architect approval to reopen. `src/cli/cli_main.cpp` is freeze-adjacent (same discipline). Only GUI work remains. EVERY GRANTED FROZEN TOUCH RECORDS ITS GRANT at the site and in the commit message ("(architect approval YYYY-MM-DD)"). A retired perf candidate needs new measured data to re-propose (perf_campaign_2026_07.md).

### Authored Domain (→ data-model.md)
Authored positions (marker times, trim bounds) are whole source frames in `int64_t` everywhere at rest; `snap_authored_frame` (app_state.h) is the ONLY double→authored conversion route. Authored tempo is 100-based integer CENTS in [25, 400]; every other value is a bracketed double via `value_format.h`. One serialization domain (`frame_format.h`): canonical integer spelling only — anything else is adversarial load-fatal. Walls are exact integer compares on the shared inclusive [0, total−1] domain. Sources: WAV-only, 16/24-bit PCM, ≥44100 Hz, stereo-only.

### Pixel Anchoring, Viewport, Zoom (→ zoom-viewport-strip.md, kdenlive-redesign.md)
Position gestures anchor to the painted column grid and commit through `authored_frame_at_column` → `snap_authored_frame`; walls win over the grid everywhere. Waveform width floors to a multiple of 16px; `clamp_viewport_start` is the one viewport chokepoint (owns the zoom-level clamp too). Zoom is continuous in [1.0, 17.0]; `c` = working zoom 2.0; `0` = full zoom out, STAMPING the level it leaves into the tab's `zoom_recall_level` (session-only, unpersisted), and at the ceiling the `c` command AT THAT STAMPED LEVEL — plain `c` when nothing is stamped, and plain `c` too when the stamp can no longer move the zoom (a ceiling that fell under it). The TOP STRIP IS SEVEN LANES (menu row / tab row / icon row / overview strip / trim bar / ruler / marker lane) plus ONE unified bottom row ON THE WINDOW'S FOOT, carrying the STATE CELL right of its clock (messaging.md; a STATUS BAR was a tenth lane under it for the one day of 2026-08-29 and folded back in that evening); the vertical stack has ONE owner in main.cpp (waveform max height `kWaveformMaxHeightPx`, waveform midpoint = window midpoint, both flexible gaps floor at 0; the kdenlive-redesign.md commit-B section is authoritative for the stack). The NAVIGATION SURFACE (whole waveform + ruler + empty marker-lane stretches; the whole waveform + lanes in the `h` view; one owner `point_on_nav_surface`) takes ONE vocabulary: PLAIN DRAG = the grab-pan (capture at the 8px crossing — 8 AUTHORED px, since 2026-08-27 scaled by gui_scale like every press-road length: the drag gate, the double-click slack and the touch slop are ONE number resolved through `scaled_px` and pushed into the core at both gui_scale application points (`GuiInputCore::set_touch_slop_px`); durations never scale — cursor hidden, notional x wraps edge-to-edge across the waveform), MOTIONLESS PLAIN CLICK = playhead placement deferred to the RELEASE, SHIFT+DRAG = the trim sweep (a direct trim write per motion event; the `h` view's is a playhead sweep alone), CTRL+DRAG = the same drag's ZOOM mode — ctrl is LIVE mid-drag; zoom is HORIZONTAL with RIGHT = IN (rate `kNavZoomPxPerLevel`, the product's one drag-zoom rate — an AUTHORED 200 px per level resolved through `nav_zoom_px_per_level()` = the constant × `gui_scale_factor()` since 2026-08-27, a length like the thresholds, so the tablet zooms slower per millimetre; the pinch is a dimensionless distance ratio and gains no scale term), pivot = a song frame seated at ctrl-down with edge clamp-and-rebind, the anchor stem painted and the cursor restored onto it. PLAIN WHEEL = the waveform magnification step (2026-08-27, the frame's detent count clamped onto the ladder in one call); ALT+WHEEL = the stepped pan; CTRL+WHEEL = one zoom level per detent; every other modified wheel is a swallowed no-op; the right and middle buttons are unbound (both decoded at the platform boundary so the translation is total, both bound nowhere). The OVERVIEW STRIP (lane 3, 24px content) carries whole-song bars + viewport box + playhead tick; its vocabulary is drag-the-outline (pan), click-teleport (outside the box, AT THE PRESS — and the SAME press then arms that pan, so a pointer or finger that keeps moving keeps panning; the grab offset is measured after the teleport, never assumed zero), drag-the-bounds (10px grab bands shared with the trim endcaps and the waveform trim overlay's bounds, `kTrimEndcapGrabPx` — a bound is dragged by its own band and nowhere else); the plain lane off the endcaps wears TrimResize throughout; the THIN LANES (overview + trim bar) ignore second fingers entirely. THE PLATE RENDERER is aliased-only ARGB32, per-column raw min/max on the authoring lattice, 13-rung peaks pyramid, one synchronous full render for every user-driven pan/zoom frame; THE VISUAL MAGNIFICATION IS ONE MULTIPLY AT THE TIP MAPPING (`render_waveform`'s `magnification_level` parameter resolved through the ONE GAIN OWNER `waveform_magnification_gain` — `2^level`, the doubling ladder capped at ×16 — the product clamped to [-1,1] so a forte clips flat and its troughs still dip), the LEVEL a member of the plate fingerprint and of `OverviewBarCache`'s reuse key — ONE GAIN ON EVERY WAVEFORM PICTURE, the overview strip included, the `h` view by construction. EVERY PAN SUPPRESSES FOLLOW for the playback session (`scroll_viewport` funnel). All view switches take the synchronous rebuild; bare 1/2/3 are absolute view selectors composing the `t`/`p` handlers.

### Render Pipeline (→ render-pipeline.md)
The parser is the sole authored→engine compiler (phase resets emit `S − N/2`, no render-time offset anywhere); geometry in `engine_geometry.h`. Trim is a prepost stage: full maps → `plan_trim` (refusal = render untrimmed, never a failed render) → trim-ignorant engine → `post_trim` → unconditional limiter → encode. No subprocesses in the render path. Ctrl+Alt+R is TWO commands selected by the iteration-mode bit (single render / iteration sweep); Ctrl+Alt+Shift+R archival is a consumed no-op in iteration mode. THE TWO OUTPUT FOLDERS (architect 2026-08-27, R1, landed 2026-08-28): the DELIVERABLE `<title>.wav` + `.fingerprint` lands in `<project>/render/` (singular, "because the command is called render"; created at dispatch; the path's ONE owner is the parser's `render_output_directory`, shared by the CLI under a granted frozen touch 2026-08-28, so both products write the identical path) AND NOTHING ELSE LIVES THERE (architect 2026-08-29 — "player should only list a file if it matches the current title, and delete the rest also"): `prune_render_folder` (renders_dir.h, one owner) deletes every regular `.wav`/`.fingerprint` whose stem is not the current title's, at TWO TRIGGERS — the render player's listing and the deliverable's publish (the single archival render's GUI-thread completion, Success and the deliverable arm alone; batch cells land in `tmp/` and are never pruned; the CLI gets no prune, the recorded asymmetry) — so what the Synchronize mirror keeps on the stick and what the prune keeps on disk are ONE definition, and the DISPOSABLE batch cells (iteration / bpm sweeps, `N_tag/NN.wav`, what `'` loads, what the render player's `tmp` branch lists and the trash road deletes) live in `<project>/tmp/` (lowercase, "to make it clear that that's temporary"; `kBatchFolderName` / `project_batch_root` in renders_dir.h, the GUI's alone). No migration — the architect moves his folders by hand (`mv renders tmp`, the deliverable into `render/`); a wav still in the root is the project model's legacy-layout refusal. A render dispatch kills the running render unconditionally; killed sessions never publish Success; target-view previews may reuse identical completed renders (fingerprint v20 — `kFingerprintVersion`, render_cache.cpp; phase-reset component trim-aware). THE BPM SWEEP KEEPS THE MAP'S SHAPE (architect 2026-08-26): each cell rewrites the span as before and rescales every ENABLED owning marker OUTSIDE it by the owner's own change, banker's-rounded onto the cents grid (`bpm_cell_warp_markers`, the cell's one owner — the sweep per cell, the editor commit at the bracket's two ends; out-of-bracket refuses, never clamps), and `m` refuses a selected run whose members carry different tempos (each a pass or the owner's own value). A DISABLED MARKER IS INVISIBLE TO THE ACT, IN THE SPAN AND OUT (architect 2026-08-29: "`m` should disregard disabled markers"): the SPAN half was already structural (`section_end_index` bounds on render PARTICIPATION, so a disabled marker never closes a section), and both of the cell's loops now skip an `effective_disabled` marker — not flattened to a pass inside, not rescaled outside, every field untouched — so the 2026-08-26 re-enable argument is superseded (a marker re-enabled later carries the tempo it had when disabled) and a disabled value can no longer trigger the out-of-bracket refusal. The `m` gate's logic is untouched.

### Colors (→ render.h palette block)
THE PALETTE IS HARD-CODED WHOLE, no user-settable colors, FULLY OPAQUE (no compositing alphas): every painted surface is a constexpr from its sampled kdenlive/Breeze crop or a recorded derivation; a retune is a recompile. The region highlight recolors the CANVAS GROUND under the ink; the marker class ladder is DISABLED > RED > (selected-bright default); stems read the CLASS alone; disabled markers paint no stem; THE MARKER LANE'S TEXT IS BLACK in every class and state (`kMarkerFlagLabel`, the lane's own ink — `kRedesignLabel` #fcfcfc stays every other row's), the disabled LABEL blending at its own `kMarkerDisabledLabelMix` while every disabled SHAPE keeps `kMarkerDisabledMix`, and SELECTED TEXT IS THE ACCENT UNDER `kRedesignLabel` EVERYWHERE (architect 2026-08-28, R28 — Breeze Light's own selection: `kRedesignAccent` #3daee9 behind the selected substring and #fcfcfc letters on it, in the flag editor and the dialog editors' field alike; the 2026-08-20 white-field/black-text band and `kMarkerEditorSelectionBand` retired with it, the carets unmoved). colors.conf and its machinery are deleted. Codex is skipped for simple color tuning.

### Settings (→ settings.md)
`app.engine_settings` is typed truth; all three boundaries are strict (whole-file schema load shared verbatim by GUI and CLI; editor red-flash; typed dispatch). `kSettingsOrder` owns write order, `kEngineKeys` membership; adding an engine key = six edits, four files. EVERY key is required — no absent state, no load-side default. GUI-kind keys commit through each key's own gesture chokepoint, history-less. Numeric values pin to one canonical spelling each. FIVE KEYS ARE PER-DEVICE, NOT PER PIECE (architect 2026-08-27, two keys under the fourth frozen grant on settings_file.{h,cpp}; `projects_repo` joined under the fifth, landed 2026-08-28; `audio_player` was a member until it RETIRED WHOLE 2026-08-28 with the in-app render player, R11 — render-player.md; `sync_path` JOINED 2026-08-30 with the mirror's destination, retiring the discovery — external_sync.h): `gui_scale`, `projects_repo`, `projects_path`, `last_project` and `sync_path` live in the ONE DEVICE CONFIG FILE `$XDG_CONFIG_HOME/warptempo_gui/config` (falling back to `~/.config/…`; the app's internal dir on Android, pointed at by `XDG_CONFIG_HOME` exactly as the cache is) — same `key=value` grammar through the shared `scan_key_value_file`, whole-file strict, EXACTLY those five keys and each once (the WRITER emits them in THAT order, the architect's own 2026-08-30 — `kDeviceConfigKeys`; the reader is order-insensitive like the sidecar's), a malformed file FATAL at startup, written from a PER-BACKEND template on first run (`GuiPlatform::device_config_defaults`: laptop 100/`$HOME/.warptempo/warptempo_gui/projects`, Android 225/`<externalDataPath>/projects`; `projects_repo` defaults to `kDefaultProjectsRepo` on both; `last_project` and `sync_path` empty — NEITHER TEMPLATE GUESSES A DESTINATION, a wrong guess aiming the mirror's creates, copies and removals at a folder the user never named) and REWRITTEN AT EACH KEY'S OWN COMMIT (`apply_gui_scale`, the `projects_repo=` editor arm, and `gui_main`'s `last_project` write on every successful open — the three writers, inventoried at `write_device_config`; `projects_path` and `sync_path` have no in-app writer; Ctrl+S carries none of them); grammars own themselves in device_config.h (the TWO PATH KEYS share ONE owner `is_config_path_value` — absolute, no line separator anywhere, no ASCII whitespace at either edge — which `is_projects_path` and `is_sync_path` are the two faces of, the second admitting EMPTY as "not set up on this device"; `last_project` empty or ONE folder name with no separator, no dot-name, NO LINE SEPARATOR ANYWHERE (`\n`/`\r` — the file is line-based `key=value` with no escaping, so such a name cannot be serialized at all) and no ASCII whitespace at either edge — a violation is adversarial and fatal, while a name whose folder is gone or invalid is LOAD-LENIENT and falls through to the first valid folder; `is_last_project_name` IS ALSO THE PROJECT MODEL'S MEMBERSHIP RULE, one predicate with three callers — this reader, `enumerate_project_names` and the argument road); `src/gui/device_config.{h,cpp}` is the unit and `is_gui_scale_percent` there is the ONE range owner. The sidecar schema dropped the three keys AND `playback_speed` STRICTLY, and a DEVICE CONFIG still carrying `audio_player=` refuses the same way — a file still carrying one is unknown-key fatal in both products, NO migration (the architect hand-edits his live files), and every checkpoint before the cut leaves the `h` walk (architect-accepted; GitHub keeps them). PLAYBACK_SPEED IS RETIRED WHOLE with its presets and the varispeed path: the render body's increment is `source_rate / output_rate` (bare 1 on JACK). `gui_scale` is an integer percent in [50, 350] (ceiling 200 → 400 under architect approval 2026-08-26 and 400 → 350 on 2026-08-29 — 400 was never needed, and at eight lanes 350 keeps the stack inside a 1080 window: 193 top + 47 bottom = 240 authored, 840 at 350 %; the tablet's 249 PPI panel runs 225), the product's ONE scale axis (`gui_scale_factor()`), applied live, INSTALLED BEFORE THE FIRST CONFIGURE on both backends (gui_main reads the config before `gui.init()`); NO COLLISION RULE — the icon row's 925-authored-px walk (26 buttons, 6 groups since the Series dropdown) crops past the right edge below ~925 logical px, so the 2304 px tablet's 225 fits every icon (1024 logical px) and 249 is the fit ceiling — a scale past it shaves the rightmost history icons (architect: the sanctioned casualty), which is why 250, tried for one afternoon 2026-08-27, was stepped back. `waveform_magnification_level` (2026-08-26, the granted frozen touch) is the WAVEFORM PICTURE'S COUNT OF DOUBLINGS in [0,4] (`is_waveform_magnification_level`, settings_file.h — the RANGE shared with the CLI so an out-of-range value refuses in both; the GAIN `2^level` is the GUI's own, `waveform_magnification_gain` in render.h), written ONLY by `apply_waveform_magnification_level`, which the two bare chords `=` / `-` (2026-08-27, taking the bare spellings the horizontal zoom pair left for Ctrl+= / Ctrl+-, bare `0` and bare `c` unmoved and Ctrl+0 retired with the reset button — bare is vertical, ctrl is horizontal, on keys and wheel alike; both repeat-eligible, read-only- and `h`-legal), the PLAIN WHEEL, the two icon-row buttons (**Magnify waveform** / **Reduce waveform**, hold-repeating) and the editor commit — the one reset road — all call; the applier's bracket is also the ladder's ends; history-less, NOT carried by `'`, and NO AUDIO ANYWHERE — no sample, no playback path, no render. Render entries carry the same sidecar set a source does; `'` loads a recipe in place — the markers and the engine block, exactly what its one undo entry restores, everything else (both bands, trim, the view bits, the camera, every pref) staying live. `'` NAMES TWO ACTS, NOT TWO ROADS ONTO ONE (2026-08-28): outside the `h` view it opens the RENDER PLAYER, whose Load in place button loads a RENDER ENTRY through `load_render_entry_in_place` (one caller — the typed renders road was deleted with the player's landing, and the two-road sanction retired with it); inside the view it raises the "Load … in place?" confirmation directly on the VIEWED member (the one-day HISTORY PICKER, R23, retired whole R42 2026-08-29 — "overengineered; I don't really need it" — no overlay stands for it). Recorded at `load_render_entry_in_place`.

### Projects (→ `src/gui/project_model.h` head prose, settings.md's device-config section, platform-seam.md; design record in tmp/previous)
A PROJECT IS A FOLDER DIRECTLY UNDER `projects_path` (architect 2026-08-27, landed 2026-08-28), folder name = project name; THE SOURCE IS DEFINED BY THE SIDECAR STEM AND BY NOTHING ELSE (`resolve_project`): a folder carrying any of `<stem>.warpmarkers/.phaseresetmarkers/.settings` names its source by that stem and `<stem>.wav` must exist; a folder with NO sidecar is a NEW project iff it holds exactly one `.wav`; anything else refuses with its reason (several stems — the whole sorted set named; no wav for the stem; several wavs and no sidecar; no wav; and the LEGACY LAYOUT "Move `X.wav` into render/" for any other root wav). Validity is folder SHAPE — regular files, exact extensions, directories invisible (`render/`, `tmp/`, `peaks/`, `score/`) — never sidecar content, AND A CONFIG-NAMEABLE NAME (architect-derived 2026-08-29): every successful open writes the folder name into `last_project`, so a folder `is_last_project_name` refuses is not a project on any road — the rule lives at the MEMBERSHIP (`enumerate_project_names` filters, so the startup fallback and the picker inherit it) with the ARGUMENT road, which walks no enumeration, asking the same predicate of the argument's own folder name and refusing with its ordinary sentence. THE APP ALWAYS HAS A PROJECT OPEN: no argument → `last_project`, else the first valid folder in byte order, else "No project under <projects_path>" and exit 1; an argument must be a project's source directly under `projects_path` (identity compare, the argument's own parent); Android calls `gui_main(nullptr)` — `resolve_source_path` and the `current` file are RETIRED (wts still writes `current`; nothing reads it). `gui_main` IS A LOOP: per-process objects outside (signals, the one `DeviceConfig`, the scale install, `GuiPlatform` + its single `init`, the touch slop, the fingerprint-keyed `RenderCache`), per-project objects inside `run_project` (AppState, GuiAudio, GuiPlayback shut down and re-inited per session, the caches, workers, handlers, every callback); `gui.run()` returns for quit or REOPEN (`GuiProjectOutcome`); the window and the input core survive; a dirty tab prompts EXACTLY as Ctrl+Q through one prompt body with `GuiCloseTarget` Exit/Reopen; a running render is killed as any dispatch kills it; `last_project` is written after every successful open. FILE → **OPEN PROJECT** (first row of the File menu, a separator before Quit; renamed from "Open" 2026-08-28, R8 — "we would call it open project instead of open file") is THE FIELD-LESS PICKER (architect 2026-08-28, R22 — "we're not allowing free-form typing there; the preview doesn't use a text field and it works fine" — replacing the same day's `Kind::OpenProject` text prompt, which was the eighth Kind and sixth dialog editor for one afternoon): the FOLDER OVERLAY stands in the keyboard's band with its SECOND CONTENT — the folder names under `projects_path` (the enumeration's own membership already dropping every name `is_last_project_name` refuses) FILTERED BY `resolve_project` (one list, `build_project_picker_rows`; an invalid folder just does not show), folder glyph + name, byte order, no `..` and no folder inside a project, built at the open and never kept fresh, the band starting on the CURRENT project — over a bottom-row modal whose row is **Cancel** FLUSH RIGHT and nothing else since 2026-08-29 (R44 — no label, no field, no OK, a click on a row being the open act; the `<projects_path>/` prefix went with the field, and the one-day OK named by its act went with the click; the outlined word buttons sit right because the architect is right-handed, only the player's borderless transport being left-aligned — THE ALIGNMENT RULE, R47). THE BAND IS THE WHOLE STATE: bare Up/Down move it, and A CLICK ON A ROW — its motionless lift, which highlights first — and Enter on the highlight both reach the one `open_project_commit(row)` — `resolve_project`, the currently open project a consumed no-op that CLOSES, then the STRICT DRY-RUN `source_load_dry_run` — THE REAL LOAD'S FAILURE VOCABULARY MINUS THE DECODE (the probe, the allocation ceiling, the sidecars present by the one predicate `sidecar_present`, the collision, the walls) — either refusal saying its reason on a NOTIFICATION CARD (messaging.md) with the picker STAYING OPEN, so the reopened session's load can fail only on a change on disk in between (today's fatal exit). THE PICKER IS THE FOURTH `ModalDialogOwner` (`Picker`, state `AppState::Picker` — one bit and a session id; the overlay's owner tag says WHICH content), ranked with the player (prompt > player | picker > editor; the picker and the player never stand together, neither beside an editor), with its own veil (the band and its one button are the pointer's whole reach; every other press is consumed) and its own router `route_picker_key` (Up/Down the walk, repeat-eligible; Enter the open act or a ring-focused button's press; Space a focused button only; Tab/Shift+Tab the ring `[list, Cancel]`; Esc = Cancel; Ctrl+S the plain save with the picker standing; Ctrl+Q the one fall-through; every other chord consumed); the one close body `close_picker` (Esc, Cancel, the close road at `request_close`'s head, the no-op, the reopen's tail) takes the overlay down with it; the roster greys as under the player (the arm reads the overlay). The no-second-road exception the prompt's picker carried ("glass needs a road that is not typing") is SPENT: the list is the only road on both backends. The on-screen keyboard's symbol layer carries a Tab key in its leading row-2 slot, which is not Open's glass road (the keyboard does not paint under the picker at all): the tablet gesture is File → Open project, tap the row — the click's motionless lift both highlights the row and opens it (R39), so a single tap is the whole act. CTRL+O IS OPEN (architect 2026-08-28; `is_open_project_key`, ctrl-exact, read by `on_key`'s dispatch and `read_only_key_blocked`; one-shot; the `h` view ADMITS it since 2026-08-29 (architect R3, "admit both" — on Ctrl+Q's own reasoning, the picker standing OVER the mode and a Cancel leaving it untouched), Synchronize joining it there the same day, so the File menu has no dead row in the view; the File row rides the ordinary chord dispatch — the item is its chord). SYNCHRONIZE TO EXTERNAL STORAGE (design §4, R3/R4, landed 2026-08-28 in `b92ea097`/`95ea84d4`): a File-menu row (Open project / Synchronize / separator / Quit; Synchronize is THE one chord-less row, `GuiPopupAct::SyncExternal`) mirrors the project into THE FOLDER THE DEVICE CONFIG NAMES — `sync_path`, TOLD AND NOT FOUND (architect 2026-08-30: the destination was DISCOVERED for three days — `GuiPlatform::removable_volume()` per backend over the ONE counting rule `sole_removable_volume` — and that rule worked on the laptop and could not work on the tablet at all, so a per-device destination went where per-device facts live; the seam member, both discoveries, the counting rule and its two sentences are DELETED, ONE road and no fallback chain, the seam 75 declarations) — an EMPTY key answering `sync_path is not set` on a card and running nothing — onto `<sync_path>/<project name>/`: `render/<title>.wav` directly and each `tmp/` batch folder AS ITSELF, wav only, copies first (each staged to `<destination>.tmp` and renamed on completion) then the mirror's deletions scoped to that folder and run only against a FINISHED destination classification, the first failure ending the act with the offending source or destination path and the system's words — or one of the three fixed sentences for a symlink / not-a-directory / not-a-regular-file, the second of which is ALSO the unplugged stick's and the mistyped path's answer, the act never creating its own sync root — on a NORMAL NOTIFICATION CARD (messaging.md), while A SUCCESS SAYS NOTHING (architect 2026-08-30, the render's precedent: the count sentence is deleted from the outcome, not merely unraised); A MIRROR THAT IS UNSURE DELETES NOTHING, the four rules at the head of external_sync.h; it runs on `GuiExternalSyncWorker` (the checkpoint worker's shape, single in flight, a fifth worker eventfd on both backends, joined at the session tail); read-only-legal, no playback stop. OPEN ON THE DEVICE: this One UI build mounts the OTG stick with `mountFlags=0` (not VISIBLE — no `/storage/<uuid>` for any app), which is a path that cannot be CONFIGURED any more than it could be found, so THE TABLET'S `sync_path` STAYS EMPTY and the act refuses there until that is solved; the SAF/Java-sliver road is the design's named contingency (a tree URI, not a path, so `sync_path` cannot name it) and needs an architect ruling. The migration checklist is in HELP; the architect's own folders migrated by hand 2026-08-28.

### The Folder Overlay and the Render Player (→ render-player.md)
THE IN-APP PLAYER FOR THE PROJECT'S RENDERS (architect design 2026-08-28, `tmp/player_design.md` §8 R1–R15 and the same-day row-act revision §8b R16–R21; landed `b86f74e1`/`069a2445`/`8308def1`) — built for the CAR, parked: open it, see the renders, start one, with no Audacious and no leaving the app. TWO OPENERS, ONE `open()`: bare `l` and bare `'` outside the `h` view (inside it `'` raises THE VIEW'S OWN LOAD CONFIRMATION instead — R19: `'` keeps its meaning, the load in place, everywhere; the typed prompt both roads once shared is retired whole, and the one-day history picker with it) plus their two icon-row buttons, which synthesize those bare chords; "No renders to play" is its one refusal. THE FOLDER OVERLAY (`src/gui/folder_overlay.h`, `GuiFolderOverlay`) is the keyboard-slot LIST PANEL: THE BAND IS THE SLOT'S AND THE ROW IS THE BUTTON'S (R3, R24/R31–R33 — the overlay REPLACES the keyboard there, and needs no term in `onscreen_keyboard::stands` to do it since the pickers lost their fields: no editor and the overlay can stand together now, so the keyboard's two-term predicate already answers false wherever the overlay paints; the band takes the slot's x, width and BOTTOM EDGE through the shared owner `keyboard_slot_band` and STANDS AT A FIXED HEIGHT — the ceiling's whole extent, from the waveform's midpoint down (`keyboard_slot_max_height_px`), whatever the listing (architect 2026-08-28, R35: "it's not a fluid height — it's always a fixed height"; a short listing leaves ground, a long one scrolls, and the content's height is now the scroll clamp's input alone); THE BAND WEARS A 1 px `kRedesignTabLine` TOP BORDER since 2026-08-29 — the twin of the bottom edge it abuts, which is row 8's own border-top, read from that row's accessor and painted inside the band's rect so the surface, damage and hit rects are unchanged (`folder_overlay::border_h_px` / `content_rect`, the line coming out of the content's room); each ROW is exactly the icon row's button — its 32 px box, its 2 px gap, its radius, its 22 px glyph at the box's own `(32-22)/2` inset, all four read from render.h — with a name too long for the line RUNNING OFF THE EDGE, no wrap and no ellipsis, and NO ALTERNATION in the file manager's colours: ground `kModalFieldGround`, a resting row no fill at all, `kFolderRowHover` + `kFolderRowHoverOutline` hovered, `kRedesignAccent` highlighted, `kFolderRowHoverSelected` both, `kRedesignLabel` ink throughout; `waveform_paint_area` gates on either tenant and CLIPS ON THE STANDING ONE'S OWN RECT against the ONE as-painted bit `keyboard_slot_painted_standing`, the panel has ONE rect since R35 — `band_damage_rect` went with the growing band, its single reader having been the listing rebuild — and the show/hide comparator keeps `onscreen_keyboard::slot_damage_rect`, the taller tenant's), listing `render/` — ONE FILE, the current title's deliverable, the listing pruning the folder to it through `deliverable_wav` — and `tmp/` THE REGULAR WAY with `..` at the top of every non-root listing, each listing built AT ENTRY and never kept fresh, carrying THE PRODUCT'S FIRST SCROLL OFFSET (one row per PLAIN wheel detent, every modified wheel a swallowed no-op there since 2026-08-29, the band's own drag on glass, the one clamp `clamp_scroll`), painted and hit through the ONE walk `for_each_row`. A CLICK ACTIVATES (architect 2026-08-29, R39, superseding R16's single-click-highlights — Plasma's single-click, which he runs, and GNOME's touch agree): the row press ARMS (it may still become the band's scroll drag) and its MOTIONLESS LIFT highlights the row and THEN OPENS IT — folder enters, `..` up, wav plays from its start — with Enter the keyboard's own click on the highlight and Up/Down walking the band without opening. THE DOUBLE-CLICK RETIRED with it: `DoubleClickSurface` is FOUR again, `FolderRow`, its seed, its press-time consume and the listing rebuild's clear of that candidate all deleted; no ctrl-click and no multi-selection; THE TRANSPORT IS ASKED AHEAD OF THE HIGHLIGHT (R36, correcting R17 — the bug his pass found: a Next left the band behind and the live button then PLAYED the row it sat on instead of pausing what sounded): `AppState::RenderPlayer::transport` STORES Idle/Live/Paused (a rest at the start is idle, and PAUSED AT FRAME ZERO IS PAUSED — the no-device pause parks a wav that never sounded, so the state cannot be read off the resume point; the one stop body's player fork is PAUSED's one writer and the four resting acts — `stop()`, the natural end, `open()`, `close()` — write IDLE after it) and `play_button_act` forks WITHOUT EVER READING THE HIGHLIGHT (R40, 2026-08-29, Audacious's "play when idle plays the current track"; R17 retires whole with the highlighted-folder open act, a tap opening a folder now) — LIVE pauses, PAUSED resumes, IDLE with an item plays it (R27's first file at the folder's end, else the item where it rests, which is its start in every rest the transport's own acts leave), IDLE with no item does nothing. Previous/Next (Page Up/Down), THE FOLDER'S ENDS (Shift+Page Up/Down and the two skips' shift-click or long press, R37) and AUTO-ADVANCE walk the TRANSPORT ITEM'S OWN folder as it was listed, never another and never a wrap (R2 — NOTHING LOOPS holds), and THE BAND FOLLOWS THE ITEM at every change the transport makes on its own (R38, superseding R17's "Previous and Next never move the highlight"): onto the new item's row where that row is listed, staying put where the user has walked elsewhere; one owner, `play_wav`. THE BOTTOM ROW IS ITS MODAL, the third `ModalDialogOwner` (prompt > player | picker > editor), RELAID 2026-08-28 (R25, his first plastic pass — the arrangement is Audacious-Qt's under Breeze Dark; R36 that evening put STOP after Play/Pause, Audacious's order again): Previous · Play/Pause · Stop · Next · separator · the PLAY-SCRUB (the one flexible item) · the monospace clock `<pos> / <len>` · separator · REPEAT ONE ... Load in place · Close FLUSH RIGHT (Close last, the escape sentinel). THE FIVE GLYPH BUTTONS WEAR NO RESTING BORDER ("the regular transport has none") and the two WORD buttons keep theirs; one ladder in one body paints all seven, the row's one LAMP (Repeat one) taking the roster's SELECTED face. PLAY/PAUSE WEARS A REAL PAUSE GLYPH while live (Breeze media-playback-pause, the roster's 51st and one fresh verbatim transcription) now that STOP is its own button wearing the stop square — the ROSTER's transport button is untouched, bare Space over the project's audio being one toggle with no pause state. STOP (R36) leaves the item resting AT ITS START with the folder-end bit cleared and the item still the transport's, so the next Play replays it; a live transport takes the one stop body, a paused one only moves its rest, and no item or a rest already at the start is a consumed no-op. THE TWO SKIPS ADMIT A MODIFIED PRESS (R37, the modal row's first): shift-click on plastic and the `kChromeShiftHoldMs` long press on glass are ONE term (`modal_dialog_press_shifted`, the arm carrying the roster's two fields) reaching the folder's ends, the membership is `player_button_shift_admits`, a shift press on a button with no twin is a consumed nothing, and the hint's second line (`render_player_button_shift_hint`, published as `ModalDialogButton::tooltip2`) says so on exactly those two. THE SCRUB IS A BREEZE SLIDER (R29/R30): a 6px groove at radius 3 under an 18px handle in its 20px box, the played part #3787b1 while `window_activated` and the shot's dimmed blue while not (the scrub is the flag's THIRD reader, and the activation hook damages the row for it), the handle's outline the accent while hovered or dragged — every metric, colour and provenance at render.h's scrub block, which the painter and THE MAPPING (which owns the handle's half-box inset at each end) both read. A press on the track seeks AT THE PRESS, a press on THE HANDLE'S OWN BOX — the one grab band, one test shared with the painter's hover — arms the drag whose seek commits AT THE RELEASE; both cells PUBLISHED for the tick's damage under the owner-tag doctrine. THE SCRUB RESTS WHILE THE TRANSPORT IS IDLE (R41, Audacious's slider dead while stopped): a press on track or handle is then a consumed no-op, LIVE and PAUSED unchanged, and the handle still paints at the resting point. THE ROW'S SPACINGS ARE RULED (R46): the three gaps around the scrub are ROW 8'S OWN separator → timestamp distance (`kTransportSepToClockPx`, one owner, the modal row reading it), the gap Repeat one → Load in place is the word buttons' own `kModalButtonGapPx`, and the dialog editors' label → field gap is the window-edge → label pad (`icon_row_pad_x`, one number read twice; the sampled 11 retired). THE ALIGNMENT RULE (R47, at `paint_modal_dialog` and in render-player.md / conventions.md): a modal carrying a FIELD OR A MESSAGE is flush LEFT with it (prompts, dialog editors, the player's transport), a LIST'S buttons flush RIGHT (the picker). REPEAT ONE (R26) is the one sanctioned exception to NOTHING LOOPS — session-only, off at every open, replaying the item at its natural end — and at the folder's end with it off THE NEXT PLAY STARTS THE FOLDER'S FIRST WAV (R27, the car's Play included; one bit, one writer at the natural end, one reader in `play_button_act`). EVERY ROSTER BUTTON IS DEAD while it stands (`redesign_button_enabled`'s first arm, the ruled exception — R3: "everything else greys as in the `h` view") and the veil consumes every press outside the band and the row; `route_render_player_key` is the whole plastic vocabulary (Tab/Shift+Tab the ring `[list, buttons…]`, Left/Right ∓5 s and NOT a ring walk, bare Home the item's start, Backspace up, Page Up/Down the neighbours and Shift+Page Up/Down the folder's ends, bare `s` STOP, `r` repeat one, `'` load, `l`/Esc close, only Ctrl+S and Ctrl+Q falling through), and Open project refuses under it. THE ENGINE ROAD: the frozen wav reader CALLED, never changed — probe, the device rate/channel EQUALITY (a refusal, never a conversion), the allocation ceiling, the read, THEN THAT SAME EQUALITY AGAIN ON THE DECODED BUFFER'S OWN SHAPE (`wav_read_full` REOPENS the path, so the probe's answer is stale by construction — every writer here publishes by rename — and the probe is kept as the CHEAP EARLY refusal the allocation ceiling needs, while the decoded `WavInfo` is what the bind rests on), and "Empty wav" — then THE FENCE and `rebind_buffer`; it has ITS OWN LAUNCH BODY, the product's SECOND, recorded beside `launch_playback_window` (the project's playhead never moves, its scanner never runs, the item's domain is the buffer's own) while THE STOP stays the one stop body with the player's fork inside it. CLOSE (the Close button, Esc, `l`, the load's success, and THE CLOSE ROAD `GuiPrompt::request_close` — Ctrl+Q and the compositor's X, `069a2445`) rebinds the VIEW's buffer through the S/T flip's own fork BEFORE freeing the item's. LOAD IN PLACE IS A BUTTON OR `'` ON THE HIGHLIGHT AND NOTHING ELSE (R18/R43, the long press retired unbuilt and confirmed never landed) on the highlighted BATCH CELL, confirmed by "Load `<id>` in place?" OK/Cancel (R21 reversing R20 minutes later — the two `'` roads must not answer opposite ways), THE ONE LOAD PROMPT RAISED ON ITS FIRST BUTTON so Enter confirms — one prompt BODY raised from its TWO subjects, and no other prompt in the product (`PromptInitialFocus`, a REQUIRED parameter of `PromptState::present`; Cancel still last, the sentinel's derivation intact across all FIVE `present()` sites — six until the error notice retired 2026-08-30). SINCE 2026-08-29 IT IS ONE PROMPT BODY WITH TWO SUBJECTS — the player's entry and the `h` view's viewed walk member — parked at `RenderPlayer::pending_load` / `HistoryMode::pending_load_member` and forked at `confirm_load_in_place`, so the symmetry R21 asked for is structural. `audio_player` RETIRED WHOLE with the spawn (R11) and bare `l` joined the read-only allowlist WITH ITS BUTTON (the Play renders icon left `redesign_button_enabled`'s read-only arm, which is SEVEN chords now; the LOAD is what the lock refuses). THE TYPED PROMPT'S RENDERS HALF IS DELETED, not left unreachable: its two guards, its entry resolve, its whole autocomplete and its composed `Load: <projects_path>/…/tmp/` prefix, leaving the `h` view its own road — `open_history_load_editor` until R23 the same day retired that editor whole, then the history picker for one day, and `history_load_in_place` since. THE CAR'S MediaSession LANDED THE SAME DAY (design §3, `3ceb01c2`; the mechanism is platform-seam.md's car section): THE SEAM IS 76 with `set_on_media_command` (Wayland stores it and never fires it) and `publish_media_state` (Wayland a no-op), the head unit's buttons arriving as `GuiMediaCommand` values that `on_media_command` replays as THE PLAYER'S OWN KEYS through `synthesize_key` — no second dispatch road, `SeekTo` the one direct act — after CLEARING THE MODAL RING (a car button is not a keyboard walking a ring: the feint can leave Close focused, and a bare Space would press it), while the head unit's `Stop` maps to the STOP KEY since R36 (a pause until the row had a stop; the key names an act, so it takes no state gate while Pause keeps its transport-live one) and nothing maps to the folder's ends, while A DEAD OR MISSING DEVICE PAUSES instead of advancing (`GuiPlayback::device_unavailable`, both backends — the AAudio latch AND a device that never came up, "No audio device"). THE OVERLAY HAS TWO CONTENTS and ONE OWNER TAG (`AppState::FolderOverlay::owner` = None/Player/ProjectPicker), which IS the standing predicate `folder_overlay_stands` — the painter, the press router, the release, the motion, the wheel context, the waveform paint gate, the touch pan zone and the greying arm all ask that one function, and the open act is the ONLY thing that forks on it (the lift's highlight is every owner's alike). THE SECOND CONTENT IS THE OPEN PROJECT PICKER (Projects digest above), the one `ModalDialogOwner::Picker` / `AppState::Picker` with one router, one veil and one close body; its ROW IS **Cancel** ALONE (R44). THE HISTORY PICKER — the third content for one day (R23) — IS RETIRED WHOLE (R42, 2026-08-29: "overengineered; I don't really need it"), with `Owner::HistoryPicker`, `history_picker_commit`, `build_history_picker_rows`, `open_history_picker` and the glyph-less `FolderOverlayRow::Kind::Text` its rows needed all DELETED (a kind exists iff a content produces it); bare `'` in the `h` view now raises the load confirmation on the VIEWED member directly (GitHub Recheck digest below). THE ON-SCREEN KEYBOARD'S THIRD TERM (`!folder_overlay_stands`) IS DELETED with the pickers' fields: its producer was the Open prompt's editor standing under the band, and no editor and the overlay can stand together now (the record at `onscreen_keyboard::stands`). NOT BUILT, by ruling: no notification / service / background playback / lock-screen transport. THE SLIVER MAPS THE MEDIA KEYCODES ITSELF (`onMediaButtonEvent` overridden, ACTION_DOWN with no repeat): the framework's default holds the play/pause key for a double-tap window and turns two presses into Next, so `Kind::PlayPause` is back in the numbered table with the override as its producer (HEADSETHOOK with it) and answers with Space unconditionally, the directional Play/Pause keeping their state gate.

### Load-Lenient, Render-Normalizing Boundaries (→ normalization-and-boundaries.md)
Gestures move freely and NOTHING POPS: ambiguous authored states normalize at the render boundary (`resolve_warp_markers_for_render`; red flags as the display cue), never refused or force-resolved. The two-category rule: a state the GUI can commit always loads and renders; a state the GUI can never produce is adversarial and hard-fails the load, first error only, identically in both binaries. The same family covers the REPOSITORY: sanctioned use is strict-exact intended use; out-of-app git under `projects/`, a detached HEAD or a hung hook get a blunt terminal error, no in-app recovery. TRIM IS ALWAYS SET (a full ordered pair always rests; crossed pairs reset to the full window; the endcap drag clamps inclusively at the partner, and a COINCIDENT RELEASE RESETS TO THE WHOLE SONG through the one owner `auto_clear_crossed_trim` — the endcap drag's escape, and since 2026-08-19 every former's, the sweep included); THE FULL WINDOW IS SEMANTICALLY THE OLD UNSET STATE through the one predicate `trim_window_is_full`. EVERY route that writes a trim bound clears the selection and parks the playhead at the new trim start (membership at input_trim.cpp's header), and no trim write hides the trim region overlay MID-GESTURE — exempt BY CONSTRUCTION since 2026-08-19, `park_playhead_at_trim_start` and the sweep's per-motion carry writing the cursor DIRECT and so reaching neither of the hide rule's two movement owners — though since 2026-08-20 THE SWEEP'S OWN END COLLAPSES IT (the hide rule's third clause: `commit_region_sweep` hides unconditionally; a stroke names "this exact region", bare `[` is the recall). THE SWEEP ENFORCES NO WIDTH AT ALL (architect 2026-08-19, retiring `kMinTrimSpanFrames` one day after it landed as distracting and too short to be worth its machinery): a stroke authors exactly the span it draws, and its DEGENERATE CASES take the no-backstop treatment — a COINCIDENT release falls into the endcap drag's own escape `auto_clear_crossed_trim`, now the whole-song reset EVERY former shares, while a merely tiny window rests and renders (honorability belongs to `validate_trim_frames` at the render boundary, whose refusal means render untrimmed, never a failed render). `[`'s degenerate-result refusal and the former's sliver dissolve stay retired. Trim has no undo — `Shift+[` is the recovery. NOTHING LOOPS, WITH ONE SANCTIONED EXCEPTION: every audition plays once to its end (song's end in source view, preview buffer's end in target; `Viewport::trim_range` is the NAVIGATION range owner) — the exception is the render player's REPEAT ONE lamp (architect 2026-08-28, R26; render-player.md), a session-only two-state toggle that replays THE ITEM at its natural end and reaches no project audio. A RESTORE PUTS BACK THE WHOLE AUTHORING VIEW (architect bug report 2026-08-28): an undo entry records THREE view tags — the A/B tab, the W/P column and, since that report, the S/T AUDIO VIEW (`UndoEntry::audio_view`) — and the restore writes them TAB, THEN THE DATA WITH ITS MAP-CHANGE RE-LAND, THEN THE COLUMN, THEN THE AUDIO VIEW, each view axis through its own chokepoint (`switch_active_tab_view_to`, the S/T set-to form `switch_active_audio_view_to`, `switch_active_markers_view_to`; the hand-kept copy of the column switch that stood inline in the restore is deleted), so NO RESTORE SYNTHESIZES A VIEW THE USER WAS NEVER IN — it lands the combination the op was authored in, and the keyless S+P it used to manufacture out of a T+P entry now arrives only when the op was authored there. Undo coalescing is a hybrid: a HELD KEY'S or HELD BUTTON'S synthesized repeats coalesce by repeat identity behind an OPENER that differs per surface — a held key's burst opens with its own PHYSICAL PRESS, a held button's with its FIRST FIRE (the button's act being the lift's) — rapid physical taps at their presses within `kTapCoalesceMs` merge on a standing subject; `record_gesture` stamps only on the accepted path. THE TAP WINDOW IS THE BEAT since 2026-08-28 (architect: "increase the coalesce wait to 575 ms, the global wait time for long press, key repeat, etc.") — `kTapCoalesceMs` READS `kHoldBeatMs` and restates no number; it was its own 500 from 2026-08-01, and what the move changes is the value alone: the window still gates the TAP arm only and the held-key arm still consults no clock.

### Interaction Model (→ selection-model.md, marker-ui.md, region-scrub-esc.md, pointer-hit-testing.md)
Every plain marker click lands the playhead, AT THE PRESS through the one act owner `run_marker_click_act` (the plain arm alone then arms `PendingMarkerPress`, whose crossing begins the drag and whose motionless lift owes only the double-click seed); shift = inclusive range from the anchor/focus, ctrl = membership toggle, both landing on the focus they set and arming nothing, both FLAG-ONLY (ADD TO SELECTION — bare `k` and its bottom-row lamp — is the STICKY CTRL for glass: while lit a plain flag click takes that same toggle branch, SHIFT BEATS IT, it is scoped to flag hits by construction, and it auto-clears at every `Selection` mutator except the toggle itself) — a modified press over the waveform is the waveform's own gesture in every view. MARKER STEMS ARE POINTER-INERT everywhere; the FLAG BOX is the marker's one pointer surface (stems still paint). EVERY MARKER MAY CARRY A MEASURE (born 2026-08-19 as the free-text comment, rebranded and grammar-tightened 2026-08-20; both columns): a strict ASCII score-position token — direct `10` / `12 7/8`, or the offset `+4` / `+1/2` / `+4 1/2` adding to the IMMEDIATE predecessor's resolved measure (chains resolve to a plain RATIONAL; a broken link makes it UNRESOLVED, gating consumers only — grammar is validity, resolution is not; byte cap 12 = `+99999 98/99`; the one-day `S:` SECTION QUALIFIER retired in the header's THIRD frozen reopen, architect approval 2026-08-21 — "the context will already be a clue"; authoritative at `marker_measure.h`) — ALWAYS VISIBLE as a blue box continuing the flag rightward, part of the same hit rect (only the DOUBLE-CLICK forks on the span, off the painter's published boundary), EVERY MEASURE ITS OWN AND INHERITING FROM NOTHING, edited in the SIXTH `text_editor::Kind` — `MeasureText`, on the flag editor's machinery, entered by bare `/`, the bottom-row button or that double-click, EMPTY COMMIT REMOVING and a bad token red-flashing in the box (no waveform edge). THE MEASURE PROPAGATE (2026-08-20): Ctrl+/ copies off a contiguous labeled run, Ctrl+Alt+/ pastes onto an anchor through the SEVENTH Kind / FIFTH dialog editor — an OFFSET modal (canonical signed integer, seed 0, Enter = confirm) whose offset shifts direct measures' whole part, `+` forms verbatim, holes clearing, out-of-bracket refusing WHOLE; label lockstep, stop at first divergence, one undo entry, no view switch, no render; unlabeled markers never propagate a measure (walk membership at `warp_marker_propagates`). THE SCORE-VIDEO EXPERIMENT IS SUNSET (architect 2026-08-21: the mpv jump grew into a full Pages/Timeline score system in two sessions and the whole arc was rolled back the same day by hard reset — "not that big of a gain over keeping a file on hand that I can toggle mpv back and forth on"; sunset record in marker-ui.md, the comments above its one survivor): Shift+/ is UNBOUND again (strict modifier validation's consumed no-op), the Measure button greys with the verbs on a locked tab, sheet.map and the extractor are gone, and the score folder is plain local material the architect curates himself behind the one `.gitignore` line `projects/*/score/`. THE MARKER LANE'S TEXT INK IS BLACK in every class and state (`kMarkerFlagLabel`, 2026-08-20, measured off kdenlive itself) — flag labels, the measure box, the `h` view's diff flags and the flag editor's own text and caret; `kRedesignLabel` #fcfcfc is untouched and stays every other row's ink. TWO SAME-DAY RETUNES ride with it: the disabled LABEL takes its own blend fraction (`kMarkerDisabledLabelMix` 0.75 — the surfaces keep `kMarkerDisabledMix` 0.25; the old value suppressed a WHITE label sticking out, the opposite of black's problem), and the flag editor's SELECTION BAND moved off the ink onto its own constant (kdenlive's white-field/black-text precedent), which R28 then SUPERSEDED WHOLE on 2026-08-28 — selected text is `kRedesignAccent` under `kRedesignLabel` on every text surface, the flag editor's selected substring being the one place the lane's ink is not black; the deleted `face.fill` re-show stays deleted (saturated glyphs fringed pale against white). It is the home-view binding's fourth exception (the flag editor itself is the fifth since 2026-08-25, so bracket authoring is no longer source-only); read-only still refuses. On disk it is the ` //<measure>` suffix in the ASCII grammar class (data-model.md). The playhead has ONE form and always paints; a selection's cue is its members' brightened flags. THE REGION IS THE TRIM (2026-08-18): `RegionState` is ONE VISIBILITY BIT and nothing else, the overlay DERIVED from the resting trim bounds every frame through `trim_overlay_span`, so the bar, the overlay and the hit test cannot drift. A SHOWN overlay is manipulable ON THE WAVEFORM ALONE (`region_manipulation_hit`, Y-gated to the waveform rect, 10px bands): drag on a bound = that ENDCAP drag, drag inside = the BRIDGE drag, else pan — trim's own drags armed from a second surface, no parallel editor, their motionless lift falling to the waveform's ordinary click act; shift/ctrl bypass it. THE SWEEP'S FIRST WRITE SHOWS THE TRIM AND ITS COMMIT COLLAPSES IT (born 2026-08-19 at the press as "touching the trim shows the trim", narrowed to the sweep 2026-08-20, moved to the first write 2026-08-21 on the architect's drive — the press-time raise necessarily showed the OLD region, the overlay deriving from the resting trim): `show_trim_region_overlay` is the ONE raise — the hide's counterpart, no framing (unlike `[`'s show half), `h` carved out inside it — called from the sweep's FIRST ACCEPTED TRIM WRITE (`apply_region_drag_motion`'s write branch) and nowhere else; the trim bar's three press claims DEPARTED the inventory 2026-08-20 (a 9 px band under a pointer is its own display — the overlay serves glass), so the overlay shows only a region the stroke itself has authored: up from the first write, collapsed at `commit_region_sweep` whatever the stroke did (a sweep names "this exact region"; bare `[` is the recall; a motionless shift click shows nothing at all). THE SWEEP (the nav surface's shift+drag and the touch region hold) writes the trim PER MOTION EVENT under no width rule (the floor retired 2026-08-19), so a shown overlay tracks the stroke live and a stroke collapsed onto its anchor clears the trim at its release; `commit_region_sweep` is its one release owner; the `h` view's shift former is a PLAYHEAD SWEEP that writes no trim and is carved out of both the write and the show. Every former drops the selection its surface owns and carries the playhead on its moving end. BARE `[` SHOWS AND HIDES (the family moved off `x`/`Shift+X` 2026-08-24, both now unbound — bare `x` was too easy to hit instead of `c` and its show half frames the viewport; `[` reads as the begin endcap; the icon row's lit rect-selection twin, named **Show trim region** — the tooltip names a constant act while the lamp carries the state, the read-only toggle's precedent; `Ctrl+Shift+X` is unbound) — the show half brings the span into view via `bring_span_into_view` (the undo restore's own scroll-else-frame, one shared owner) and HIDING DISCARDS NOTHING, the trim persisting; `Shift+[` maximizes to the whole song (the key, or that same button shift-clicked or long-pressed) and is the recovery, trim having no undo. THE OVERLAY HIDES WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES, WHEN A MARKER IS TOUCHED, AND WHEN THE SWEEP ENDS, AND AT NO OTHER TIME (architect 2026-08-19, the hand-listed inventory collapsed to a RULE; the sweep clause 2026-08-20): a TRANSLATION IS NOT A MOVEMENT (the `t` flip, the cent step's target re-land), a RESTORE IS NOT A MOVEMENT (the A/B tabs are two virtual playheads over one piece) and THE CAMERA IS NEVER A MOVEMENT (pan, zoom, the overview strip, the `h` view's edges), while the TRIM'S OWN SURFACES stay EXEMPT BY CONSTRUCTION MID-GESTURE — the trim family writes the cursor DIRECT and so reaches no owner, the sweep's END being the rule's explicit third clause instead. FIVE CALLS IN THREE CLASSES: the TWO MOVEMENT OWNERS `Viewport::move_playhead_to` and `land_playhead_on_marker`/`land_playhead_on_source_frame` (all hiding UNCONDITIONALLY, so a no-op Home still hides), `run_marker_click_act` for the touch half (it hides even where an arm lands nothing), and `commit_region_sweep` (unconditional at every sweep end path, the `h` former's included); bare `[` and the file load write the bit themselves. BARE ESC'S HIDE IS RETIRED (architect 2026-08-21: `[` is the one manual road onto and off the overlay — the durable show's twin is the durable hide), and with it Esc's rank above the render cancel: an Esc over a shown overlay now cancels an in-flight render in ONE press. THE THREE NON-HIDING ENTRY POINTS are named, never a flag: `Viewport::reseat_playhead_to` (every MAP-CHANGE re-land: both cent-step arms, since 2026-08-25 the W+target status/value family's four tails, and since 2026-08-28 the undo/redo restore, which installs the entry's data and re-lands the cursor's own instant BEFORE its S/T switch so the translation reads the restored map) and `reseat_playhead_on_marker` (the S/T flip's re-express, the coincidence auto-select's provable no-op). The rule is stated once at `clear_region_highlight`'s declaration, input_handler.h. CURSORS: named XCursor-theme cursors only, seven kinds, ONE zone map derived from the press routers (`pointer_cursor_kind`), resolved once per run-loop iteration by one owner; a live trim gesture keeps its own cue; the cursor promises the gesture. READ-ONLY protects the authored musical content only — trim gestures, `[`/`Shift+[`, Ctrl+S and both render chords stay legal on a locked tab; the settings editor is disabled there (`read_only_key_blocked`). SPACE always plays from the playhead; SHIFT+SPACE IS THE A/B AUDITION (2026-08-26; the play button's shift-click / long press is the same chord, the roster's fifth shift-admitting button): switch to the OTHER tab, play `kAuditionMs` from its resting playhead twice, switch back, play the same span from this tab's twice — four BOUNDED plays (end = `start + span`, one launch body `launch_playback_window` under Space's view-end form and this one; NOTHING LOOPS still holds, each a discrete play to its own end), no resting playhead moving by the act's own doing — and EACH HALF OPENS WITH THE `c` COMMAND ON ITS TAB, in the switch's own frame (architect 2026-08-28: at the press, on the switch, and again on the switch back, through `c`'s one owner `run_center_command` — so `c`'s focused land is the act's one cursor write and all three calls sit where the sequence is Idle, the switch-back one ahead of the arm; `GuiAbAudition::apply_working_zoom`); IT IS PACED (2026-08-26): `kAuditionPairGapMs` rests between a pair's two plays and `kAuditionSwitchGapMs` across the tab switch — the architect's own hand pacing, a retune touching only their declaration (app_state.h) — none before the first play (the rest is between SOUNDS); SEQUENCED IN TWO HALVES WITH NO TIMER OF ITS OWN — the tick's natural-end branch switches (synchronously, the flip seen at once) and ARMS the rest after that switch, `GuiAbAudition::fire_if_due` launches it off the RUN LOOP'S EXISTING DEADLINE TICK beside the key-repeat and touch-window deadlines (granularity one timer period); `phase` stays the act's ONE running bit across both halves (`waiting`/`launch_due_ms` beside it), so `phase != Idle` is still the whole refusal test and A REST IS INTERRUPTED EXACTLY AS A PLAY IS (no owner tests for live playback); state `GuiAuditionSequence` (edge inventory at the struct), ops `GuiAbAudition`; every interrupt ends it where it stands — the clear lives at FOUR OWNERS ACROSS EIGHT CALL SITES, inventoried at `GuiAuditionSequence` — the one stop body ahead of its guard, the one launch body's head, target_render's three clears (`trigger()`'s unconditional on the target path) and, since 2026-08-26, THE TWO MOVEMENT OWNERS beside their trim-region hide (`Viewport::move_playhead_to`, `land_playhead_on_marker` / `land_playhead_on_source_frame`): THE ACT ENDS WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES, the overlay-hide rule's own membership, so a MOVEMENT interrupts while a TRANSLATION and a RESTORE do not — which is both what makes "the resting cursor cannot move under a standing act" structural and what lets the act's own two tab switches run inside it, and it SUBSUMES the former fourth owner `reseek_keeping_alive` (its one caller runs `move_playhead_to` first), whose clear is deleted for a pointer; THE ACT IS ONE TRANSPORT SESSION FROM ITS FIRST PLAY TO ITS LAST (2026-08-26) — a REST is transport-LIVE, so bare Space (and so the play button's plain lift) STOPS the act through the one stop body at `toggle_playback`'s fork, and `redesign_button_glyph_swapped` reads `playhead_scanner_active || phase != Idle` so the button wears STOP for the act's whole duration instead of flickering to Play three times per act (the transport row never lies about live state); refusals silent at the press (running sequence = consumed no-op, either tab not ready in target view via `GuiTargetRender::preview_ready` + the bound domain, `h` at the allowlist); read-only-legal, Esc not added. Home/End jump to the TRIM bounds; CTRL+HOME / CTRL+END (2026-08-24) jump to frame 0 / the active domain's last frame TRIM-BLIND (`playhead_skip_landing_frame` whole-piece arm, the `h` view's own), and the skip buttons' CTRL-CLICK IS that ctrl chord — the roster's first ctrl-click (`redesign_button_ctrl_admits`; they admit no shift, so the long press never reaches the act by construction, and Ctrl+Shift is refused at the band gate). HORIZONTAL MOVEMENT IS A FOCUS ACT — groups are never moved; singleton steps CLAMP at walls, group presses REFUSE whole (position_nudge.h). Bare arrows nudge the FOCUSED marker in its home view; every focus-changing route lands the playhead (`land_playhead_on_marker`). THE SELECTION IS NEVER PARKED: `p`/Ctrl+Tab clear it; coincidence auto-select at the four entry chokepoints (`auto_select_marker_at_playhead`). Hit geometry rides the DISPLAYED basis (plate_viewport_basis / item_viewport_basis); DAMAGE FOLLOWS THE BASIS OF THE PIXELS IT ERASES (scanner sites narrow-on-plate, discrete commands full waveform-area). POINTER GESTURES HAVE NO CANCEL: Esc mid-gesture is a consumed no-op, any end commits, undo is the mitigation. BARE ESC IS BOUND IN SEVEN PLACES AND NOWHERE ELSE (enumeration at input_handler.cpp; the trim-overlay hide left 2026-08-21, the render player's close joined 2026-08-28 and the picker's close the same day — the two typed prompts it replaced were members of the editors' place). The keyboard stop rule is authoritative at `stop_playback_if_playing`'s declaration.

### The Home-View Binding (→ tempo-and-home-view.md)
Each marker column authors in its HOME view only (warp = source, phase reset = target) via `active_column_authoring_allowed`; off home, what the predicate gates is the POSITIONAL family — the `s` drop, the flag drag, the Left/Right nudge, the `m` bpm open, and the phase-reset arms of Ctrl+D / Delete — with silent refusals. FIVE ruled exceptions: the bare Up/Down tempo cent step in W+target (singleton + group), the phase-reset propagate, the iteration-bracket wipe in W+target (iteration mode is target-legal), THE MARKER MEASURE (2026-08-19, rebranded 2026-08-20 — both columns, both views, wherever the flag paints, the measure propagate riding under it; read-only still refuses), and THE WARP STATUS/VALUE FAMILY in W+target (architect 2026-08-24, landed 2026-08-25 — Ctrl+D, Ctrl+N, Delete and the flag editor's payload surface through both open routes; the binding's rationale was always POSITIONAL, so the split is positional vs not; each act's target-view tail takes the cent step's contract: `kick_waveform_sync` then a TRANSLATION re-land through `Viewport::reseat_playhead_to` — the focus's post-change image, or for Delete the cursor's own musical instant through `active_domain_to_source_frame`; the flag editor's arm runs on `canonical_changed` only, a bracket-only commit moving no image; the S↔T flip closes a live editor in BOTH directions; bracket authoring is no longer source-only); inventory at the predicate, app_state.h. SHIFT+S IS NOT A SIXTH EXCEPTION (2026-08-28): it drops a phase reset FROM ANY VIEW, but it does not author off home — it runs the two view chokepoints FIRST and then calls the one lead-in drop bare `s` calls, so the body executes in T+P under the predicate's ordinary answer; the drop stays positional and home-view-only, and what the chord adds is a trip, not a permission. W+target has no pointer authoring gesture that MOVES anything — its pointer vocabulary is the measure double-click and the payload double-click, both editor opens.

### Modality and Input (→ conventions.md)
ICONS ARE UP, HOTKEYS ARE DOWN; TOUCH USES ICONS, PLASTIC USES BOTH; NO DISAGREEMENT (architect 2026-08-16, closing the same day's keyup experiment — key commands moved to the release and were returned to the press that evening on the press-timing principle; conventions.md carries the retirement record whole, the extension that was never built included). Every control classifies once and keeps ONE timing everywhere: HOTKEYS ACT AT THE PRESS (`GuiInputHandler::on_key`, the one ranked dispatch), ICONS ACT AT THE LIFT whoever operates them — which is why bare Enter/Space on a focused modal button is the keyboard operating an ICON rather than a hotkey exception, and bare `e` is the keyboard operating the POINTER. THE THIRD CLAUSE COMPLETES IT (architect 2026-08-17, on his first real drive of the 2026-08-15 pointer sweep): CONTENT ACTS THE MOMENT ITS IDENTITY IS CERTAIN — at the PRESS where the press can only mean one thing, at the motionless RELEASE only where the SAME press arms a drag. So the marker flag's three clicks, the `h` view's three diff-flag clicks, the OVERVIEW'S OUTSIDE-THE-BOX TELEPORT (which since 2026-08-18 also ARMS the box pan — the flag's own act-then-arm shape, not a breach: the drag continues the act rather than revealing a second meaning) and EVERY double-click's second press (flag editor, empty-lane create, trim-bar framing) act at the press, while the navigation surfaces' plain click, THE WAVEFORM TRIM OVERLAY'S three presses (each arming a trim bound or bridge drag, their motionless lift falling to the waveform's own click act through `PendingTrimDrag::waveform_click_act`), the overview's inside-box and endcap presses and the trim bar's ctrl / ctrl+shift bound sets — the product's ONE deferred click — wait for the lift; conventions.md carries the certainty audit, the touch answer (the disambiguation window, not the lift, is what keeps a two-finger landing off a press-time act) and the one recorded regression. THE MODAL SURFACE IS THE BOTTOM ROW: while a prompt, a dialog editor, THE RENDER PLAYER (the third `ModalDialogOwner` since 2026-08-28, its transport row taking the lane whole and every roster button dead under it) or THE PICKER (the fourth, the same day — the field-less Open project picker, its row **Cancel** ALONE since 2026-08-29 because a click on a row is the open act, flush right, ranked with the player) stands the row yields whole and `paint_modal_dialog` paints into it — one renderer, one window, one frame. Modal buttons are real buttons acting at the LIFT, with the two-strength keyboard focus ring (passive assigned / active walked, both act) and roster-style tooltips; bare Enter/Space press the focused button and commit on the key RELEASE. A prompt answers only after it has painted (`PromptState::painted`); PUBLISHED GEOMETRY MAY ONLY SELECT, LIVE STATE DECIDES (the owner-tag doctrine at `ModalDialogGeometry`). The veil consumes every press outside the modal surface. All six editor kinds (four dialog editors + the two flag-editor kinds; `LoadInPlace` and `OpenProject` retired 2026-08-28 into the picker) admit exactly their own keys plus bare Esc, Ctrl+S, Ctrl+Q through ONE `route_modal_editor_key` contract; the flag editor is keyboard-modal but pointer/wheel-transparent and stops no playback (`keyboard_modal_editor_active` vs `modal_dialog_editor_active`). Tab in a dialog field offers the completion FIRST and is consumed iff the buffer advanced, else it walks the ring — one condition, no counter. THE CLIPBOARD IS THE SYSTEM CLIPBOARD (editors' ctrl-exact Ctrl+C/X/V + the VALUE COPY, bare `j` since 2026-08-29 — the readout's global Ctrl+C retired with the readout and Ctrl+C is unbound globally again; empty answer = consumed no-op). STRICT MODIFIER VALIDATION: a modifier combination with no explicit binding is a no-op everywhere; three families deliberately untightened (`is_tab_cycle`, Ctrl+Z's shift bit, the editors' motion arm — alt excluded). SUPER IS DROPPED AT THE PLATFORM BOUNDARY (no key PRESS delivered while held; the modal arm's armed act still completes at its release — outside the GUI's providence, conventions.md). ALT IS THE FIVE Ctrl+Alt RENDER/PROPAGATE CHORDS ON THE KEYBOARD (Ctrl+Alt+/ joined 2026-08-20) AND ONE POINTER BINDING, Alt+wheel = the stepped pan (2026-08-27); no alt-modified press binds anything. Bare `e` is the left mouse button, translated at the platform boundary — never bind it in dispatch. TOUCH IS THE POINTER: wl_touch capability-gated, one finger translated into pointer deliveries behind the disambiguation window (pan-zone deadline `kTouchRegionHoldMs` = the region hold; elsewhere 60ms/8px), two fingers = seated-pivot pinch zoom ONLY (downgrades to the survivor's pan), ONE HARD-END CONTRACT for cancel/capability loss, whose Pointer arm takes the ABNORMAL end since 2026-08-29 (architect: A TOUCH THE SYSTEM TAKES AWAY IS NOT A CLICK UNLESS IT MOVED — a moved drag finalizes, an unmoved press commits nothing, the drag-modal gate un-latches either way; the finger's own lift is the one clean end left); touch.md is authoritative. EVERY CHROME BUTTON ACTS AT THE LIFT (`AppState::ChromePress`: arm tracks the pointer, lift re-hits the published rect and re-asks every gate; modifiers read at the press); the four menu anchors keep the press (reasoned exception). THE HOLD-REPEATING BUTTONS are the four cardinal arrows (2026-08-16, reversing their 2026-08-13 deletion) and, since 2026-08-26, the WAVEFORM MAGNIFICATION PAIR (membership = `kToolbarChords`' `repeats` column, never a second list): first fire at `kHoldBeatMs`, later fires at the COMPOSITOR'S advertised rate read per fire (0 = no repeat), the first fire the burst's undo opener and a fired burst suppressing the lift act; the burst rides the arm and its edge inventory is at `AppState::ChromePress`. ALL PRODUCT HOLD THRESHOLDS READ `kHoldBeatMs`=575 (gui_input.h — the chrome shift hold, the touch region hold, the hold-repeat's first fire and the Android backend's key-repeat delay, and, since 2026-08-27, the DOUBLE-CLICK WINDOW `kDoubleClickMs` and, since 2026-08-28, the UNDO TAP-COALESCE WINDOW `kTapCoalesceMs` — neither a hold, both the same beat asked of a second tap; the readers' one inventory is at the constant; 575 is the architect's labwc `<repeatDelay>` matched BY CONVENTION, and on Wayland the key delay itself stays `wl_keyboard.repeat_info`'s, never hard-coded). The menu row: File / Edit / Settings dropdowns over ONE popup state (`AppState::Dropdown`), hover-crossing switches, the armed menu-row mode, items never grey; the Edit menu (2026-08-20) is the FIVE propagate commands' one pointer home, IconCopy/IconPaste deleted with the move (a RELOCATION under the no-second-road doctrine). All text proper capitalization, never a double space. Ctrl+Shift+Tab means the PAIRED MARCH in every state and hold-repeats in every state (the `h` view marches its diff-flag cycle); Ctrl+Tab stays one-shot everywhere. Bare 1/2/3 are absolute view selectors; digits 4..9 unbound. EVERY SHIFT-ENABLED GESTURE WHOSE BARE FORM HAS A BUTTON ADMITS THAT BUTTON'S SHIFT-CLICK AND ITS LONG PRESS, AND THE TOOLTIP'S SECOND LINE NAMES THE ACT (architect 2026-08-28) — membership is `redesign_button_shift_admits` alone (the modal row's own copy is `player_button_shift_admits`) with a static_assert binding line2-presence to it, so the admission and the advertisement are one edit; a shift chord that is somebody's own plain button act needs no admission (Redo, Walk previous, Walk both tabs carry theirs in `kToolbarChords`' `shift` column). SHIFT+S DROPS A PHASE RESET FROM ANY VIEW (architect 2026-08-28, `is_phase_reset_drop_key`, shift-exact, one-shot, read-only-blocked and `h`-refused like bare `s`): the act `drop_phase_reset_in_target_view` runs `switch_active_audio_view_to('T')` then `switch_active_markers_view_to('P')` — a refused target entry stopping the whole press, the absolute selectors' own rule — and then the ONE lead-in drop, which is why the order is switches-then-drop (the body offsets by N/2 OUTPUT samples, and the column switch's selection clear must precede the drop's own single-select); ONE undo entry, the drop's, tagged T/P/the standing tab; the bottom row's Drop marker button joined `redesign_button_shift_admits` with it, so glass reaches it by long press.

### Messaging (→ messaging.md)

EVERY MESSAGE IS STATE OR AN EVENT (architect design 2026-08-29), and the split decides the surface. STATE — what is true right now, replaced as it changes, never timed out and never cleared by a key press — lives in ROW 8'S STATE CELL, right of the clock: `| sep | kTransportSepToClockPx | 02:42.608 | kTransportSepToClockPx | <state text> |`, the clock's own separator-to-digits distance read a second time as the digits-to-text distance (ONE constant, no new number), no separator between them, the row's ordinary sans ink at the row's size on the row's own baseline, CLIPPED (no ellipsis, no wrap) where the right-hand button cluster begins less one `icon_row_pad_x`. ITS TWO STRINGS are the process line and the `h` walk line, THE WALK LINE WINNING while the view stands (the recorded coexistence). THE ROW YIELDS WHOLE TO A MODAL, so the text is hidden under a prompt, a dialog editor, the player or the picker and returns when it closes (architect-accepted; it is why the player's load-under-a-running-render refusal is a CARD). Damage is `Viewport::invalidate_status_cell_area`, THE ROW'S LANE WHOLE (the cell reserves no width), and its callers are the progress line's writers alone — every readout-only call is deleted. THE STATUS BAR LASTED ONE DAY (2026-08-29): a tenth lane on the window's foot, 1+31+1 on the row-7 crop, two cells with the resolved `~=` readout at its right; the architect ruled it off that evening — the state text belongs on the toolbar he already reads and the bar duplicated that panel's own foot — and the lane, `status_bar_area` / `status_bar_content_area`, `kStatusBarContentPx`, the three accessors, `paint_status_bar` and the window-foot seam `kRedesignBottomLine` all went with it, the SEAM FOR GOOD on a corrected provenance ("it duplicates the xfce4-panel border and was never a kdenlive border"). THE RESOLVED READOUT RETIRED WITH IT and so did its global Ctrl+C: nothing displays a resolved value now — BARE `j` copies the focused marker's value to the system clipboard and SHIFT+`j` jumps to the marker it came from on the OTHER A/B tab (`run_center_command` → `switch_active_tab_view_to` → `Selection::set_single_selection` on the returned source index → `land_playhead_on_marker` → `run_center_command`, in that order — FIVE ACTS since the architect's 2026-08-29 evening pass, "with the c on the current tab first to ensure it gets done": the leading `c` frames the tab being LEFT on the reference and, by its own focused land, rests that tab's playhead there (the A/B audition's shape), the switch's coincidence auto-select may seat the entering tab's parked marker, and the single-select by INDEX overwrites it so the act ends on the exact marker the value came from, coincident frames included, with the trailing `c` framing the tab that landed — both tabs end framed, each on its own half of the pair; `run_center_command`'s inventory is FIVE CALLERS, this one calling twice), both read-only-legal, both refused in the `h` view, both silent on an ineligible focus, with ONE bottom-row button **Copy resolved value** (the verb group's seventh, after the Measure since that same pass — it sat after Toggle inherit for the afternoon before — `IconCopyValue` / edit-copy, `redesign_button_shift_admits`' seventh member, tooltip *"Copy resolved value (j)"* / *"Press Shift to jump to defining/previous marker."*) carrying the pair at its lift and its shift-click or long press; the parser's composer is `resolved_marker_payload` — the payload and the SOURCE MARKER'S INDEX, no prefix and no provenance (granted frozen touch, architect approval 2026-08-29) — under the renamed gate `payload_eligible_marker`. The TAB ROW'S STATUS CHAIN, its four tiers and `status_chain_pad_x` were deleted at the bar's landing — the tab row paints tabs. EVENTS — an act answered with a sentence, or a background act the user was not watching finishing badly — are NOTIFICATION CARDS in a top-right stack under row 1, two classes: NORMAL (the `dialog-information` glyph, `kNotificationMs` = 5 s, hover pausing the clock) and CRITICAL (the `dialog-error` glyph, the four checkpoint verdicts, standing until its X). THE X, AND ONLY THE X, DISMISSES, on both hosts and above every veil; the text WRAPS DOWNWARD under the two icons to `kNotificationMaxLines` (3), the last line taking the remainder and clipping, the glyph and the X at the first line's height and every internal distance the ONE derived pad `notification_pad_px()` = the icon row's box margin (architect 2026-08-30, the strictness arc's steps 1–2; the room from row 1 to the bottom row is the stack's damage bound and its clip); THE STACK IS UNCAPPED AND THE QUEUE IS RETIRED (step 3): every card is visible from its push, `notification_capacity` = the room's capacity in ONE-LINE cards (20 on the laptop, 11 on the tablet — a count, the model layer having no font to measure with), a push past it bumps the OLDEST NORMAL card that is not the one just pushed, critical cards are never bumped, and what the count cannot see (multiline bursts, a critical-only overflow) clips at the room's foot; a path names its basename. THE DISMISS-ONLY ERROR NOTICE RETIRED WHOLE 2026-08-30 (architect), the pre-split modal for a sentence the user had to be shown: its two synchronous callers split by the rule — the ITERATION SWEEP'S CELL CAP is a NORMAL CARD with its sentence unchanged, and the TARGET-VIEW ENTRY GATE (which STAYS: the preview must never receive a map the builder refuses) refuses SILENTLY the way its twin the load restore always did, one stderr line `Target view entry refused: <error>` and nothing on screen, because its refusals are unreachable from program-written input and the tripwire that would catch a builder/resolver disagreement is the preview's own `Target render failed` card; `DialogTrigger` is three kinds now and EVERY PROMPT IS A QUESTION. A RENDER'S COMPLETION IS NOT NOTIFIED ("that would get annoying"), questions stay modals, and the silent refusals are the consumed no-ops plus that gate — the player's load under a running render left that list the same evening and says "Cannot load in place while a render is running" on a card, the progress line it used to lean on being hidden under the player's own modal. THE CARD STACK'S MARGINS ARE ONE NUMBER since that day too: `kPanelPadPx` above, to the right, and (since the room) above the bottom row alike.

### The GitHub Recheck (→ github-recheck.md)
Bare `h` toggles the READ-ONLY HISTORY VIEW: the marker lane paints only the viewed checkpoint's delta (green added / red removed), `,`/`.` walk the piece's whole eligible history — uncapped, background-prefetched (`GuiHistoryPrefetch`), LOAD-GATED by `load_commit_sidecars_strict` (membership gates nothing but membership; an empty walk opens at 0/0 and Save-and-Commit is live there). The source's parent folder under the clone's `projects/` is the project directory — the whole bootstrap rule. TWO COMPARE READINGS (iterative = forward toward now, the default; cumulative = vs the frozen live side; bare `u`, session bit `history_cumulative`, never serialized); the WALK selector is the ICON ROW'S OWN RADIO PAIR on bare `g` (`HistoryWalkGit` / `HistoryWalkSession` over the one switch owner `set_history_reading`; Local = the session's undo/redo timeline as states, `GuiHistoryLocalWalk`, both stacks frozen while the view stands) and ROW 3 IS THE A/B TABS IN EVERY STATE — Ctrl+Tab switches them inside the view too, needing no re-bind, and Ctrl+Shift+Tab IS THE MODE'S OWN PAIRED MARCH there (the mode's Tab act, the tab switch, the mode's Tab act again, over `cycle_history_diff_flag_focus`), lit on the bottom row and hold-repeating in every state. `'` RAISES THE LOAD CONFIRMATION ON THE VIEWED MEMBER (architect 2026-08-29, R42, retiring the one-day history picker as "overengineered; I don't really need it" — `,`/`.` already walk the members and the lane shows the one they stand on): "Load `<member label>` in place?" OK/Cancel with Enter answering OK, the render player's own prompt body with a second subject, and its OK loads that member in place (Remote: the store's SHA, strict sidecar load via RAII scratch; Local: the member number, typed snapshots) — one undo entry on top, mode closes; an empty walk refuses at the allowlist, which is also what greys the icon row's Load in place button here. Mode-local multi-selection over the diff flags + CTRL+H REVERT (each flag's inverse applied to the live store, fully manual and always forcing, one undo entry, closes the view). CTRL+S IN THE MODE IS SAVE AND COMMIT: the commit-title editor, then `GuiSaveOps::save`, then the STRICT-SANCTIONED seven-step act on a background worker (single in flight, every save refused while publishing; failures decide on the advisory return or an observation, success only on an observation; failures raise a CRITICAL NOTIFICATION CARD — top-right, persistent until its X, no clock (messaging.md; the tab row's permanent critical chip until 2026-08-29); NO in-app retry, a failed push is fixed in the terminal). The head delta greys the act when empty. LOCAL GIT ONLY: `run_git_capture` (reads) / `run_git_mutate` (add/commit/push, the one fenced writer); `projects_repo` guard (the DEVICE CONFIG's key since 2026-08-28) at init and again before the push, the push URL pinned into the child's own config. THE VIEW OWNS NO NAVIGATION STATE: no entry snapshot, no exit restore, no edge that frames — entering and leaving disturb nothing, which is what lets one tab stand framed wide while the other stands zoomed; THE TRIM BAR SHOWS THE TAB'S REAL TRIM in every state (the display-only diff-span substitution is deleted, so paint equals hit), and its double-click runs the ORDINARY `run_span_framing_command` — the view's one framing gesture, its own two framing owners deleted with the substitution; playback is removed from the view whole.

### Validation Topology (architect-blessed 2026-07-17)
Every guard classifies under docs/engineering/validation_topology.md — five classes, one owner each; a duplicate predicate needs an (a)/(b)/(c) justification; an error arm exists iff a producer exists. A new guard that fits no class is a design smell to resolve, not a row to force.

### Type Names / Naming Symmetry
Marker and related GUI types carry the `Gui` prefix; new marker-side types follow. Warp and phase reset are co-equal axes: new pipeline surfaces ship both columns or record the asymmetry in a comment at the site. "Phase reset" is one concept token, never shortened to "reset" in names; warp is never the unmarked default (recorded-asymmetry inventory in conventions.md).

### Rounding
`std::nearbyint` for banker's rounding wherever fractional values convert to POINTS ON A GRID (authored frames, lattice columns, sample indices). No epsilon nudge. For authored positions, `snap_authored_frame` (app_state.h) is the single conversion chokepoint; no other site may round or cast an authored position. SCREEN PIXELS ARE CELLS, NOT POINTS (architect 2026-08-25, landed 2026-08-26): a pixel x covers [x, x+1), so a surface coordinate names the pixel that CONTAINS it — floor, through the ONE owner `containing_pixel` (input_core.h), which every pointer, touch and capture coordinate passes through on its way to an int; before that owner the ABSOLUTE mouse path truncated (`wl_fixed_to_int`) while the finger AND the captured-pointer ledger (the nav drag's virtual position and its release write-back) rounded (`nearbyint`) — the mouse and finger one pixel apart on the 10px endcap bands and the 8px crossing; the ledger's move from rounding to floor is the one LAPTOP-visible consequence (≤1px on the grab-pan's delivered coordinates and on a motionless click right after a capture release), accepted as the rule's own reach into a continuous coordinate. The cell/point distinction is the whole rule; no site restates it.

### Comments
Comments describe current behavior and rationale only. No dead-process citations (Brief/chunk/Stage/spec tags or "per the brief" prose).

### Grep Before Asserting an Invariant
Before claiming "every site that does X" or "no other caller does Y" inside a brief or a code change, grep it — AND AT RETELLS, NOT ONLY AT CREATION: an inventory retell RE-GREPS membership, never edits the inherited list in place. Structural fix preferred over discipline: an exhaustive enumeration lives at exactly ONE authoritative site per concept, and every other site states only its own class plus a pointer.
