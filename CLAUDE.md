# Warptempo GUI

Custom C++23 in-process PGHI phase-vocoder GUI (Arch / labwc / Wayland / JACK) for time-warping audio toward historically-informed tempos. Single developer/user (the architect); one authoring laptop. PROJECT STATUS: FEATURE COMPLETE (architect, 2026-07-25). The authoritative architecture rulings live in `docs/engineering/architecture/` — see the docs map below. This file carries the build/process rules, short per-topic digests, and pointers; the topic files are the authoritative detail. When a ruling lands, update the topic file (committed) and this file's digest ONLY if the headline changes — history, quotes and successions belong in the topic files and in git, never here. (Cut down from a ~190KB digest-of-everything on 2026-08-16; CUT A SECOND TIME on 2026-09-02 from ~125KB, the regrowth being appended histories the topic files already carry — the full old text is in git history. A digest here is THE HEADLINE THAT STANDS TODAY: the rule, its one owner's name in code, the pointer to the topic file.)

## Build Commands

```bash
# Configure (from project root, first time or after CMakeLists changes)
cmake -B build -S . -DWARPTEMPO_BUILD_CLI=ON

# Build (no CMAKE_BUILD_TYPE is set, so asserts stay live; -O3 -march=native -ffp-contract=off is always on)
cmake --build build -j$(nproc)

# Debug build into a separate dir; never reconfigure `build/` itself with -O0
cmake -B build-debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug -j$(nproc)
```

```bash
# Android (the tablet): cross-build + APK, statically against android/prebuilt
bash android/app/build_apk.sh        # then: adb install -r android/app/build-android/warptempo.apk
```

`build/warptempo_gui` is the GUI binary and the default build's only product. No library archives: four per-directory source-list variables (`WARPTEMPO_AUDIO_IO_SOURCES`, `WARPTEMPO_PARSER_SOURCES`, `WARPTEMPO_ENGINE_SOURCES`, `WARPTEMPO_PREPOST_SOURCES`) each binary compiles directly — object-code duplication accepted deliberately. One opt-in binary, default OFF: `warptempo_cli` (`-DWARPTEMPO_BUILD_CLI=ON`, the headless insurance render, byte-identical to the GUI by shared stages; a headless host builds it with `-DWARPTEMPO_BUILD_GUI=OFF`). `tools/` holds standalone one-shot utilities (own CMakeLists, no link path from product targets). `-ffp-contract=off` is load-bearing: renders are byte-reproducible across rebuilds and `-march` choices under the same toolchain (scope in `docs/engineering/perf_campaign_2026_07.md` §15).

THERE IS ONE BUILD HOST, the authoring laptop, and ONE GLASS HOST, the Android tablet (Galaxy Tab S10 FE; the Pi rig is returned — "from the rig" in a source comment is the PROVENANCE of a ruling taken on that panel). The laptop builds BOTH: the Wayland product and the Android APK over the same portable GUI (`platform-seam.md`). A laptop build proves compilation and the pointer/keyboard path only; the tablet is adb-drivable (install, launch, screencap, single-touch injection, logcat), so a coder CAN verify paint and one-finger touch on the device — multi-touch and feel stay the architect's glass pass. Linux binaries are not portable (`-march=native`); the APK is.

## Dependencies

pkg-config: `fftw3` (+`fftw3_threads`), `wayland-client`, `wayland-cursor`, `wayland-protocols`, `xkbcommon`, `cairo`, `cairo-ft`, `harfbuzz`, `jack`; plus `wayland-scanner` at build time. All audio container I/O is in-tree and WAV-only, so the non-GUI sources need only `fftw3`. C++23 (`std::expected` in the parser), `-Wall -Wextra`. Do not introduce Pango, SDL, GTK, GLFW, or wlroots-specific protocols — the GUI is libwayland-client only (`wayland-cursor` the canonical exception). Generated protocol stubs: xdg-shell, xdg-decoration (REQUIRED at startup) and THE THREE OPTIONAL PROTOCOLS — pointer-constraints-unstable-v1 + relative-pointer-unstable-v1 (the strip-drag pointer capture; absence degrades to clamped absolute motion) and presentation-time (`wp_presentation`, bound at init and REQUESTING NOTHING until the AV Sync Stats panel arms it — the gate is the feature). THE PLAYBACK LINE CARRIES NO COMPENSATION (architect 2026-09-03, on a blind A/B at his rig, rolling back the 2026-09-01/02 arc): the predictor anchors at the PUBLISH instant and extrapolates on the clock, so the line leads the sound by the output latency less the compositor's paint-to-light delay plus the JACK pickup phase, and he chose that over every compensated build — no heard offset, no display lead, no natural-end hold, and NO LATENCY MEASUREMENT ANYWHERE UNTIL ASKED FOR (`platform-seam.md`'s playback bullet is the record; the mechanism is in git alone). THE MEASUREMENTS LIVE BEHIND `Help → AV Sync Stats` AND RUN ONLY WHILE IT STANDS (architect 2026-09-03, the same ruling's other half — no instrument may measure unrequested): the JACK port latency and buffer are read ON DEMAND, `wp_presentation` requests no feedback and drives no frame with the panel down, and the panel's net line spells in words how far ahead of the sound the line runs; the tablet answers 'not available on this backend' rather than inventing a Bluetooth estimate. WHAT THE ARC LEFT BEHIND IS KEPT AND IS NOT COMPENSATION: the audio-thread race fixes (the command packet's seqlock, the session word's generation, the terminal qualified by the word the gate acquired), the CYCLE-STAMPED RESYNC (an accuracy device — a resync's step is the accumulated drift alone), and the Wayland SINGLE-COMMIT ORDERING (frame request → damage → attach → one commit, kept by his ruling although the feedback it was landed for is gone). The required-globals set is five, `wl_data_device_manager` (system clipboard, core protocol) included; EVERY `wl_output` is bound and the window's own output (`wl_surface.enter`/`leave`, the most recent enter) supplies the refresh the tick and the fallback read.

Text: proportional text measures AND paints through the ONE shaping chokepoint `src/gui/text_shape.{h,cpp}` (contract in its header) on the one sans face (fontconfig "sans", 12pt × gui_scale); the row-8 CLOCK, the render player's modal clock and THE AV SYNC STATS PANEL'S LINES are the product's monospace surface (fontconfig "monospace", normal weight) — one face, THREE CELLS since 2026-09-03, the panel the architect's own widening of a scope that had been the clocks' alone; THE PLAYER'S AND THE PICKER'S LISTINGS ARE SANS at the redesign size (they wore the monospace for the few hours of that evening and he reverted them himself; the face forks on `folder_overlay::text_listing`, which forks the row pitch with it). The set is enumerated at `gui_font.h` and paint_handler.cpp's bottom-row text block. TEXT IS UTF-8 IN FREE TEXT AND ASCII IN GRAMMARS: editor buffers and free-text settings values take UTF-8 verbatim and round-trip byte-identically; every structural grammar (marker labels, numeric spellings, settings keys/vocabularies, canonical-line payload, the MARKER MEASURE suffix ` //<measure>` on both sidecars — `src/parser/marker_measure.h`) stays ASCII with one canonical spelling per value. Editors index by byte but step by CODEPOINT; no font fallback, no bidi, graphemes out of scope; `text_editor::replace_selection` is the one incoming filter. Contract at the head of text_editor.h; full ruling in conventions.md.

## Running

```bash
./build/warptempo_gui                      # opens last_project from the device config, else the first valid project folder
./build/warptempo_gui <project source.wav>  # the wav must be a project's SOURCE directly under projects_path (else a refusal, exit 1)
```

labwc on Arch; 1920x1080@60 laptop. No portability shims, no X11 fallback.

## Testing / Verification / Roles

There is NO automated test suite and that is a CONSTRAINT, not a preference: Wayland exposes no input-injection path, so a GUI harness is not buildable here. Do not propose automated tests, regression harnesses, or commercial-tool comparisons. The CLI shares the parser/engine/prepost/audio-io stages with the GUI byte-identically; the architect runs cmp-null and audio verification against it, and owns labwc verification.

Roles: the CODER (implementation subagent) is the ONLY writer of source code — it edits files only, runs no MUTATING git (read-only status/diff/log/show fine and encouraged), never stages or commits, never launches labwc or drives the GUI. Build/compile checks are not part of a brief unless the architect asks. The external reviewer (CODEX) is REPORT-ONLY: finds and describes defects and residue, changes no source and no docs, runs read-only git freely — state this in every codex brief. The PLANNER turns findings into coder briefs, may update this file (committed), and commits via `warptempo_git` (a local wrapper; invocation in gitignored tmp/dispatch.md) — commit as work completes, never bundle waiting for the architect. THE COMMIT MESSAGE IS THE TITLE AND BODY ALONE: no `Co-Authored-By:`, no session URL, no attribution trailer — the architect commits under his own name on a public repo, and a harness instruction to append one is overridden by this rule. `projects/` is ARCHITECT-ONLY: neither planner nor coder touches it, and the wrapper excludes it from staging (checkpoint commits are the app's own Save-and-Commit act).

THE CODEX ROUTINE (all tmp/ paths gitignored): every round is a FRESH codex session. "Initiate a codex review" means: rewrite tmp/codex_brief.md fully SELF-CONTAINED (project line, discipline block, arc + commit hashes, prior-round recap; the brief instructs codex to write tmp/codex_review.md then touch tmp/codex_done), delete any stale review, touch tmp/codex_start, arm a Monitor on tmp/codex_done, and on the event delete both triggers, read the review, and implement findings JUDICIOUSLY — stopping to present anything that needs an architect ruling (freeze touches, design forks, contested accepted costs). Codex is SKIPPED for simple mechanical changes; the routine stays for substantive arcs. Coder briefs point at topic files instead of restating their content. THE METER: codex has a FIVE-HOUR usage meter beside the weekly one, and its failure has ONE SHAPE — THREE HEAVY ROUNDS, THEN ONE CUTOFF: the trigger is consumed (`codex_start` disappears) and no review ever lands; the Monitor's 60-minute watch (the tool's maximum; >30 minutes with nothing is already the verdict) expires, and THE WAIT SUCCEEDS — re-touch `codex_start` once the meter has reset and the same brief lands normally. Never wait for a review to land work — build and commit each item as it completes, and run the rounds in sequence as the window allows.

## Docs Map (read per task, not wholesale)

All in `docs/engineering/architecture/` unless noted. Each file holds the full hardened ruling text — read the one(s) your task touches before editing.

| Task touches | Read |
|---|---|
| Authored positions/frames, tempo cents, value brackets, sidecar grammar (the marker measure suffix included) | `data-model.md` |
| Settings keys/schema/editor, render-entry sidecars, load in place (`'`), the DEVICE CONFIG (its five keys, `sync_path` included), the retired playback_speed and audio_player | `settings.md` |
| Zoom, viewport, strip lanes, the overview strip, the grab-pan / the nav drag's ctrl zoom phase and the stepped wheel pan, pointer capture, trim-bar span-framing double-click, playback scanner | `zoom-viewport-strip.md` |
| The kdenlive redesign (row-by-row protocol, per-row records, the hard-coded-color/sans/text-shape/gui_scale standing rulings, the menu row + quit button) | `kdenlive-redesign.md` |
| Resolver normalization, red flags, marker walls/stores, always-set trim + commit rules, undo coalescing, adversarial load taxonomy, tripwires | `normalization-and-boundaries.md` |
| Pointer reach/modifiers, trim bar router, hit geometry, displayed paint basis, damage ownership | `pointer-hit-testing.md` |
| Selection model, marker click land, the marker drag, undo/redo restore visuals, shift/ctrl clicks, the never-parked rule + coincidence auto-select, the retired span form | `selection-model.md` |
| Flag shapes, lane text occlusion, stems, focus model, flag/BPM/settings editors, the MARKER MEASURE box and its editor, the score-video experiment's sunset record, double-click, key repeat, Space | `marker-ui.md` |
| The trim region overlay (the region IS the trim), the sweep and its retired minimum-width floor, scrub area, the Esc bindings, `[`/`Shift+[` | `region-scrub-esc.md` |
| The GitHub recheck (`h` history mode, the diff lane, `,`/`.` walk, `projects_repo`, load-in-place-from-a-commit) | `github-recheck.md` |
| The folder overlay (the ONE list panel in the on-screen keyboard's band and its THREE contents: the player mode over `render/` and `tmp/` with the modal transport row, the play-scrub, the load-by-button road and the retired `audio_player`; the Open project picker; and the AV Sync Stats panel, whose measurements run only while it stands), the click-activates ruling and the modal alignment rule | `render-player.md` |
| Touch (wl_touch binding, the one-finger pointer translation, the disambiguation window, the phone-model pan, the region hold, the two-finger pan/zoom, the hard-end contract, the out-of-scope list) | `touch.md` |
| The platform seam (`GuiPlatform` per backend over the ONE portable `GuiInputCore`; the loop contract; pixel containment's owner; the playback split; the font owner; `gui_main`; the Android stubs, the Java sliver, the APK build, the freeze posture, the tablet's facts) | `platform-seam.md` |
| Home-view binding, the bare Up/Down tempo cent step (singleton + group), propagate paste | `tempo-and-home-view.md` |
| Parser phase-reset compilation, engine geometry, trim prepost stage, render dispatch/cancel/reuse | `render-pipeline.md` |
| Modality (all seven editor kinds, the field-less picker), the keyup retirement record + the third clause (content acts at the press when its identity is certain; the certainty audit), strict modifier validation, the alt vocabulary, bare-`e` mouse key, type names, warp/phase-reset naming symmetry | `conventions.md` |
| Messages: row 8's state cell (state), the notification cards (events), what is notified and what is silent | `messaging.md` |
| Any color, ground recolors, disabled/trim/line color classes | `src/gui/render.h` palette block (code comments are authoritative) |
| Any guard/validator (add/move/remove) | `docs/engineering/validation_topology.md` |
| Engine performance (closed campaign; retired candidates need new data) | `docs/engineering/perf_campaign_2026_07.md` |
| The retired waveform antialiasing (technique record + reinstatement seed) | `docs/engineering/waveform_antialiasing_retired.md` |
| User-facing behavior reference | `docs/HELP.md` |

Trim spans several files by nature: store/commit rules in `normalization-and-boundaries.md`, endcaps/router/geometry in `pointer-hit-testing.md` and `marker-ui.md`, the prepost render window in `render-pipeline.md`, the waveform overlay (which IS the trim, derived) and its show/hide in `region-scrub-esc.md`.

## Architecture Rules (MUST follow — digests; the topic files are authoritative)

### Freeze Status (architect-declared 2026-07-12)
`src/engine/`, `src/parser/`, `src/audio_io/`, and `src/prepost/` are under PERMANENT HARD FREEZE: surgical changes only, each requiring explicit architect approval to reopen. `src/cli/cli_main.cpp` is freeze-adjacent (same discipline). Only GUI work remains. EVERY GRANTED FROZEN TOUCH RECORDS ITS GRANT at the site and in the commit message ("(architect approval YYYY-MM-DD)"). A retired perf candidate needs new measured data to re-propose (perf_campaign_2026_07.md).

### Authored Domain (→ data-model.md)
Authored positions (marker times, trim bounds) are whole source frames in `int64_t` everywhere at rest; `snap_authored_frame` (app_state.h) is the ONLY double→authored conversion route. Authored tempo is 100-based integer CENTS in [25, 400]; every other value is a bracketed double via `value_format.h`. One serialization domain (`frame_format.h`): canonical integer spelling only — anything else is adversarial load-fatal. Walls are exact integer compares on the shared inclusive [0, total−1] domain. Sources: WAV-only, 16/24-bit PCM, ≥44100 Hz, stereo-only.

### Pixel Anchoring, Viewport, Zoom (→ zoom-viewport-strip.md, kdenlive-redesign.md)
Position gestures anchor to the painted column grid through `authored_frame_at_column` → `snap_authored_frame`; walls win over the grid everywhere. Waveform width floors to a multiple of 16px; `clamp_viewport_start` is the ONE viewport chokepoint, the zoom-level clamp too. Zoom is continuous in [1.0, 17.0]; `c` = working zoom 2.0; `0` = full zoom out, stamping the level it leaves into `zoom_recall_level` and setting the tab's `whole_song_visible` bit, which `clamp_viewport_start` re-derives so the S/T flip and the resize keep the whole song visible; at the ceiling `c` runs at that stamped level. THE TOP STRIP IS SEVEN LANES (menu row / tab row / icon row / overview strip / trim bar / ruler / marker lane) plus ONE unified bottom row on the window's foot carrying the STATE CELL right of its clock; the vertical stack has ONE owner in main.cpp (`kWaveformMaxHeightPx`, waveform midpoint = window midpoint), and THE FLEXIBLE GAP 1 OPENS ABOVE THE MENU ROW, which sits on the tab row and paints the gap in its own ground as one tall lane with its content at the foot (architect 2026-09-03; `strip_row_rect`'s inset for every top lane, `top_flex_gap_area` the band, `paint_menu_row` its painter), gap 2 staying below the waveform. THE NAVIGATION SURFACE (one owner `point_on_nav_surface`) takes ONE vocabulary: PLAIN DRAG = the grab-pan at the 8px crossing; MOTIONLESS PLAIN CLICK = playhead placement at the RELEASE; SHIFT+DRAG = the trim sweep (a playhead sweep alone in `h`); CTRL+DRAG = that drag's ZOOM mode, ctrl live mid-drag, horizontal, RIGHT = IN at `nav_zoom_px_per_level()` (`kNavZoomPxPerLevel` × the scale factor) about a frame seated at ctrl-down. EVERY PRESS-ROAD LENGTH IS AUTHORED AND SCALED through `scaled_px` — drag gate, double-click slack and touch slop ONE number, pushed in at `GuiInputCore::set_touch_slop_px` — while DURATIONS NEVER SCALE and the pinch, a dimensionless ratio, takes no scale term. PLAIN WHEEL = the magnification step, ALT+WHEEL = the stepped pan, CTRL+WHEEL = one zoom level per detent, every other modified wheel a swallowed no-op; the right and middle buttons bind nowhere. THE OVERVIEW STRIP (lane 3) takes drag-the-outline, click-teleport at the press (the same press arming that pan) and drag-the-bounds on `kTrimEndcapGrabPx` bands shared with the trim endcaps and the waveform overlay, a bound dragged by its own band and nowhere else; THE THIN LANES ignore second fingers. THE TRIM SHOWS IN THE OVERVIEW TOO: a 1px line on the lane's top row from the column containing the trim begin through the column containing the trim end INCLUSIVE (`overview_trim_span`, through `overview_column_containing`), in the trim bar body's own `kTrimLaneBar` as a second reader, hidden at a full window, pointer-inert, and owed no damage of its own since every trim write already raises `invalidate_waveform_area`, whose rect contains the lane — AND THE TRIM REGION OVERLAY SHOWS THERE TOO (architect 2026-09-04): while it stands, the lane's ground and bars over the same span take the region pair, the LINE the always-on presence and the RECOLOR the emphasis, the LINE alone hidden at a full window while the RECOLOR takes the whole content band there (`overview_region_span`, the span owner's second face) so the lane recolors exactly what the waveform recolors, both pointer-inert. THE PLATE RENDERER is aliased-only ARGB32, per-column raw min/max over a peaks pyramid, one synchronous full render per user-driven frame; THE MAGNIFICATION IS ONE MULTIPLY AT THE TIP MAPPING through the ONE GAIN OWNER `waveform_magnification_gain` (`2^level`, capped ×16), the level a member of the plate fingerprint and `OverviewBarCache`'s key — ONE GAIN ON EVERY WAVEFORM PICTURE. EVERY PAN SUPPRESSES FOLLOW AND THE CENTERED PIN for the session (`scroll_viewport`, one bit `follow_overridden_for_session`). THE CENTERED VIEWPORT (bare `y`, `IconCentered`): while lit the camera derives from the playhead at the STANDING zoom through the ONE body `Viewport::derive_centered_viewport`, superseding follow during playback and leaving it untouched otherwise, while THE A/B AUDITION DISREGARDS IT (`centered_pin_engaged`); read-only-legal, `h`-admitted, persisted as `centered`. All view switches take the synchronous rebuild; bare 1/2/3 are absolute view selectors composing the `t`/`p` handlers, and THE ICON ROW (28 members in 6 groups since 2026-09-04) carries TWO VIEW LAMPS that are those two chords' twins — `IconAudioView` and `IconMarkerColumn`, one button per AXIS since the architect collapsed the row's three radio pairs on 2026-09-04, each lit AWAY FROM HOME (target, phase reset) and wearing the lit state's glyph.

### Render Pipeline (→ render-pipeline.md)
The parser is the sole authored→engine compiler (phase resets emit `S − N/2`, no render-time offset anywhere); geometry in `engine_geometry.h`. THE T+P LEAD-IN IS DERIVED, NOT CHOSEN: the engine is window-CENTRED and coherence at the drop point holds iff the reset is ≥ N/2 ahead, so `kPhaseResetLeadInSamples` = kN/2 (app_state.h — the ONE quantity the drop, Space's inverse launch and the band's maximum read, a static_assert pinning it to two hops), the derivation's one prose home at the drop's comment and its sub-pixel rounding residue accepted. THE OVERLAY BAND IS DERIVED TOO: left edge the marker, right edge the SEED CENTRE + N/2 on the hop lattice (`phase_reset_seed_frame_index`), both edges mapped columns through `displayed_column_at`; S+P stays a stub. Trim is a prepost stage: full maps → `plan_trim` (refusal = render untrimmed, never a failed render) → trim-ignorant engine → `post_trim` → unconditional limiter → encode. No subprocesses in the render path. Ctrl+Alt+R is TWO commands selected by the iteration-mode bit; Ctrl+Alt+Shift+R archival is a consumed no-op in iteration mode. THE TWO OUTPUT FOLDERS: the DELIVERABLE lands in `<project>/render/` (ONE path owner, the parser's `render_output_directory`, shared by the CLI) AND NOTHING ELSE LIVES THERE — `prune_render_folder` (renders_dir.h) deletes every regular `.wav`/`.fingerprint` whose stem is not the current title's at ONE TRIGGER, the deliverable's publish, so the mirror and the prune enforce ONE definition (no prune in the CLI, the recorded asymmetry) — while the DISPOSABLE batch cells live in `<project>/tmp/` (`kBatchFolderName` / `project_batch_root`) and a wav in the project root is the legacy-layout refusal. A render dispatch kills the running render unconditionally; killed sessions never publish Success. THE TARGET PREVIEW HONOURS ITS OWN GENERATION (`dirty_generation_`, `dispatched_generation_`, `in_flight_generation_`): `ensure_ready` never redispatches an identical render and `complete_successful_buffer` DISCARDS a superseded Success — "no wrong audio" outranks a stale-but-present picture. Previews may reuse identical completed renders (`kFingerprintVersion`, render_cache.cpp). THE BPM SWEEP KEEPS THE MAP'S SHAPE: each cell rewrites the span and rescales every ENABLED owning marker outside it by the owner's own change, banker's-rounded onto the cents grid (`bpm_cell_warp_markers`; out-of-bracket refuses, never clamps), and `m` refuses a selected run of differing tempos. A DISABLED MARKER IS INVISIBLE TO THE ACT, IN THE SPAN AND OUT: `section_end_index` bounds on render PARTICIPATION, the cell's loops skip an `effective_disabled` marker, `iter_popup_eligible_marker` is the ONE sweep eligibility — read by the sweep's walk, `iteration_sweep_plan`, the flag painter's bound cells and the arrows' bound step — with its BRACKET KEPT DORMANT (the flag paints the bounds as cells and no editor reaches a dormant bracket — the cells are its only authoring surface and a disabled owner paints none), and `tempo_cent_step_group_verdict` skips disabled and cascade-disabled members.

### Colors (→ render.h palette block)
THE PALETTE IS HARD-CODED WHOLE, no user-settable colors, FULLY OPAQUE (no compositing alphas): every painted surface is a constexpr from its sampled kdenlive/Breeze crop or a recorded derivation; a retune is a recompile. The region highlight recolors the CANVAS GROUND under the ink; the marker class ladder is DISABLED > RED > (selected-bright default); stems read the CLASS alone; disabled markers paint no stem. THE MARKER LANE'S TEXT IS BLACK in every class and state (`kMarkerFlagLabel`; `kRedesignLabel` #fcfcfc stays every other row's ink), the disabled LABEL blending at `kMarkerDisabledLabelMix` while every disabled SHAPE keeps `kMarkerDisabledMix`, and SELECTED TEXT IS `kRedesignAccent` UNDER `kRedesignLabel` on every text surface. `kRedesignAccentInactive` replaces the accent on the overlay's highlighted row and the modal row's focus outline while the window is unfocused — one helper `accent_for_focus`. colors.conf and its machinery are deleted. Codex is skipped for simple color tuning.

### Settings (→ settings.md)
`app.engine_settings` is typed truth; all three boundaries are strict (whole-file schema load shared verbatim by GUI and CLI; editor red-flash; typed dispatch). `kSettingsOrder` owns write order, `kEngineKeys` membership; adding an engine key = six edits, four files. EVERY key is required — no absent state, no load-side default. GUI-kind keys commit through each key's own gesture chokepoint, history-less; numeric values pin to one canonical spelling each. FIVE KEYS ARE PER-DEVICE, NOT PER PIECE — `gui_scale`, `projects_repo`, `projects_path`, `last_project`, `sync_path` — in the ONE DEVICE CONFIG FILE `$XDG_CONFIG_HOME/warptempo_gui/config` (the app's internal dir on Android): whole-file strict through the shared `scan_key_value_file`, EXACTLY those five and each once (`kDeviceConfigKeys` the writer's order), a malformed file FATAL at startup, written from a PER-BACKEND template on first run (`GuiPlatform::device_config_defaults`; the tablet's scale 225 %, `last_project` and `sync_path` EMPTY — NEITHER TEMPLATE GUESSES A DESTINATION). THE WRITE HAS ONE OWNER, `write_device_config`, which carries the inventory: THREE CALL SITES (`commit_device_setting`, the one body behind the Settings dropdown's device group, where bare Tab completes a filesystem DIRECTORY on the two path keys — `complete_path_value`, dot-names skipped unless the tail asks for them; `apply_gui_scale`; and gui_main's `last_project` write) carrying FIVE KEY COMMITS, and it returns an optional `GuiFailure` its callers print and card, the card blessed 2026-09-04 under the deliberate-press rule; Ctrl+S carries none of them. THE LOCK GOVERNS THE KEYS, NOT THE SURFACE (architect 2026-09-04): the settings editor opens on a read-only tab and `commit()`'s ENGINE-KEY arm is where the lock refuses, with `kTabReadOnlyCard`, while the device keys and the GUI-kind band commit there — bare `;` keeps its own refusal at the keyboard gate. GRAMMARS OWN THEMSELVES IN device_config.h: `is_config_path_value` is the two path keys' ONE owner (`is_projects_path` / `is_sync_path` its faces, the second admitting EMPTY as "not set up on this device"), `is_last_project_name` IS ALSO THE PROJECT MODEL'S MEMBERSHIP RULE — a violation adversarial and fatal, a gone folder LOAD-LENIENT and falling through to the first valid one — and `is_gui_scale_percent` is the ONE range owner. An unknown key is fatal in both products; there is NO migration. PLAYBACK_SPEED IS RETIRED WHOLE with the varispeed path: the render body's increment is `source_rate / output_rate`. `gui_scale` is an integer percent in [50, 350], the product's ONE scale axis (`gui_scale_factor()`), applied live, INSTALLED BEFORE THE FIRST CONFIGURE on both backends; NO COLLISION RULE — the icon row's 993-authored-px walk (28 members in 6 groups) crops below ~993 logical px, so the tablet's 225 clears every icon by 31 logical px and 232 is the fit ceiling, a scale past it shaving the rightmost history icons (the sanctioned casualty). `waveform_magnification_level` is the WAVEFORM PICTURE'S COUNT OF DOUBLINGS in [0,4] (`is_waveform_magnification_level`, settings_file.h — the RANGE shared with the CLI, the GAIN the GUI's own), written ONLY by `apply_waveform_magnification_level`, which the bare chords `=` / `-` (bare is vertical, ctrl is horizontal, on keys and wheel alike), the PLAIN WHEEL, the two icon-row buttons and the editor commit all call; history-less, NOT carried by `'`, NO AUDIO ANYWHERE. `centered` is the centered-viewport lamp's per-piece boolean, required after `follow`, through `set_centered_mode`, persisted on the next Ctrl+S; `center_on_next_marker` IS THE BARE TAB WALK'S FRAMING LAMP, required after `centered`, DEFAULT TRUE, bare `n` through the one setter `set_center_on_next_marker`, history-less, read-only-legal, `h`-refused, NOT carried by `'`, and its ONE reader is `marker_walk_frame`, which the three bare Tab arms call and nothing else does — dark, the walk lands and the camera holds unless the landing was offscreen, where follow's own `follow_scroll_if_needed` pages it in. FRAMING IS NOT IN THE WALK: `cycle_marker_focus` and `jump_playhead_to_focused_marker` each take a REQUIRED `MarkerLandingFrame` (`Center` / `FollowPage`) with NO DEFAULT, so the Ctrl+Shift+Tab PAIRED MARCH — which composes the walk — states `Center` at BOTH of its steps by its own statement and frames both tabs whatever the lamp says, tab A's centring persisting through `switch_active_tab_view_to`'s save (selection-model.md). Render entries carry the same sidecar set a source does; `'` LOADS A RECIPE IN PLACE — the markers and the engine block, exactly what its one undo entry restores, everything else staying live, BOTH SWEEP-MODE BITS included (iteration mode is WHERE YOU STAND, the load no exit route of `wipe_iter_state`). `'` NAMES TWO ACTS, NOT TWO ROADS ONTO ONE: outside `h` it opens the render player, whose Load in place button calls `load_render_entry_in_place`; inside it raises the load confirmation on the VIEWED member.

### Projects (→ `src/gui/project_model.h` head prose, settings.md's device-config section, platform-seam.md; design record in tmp/previous)
A PROJECT IS A FOLDER DIRECTLY UNDER `projects_path`, folder name = project name; THE SOURCE IS DEFINED BY THE SIDECAR STEM AND BY NOTHING ELSE (`resolve_project`): a folder carrying any of `<stem>.warpmarkers/.phaseresetmarkers/.settings` names its source by that stem and `<stem>.wav` must exist, a folder with NO sidecar is a NEW project iff it holds exactly one `.wav`, and anything else refuses with its reason. Validity is folder SHAPE — regular files, exact extensions, directories invisible — never sidecar content, AND A CONFIG-NAMEABLE NAME: every successful open writes the folder name into `last_project`, so a folder `is_last_project_name` refuses is not a project on any road; the rule lives at the MEMBERSHIP (`enumerate_project_names`, inherited by the startup fallback and the picker), the ARGUMENT road asking the same predicate of its own folder name. THE APP ALWAYS HAS A PROJECT OPEN: no argument → `last_project`, else the first valid folder in byte order, else "No project under <projects_path>" and exit 1; an argument must be a project's source directly under `projects_path`; Android calls `gui_main(nullptr)`. `gui_main` IS A LOOP: per-process objects outside (signals, the one `DeviceConfig`, the scale install, `GuiPlatform` + its single `init`, the touch slop, the fingerprint-keyed `RenderCache`), per-project objects inside `run_project`; `gui.run()` returns for quit or REOPEN (`GuiProjectOutcome`), the window and the input core surviving; a dirty tab prompts EXACTLY as Ctrl+Q through one prompt body with `GuiCloseTarget` Exit/Reopen; a running render is killed as any dispatch kills it. FILE → **OPEN PROJECT** IS THE FIELD-LESS PICKER: the folder overlay's SECOND CONTENT — folder names under `projects_path` filtered by `resolve_project` (`build_project_picker_rows`), built at the open and never kept fresh, the band starting on the CURRENT project — over a bottom-row modal whose row is **Cancel** ALONE, a click on a row being the open act; a reopen under a running Synchronize refuses with `kSyncRunning`. THE BAND IS THE WHOLE STATE: bare Up/Down move it, and a row's motionless lift and Enter on the highlight both reach the one `open_project_commit(row)` — the open project a consumed no-op that CLOSES, then the STRICT DRY-RUN `source_load_dry_run` (the real load's failure vocabulary minus the decode, the sidecars present by `sidecar_present`), either refusal carding its reason with the picker STAYING OPEN. THE PICKER IS THE FOURTH `ModalDialogOwner` (state `AppState::Picker`), ranked with the player (prompt > player | picker | stats > editor; the three list owners never stand together, none beside an editor), with its own veil, its own router `route_picker_key` (Ctrl+S consumed as the save, Ctrl+Q the one fall-through; Synchronize reaches the picker only as the File menu's row, which calls the act direct) and one close body `close_picker`; the list is the only road on both backends. CTRL+O IS OPEN (`is_open_project_key`, ctrl-exact, one-shot; the `h` view ADMITS it, and Synchronize, so the File menu has no dead row there — the item is its chord). SYNCHRONIZE TO EXTERNAL STORAGE mirrors the project into THE FOLDER THE DEVICE CONFIG NAMES, `sync_path` — TOLD AND NOT FOUND, an EMPTY key answering `sync_path is not set` on a card and running nothing — onto `<sync_path>/<project name>/`: every regular `.wav` DIRECTLY INSIDE `render/` (the folder's real contents listed at the act: stick = disk by construction, the title no term of the act) and each `tmp/` batch folder AS ITSELF, wav only, both through ONE listing owner `list_wav_files`, copies first (staged and renamed on completion) then deletions scoped to that folder and run only against a FINISHED destination classification; the first failure ends the act on a NORMAL card naming the offending path, the act never creating its own sync root, while A SUCCESS SAYS NOTHING. A MIRROR THAT IS UNSURE DELETES NOTHING — the FIVE rules at the head of external_sync.h. It runs on `GuiExternalSyncWorker` (single in flight, a fifth worker eventfd on both backends); it rides bare `\` and the File-menu row calls the act directly (two roads onto one gated body, the recorded exception to "the item is its chord"); read-only-legal, no playback stop. A RUNNING MIRROR IS STATE AND REFUSES THE QUIT (architect 2026-09-04): row 8's state cell carries `Synchronizing...` while it runs, YIELDING to a render's line — the mirror's line is DERIVED at the cell's ONE reader out of `GuiExternalSyncWorker::is_busy`, below whatever `queue_progress_text` holds (`process_line_text`, paint_handler.cpp), never written into the cell, so a render's line wins exactly while it stands and the mirror's is back the instant it goes; the act's dispatch and completion own the DAMAGE and nothing else, and every close road — Ctrl+Q and every router's fall-through, the File→Quit row, the compositor's X, the tablet's BACK, the picker's reopen — meets ONE gate at `GuiPrompt::request_close`'s head (`close_refused_by_external_sync`, `kSyncRunning` on a card, nothing torn down), because `shutdown` has no cancel and its join must never run under a live window. OPEN ON THE DEVICE: this One UI build mounts the OTG stick with `mountFlags=0`, a path that cannot be CONFIGURED any more than it could be found, so THE TABLET'S `sync_path` STAYS EMPTY and the act refuses there; the SAF/Java-sliver road is the design's named contingency (a tree URI, not a path) and needs an architect ruling. The migration checklist is in HELP.

### The Folder Overlay and the Render Player (→ render-player.md)
THE IN-APP PLAYER FOR THE PROJECT'S RENDERS, built for the CAR: open it, see the renders, start one, without leaving the app. TWO OPENERS, ONE `open()`: bare `l` and bare `'` outside the `h` view (inside it `'` raises the view's own load confirmation — `'` keeps its meaning everywhere), plus the icon-row **Play renders** button, which synthesizes bare `l` AND, on its shift-click or long press, `Shift+L` — THE AV SYNC STATS PANEL, bare `l`'s shifted twin and the letter's other overlay content (`redesign_button_shift_admits`, the tooltip's second line bound to it); bare `'` is admitted on a locked tab while the player's own load refuses it — keys may alias where buttons do not. THE FOLDER OVERLAY (`src/gui/folder_overlay.h`, `GuiFolderOverlay`) is the keyboard-slot LIST PANEL: THE BAND IS THE SLOT'S AND THE ROW IS THE BUTTON'S — the band takes the slot's geometry through `keyboard_slot_band` and STANDS AT A FIXED HEIGHT, FROM THE TAB ROW'S FIRST PIXEL TO THE BOTTOM ROW, the menu row standing above it VISIBLE AND CARRYING THE `h` VIEW'S PARTITION — FILE LIVE, THE OTHER THREE ANCHORS AND THE VIEW BAR DEAD — and WEARING THE TAB ROW'S OWN TOP BORDER, `kRedesignTabLine` at `tab_row_border_h_px()`, painted as the band's first row and taken out of the CONTENT'S room (`folder_overlay::border_h_px` / `content_rect`, which the rows, the scroll ceiling, the keep-visible walk, the painter's row-walk clip and `row_at` read while the surface rect stays the ground, the damage and the band's OUTER CLAIM — a press on the line is consumed and arms no row) (`keyboard_slot_max_height_px` = the bottom row's top less the tab row's; architect 2026-09-03: `menu_anchor_dead_in_mode` asks File's exception ahead of BOTH mode terms, `redesign_button_enabled`'s first arm defers to it for the anchors and greys the rest, the veil exempts a live anchor at `press_on_live_menu_anchor`, `chrome_focused` is `window_activated` ALONE again and the modal term lives at the bar's own `view_bar_focused`), whatever the listing and under all three contents; THE FACE IS THE CONTENT'S — the two listings the SANS, the stats panel the MONOSPACE at the clock's size, `folder_overlay::text_listing` forking the face AND the pitch (architect 2026-09-03, reverting his own monospace-everywhere try of the same evening); each ROW is exactly the icon row's button, every metric read from render.h, and a TEXT row a monospace line (the stats panel's rows stand at the face's measured ascent + descent with no gap), a too-long name RUNNING OFF THE EDGE with no wrap and no ellipsis; `waveform_paint_area` gates on either tenant and clips against `keyboard_slot_painted_standing`. THE LISTING LIVES INSIDE `tmp/`: `Folder` is `{Root, Batch}`, the root IS the batch root, the modal row's UP button greys at the root through `render_player_up_actionable` and UNLOADS THE ITEM on its way out through the ONE ORDERING OWNER `unload_item` — the stop body's fence, the rest act, THE MODE BIT WHERE THE REQUIRED TAIL PUTS IT (`UnloadTail::Up` keeps it up, `::Close` lowers it BEHIND the fence and AHEAD of the re-express, because the fork asks `active` and `complete_successful_buffer` refuses to rebind while the player is active), the VIEW's buffer rebound, then the item's fields cleared to their `open()` values and its buffer freed, `close()`'s own body too, the head unit published after the root entry as the last word over the fork's stale paused (architect 2026-09-04 evening, superseding his pause of that afternoon: "make it like when you first open the player: nothing is loaded" — the row has no stop button and none is wanted, so Up, the one act that LEAVES the item's folder, is where stopping lives; Repeat one survives it, the lamp being cleared by an open alone); each listing is built AT ENTRY and never kept fresh, carries the product's ONE scroll offset (`clamp_scroll`) and is painted and hit through the ONE walk `for_each_row`. A CLICK ACTIVATES: the row press ARMS and its MOTIONLESS LIFT highlights the row and THEN OPENS IT, Enter being the keyboard's click on the highlight while Up/Down walk without opening; no double-click, no ctrl-click, no multi-selection. THE TRANSPORT IS ASKED AHEAD OF THE HIGHLIGHT: `AppState::RenderPlayer::transport` STORES Idle/Live/Paused, PAUSED AT FRAME ZERO IS PAUSED, and `play_button_act` IS HIGHLIGHT-DRIVEN through `render_player_highlight_act_row` — a highlighted folder OPENS, a highlighted OTHER wav PLAYS even mid-play, the item's own row taking the TRANSPORT TAIL `transport_toggle_act`. THE TWO SKIPS ARE HOME AND NEXT TRACK (architect 2026-09-04 evening: "this isn't a playhead, this is audio playback" — the End KEY and the third button play the NEXT WAV of the transport item's own folder from its start, live, paused or idle, OUTRANKING REPEAT ONE, a silent walled no-op at the folder's last wav with the button greyed through `render_player_next_track_actionable`, the walk `advance_to_next_in_item_folder` shared with the natural end's auto-advance), Shift+Home/Shift+End the FOLDER'S ENDS; HOME CARRIES THE PREVIOUS-TRACK WINDOW (`kPlayerPreviousThresholdMs` 3000, an authored duration never scaled, POSITION-based and never press-timed); that walk and AUTO-ADVANCE take the TRANSPORT ITEM'S OWN folder as listed, never another and never a wrap; THE BAND FOLLOWS THE ITEM at every change the transport makes on its own (`play_wav`). THE BOTTOM ROW IS ITS MODAL, the third `ModalDialogOwner`: Home · Play/Pause · End · separator · the PLAY-SCRUB · the monospace clock · separator · Repeat one · Up · Load in place · Close FLUSH RIGHT, the escape sentinel; all seven are GLYPH BUTTONS WEARING NO RESTING BORDER painted by one ladder, Repeat one taking the roster's selected face, and the row has ITS OWN DISABLED FACE through `render_player_button_enabled`, which the router and the Tab ring consume on. THE TWO SKIPS ADMIT A MODIFIED PRESS — shift-click and the `kChromeShiftHoldMs` long press are ONE term (`modal_dialog_press_shifted`), membership `player_button_shift_admits`, advertised by `render_player_button_shift_hint`. THE SCRUB IS A BREEZE SLIDER (metrics and provenance at render.h's scrub block): a press on the track seeks AT THE PRESS, a press on THE HANDLE'S OWN BOX arms the drag whose seek commits AT THE RELEASE, and THE SCRUB RESTS WHILE THE TRANSPORT IS IDLE. THE ALIGNMENT RULE (at `paint_modal_dialog`): a modal carrying a FIELD OR A MESSAGE is flush LEFT with it, a LIST'S buttons flush RIGHT; the row's spacings are ruled off `kTransportSepToClockPx`, `kModalButtonGapPx` and `icon_row_pad_x`. REPEAT ONE is the one sanctioned exception to NOTHING LOOPS, session-only and off at every open; a folder-end rest with it off RESUMES THE LAST ITEM on the next Play. EVERY ROSTER BUTTON BUT THE FILE ANCHOR IS DEAD while it stands (`redesign_button_enabled`'s first arm over `menu_anchor_dead_in_mode`, ONE partition for the `h` view and all three contents) and the veil consumes every press outside the band, the row and that anchor. FILE'S THREE ROWS UNDER ALL THREE: Quit works (the routers' Ctrl+Q fall-through — on the tablet the player's first quit road), Synchronize works (its row calls the gated body direct), Open Project is a silent no-op (its row is Ctrl+O, which every router's catch-all drops). `route_render_player_key` is the whole plastic vocabulary; only Ctrl+S and Ctrl+Q fall through (Ctrl+Q the keyboard's close road on the laptop; the tablet closes by its button or that File row). THE ENGINE ROAD calls the frozen wav reader and never changes it — probe, the device rate/channel EQUALITY (a refusal, never a conversion), the allocation ceiling, the read, THEN THAT SAME EQUALITY AGAIN ON THE DECODED BUFFER'S OWN SHAPE (`wav_read_full` reopens the path, so the probe's answer is stale by construction), then THE FENCE and `rebind_buffer`; the player has ITS OWN LAUNCH BODY, the product's SECOND, recorded beside `launch_playback_window`, while THE STOP stays the one stop body with the player's fork inside it. CLOSE (the button, Esc, `l`, the load's success, `GuiPrompt::request_close`) rebinds the VIEW's buffer BEFORE freeing the item's. LOAD IN PLACE IS A BUTTON OR `'` ON THE HIGHLIGHT AND NOTHING ELSE, on the highlighted BATCH CELL, confirmed by an OK/Cancel prompt RAISED ON ITS FIRST BUTTON so Enter confirms — ONE PROMPT BODY WITH TWO SUBJECTS (`confirm_load_in_place`) and no other prompt in the product (`PromptInitialFocus` a REQUIRED parameter of `PromptState::present`, Cancel last). THE CAR'S MediaSession (mechanism in platform-seam.md): the seam carries `set_on_media_command` and `publish_media_state` (Wayland stores and no-ops them) and `on_media_command` replays the head unit's `GuiMediaCommand` values as THE PLAYER'S OWN KEYS through `synthesize_key` — no second dispatch road — after CLEARING THE MODAL RING, while `SeekTo`, the direction-named Play/Pause/focus-loss arms and `Stop` are DIRECT acts calling `transport_toggle_act` past the highlight fork, and A DEAD OR MISSING DEVICE PAUSES instead of advancing (`GuiPlayback::device_unavailable`), A REFUSED DIRECTIONAL HEAD-UNIT COMMAND RE-PUBLISHES the true state so a drifted display is corrected at the press, and THE PLAYER'S REST SUSPENDS THE AAUDIO STREAM (`GuiPlayback::suspend_stream`, JACK a no-op) AND THE REST IS THE PLAYER'S OWN ACT, NOT THE STOP BODY'S (`GuiRenderPlayer::rest_stream`, its FIVE ROADS `toggle_pause`'s pause arm, the tick's dead-device arm, `on_natural_end`'s TWO rests — the terminal one and the Repeat One arm whose replay refused to decode, a rest being a rest whichever way the player arrived at it — and `unload_item` on both tails): the stop body's player fork is taken by every LIVE-TO-LIVE TRANSITION too — the rebind ahead of another item, the Repeat One replay that SOUNDS, the auto-advance — and a track change that stopped the device would pay the AAudio start's settle wait and the link's own reactivation on an act that must be immediate, so the suspension left that fork the night it landed; the ONE narrowing of the 2026-08-27 no-click ruling, the resume transient and the link's reactivation AFTER A PAUSE the accepted player-only costs; Bluetooth otherwise reads a stream writing silence as an active player and the head unit sticks in pause). THE OVERLAY HAS THREE CONTENTS AND ONE OWNER TAG (`AppState::FolderOverlay::owner` — the player, the Open-project picker and, since 2026-09-03, the AV SYNC STATS PANEL, whose opener is `Shift+L`, the Help row that spells it and the Play renders button's shift press), which IS the standing predicate `folder_overlay_stands` that the painter, all three routers, the wheel context, the waveform paint gate, the touch pan zone and the greying arm all ask, and the open act, the row pitch and the row face are the ONLY things that fork on it; a `FolderOverlayRow::Kind` exists iff a content produces it — `Text`, the retired history picker's kind, is BACK WITH A PRODUCER and is INERT (no hover face, no highlight, no act) while the band still consumes presses and still scrolls. NOT BUILT, by ruling: no notification / service / background playback / lock-screen transport. THE SLIVER MAPS THE MEDIA KEYCODES ITSELF (`onMediaButtonEvent` overridden, ACTION_DOWN with no repeat).

### Load-Lenient, Render-Normalizing Boundaries (→ normalization-and-boundaries.md)
Gestures move freely and NOTHING POPS: ambiguous authored states normalize at the render boundary (`resolve_warp_markers_for_render`; red flags as the display cue), never refused or force-resolved. THE TWO-CATEGORY RULE: a state the GUI can commit always loads and renders; a state the GUI can never produce is adversarial and hard-fails the load, first error only, identically in both binaries. The same family covers the REPOSITORY: sanctioned use is strict-exact intended use; out-of-app git under `projects/`, a detached HEAD or a hung hook get a blunt terminal error and no in-app recovery. TRIM IS ALWAYS SET — a full ordered pair always rests, crossed pairs reset to the full window, the endcap drag clamps inclusively at the partner, and a COINCIDENT RELEASE RESETS TO THE WHOLE SONG through the one owner `auto_clear_crossed_trim` — and THE FULL WINDOW IS SEMANTICALLY THE OLD UNSET STATE through the one predicate `trim_window_is_full`. EVERY route that writes a trim bound clears the selection and parks the playhead at the new trim start (membership at input_trim.cpp's header), and no trim write hides the trim region overlay MID-GESTURE (the family writes the cursor DIRECT and so reaches neither movement owner), the sweep's own end being the hide rule's third clause. THE SWEEP ENFORCES NO WIDTH AT ALL: a stroke authors exactly the span it draws, a coincident release falls into `auto_clear_crossed_trim`, and a merely tiny window rests and renders (honorability belongs to `validate_trim_frames` at the render boundary, whose refusal means render untrimmed, never a failed render). Trim has no undo — `Shift+[` is the recovery. NOTHING LOOPS, WITH ONE SANCTIONED EXCEPTION: every audition plays once to its end (`Viewport::trim_range` is the NAVIGATION range owner); the exception is the render player's REPEAT ONE lamp, which reaches no project audio. A RESTORE PUTS BACK THE WHOLE AUTHORING VIEW: an undo entry records THREE view tags (A/B tab, W/P column, S/T audio view — `UndoEntry::audio_view`) and the restore writes them TAB, THEN THE DATA WITH ITS MAP-CHANGE RE-LAND, THEN THE COLUMN, THEN THE AUDIO VIEW, each axis through its own chokepoint (`switch_active_tab_view_to`, `switch_active_audio_view_to`, `switch_active_markers_view_to`), so NO RESTORE SYNTHESIZES A VIEW THE USER WAS NEVER IN. UNDO COALESCING IS A HYBRID: a held key's or button's synthesized repeats coalesce by repeat identity behind an OPENER that differs per surface (a held key's burst opens with its PHYSICAL PRESS, a held button's with its FIRST FIRE), while rapid physical taps within `kTapCoalesceMs` merge on a standing subject and `record_gesture` stamps only on the accepted path; A SAVE ENDS THE TAP WINDOW (`Undo::note_saved` moves the saved reference and clears the stamp, so a merge can never rewrite the entry the reference rests on); and the merge tail ASKS THE PRODUCERS' OWN QUESTION — a merged burst byte-equal to the live store POPS the entry and clears the stamp (`pop_undo_top_with_saved_ref`), so a tap-wobble that nets to zero leaves no invisible restore and the dirty dot goes out with it. `kTapCoalesceMs` READS `kHoldBeatMs` and restates no number.

### Interaction Model (→ selection-model.md, marker-ui.md, region-scrub-esc.md, pointer-hit-testing.md)
EVERY ROSTER BUTTON IS TRUTHFUL — any time a button would be a no-op it greys: a face arm NEVER restates an act's condition, it reads the predicate the act's own refusal reads (inventory at `redesign_button_enabled`'s head and every enum case), and faces repaint through the per-tick comparator with no damage call at any mutator. THE TWIN RULE: a button with a LIVE modified twin stays ENABLED and greys only when EVERY admitted variant would change nothing, the plain lift reaching the act whose refusal CARDS. THE THREE LAUNCH GATES REOPEN A DEAD STREAM AT THE PRESS through `ensure_device_available_for_play()`, carding only when the reopen fails. THE FACES READ `GuiPlayback::device_absent()` — the never-came-up half alone, so a reopenable stream never greys a launch button (the roster's Play, the audition twin, the player's Play); `device_unavailable()` is the render player's tick's read. ZOOM IN NEVER GREYS. A greyed button's press raises no card; the KEY's refusal does. EVERY PLAIN MARKER CLICK LANDS THE PLAYHEAD, AT THE PRESS through the one act owner `run_marker_click_act`, the plain arm alone arming `PendingMarkerPress`; shift = inclusive range from the anchor/focus, ctrl = membership toggle, both landing on the focus they set and arming nothing, both FLAG-ONLY — ADD TO SELECTION (bare `k` and its lamp) is the STICKY CTRL for glass, SHIFT BEATS IT, and it auto-clears at every `Selection` mutator except the toggle itself. MARKER STEMS ARE POINTER-INERT everywhere; the FLAG BOX is the marker's one pointer surface. EVERY MARKER MAY CARRY A MEASURE on both columns: a strict ASCII score-position token, direct or an offset onto the immediate predecessor's resolved measure, a broken link making it UNRESOLVED — grammar is validity, resolution is not (`marker_measure.h` authoritative) — ALWAYS VISIBLE as a blue box continuing the flag rightward and part of the same hit rect, EVERY MEASURE ITS OWN AND INHERITING FROM NOTHING, edited in the `MeasureText` kind, an EMPTY COMMIT REMOVING and a bad token red-flashing in the box. GRID ITERATIONS SHOW EACH BOUND IN ITS OWN CELL, AND EVERY CELL IS A MINI FLAG (architect 2026-09-04 from the car; 2026-09-05 on the tablet): while the mode is on, every marker the sweep reads extends its flag with two cells — the lower bound, then the upper, then the measure — each painted as the flag box is (own class pair, seam column, black ink) carrying the signed two-decimal bound, THE SIGN THE WHOLE SYNTAX; the cells are part of the marker's one pointer surface (`FlagHitRect`'s three published boundaries, `hit_test_flag_cell` answering the ONE enum `MarkerCell { Payload, Lower, Upper, Measure }`), and EACH CELL HAS ITS OWN EDITOR: a double-click opens the cell under the seed — the flag editor, the BOUND EDITOR (`Kind::IterBound`, `enter_iter_bound_edit`, the seventh kind, painting over its cell at the cell's own seam and never narrower than it; its commit parses the signed two-decimal grammar and refuses the partner bound and the tempo window on a `Range bound rejected` card, an empty commit clearing the whole bracket, the pair written at the ONE write site `iter_bound_step_write`) or the measure editor — and THE FLAG EDITOR CARRIES NO BRACKET (the `+[lo,hi]` grammar is deleted whole; its one contact with the bracket is the carrier-loss clear). THE ADDRESSED CELL IS THE BRIGHT ONE: a selected marker paints its addressed cell in the selected pair and its other cells in the class pair — the payload for every selected marker but the focus, whose addressed cell is ONE SESSION AXIS `AppState::addressed_cell`, written by a marker press's cell in `run_marker_click_act` (all four cells, all three shapes) and by the two cell editors' opens, and RESET TO PAYLOAD BY EVERY OTHER FOCUS WRITE at the one chokepoint `Selection::seat_focus` (the Tab walk, the march, Shift+J, the restores, `p`/Ctrl+Tab, the auto-selects, the flag editor's open) and, for a bound cell, when the mode goes off (`wipe_iter_state`); no underline, no cue of its own. The axis names what bare Up/Down step — the tempo, a bound through the SECOND step body `adjust_iter_bound_cents` (walls = the clamp window and the partner bound via `iter_bound_step_landing`; [0,0] CLEARS BACK TO THE BLANK BRACKET at the write site; the addressed cell joins the coalescing stamp as `IterBoundStep`'s own fourth subject term; singleton clamps silently, group refuses whole; ineligible members skipped, an ineligible singleton carded; no render; `GestureKind::IterBoundStep`, a bracket-only entry), or NOTHING on the measure (`addressed_cell_step_refusal`, `A measure has no value to step`, the arrow buttons greyed) — and what Enter opens (`flag_editor_open_actionable` forks on it; the Edit Flag button is Enter and its tooltip names the cell). THE MEASURE PROPAGATE: Ctrl+/ copies off a contiguous labeled run, Ctrl+Alt+/ pastes onto an anchor through an OFFSET modal — label lockstep, stop at first divergence, one undo entry, no view switch, no render; unlabeled markers never propagate (`warp_marker_propagates`). Shift+/ is unbound. THE PLAYHEAD HAS ONE FORM and always paints; a selection's cue is its members' brightened flags. THE REGION IS THE TRIM: `RegionState` is ONE VISIBILITY BIT and nothing else, the overlay DERIVED from the resting trim bounds every frame through `trim_overlay_span`, so bar, overlay and hit test cannot drift. THE OVERLAY HAS A SECOND SURFACE: while shown, the overview lane recolors the trim's own columns in the waveform's `kWaveformRegionCanvas` / `kWaveformRegionInk` pair under and through its cached bars, off `overview_region_span` (the trim line's `overview_trim_span` wearing its other face — the trim's columns at a sub-window, the whole content band at a full one, no asymmetry with the waveform's overlay), clipped to the content band so the line and the box draw over it — so the strip and the waveform cannot drift either. A SHOWN overlay is manipulable ON THE WAVEFORM ALONE (`region_manipulation_hit`, Y-gated): a bound = that ENDCAP drag, inside = the BRIDGE drag, else pan, their motionless lift falling to the waveform's ordinary click act; shift/ctrl bypass it. THE SWEEP'S FIRST WRITE SHOWS THE TRIM AND ITS COMMIT COLLAPSES IT — `show_trim_region_overlay` the ONE raise, `commit_region_sweep` the one release owner; the sweep writes the trim PER MOTION EVENT under no width rule, the `h` view's shift former is a PLAYHEAD SWEEP carved out of both, and every former drops the selection its surface owns and carries the playhead on its moving end. BARE `[` SHOWS AND HIDES AND MOVES NO CAMERA (architect 2026-09-04 — the show half's framing through `bring_span_into_view` is DELETED, that owner keeping its one remaining caller, the group undo/redo restore; the overview lane answers where the trim is and the trim bar's double-click is the framing gesture; hiding discards nothing); `Shift+[` maximizes to the whole song and is the recovery, trim having no undo. THE OVERLAY HIDES WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES, WHEN A MARKER IS TOUCHED, AND WHEN THE SWEEP ENDS, AND AT NO OTHER TIME: a TRANSLATION is not a movement, a RESTORE is not a movement, THE CAMERA IS NEVER A MOVEMENT, the trim's own surfaces exempt by construction; the calls are the TWO MOVEMENT OWNERS (`Viewport::move_playhead_to`, `land_playhead_on_marker`/`land_playhead_on_source_frame`, hiding UNCONDITIONALLY), `run_marker_click_act` and `commit_region_sweep`, while THE NON-HIDING ENTRY POINTS ARE NAMED, NEVER A FLAG (`Viewport::reseat_playhead_to`, `reseat_playhead_on_marker`). The rule is stated once at `clear_region_highlight`'s declaration, input_handler.h. CURSORS: named XCursor-theme cursors only, seven kinds, ONE zone map derived from the press routers (`pointer_cursor_kind`) resolved once per run-loop iteration; the cursor promises the gesture. READ-ONLY protects the authored musical content only — trim gestures, `[`/`Shift+[`, Ctrl+S and both render chords stay legal on a locked tab (`read_only_key_blocked`). SPACE always plays from the playhead; SHIFT+SPACE IS THE A/B AUDITION: switch to the OTHER tab, play `kAuditionMs` from its resting playhead twice, switch back, play the same span twice — four BOUNDED plays through the one launch body `launch_playback_window`, no resting playhead moving by the act's own doing, each half opening with `run_center_command`, PACED by `kAuditionPairGapMs` and `kAuditionSwitchGapMs` (a retune touches only their declaration in app_state.h) and SEQUENCED WITH NO TIMER OF ITS OWN off the run loop's deadline tick (`GuiAbAudition::fire_if_due`); `phase != Idle` is the whole refusal test, A REST IS INTERRUPTED EXACTLY AS A PLAY IS, and THE ACT IS ONE TRANSPORT SESSION from its first play to its last, so bare Space stops it through the one stop body. THE ACT ENDS WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES — the overlay-hide rule's own membership — the clear living at FOUR OWNERS inventoried at `GuiAuditionSequence`, and a DEVICE LOST MID-ACT ENDS IT ON THE TICK (architect 2026-09-04): `GuiAbAudition::fire_if_due` asks `device_unavailable` ahead of its waiting and deadline tests, so a play stalled on a dead stream ends as a rest does — through the one stop body, a caller of owner (1) and not a fifth owner — carding `kPlaybackDeviceUnavailableCard`, the launch gate's own sentence; read-only-legal. Home/End jump to the TRIM bounds, CTRL+HOME / CTRL+END to frame 0 / the domain's last frame TRIM-BLIND (`playhead_skip_landing_frame`), the skip buttons' CTRL-CLICK being that chord (`redesign_button_ctrl_admits`). HORIZONTAL MOVEMENT IS A FOCUS ACT — groups are never moved; singleton steps CLAMP at walls, group presses REFUSE whole (position_nudge.h). Bare arrows nudge the FOCUSED marker in its home view; every focus-changing route lands the playhead. THE SELECTION IS NEVER PARKED: `p`/Ctrl+Tab clear it; coincidence auto-select at the four entry chokepoints (`auto_select_marker_at_playhead`). Hit geometry rides the DISPLAYED basis (plate_viewport_basis / item_viewport_basis); DAMAGE FOLLOWS THE BASIS OF THE PIXELS IT ERASES. POINTER GESTURES HAVE NO CANCEL: Esc mid-gesture is a consumed no-op, any end commits, undo is the mitigation. BARE `z` IS THE RESTRICT UNDO TO VIEWPORT LAMP (`IconRestrictUndo`, closing the viewport-class group behind the Center on Next Marker lamp — "it is also a viewport gesture"), a SESSION-ONLY bit off at every launch and in no settings vocabulary: while it stands, an undo or redo whose restore would move the viewport is a consumed no-op that cards and leaves both stacks byte-identical, the verdict having ONE owner — `undo_restore_within_viewport` (the entry's three view tags first, source view always inside, else the restore's touched span judged by `span_columns_visible`) — read by the refusal, both buttons' faces and their tooltips' reasons, MEASURED UNDER THE MAP THE RESTORE INSTALLS (`proposed_display_context` of the entry's own snapshot and engine block; `source_frame_to_domain` the one forward-translation body, its context a parameter) with the restore's singleton arm sharing `span_columns_visible`, the re-land's own keep-visible scroll the one camera writer it does not measure; read-only-legal, `h`-refused; the one exception to the whole-view restore rule, refusing rather than restoring a partial view. BARE ESC IS BOUND IN NINE PLACES AND NOWHERE ELSE (enumeration at input_handler.cpp). The keyboard stop rule is authoritative at `stop_playback_if_playing`'s declaration.

### The Home-View Binding (→ tempo-and-home-view.md)
`active_column_authoring_allowed` answers `markers_view == 'P' || audio_view == 'S'` — warp markers author in SOURCE view only, THE PHASE-RESET COLUMN AUTHORS IN BOTH AUDIO VIEWS, and the S+P drop takes NO LEAD-IN at all (the drop body's one fork: the target arm keeps the kN/2 output-sample subtraction, the source arm seeds exactly at the cursor). What the predicate gates is the POSITIONAL family IN T+W alone — the `s` drop, the flag drag, the Left/Right nudge, the `m` bpm open — the keyboard refusals carded, the pointer refusals silent (the gesture's non-event is its own answer). Ctrl+D and Delete ask `marker_selection_verb_actionable` = a standing selection. SHIFT+S IS THE CROSSING, not an exception: it switches to T+P and then runs the one lead-in drop, REFUSING WHOLE IN THE P COLUMN before any switch, and the Drop button never greys past the lock (the twin rule: one half is always live). W+target has no pointer authoring gesture that MOVES anything — its pointer vocabulary is the measure and payload double-clicks, both editor opens. Inventory at the predicate, app_state.h.

### Modality and Input (→ conventions.md)
ICONS ARE UP, HOTKEYS ARE DOWN; TOUCH USES ICONS, PLASTIC USES BOTH; NO DISAGREEMENT. Every control classifies once and keeps ONE timing everywhere: HOTKEYS ACT AT THE PRESS (`GuiInputHandler::on_key`, the one ranked dispatch), ICONS ACT AT THE LIFT whoever operates them — bare Enter/Space on a focused modal button is the keyboard operating an ICON, bare `e` the keyboard operating the POINTER. THE THIRD CLAUSE: CONTENT ACTS THE MOMENT ITS IDENTITY IS CERTAIN — at the PRESS where the press can only mean one thing, at the motionless RELEASE only where the SAME press arms a drag (conventions.md carries the certainty audit). THE MODAL SURFACE IS THE BOTTOM ROW: while a prompt, a dialog editor, the render player, the picker or the AV Sync Stats panel stands, the row yields whole and `paint_modal_dialog` paints into it — one renderer, one window, one frame; modal buttons act at the LIFT, a prompt answers only after it has painted (`PromptState::painted`), PUBLISHED GEOMETRY MAY ONLY SELECT and LIVE STATE DECIDES (the owner-tag doctrine at `ModalDialogGeometry`), and the veil consumes every press outside the surface. All seven editor kinds admit exactly their own keys plus bare Esc, Ctrl+S, Ctrl+Q through ONE `route_modal_editor_key` contract; the flag editor is keyboard-modal but pointer/wheel-transparent and stops no playback (`keyboard_modal_editor_active` vs `modal_dialog_editor_active`). EVERY EDITOR FIELD TAKES THE PHONE'S DRAG ON BOTH DEVICES (architect 2026-09-04): a press seats the caret, a drag whose press is younger than `kEditorSelectHoldMs` (= `kHoldBeatMs`) at the drag-gate crossing MOVES THE CARET, one at least that old SELECTS from the press, shift always sweeps — one arm, `EditorTextDragState::form`, no device branch (marker-ui.md, touch.md). THE CLIPBOARD IS THE SYSTEM CLIPBOARD (Ctrl+C is unbound globally; the editors and the AV Sync Stats panel own it as text surfaces). STRICT MODIFIER VALIDATION: a modifier combination with no explicit binding is a no-op everywhere, three families deliberately untightened (`is_tab_cycle`, Ctrl+Z's shift bit, the editors' motion arm). SUPER IS DROPPED AT THE PLATFORM BOUNDARY. ALT IS THE FIVE Ctrl+Alt RENDER/PROPAGATE CHORDS AND ONE POINTER BINDING, Alt+wheel; no alt-modified press binds anything. BARE `\` IS SYNCHRONIZE (bare-exact, one-shot, read-only-legal, `h`-admitted). Bare `e` is the left mouse button, translated at the platform boundary — never bind it in dispatch. TOUCH IS THE POINTER: wl_touch capability-gated, one finger translated into pointer deliveries behind the disambiguation window (`kTouchRegionHoldMs` in the pan zone, 60ms/8px elsewhere), two fingers = seated-pivot pinch zoom ONLY, ONE HARD-END CONTRACT for cancel/capability loss — A TOUCH THE SYSTEM TAKES AWAY IS NOT A CLICK UNLESS IT MOVED; THE PAN ZONE YIELDS WHERE A POINTER CLAIM LIVES, five clauses in `touch_point_in_pan_zone`'s body (the cards, the shown trim overlay, the OPEN EDITOR'S BOX, the keyboard's band, the folder overlay's band) over the flag boxes' own carve-out; touch.md is authoritative. EVERY CHROME BUTTON ACTS AT THE LIFT (`AppState::ChromePress`: the lift re-hits the published rect and re-asks every gate, modifiers read at the press); the four menu anchors keep the press, the reasoned exception. THE HOLD-REPEATING BUTTONS are the four cardinal arrows and the waveform magnification pair (membership = `kToolbarChords`' `repeats` column, never a second list): first fire at `kHoldBeatMs`, later fires at the COMPOSITOR'S advertised rate read per fire, the first fire the burst's undo opener, a fired burst suppressing the lift act. THE ARROW STEP LADDER: shift = 3, ctrl = 10, bare = 1 — one owner `arrow_step_magnitude` (gui_input.h) on the arrows' EVERY subject; the arrow buttons are the roster's first DUAL admission and GLASS REACHES NEITHER RUNG; held repeats carry their modifier; A WALL NO-OP RUNS BEFORE THE COALESCE STAMP on every surface (`Undo::coalesce_gesture`). ALL PRODUCT HOLD THRESHOLDS READ `kHoldBeatMs` = 575 (gui_input.h, the readers' one inventory at the constant: the chrome shift hold, the touch region hold, the hold-repeat's first fire, the Android key-repeat delay, `kDoubleClickMs`, `kTapCoalesceMs`), matched BY CONVENTION to the architect's labwc `<repeatDelay>`, while on Wayland the key delay itself stays `wl_keyboard.repeat_info`'s, never hard-coded. THE MENU ROW: File / Edit / Settings / Help dropdowns over ONE popup state (the ITERATIONS anchor deleted 2026-09-04, its two commands back as the icon row's own group of two — BPM Iterations a command button on bare `m` whose face reads `bpm_sweep_plan`, the one owner of `m`'s ten-arm refusal ladder, Grid Iterations a lamp on bare `i`), the row taking the `h` VIEW'S PARTITION under every folder-overlay content since 2026-09-03 (FILE LIVE — Quit and Synchronize working, Open Project a silent no-op — the other three anchors and the view bar dead; `menu_anchor_dead_in_mode` is the one owner for both modes, the veil exempting a live anchor at `press_on_live_menu_anchor`) — SETTINGS IS LAST OF THE APPLICATION'S OWN MENUS AND HELP FOLLOWS IT, kdenlive's own order (architect 2026-09-03, restating the former Settings-last rule); Help's one item `AV Sync Stats` IS ITS CHORD, `Shift+L` (architect 2026-09-03 evening — bare `l` the render player, `Shift+L` the panel, THE OVERLAY'S TWO CONTENTS ON ONE LETTER; `is_av_sync_stats_key` shift-exact and one-shot, `toggle_av_sync_stats` beside bare `l` in `handle_mode_keys`, the same chord the panel's own closer, read-only-legal, `h`-carded, and the **Play renders** button's shift-click and long press the glass road), so THE ITEM IS ITS CHORD holds for every command row and `GuiPopupAct` is back to `Chord` and `SyncExternal` (`AppState::Dropdown`), hover-crossing switches, items never grey; the Edit menu is the five propagate commands' one pointer home. THE TEXT CONVENTION IS ONE AND IT IS KDENLIVE'S (stated in code at paint_handler.cpp's capitalization block, the speller's head in gui_input.h owning key spelling): KEY NAMES are Qt's, BARE LETTERS UPPERCASE, SHIFTED PUNCTUATION BY ITS CAP, modifiers Ctrl+Alt+Shift, TWO CASE CLASSES SINCE 2026-09-03, kdenlive's own (the block in paint_handler.cpp is the owner): a NAME — a dropdown item, a modal row's word button, a panel title, a tooltip that NAMES a glyph button's act — is TITLE CASE, and a DESCRIPTION — a label, a field prefix, a state-or-reason tooltip, the second modifier line, a card, a prompt, the row-8 state cell — is SENTENCE CASE, so the dropdown items are an instance and no longer the one declared exception; TOOLTIPS NEVER LIE — `redesign_button_tooltip`'s stateful overload forks on the predicate the act or the face already reads, a LAMP TOGGLE reads "Toggle <Mode>", and a MODIFIER LINE exists exactly where the modified press does something different IN THAT STATE (the overload may drop a line the table carries, never add one); AN APPENDED REASON IS LOWERCASE (`lowercase_initial`), a card is ONE CLAUSE, a prompt is a QUESTION, names are SINGLE-QUOTED. Engineering docs, this file and code comments keep the literal keysym in code font. Ctrl+Shift+Tab means the PAIRED MARCH in every state and hold-repeats in every state; Ctrl+Tab stays one-shot everywhere. Bare 1/2/3 are absolute view selectors; digits 4..9 unbound. EVERY SHIFT-ENABLED GESTURE WHOSE BARE FORM HAS A BUTTON ADMITS THAT BUTTON'S SHIFT-CLICK AND ITS LONG PRESS, AND THE TOOLTIP'S SECOND LINE NAMES THE ACT — membership `redesign_button_shift_admits` alone (the modal row's copy `player_button_shift_admits`), a static_assert binding line2-presence to it. SHIFT+S DROPS A PHASE RESET FROM ANY VIEW (`is_phase_reset_drop_key`, shift-exact, one-shot, read-only-blocked and `h`-refused like bare `s`): `drop_phase_reset_in_target_view` switches to T then to P — a refused target entry stopping the whole press — and then runs the ONE lead-in drop; ONE undo entry.

### Messaging (→ messaging.md)
EVERY MESSAGE IS STATE OR AN EVENT, and the split decides the surface. STATE — true right now, replaced as it changes, never timed out and never cleared by a key press — lives in ROW 8'S STATE CELL as ONE MONOSPACE RUN right of the clock; the CLOCK is never clipped, the STATE clips with no ellipsis and no wrap, and its two strings are the process line and the `h` walk line, the walk line winning while the view stands. THE ROW YIELDS WHOLE TO A MODAL, which is why the player's load-under-a-running-render refusal is a CARD. Damage is `Viewport::invalidate_status_cell_area`, THE ROW'S LANE WHOLE. NOTHING DISPLAYS A RESOLVED VALUE: bare `j` copies the focused marker's value to the system clipboard and SHIFT+`j` jumps to its source marker on the OTHER A/B tab, both read-only-legal, both refused in the `h` view, both CARDING an ineligible focus, with ONE bottom-row button **Copy Resolved Value** carrying the pair at its lift and its shift-click or long press; the composer is `resolved_marker_payload` under the gate `payload_eligible_marker`, whose three-way `payload_eligibility` the face and the tooltip read too. EVENTS ARE NOTIFICATION CARDS in a top-right stack under row 1, two classes: NORMAL (`kNotificationMs` = 5 s, hover pausing the clock) and CRITICAL (the four checkpoint verdicts — never timed out and never bumped). THE X DISMISSES ON THE POINTER above every veil, AND BARE ESC IS ITS BULK KEYBOARD TWIN at the tail of its own ranking, clearing EVERY card of any class. The text wraps downward to `kNotificationMaxLines` (3), every internal distance the ONE derived pad `notification_pad_px()`. THE STACK IS UNCAPPED AND THE QUEUE IS RETIRED: `notification_capacity` counts the room in ONE-LINE cards (20 on the laptop, 11 on the tablet), a push past it bumps the OLDEST NORMAL card, critical cards never. A PATH NAMES ITS BASENAME through structured failure data — `GuiFailure {diagnostic, display}` (failure.h), stderr taking the diagnostic with full paths and the card the display clause, nothing parsing composed English. THE TARGET-VIEW ENTRY GATE STAYS and refuses SILENTLY on one stderr line: the preview must never receive a map the builder refuses. A RENDER'S SUCCESSFUL OR CANCELLED COMPLETION IS NOT NOTIFIED, but A FAILED ARCHIVAL RENDER cards its reason AND DELETES THE PREVIOUS DELIVERABLE PAIR in `render/` (the standing file is stale by construction; a cancel leaves it), a failed batch cell removes its own partial files, and a short sweep cards "Rendered N of M". THE SILENT REFUSALS ARE GONE: every consumed no-op says its sentence on a NORMAL card — the gates at on_key's head firing only for a chord BOUND IN THE STANDING MODE (`chord_is_bound`, the lock the OUTERMOST refusal) and naming it through the ONE speller `spell_chord` (`every_bound_key_is_spellable` pins the table at compile time), and the freeze-adjacent ops returning a `GuiOpRefusal` that `card_op_refusal` raises. WHAT STAYS SILENT: bare Esc, the defensive arms, a greyed button's lift (the grey is the message), the target-view gate, every success, every BENIGN ONE-DIMENSIONAL REFUSAL ALREADY AT ITS STATE, and A BOUND KEY'S REFUSAL WHOSE REASON ROW 8'S PROCESS LINE ALREADY NAMES (architect 2026-09-04, that class's second member — the target preview's two play gates, Space's `preview_ready` edge and the scrub's `is_updating` one, whose shared card is deleted while both chords stay BOUND) — while a MULTI-PLACE command keeps its card even where its button greys. ONE CARD PER PRESS at the outermost site that has the reason, EACH DELIBERATE PRESS STACKING ITS OWN while a held key's or button's repeats refresh one (`HeldRepeatDispatchScope` the one seam); the card floor is 272 authored px. UNBOUND KEYS SAY NOTHING — bound keys act or card, so silence identifies unbound, the process-line silence above its ONE RECORDED EXCEPTION. THE CLIPBOARD SUCCESSES CARD, the one success class whose result nothing paints; the SAVE'S SUCCESS stays silent while ITS WRITE FAILURE CARDS through one composer in save_ops.cpp. THE SUCCESS RULE IS ONE SENTENCE: a success is silent when the screen shows what happened truthfully, and cards when nothing shows or when what shows would mislead — the PRODUCED-NOTHING PASTES keeping their card but STANDING STILL (`applied_pairs == 0` the one test). THE PLAN_TRIM FALLBACK CARDS: a sub-window whose target span rounds below one output sample renders the WHOLE piece under a hairline trim bar (`kTrimFallbackCard`), ONE verdict owner `compute_buffer_start_frame_for` with three readers, raised on the FALLBACK-SUBJECT EDGE (`TrimFallbackSubject {fell_back, tab, begin, end}`) at the preview's dispatch and at the two archival chords' press; the two SWEEPS keep the stderr line alone. THE CARD STACK'S MARGINS ARE ONE NUMBER, `kPanelPadPx`, and THE CEILING IS AUTHORED AND SCALED — `kNotificationMaxWidthPx` 640 through `scaled_px`, the window less the margins a SAFETY under it and never below the floor.

### The GitHub Recheck (→ github-recheck.md)
Bare `h` toggles the READ-ONLY HISTORY VIEW: the marker lane paints only the viewed checkpoint's delta (green added / red removed), `,`/`.` walk the piece's whole eligible history — uncapped, background-prefetched (`GuiHistoryPrefetch`), LOAD-GATED by `load_commit_sidecars_strict` (an empty walk opens at 0/0 and Save-and-Commit is live there), AND LOCAL HISTORY IS THE FALLBACK WHEN GIT CANNOT BE ASKED (architect 2026-09-04, from the car): a bootstrap the REMOTE walk cannot answer no longer refuses `h` — the view OPENS ON THE LOCAL WALK, everything that walk supports working exactly as when `g` chooses it, so the tablet, which carries no git binary, has a working history view; ONE PREDICATE, the entry's stored verdict and no git re-run (`history_remote_walk_available` over `GuiHistoryDiff::available`, six readers inventoried at it): bare `g` toward the remote walk is refused at the one switch owner with the bootstrap's reason on a card and the lamp greys on it, Save and Commit greys through `history_checkpoint_actionable` and Ctrl+S cards the same, the entry raises no card (its stderr line alone), the now side is captured above init's refusal arms, and the failed-scan arrival's closer carves out a fallback visit; on the laptop nothing changes. The source's parent folder under the clone's `projects/` is the project directory — the whole bootstrap rule. TWO COMPARE READINGS (iterative = forward toward now, the default; cumulative = vs the frozen live side; bare `u`, session bit `history_cumulative`, never serialized); the WALK selector is the icon row's own lamp `HistoryWalk` on bare `g`, lit on Session, over the one switch owner `set_history_delta` (Local = the session's undo/redo timeline as states, `GuiHistoryLocalWalk`, both stacks frozen while the view stands); ROW 3 IS THE A/B TABS IN EVERY STATE, Ctrl+Tab switching them inside the view too and Ctrl+Shift+Tab being the mode's own PAIRED MARCH (`cycle_history_diff_flag_focus`). `'` RAISES THE LOAD CONFIRMATION ON THE VIEWED MEMBER (the render player's own prompt body with a second subject; Remote loads the store's SHA by strict sidecar load via RAII scratch, Local the member number's typed snapshot) — one undo entry on top, the mode closing; an empty walk refuses at the allowlist, which is also what greys the icon row's Load in place button here. Mode-local multi-selection over the diff flags + BARE `v` REVERT (each flag's inverse applied to the live store, fully manual and always forcing, one undo entry, closing the view). CTRL+S IN THE MODE IS SAVE AND COMMIT: the commit-title editor, then `GuiSaveOps::save`, then the STRICT-SANCTIONED seven-step act on a background worker (single in flight, every save refused while publishing; failures decide on the advisory return or an observation, success only on an observation; failures raise a CRITICAL notification card; NO in-app retry, a failed push is fixed in the terminal). The head delta greys the act when empty. LOCAL GIT ONLY: `run_git_capture` (reads) / `run_git_mutate` (add/commit/push, the one fenced writer); the `projects_repo` guard at init and again before the push, the push URL pinned into the child's own config. THE VIEW OWNS NO NAVIGATION STATE: no entry snapshot, no exit restore, no edge that frames — entering and leaving disturb nothing; THE TRIM BAR SHOWS THE TAB'S REAL TRIM in every state, so paint equals hit, and its double-click runs the ORDINARY `run_span_framing_command`; playback is removed from the view whole.

### Validation Topology (architect-blessed 2026-07-17)
Every guard classifies under docs/engineering/validation_topology.md — five classes, one owner each; a duplicate predicate needs an (a)/(b)/(c) justification; an error arm exists iff a producer exists. A new guard that fits no class is a design smell to resolve, not a row to force.

### Type Names / Naming Symmetry
Marker and related GUI types carry the `Gui` prefix; new marker-side types follow. Warp and phase reset are co-equal axes: new pipeline surfaces ship both columns or record the asymmetry in a comment at the site. "Phase reset" is one concept token, never shortened to "reset" in names; warp is never the unmarked default (recorded-asymmetry inventory in conventions.md).

### Rounding
`std::nearbyint` for banker's rounding wherever fractional values convert to POINTS ON A GRID (authored frames, lattice columns, sample indices). No epsilon nudge. For authored positions, `snap_authored_frame` (app_state.h) is the single conversion chokepoint; no other site may round or cast an authored position. SCREEN PIXELS ARE CELLS, NOT POINTS: a pixel x covers [x, x+1), so a surface coordinate names the pixel that CONTAINS it — floor, through the ONE owner `containing_pixel` (input_core.h), which every pointer, touch and capture coordinate passes through on its way to an int. The cell/point distinction is the whole rule and no site restates it — but a SURFACE may declare which class its own axis is: THE OVERVIEW LANE IS A CELL LANE (it partitions the song into `lane.w` columns of `spp` frames, so a frame names the column CONTAINING it), its tick and its viewport box sharing `floor(s / spp)` through the lane's own owner `overview_column_containing` (app_state.cpp; a second APPLICATION of the rule, `containing_pixel` staying the pointer axis's), the box's right edge the column containing the last visible frame plus one, so the tick is inside the box whenever the playhead is inside the viewport; the inverse road returns the cell's origin, the exact inverse of a floor. The waveform lattice stays POINTS.

### Comments
Comments describe current behavior and rationale only. No dead-process citations (Brief/chunk/Stage/spec tags or "per the brief" prose).

### Grep Before Asserting an Invariant
Before claiming "every site that does X" or "no other caller does Y" inside a brief or a code change, grep it — AND AT RETELLS, NOT ONLY AT CREATION: an inventory retell RE-GREPS membership, never edits the inherited list in place. Structural fix preferred over discipline: an exhaustive enumeration lives at exactly ONE authoritative site per concept, and every other site states only its own class plus a pointer.
