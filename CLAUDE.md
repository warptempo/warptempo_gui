# Warptempo GUI

Custom C++23 in-process PGHI phase-vocoder GUI (Arch / labwc / Wayland / JACK) for time-warping audio toward historically-informed tempos. Single developer/user (the architect); one authoring laptop. PROJECT STATUS: FEATURE COMPLETE (architect, 2026-07-25). The authoritative architecture rulings live in `docs/engineering/architecture/` — see the docs map below. This file carries the build/process rules, short per-topic digests, and pointers; the topic files are the authoritative detail. When a ruling lands, update the topic file (committed) and this file's digest ONLY if the headline changes — history, quotes and successions belong in the topic files and in git, never here. (This file was deliberately cut down from a ~190KB digest-of-everything on 2026-08-16; the full old text is in git history.)

## Build Commands

```bash
# Configure (from project root, first time or after CMakeLists changes)
cmake -B build -S . -DWARPTEMPO_BUILD_CLI=ON

# Build (Release is the project default; -O3 -march=native -ffp-contract=off is always on)
cmake --build build -j$(nproc)

# Debug build into a separate dir; never reconfigure `build/` itself with -O0
cmake -B build-debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build-debug -j$(nproc)
```

```bash
# Android (the tablet): cross-build + APK, statically against android/prebuilt
bash android/app/build_apk.sh        # then: adb install -r android/app/build-android/warptempo.apk
```

`build/warptempo_gui` is the GUI binary and the default build's only product. No library archives: four per-directory source-list variables (`WARPTEMPO_AUDIO_IO_SOURCES`, `WARPTEMPO_PARSER_SOURCES`, `WARPTEMPO_ENGINE_SOURCES`, `WARPTEMPO_PREPOST_SOURCES`) each binary compiles directly — object-code duplication accepted deliberately. One opt-in binary, default OFF: `warptempo_cli` (`-DWARPTEMPO_BUILD_CLI=ON`, the headless insurance render — exactly the terminal Ctrl+Alt+R, byte-identical to the GUI by shared stages; a headless host builds it with `-DWARPTEMPO_BUILD_GUI=OFF`). `tools/` holds standalone one-shot utilities (own CMakeLists, no link path from product targets). `-ffp-contract=off` is load-bearing: renders are byte-reproducible across rebuilds and `-march` choices under the same toolchain (scope in `docs/engineering/perf_campaign_2026_07.md` §15).

THERE IS ONE BUILD HOST, the authoring laptop, and ONE GLASS HOST, the Android tablet (Galaxy Tab S10 FE; the Pi rig is returned — "from the rig" in a source comment is the PROVENANCE of a ruling taken on that panel). The laptop builds BOTH: the Wayland product and, since 2026-08-26, the Android APK over the same portable GUI (`docs/engineering/architecture/platform-seam.md`). A laptop build proves compilation and the pointer/keyboard path only; the tablet is adb-drivable (install, launch, screencap, single-touch injection, logcat), so a coder CAN verify paint and one-finger touch on the device — multi-touch and feel stay the architect's glass pass. Linux binaries are not portable (`-march=native`); the APK is.

## Dependencies

pkg-config: `fftw3` (+`fftw3_threads`), `wayland-client`, `wayland-cursor`, `wayland-protocols`, `xkbcommon`, `cairo`, `cairo-ft`, `harfbuzz`, `jack`; plus `wayland-scanner` at build time. All audio container I/O is in-tree and WAV-only, so the non-GUI sources need only `fftw3`. C++23 (`std::expected` in the parser), `-Wall -Wextra`. Do not introduce Pango, SDL, GTK, GLFW, or wlroots-specific protocols — the GUI is libwayland-client only (`wayland-cursor` the canonical exception). Generated protocol stubs: xdg-shell, xdg-decoration (REQUIRED at startup), pointer-constraints-unstable-v1 + relative-pointer-unstable-v1 (the strip-drag pointer capture — the ONLY optional protocols; absence degrades to clamped absolute motion). The required-globals set is five, `wl_data_device_manager` (system clipboard, core protocol) included.

Text: proportional text measures AND paints through the ONE shaping chokepoint `src/gui/text_shape.{h,cpp}` (contract in its header) on the one sans face (fontconfig "sans", 12pt × gui_scale); the row-8 CLOCK is the product's one monospace surface (fontconfig "monospace", normal weight). TEXT IS UTF-8 IN FREE TEXT AND ASCII IN GRAMMARS: editor buffers and free-text settings values take UTF-8 verbatim and round-trip byte-identically; every structural grammar (marker labels, numeric spellings, settings keys/vocabularies, canonical-line payload, the MARKER MEASURE suffix ` //<measure>` on both sidecars — `src/parser/marker_measure.h`, its one-day free-UTF-8 comment predecessor retired 2026-08-20) stays ASCII with one canonical spelling per value. Editors index by byte but step by CODEPOINT; no font fallback, no bidi, graphemes out of scope; `text_editor::replace_selection` is the one incoming filter. Contract at the head of text_editor.h; full ruling in conventions.md.

## Running

```bash
./build/warptempo_gui <source.wav>
```

labwc on Arch; 1920x1080@60 laptop. No portability shims, no X11 fallback.

## Testing / Verification / Roles

There is NO automated test suite and that is a CONSTRAINT, not a preference: Wayland exposes no input-injection path, so a GUI harness is not buildable here. Do not propose automated tests, regression harnesses, or commercial-tool comparisons. The CLI shares the parser/engine/prepost/audio-io stages with the GUI byte-identically; the architect runs cmp-null and audio verification against it, and owns labwc verification.

Roles: the CODER (implementation subagent) is the ONLY writer of source code — it edits files only, runs no MUTATING git (read-only status/diff/log/show fine and encouraged), never stages or commits, never launches labwc or drives the GUI. Build/compile checks are not part of a brief unless the architect asks. The external reviewer (CODEX) is REPORT-ONLY: finds and describes defects and residue, changes no source and no docs, runs read-only git freely — state this in every codex brief. The PLANNER turns findings into coder briefs, may update this file (committed), and commits via `warptempo_git` (a local wrapper; invocation in gitignored tmp/dispatch.md) — commit as work completes, never bundle waiting for the architect. `projects/` is ARCHITECT-ONLY: neither planner nor coder touches it, and the wrapper excludes it from staging (checkpoint commits are the app's own Save-and-Commit act).

THE CODEX ROUTINE (architect-codified 2026-07-22; all tmp/ paths gitignored): every round is a FRESH codex session. "Initiate a codex review" means: rewrite tmp/codex_brief.md fully SELF-CONTAINED (project line, discipline block, arc + commit hashes, prior-round recap; the brief instructs codex to write tmp/codex_review.md then touch tmp/codex_done), delete any stale review, touch tmp/codex_start, arm a Monitor on tmp/codex_done, and on the event delete both triggers, read the review, and implement findings JUDICIOUSLY — stopping to present anything that needs an architect ruling (freeze touches, design forks, contested accepted costs). Codex is SKIPPED for simple mechanical changes (architect 2026-07-27); the routine stays for substantive arcs. Coder briefs point at topic files instead of restating their content.

## Docs Map (read per task, not wholesale)

All in `docs/engineering/architecture/` unless noted. Each file holds the full hardened ruling text — read the one(s) your task touches before editing.

| Task touches | Read |
|---|---|
| Authored positions/frames, tempo cents, value brackets, playback_speed, sidecar grammar (the marker measure suffix included) | `data-model.md` |
| Settings keys/schema/editor, render-entry sidecars, load in place (`'`), audio_player, gui_scale | `settings.md` |
| Zoom, viewport, strip lanes, the overview strip, the grab-pan / the nav drag's ctrl zoom phase and the stepped wheel pan, pointer capture, trim-bar span-framing double-click, playback scanner | `zoom-viewport-strip.md` |
| The kdenlive redesign (row-by-row protocol, per-row records, the hard-coded-color/sans/text-shape/gui_scale standing rulings, the menu row + quit button) | `kdenlive-redesign.md` |
| Resolver normalization, red flags, marker walls/stores, always-set trim + commit rules, undo coalescing, adversarial load taxonomy, tripwires | `normalization-and-boundaries.md` |
| Pointer reach/modifiers, trim bar router, hit geometry, displayed paint basis, damage ownership | `pointer-hit-testing.md` |
| Selection model, marker click land, the marker drag, undo/redo restore visuals, shift/ctrl clicks, the never-parked rule + coincidence auto-select, the retired span form | `selection-model.md` |
| Flag shapes, lane text occlusion, stems, focus model, flag/BPM/settings editors, the MARKER MEASURE box and its editor, the score-video experiment's sunset record, double-click, key repeat, Space | `marker-ui.md` |
| The trim region overlay (the region IS the trim), the sweep and its retired minimum-width floor, scrub area, the Esc bindings, `[`/`Shift+[` | `region-scrub-esc.md` |
| The GitHub recheck (`h` history mode, the diff lane, `,`/`.` walk, `projects_repo`, load-in-place-from-a-commit) | `github-recheck.md` |
| Touch (wl_touch binding, the one-finger pointer translation, the disambiguation window, the phone-model pan, the region hold, the two-finger pan/zoom, the hard-end contract, the out-of-scope list) | `touch.md` |
| The platform seam (`GuiPlatform` per backend over the ONE portable `GuiInputCore`; the loop contract; pixel containment's owner; the playback split; the font owner; `gui_main`; the Android stubs, the Java sliver, the APK build, the freeze posture, the tablet's facts) | `platform-seam.md` |
| Home-view binding, the bare Up/Down tempo cent step (singleton + group), propagate paste | `tempo-and-home-view.md` |
| Parser phase-reset compilation, engine geometry, trim prepost stage, render dispatch/cancel/reuse | `render-pipeline.md` |
| Modality (all seven editors), the keyup retirement record + the third clause (content acts at the press when its identity is certain; the certainty audit), strict modifier validation, the alt vocabulary, bare-`e` mouse key, type names, warp/phase-reset naming symmetry | `conventions.md` |
| Any color, ground recolors, disabled/trim/line color classes | `src/gui/render.h` palette block (code comments are authoritative) |
| Any guard/validator (add/move/remove) | `docs/engineering/validation_topology.md` |
| Engine performance (closed campaign; retired candidates need new data) | `docs/engineering/perf_campaign_2026_07.md` |
| The retired waveform antialiasing (technique record + reinstatement seed) | `docs/engineering/waveform_antialiasing_retired.md` |
| User-facing behavior reference | `docs/HELP.md` |

Trim spans several files by nature: store/commit rules in `normalization-and-boundaries.md`, endcaps/router/geometry in `pointer-hit-testing.md` and `marker-ui.md`, the prepost render window in `render-pipeline.md`, the waveform overlay (which IS the trim, derived) and its show/hide in `region-scrub-esc.md`.

## Architecture Rules (MUST follow — digests; the topic files are authoritative)

### Freeze Status (architect-declared 2026-07-12)
`src/engine/`, `src/parser/`, `src/audio_io/`, and `src/prepost/` are under PERMANENT HARD FREEZE: surgical changes only, each requiring explicit architect approval to reopen. `src/cli/cli_main.cpp` is freeze-adjacent (same discipline). Only GUI work remains. EVERY GRANTED FROZEN TOUCH RECORDS ITS GRANT at the site and in the commit message ("(architect approval YYYY-MM-DD)"). A retired perf candidate needs new measured data to re-propose (perf_campaign_2026_07.md).

### Authored Domain (→ data-model.md)
Authored positions (marker times, trim bounds) are whole source frames in `int64_t` everywhere at rest; `snap_authored_frame` (app_state.h) is the ONLY double→authored conversion route. Authored tempo is 100-based integer CENTS in [25, 400]; every other value is a bracketed double via `value_format.h`. One serialization domain (`frame_format.h`): canonical integer spelling only — anything else is adversarial load-fatal. Walls are exact integer compares on the shared inclusive [0, total−1] domain. Sources: WAV-only, 16/24-bit PCM, ≥44100 Hz, stereo-only.

### Pixel Anchoring, Viewport, Zoom (→ zoom-viewport-strip.md, kdenlive-redesign.md)
Position gestures anchor to the painted column grid and commit through `authored_frame_at_column` → `snap_authored_frame`; walls win over the grid everywhere. Waveform width floors to a multiple of 16px; `clamp_viewport_start` is the one viewport chokepoint (owns the zoom-level clamp too). Zoom is continuous in [1.0, 17.0]; `c` = working zoom 2.0; `0` = full zoom out, STAMPING the level it leaves into the tab's `zoom_recall_level` (session-only, unpersisted), and at the ceiling the `c` command AT THAT STAMPED LEVEL — plain `c` when nothing is stamped, and plain `c` too when the stamp can no longer move the zoom (a ceiling that fell under it). The TOP STRIP IS SEVEN LANES (menu row / tab row / icon row / overview strip / trim bar / ruler / marker lane) plus ONE unified bottom row; the vertical stack has ONE owner in main.cpp (waveform max height `kWaveformMaxHeightPx`, waveform midpoint = window midpoint, both flexible gaps floor at 0; the kdenlive-redesign.md commit-B section is authoritative for the stack). The NAVIGATION SURFACE (whole waveform + ruler + empty marker-lane stretches; the whole waveform + lanes in the `h` view; one owner `point_on_nav_surface`) takes ONE vocabulary: PLAIN DRAG = the grab-pan (capture at the 8px crossing, cursor hidden, notional x wraps edge-to-edge across the waveform), MOTIONLESS PLAIN CLICK = playhead placement deferred to the RELEASE, SHIFT+DRAG = the trim sweep (a direct trim write per motion event; the `h` view's is a playhead sweep alone), CTRL+DRAG = the same drag's ZOOM mode — ctrl is LIVE mid-drag; zoom is HORIZONTAL with RIGHT = IN (rate `kNavZoomPxPerLevel`, the product's one drag-zoom rate), pivot = a song frame seated at ctrl-down with edge clamp-and-rebind, the anchor stem painted and the cursor restored onto it. PLAIN WHEEL = the stepped pan; CTRL+WHEEL = one zoom level per detent; every other modified wheel is a swallowed no-op; the right button is unbound. The OVERVIEW STRIP (lane 3, 24px content) carries whole-song bars + viewport box + playhead tick; its vocabulary is drag-the-outline (pan), click-teleport (outside the box, AT THE PRESS — and the SAME press then arms that pan, so a pointer or finger that keeps moving keeps panning; the grab offset is measured after the teleport, never assumed zero), drag-the-bounds (10px grab bands shared with the trim endcaps and the waveform trim overlay's bounds, `kTrimEndcapGrabPx` — a bound is dragged by its own band and nowhere else); the plain lane off the endcaps wears TrimResize throughout; the THIN LANES (overview + trim bar) ignore second fingers entirely. THE PLATE RENDERER is aliased-only ARGB32, per-column raw min/max on the authoring lattice, 13-rung peaks pyramid, one synchronous full render for every user-driven pan/zoom frame; THE VISUAL MAGNIFICATION IS ONE MULTIPLY AT THE TIP MAPPING (`render_waveform`'s `magnification_level` parameter resolved through the ONE GAIN OWNER `waveform_magnification_gain` — `2^(level/2)`, the √2 ladder capped at ×16 — the product clamped to [-1,1] so a forte clips flat and its troughs still dip), the LEVEL a member of the plate fingerprint and of `OverviewBarCache`'s reuse key — ONE GAIN ON EVERY WAVEFORM PICTURE, the overview strip included, the `h` view by construction. EVERY PAN SUPPRESSES FOLLOW for the playback session (`scroll_viewport` funnel). All view switches take the synchronous rebuild; bare 1/2/3 are absolute view selectors composing the `t`/`p` handlers.

### Render Pipeline (→ render-pipeline.md)
The parser is the sole authored→engine compiler (phase resets emit `S − N/2`, no render-time offset anywhere); geometry in `engine_geometry.h`. Trim is a prepost stage: full maps → `plan_trim` (refusal = render untrimmed, never a failed render) → trim-ignorant engine → `post_trim` → unconditional limiter → encode. No subprocesses in the render path. Ctrl+Alt+R is TWO commands selected by the iteration-mode bit (single render / iteration sweep); Ctrl+Alt+Shift+R archival is a consumed no-op in iteration mode. A render dispatch kills the running render unconditionally; killed sessions never publish Success; target-view previews may reuse identical completed renders (fingerprint v19, phase-reset component trim-aware).

### Colors (→ render.h palette block)
THE PALETTE IS HARD-CODED WHOLE, no user-settable colors, FULLY OPAQUE (no compositing alphas): every painted surface is a constexpr from its sampled kdenlive/Breeze crop or a recorded derivation; a retune is a recompile. The region highlight recolors the CANVAS GROUND under the ink; the marker class ladder is DISABLED > RED > (selected-bright default); stems read the CLASS alone; disabled markers paint no stem; THE MARKER LANE'S TEXT IS BLACK in every class and state (`kMarkerFlagLabel`, the lane's own ink — `kRedesignLabel` #fcfcfc stays every other row's), the disabled LABEL blending at its own `kMarkerDisabledLabelMix` while every disabled SHAPE keeps `kMarkerDisabledMix`, and the flag editor's selection band white off `kMarkerEditorSelectionBand` rather than off the ink. colors.conf and its machinery are deleted. Codex is skipped for simple color tuning.

### Settings (→ settings.md)
`app.engine_settings` is typed truth; all three boundaries are strict (whole-file schema load shared verbatim by GUI and CLI; editor red-flash; typed dispatch). `kSettingsOrder` owns write order, `kEngineKeys` membership; adding an engine key = six edits, four files. EVERY key is required — no absent state, no load-side default. GUI-kind keys commit through each key's own gesture chokepoint, history-less. Numeric values pin to one canonical spelling each. `gui_scale` is an integer percent in [50, 400] (ceiling 200 → 400, architect approval 2026-08-26, the third frozen grant on this key — the tablet's 280 dpi panel needs 212–225 % to reproduce the retired rig's layout; the parser arm in settings_file.cpp is the ONE range owner), the product's ONE scale axis (`gui_scale_factor()`), applied live; the ceiling widened but NO COLLISION RULE was added — the icon row's 1041-authored-px walk crops past the right edge below ~1040 logical px, the history icons first (architect: the sanctioned casualties). `waveform_magnification_level` (2026-08-26, the granted frozen touch) is the WAVEFORM PICTURE'S COUNT OF HALF-DOUBLINGS in [0,8] (`is_waveform_magnification_level`, settings_file.h — the RANGE shared with the CLI so an out-of-range value refuses in both; the GAIN `2^(level/2)` is the GUI's own, `waveform_magnification_gain` in render.h), written ONLY by `apply_waveform_magnification_level`, which the three chords Ctrl+= / Ctrl+- / Ctrl+0 (the reset, ctrl-exact like the two steps; the STEPS repeat-eligible, the reset not; all three read-only- and `h`-legal), the three icon-row buttons (**Magnify waveform** / **Reduce waveform**, hold-repeating, plus **Reset waveform magnification**, which does not) and the editor commit all call; the applier's bracket is also the ladder's ends; history-less, NOT carried by `'`, and NO AUDIO ANYWHERE — no sample, no playback path, no render. Render entries carry the same sidecar set a source does; `'` loads a recipe in place — the markers and the engine block, exactly what its one undo entry restores, everything else (both bands, trim, the view bits, the camera, every pref) staying live.

### Load-Lenient, Render-Normalizing Boundaries (→ normalization-and-boundaries.md)
Gestures move freely and NOTHING POPS: ambiguous authored states normalize at the render boundary (`resolve_warp_markers_for_render`; red flags as the display cue), never refused or force-resolved. The two-category rule: a state the GUI can commit always loads and renders; a state the GUI can never produce is adversarial and hard-fails the load, first error only, identically in both binaries. The same family covers the REPOSITORY: sanctioned use is strict-exact intended use; out-of-app git under `projects/`, a detached HEAD or a hung hook get a blunt terminal error, no in-app recovery. TRIM IS ALWAYS SET (a full ordered pair always rests; crossed pairs reset to the full window; the endcap drag clamps inclusively at the partner, and a COINCIDENT RELEASE RESETS TO THE WHOLE SONG through the one owner `auto_clear_crossed_trim` — the endcap drag's escape, and since 2026-08-19 every former's, the sweep included); THE FULL WINDOW IS SEMANTICALLY THE OLD UNSET STATE through the one predicate `trim_window_is_full`. EVERY route that writes a trim bound clears the selection and parks the playhead at the new trim start (membership at input_trim.cpp's header), and no trim write hides the trim region overlay MID-GESTURE — exempt BY CONSTRUCTION since 2026-08-19, `park_playhead_at_trim_start` and the sweep's per-motion carry writing the cursor DIRECT and so reaching neither of the hide rule's two movement owners — though since 2026-08-20 THE SWEEP'S OWN END COLLAPSES IT (the hide rule's third clause: `commit_region_sweep` hides unconditionally; a stroke names "this exact region", bare `[` is the recall). THE SWEEP ENFORCES NO WIDTH AT ALL (architect 2026-08-19, retiring `kMinTrimSpanFrames` one day after it landed as distracting and too short to be worth its machinery): a stroke authors exactly the span it draws, and its DEGENERATE CASES take the no-backstop treatment — a COINCIDENT release falls into the endcap drag's own escape `auto_clear_crossed_trim`, now the whole-song reset EVERY former shares, while a merely tiny window rests and renders (honorability belongs to `validate_trim_frames` at the render boundary, whose refusal means render untrimmed, never a failed render). `[`'s degenerate-result refusal and the former's sliver dissolve stay retired. Trim has no undo — `Shift+[` is the recovery. NOTHING LOOPS: every audition plays once to its end (song's end in source view, preview buffer's end in target; `Viewport::trim_range` is the NAVIGATION range owner). Undo coalescing is a hybrid: a HELD KEY'S or HELD BUTTON'S synthesized repeats coalesce by repeat identity behind an OPENER that differs per surface — a held key's burst opens with its own PHYSICAL PRESS, a held button's with its FIRST FIRE (the button's act being the lift's) — rapid physical taps at their presses within `kTapCoalesceMs`=500 merge on a standing subject; `record_gesture` stamps only on the accepted path.

### Interaction Model (→ selection-model.md, marker-ui.md, region-scrub-esc.md, pointer-hit-testing.md)
Every plain marker click lands the playhead, AT THE PRESS through the one act owner `run_marker_click_act` (the plain arm alone then arms `PendingMarkerPress`, whose crossing begins the drag and whose motionless lift owes only the double-click seed); shift = inclusive range from the anchor/focus, ctrl = membership toggle, both landing on the focus they set and arming nothing, both FLAG-ONLY (ADD TO SELECTION — bare `k` and its bottom-row lamp — is the STICKY CTRL for glass: while lit a plain flag click takes that same toggle branch, SHIFT BEATS IT, it is scoped to flag hits by construction, and it auto-clears at every `Selection` mutator except the toggle itself) — a modified press over the waveform is the waveform's own gesture in every view. MARKER STEMS ARE POINTER-INERT everywhere; the FLAG BOX is the marker's one pointer surface (stems still paint). EVERY MARKER MAY CARRY A MEASURE (born 2026-08-19 as the free-text comment, rebranded and grammar-tightened 2026-08-20; both columns): a strict ASCII score-position token — direct `10` / `12 7/8`, or the offset `+4` / `+1/2` / `+4 1/2` adding to the IMMEDIATE predecessor's resolved measure (chains resolve to a plain RATIONAL; a broken link makes it UNRESOLVED, gating consumers only — grammar is validity, resolution is not; byte cap 12 = `+99999 98/99`; the one-day `S:` SECTION QUALIFIER retired in the header's THIRD frozen reopen, architect approval 2026-08-21 — "the context will already be a clue"; authoritative at `marker_measure.h`) — ALWAYS VISIBLE as a blue box continuing the flag rightward, part of the same hit rect (only the DOUBLE-CLICK forks on the span, off the painter's published boundary), EVERY MEASURE ITS OWN AND INHERITING FROM NOTHING, edited in the SIXTH `text_editor::Kind` — `MeasureText`, on the flag editor's machinery, entered by bare `/`, the bottom-row button or that double-click, EMPTY COMMIT REMOVING and a bad token red-flashing in the box (no waveform edge). THE MEASURE PROPAGATE (2026-08-20): Ctrl+/ copies off a contiguous labeled run, Ctrl+Alt+/ pastes onto an anchor through the SEVENTH Kind / FIFTH dialog editor — an OFFSET modal (canonical signed integer, seed 0, Enter = confirm) whose offset shifts direct measures' whole part, `+` forms verbatim, holes clearing, out-of-bracket refusing WHOLE; label lockstep, stop at first divergence, one undo entry, no view switch, no render; unlabeled markers never propagate a measure (walk membership at `warp_marker_propagates`). THE SCORE-VIDEO EXPERIMENT IS SUNSET (architect 2026-08-21: the mpv jump grew into a full Pages/Timeline score system in two sessions and the whole arc was rolled back the same day by hard reset — "not that big of a gain over keeping a file on hand that I can toggle mpv back and forth on"; sunset record in marker-ui.md, the comments above its one survivor): Shift+/ is UNBOUND again (strict modifier validation's consumed no-op), the Measure button greys with the verbs on a locked tab, sheet.map and the extractor are gone, and the score folder is plain local material the architect curates himself behind the one `.gitignore` line `projects/*/score/`. THE MARKER LANE'S TEXT INK IS BLACK in every class and state (`kMarkerFlagLabel`, 2026-08-20, measured off kdenlive itself) — flag labels, the measure box, the `h` view's diff flags and the flag editor's own text and caret; `kRedesignLabel` #fcfcfc is untouched and stays every other row's ink. TWO SAME-DAY RETUNES ride with it: the disabled LABEL takes its own blend fraction (`kMarkerDisabledLabelMix` 0.75 — the surfaces keep `kMarkerDisabledMix` 0.25; the old value suppressed a WHITE label sticking out, the opposite of black's problem), and the flag editor's SELECTION BAND is white off its own constant rather than off the ink (kdenlive's white-field/black-text precedent — WHITE FIELD, BLACK INK for glyphs and caret alike; the selected substring's fill-coloured re-show is deleted, its saturated glyphs having fringed pale where they antialiased against the white). It is the home-view binding's fourth exception (the flag editor itself is the fifth since 2026-08-25, so bracket authoring is no longer source-only); read-only still refuses. On disk it is the ` //<measure>` suffix in the ASCII grammar class (data-model.md). The playhead has ONE form and always paints; a selection's cue is its members' brightened flags. THE REGION IS THE TRIM (2026-08-18): `RegionState` is ONE VISIBILITY BIT and nothing else, the overlay DERIVED from the resting trim bounds every frame through `trim_overlay_span`, so the bar, the overlay and the hit test cannot drift. A SHOWN overlay is manipulable ON THE WAVEFORM ALONE (`region_manipulation_hit`, Y-gated to the waveform rect, 10px bands): drag on a bound = that ENDCAP drag, drag inside = the BRIDGE drag, else pan — trim's own drags armed from a second surface, no parallel editor, their motionless lift falling to the waveform's ordinary click act; shift/ctrl bypass it. THE SWEEP'S FIRST WRITE SHOWS THE TRIM AND ITS COMMIT COLLAPSES IT (born 2026-08-19 at the press as "touching the trim shows the trim", narrowed to the sweep 2026-08-20, moved to the first write 2026-08-21 on the architect's drive — the press-time raise necessarily showed the OLD region, the overlay deriving from the resting trim): `show_trim_region_overlay` is the ONE raise — the hide's counterpart, no framing (unlike `[`'s show half), `h` carved out inside it — called from the sweep's FIRST ACCEPTED TRIM WRITE (`apply_region_drag_motion`'s write branch) and nowhere else; the trim bar's three press claims DEPARTED the inventory 2026-08-20 (a 9 px band under a pointer is its own display — the overlay serves glass), so the overlay shows only a region the stroke itself has authored: up from the first write, collapsed at `commit_region_sweep` whatever the stroke did (a sweep names "this exact region"; bare `[` is the recall; a motionless shift click shows nothing at all). THE SWEEP (the nav surface's shift+drag and the touch region hold) writes the trim PER MOTION EVENT under no width rule (the floor retired 2026-08-19), so a shown overlay tracks the stroke live and a stroke collapsed onto its anchor clears the trim at its release; `commit_region_sweep` is its one release owner; the `h` view's shift former is a PLAYHEAD SWEEP that writes no trim and is carved out of both the write and the show. Every former drops the selection its surface owns and carries the playhead on its moving end. BARE `[` SHOWS AND HIDES (the family moved off `x`/`Shift+X` 2026-08-24, both now unbound — bare `x` was too easy to hit instead of `c` and its show half frames the viewport; `[` reads as the begin endcap; the icon row's lit rect-selection twin, named **Show trim region** — the tooltip names a constant act while the lamp carries the state, the read-only toggle's precedent; `Ctrl+Shift+X` is unbound) — the show half brings the span into view via `bring_span_into_view` (the undo restore's own scroll-else-frame, one shared owner) and HIDING DISCARDS NOTHING, the trim persisting; `Shift+[` maximizes to the whole song (the key, or that same button shift-clicked or long-pressed) and is the recovery, trim having no undo. THE OVERLAY HIDES WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES, WHEN A MARKER IS TOUCHED, AND WHEN THE SWEEP ENDS, AND AT NO OTHER TIME (architect 2026-08-19, the hand-listed inventory collapsed to a RULE; the sweep clause 2026-08-20): a TRANSLATION IS NOT A MOVEMENT (the `t` flip, the cent step's target re-land), a RESTORE IS NOT A MOVEMENT (the A/B tabs are two virtual playheads over one piece) and THE CAMERA IS NEVER A MOVEMENT (pan, zoom, the overview strip, the `h` view's edges), while the TRIM'S OWN SURFACES stay EXEMPT BY CONSTRUCTION MID-GESTURE — the trim family writes the cursor DIRECT and so reaches no owner, the sweep's END being the rule's explicit third clause instead. FIVE CALLS IN THREE CLASSES: the TWO MOVEMENT OWNERS `Viewport::move_playhead_to` and `land_playhead_on_marker`/`land_playhead_on_source_frame` (all hiding UNCONDITIONALLY, so a no-op Home still hides), `run_marker_click_act` for the touch half (it hides even where an arm lands nothing), and `commit_region_sweep` (unconditional at every sweep end path, the `h` former's included); bare `[` and the file load write the bit themselves. BARE ESC'S HIDE IS RETIRED (architect 2026-08-21: `[` is the one manual road onto and off the overlay — the durable show's twin is the durable hide), and with it Esc's rank above the render cancel: an Esc over a shown overlay now cancels an in-flight render in ONE press. THE THREE NON-HIDING ENTRY POINTS are named, never a flag: `Viewport::reseat_playhead_to` (every MAP-CHANGE re-land: both cent-step arms and, since 2026-08-25, the W+target status/value family's four tails) and `reseat_playhead_on_marker` (the S/T flip's re-express, the coincidence auto-select's provable no-op). The rule is stated once at `clear_region_highlight`'s declaration, input_handler.h. CURSORS: named XCursor-theme cursors only, seven kinds, ONE zone map derived from the press routers (`pointer_cursor_kind`), resolved once per run-loop iteration by one owner; a live trim gesture keeps its own cue; the cursor promises the gesture. READ-ONLY protects the authored musical content only — trim gestures, `[`/`Shift+[`, Ctrl+S and both render chords stay legal on a locked tab; the settings editor is disabled there (`read_only_key_blocked`). SPACE always plays from the playhead; SHIFT+SPACE IS THE A/B AUDITION (2026-08-26; the play button's shift-click / long press is the same chord, the roster's fifth shift-admitting button): switch to the OTHER tab, play `kAuditionMs` from its resting playhead twice, switch back, play the same span from this tab's twice — four BOUNDED plays (end = `start + span`, one launch body `launch_playback_window` under Space's view-end form and this one; NOTHING LOOPS still holds, each a discrete play to its own end), no resting playhead ever moving; IT IS PACED (2026-08-26): `kAuditionPairGapMs` rests between a pair's two plays and `kAuditionSwitchGapMs` across the tab switch — the architect's own hand pacing, a retune touching only their declaration (app_state.h) — none before the first play (the rest is between SOUNDS); SEQUENCED IN TWO HALVES WITH NO TIMER OF ITS OWN — the tick's natural-end branch switches (synchronously, the flip seen at once) and ARMS the rest after that switch, `GuiAbAudition::fire_if_due` launches it off the RUN LOOP'S EXISTING DEADLINE TICK beside the key-repeat and touch-window deadlines (granularity one timer period); `phase` stays the act's ONE running bit across both halves (`waiting`/`launch_due_ms` beside it), so `phase != Idle` is still the whole refusal test and A REST IS INTERRUPTED EXACTLY AS A PLAY IS (no owner tests for live playback); state `GuiAuditionSequence` (edge inventory at the struct), ops `GuiAbAudition`; every interrupt ends it where it stands — the clear lives at FOUR OWNERS ACROSS EIGHT CALL SITES, inventoried at `GuiAuditionSequence` — the one stop body ahead of its guard, the one launch body's head, target_render's three clears (`trigger()`'s unconditional on the target path) and, since 2026-08-26, THE TWO MOVEMENT OWNERS beside their trim-region hide (`Viewport::move_playhead_to`, `land_playhead_on_marker` / `land_playhead_on_source_frame`): THE ACT ENDS WHEN THE PLAYHEAD'S POSITION IN THE MUSIC CHANGES, the overlay-hide rule's own membership, so a MOVEMENT interrupts while a TRANSLATION and a RESTORE do not — which is both what makes "the resting cursor cannot move under a standing act" structural and what lets the act's own two tab switches run inside it, and it SUBSUMES the former fourth owner `reseek_keeping_alive` (its one caller runs `move_playhead_to` first), whose clear is deleted for a pointer; THE ACT IS ONE TRANSPORT SESSION FROM ITS FIRST PLAY TO ITS LAST (2026-08-26) — a REST is transport-LIVE, so bare Space (and so the play button's plain lift) STOPS the act through the one stop body at `toggle_playback`'s fork, and `redesign_button_glyph_swapped` reads `playhead_scanner_active || phase != Idle` so the button wears STOP for the act's whole duration instead of flickering to Play three times per act (the transport row never lies about live state); refusals silent at the press (running sequence = consumed no-op, either tab not ready in target view via `GuiTargetRender::preview_ready` + the bound domain, `h` at the allowlist); read-only-legal, Esc not added. Home/End jump to the TRIM bounds; CTRL+HOME / CTRL+END (2026-08-24) jump to frame 0 / the active domain's last frame TRIM-BLIND (`playhead_skip_landing_frame` whole-piece arm, the `h` view's own), and the skip buttons' CTRL-CLICK IS that ctrl chord — the roster's first ctrl-click (`redesign_button_ctrl_admits`; they admit no shift, so the long press never reaches the act by construction, and Ctrl+Shift is refused at the band gate). HORIZONTAL MOVEMENT IS A FOCUS ACT — groups are never moved; singleton steps CLAMP at walls, group presses REFUSE whole (position_nudge.h). Bare arrows nudge the FOCUSED marker in its home view; every focus-changing route lands the playhead (`land_playhead_on_marker`). THE SELECTION IS NEVER PARKED: `p`/Ctrl+Tab clear it; coincidence auto-select at the four entry chokepoints (`auto_select_marker_at_playhead`). Hit geometry rides the DISPLAYED basis (plate_viewport_basis / item_viewport_basis); DAMAGE FOLLOWS THE BASIS OF THE PIXELS IT ERASES (scanner sites narrow-on-plate, discrete commands full waveform-area). POINTER GESTURES HAVE NO CANCEL: Esc mid-gesture is a consumed no-op, any end commits, undo is the mitigation. BARE ESC IS BOUND IN FIVE PLACES AND NOWHERE ELSE (enumeration at input_handler.cpp; the trim-overlay hide left 2026-08-21). The keyboard stop rule is authoritative at `stop_playback_if_playing`'s declaration.

### The Home-View Binding (→ tempo-and-home-view.md)
Each marker column authors in its HOME view only (warp = source, phase reset = target) via `active_column_authoring_allowed`; off home, what the predicate gates is the POSITIONAL family — the `s` drop, the flag drag, the Left/Right nudge, the `m` bpm open, and the phase-reset arms of Ctrl+D / Delete — with silent refusals. FIVE ruled exceptions: the bare Up/Down tempo cent step in W+target (singleton + group), the phase-reset propagate, the iteration-bracket wipe in W+target (iteration mode is target-legal), THE MARKER MEASURE (2026-08-19, rebranded 2026-08-20 — both columns, both views, wherever the flag paints, the measure propagate riding under it; read-only still refuses), and THE WARP STATUS/VALUE FAMILY in W+target (architect 2026-08-24, landed 2026-08-25 — Ctrl+D, Ctrl+N, Delete and the flag editor's payload surface through both open routes; the binding's rationale was always POSITIONAL, so the split is positional vs not; each act's target-view tail takes the cent step's contract: `kick_waveform_sync` then a TRANSLATION re-land through `Viewport::reseat_playhead_to` — the focus's post-change image, or for Delete the cursor's own musical instant through `active_domain_to_source_frame`; the flag editor's arm runs on `canonical_changed` only, a bracket-only commit moving no image; the S↔T flip closes a live editor in BOTH directions; bracket authoring is no longer source-only); inventory at the predicate, app_state.h. W+target has no pointer authoring gesture that MOVES anything — its pointer vocabulary is the measure double-click and the payload double-click, both editor opens.

### Modality and Input (→ conventions.md)
ICONS ARE UP, HOTKEYS ARE DOWN; TOUCH USES ICONS, PLASTIC USES BOTH; NO DISAGREEMENT (architect 2026-08-16, closing the same day's keyup experiment — key commands moved to the release and were returned to the press that evening on the press-timing principle; conventions.md carries the retirement record whole, the extension that was never built included). Every control classifies once and keeps ONE timing everywhere: HOTKEYS ACT AT THE PRESS (`GuiInputHandler::on_key`, the one ranked dispatch), ICONS ACT AT THE LIFT whoever operates them — which is why bare Enter/Space on a focused modal button is the keyboard operating an ICON rather than a hotkey exception, and bare `e` is the keyboard operating the POINTER. THE THIRD CLAUSE COMPLETES IT (architect 2026-08-17, on his first real drive of the 2026-08-15 pointer sweep): CONTENT ACTS THE MOMENT ITS IDENTITY IS CERTAIN — at the PRESS where the press can only mean one thing, at the motionless RELEASE only where the SAME press arms a drag. So the marker flag's three clicks, the `h` view's three diff-flag clicks, the OVERVIEW'S OUTSIDE-THE-BOX TELEPORT (which since 2026-08-18 also ARMS the box pan — the flag's own act-then-arm shape, not a breach: the drag continues the act rather than revealing a second meaning) and EVERY double-click's second press (flag editor, empty-lane create, trim-bar framing) act at the press, while the navigation surfaces' plain click, THE WAVEFORM TRIM OVERLAY'S three presses (each arming a trim bound or bridge drag, their motionless lift falling to the waveform's own click act through `PendingTrimDrag::waveform_click_act`), the overview's inside-box and endcap presses and the trim bar's ctrl / ctrl+shift bound sets — the product's ONE deferred click — wait for the lift; conventions.md carries the certainty audit, the touch answer (the disambiguation window, not the lift, is what keeps a two-finger landing off a press-time act) and the one recorded regression. THE MODAL SURFACE IS THE BOTTOM ROW: while a prompt or dialog editor stands the row yields whole and `paint_modal_dialog` paints into it — one renderer, one window, one frame. Modal buttons are real buttons acting at the LIFT, with the two-strength keyboard focus ring (passive assigned / active walked, both act) and roster-style tooltips; bare Enter/Space press the focused button and commit on the key RELEASE. A prompt answers only after it has painted (`PromptState::painted`); PUBLISHED GEOMETRY MAY ONLY SELECT, LIVE STATE DECIDES (the owner-tag doctrine at `ModalDialogGeometry`). The veil consumes every press outside the modal surface. All seven editors admit exactly their own keys plus bare Esc, Ctrl+S, Ctrl+Q through ONE `route_modal_editor_key` contract; the flag editor is keyboard-modal but pointer/wheel-transparent and stops no playback (`keyboard_modal_editor_active` vs `modal_dialog_editor_active`). Tab in a dialog field offers the completion FIRST and is consumed iff the buffer advanced, else it walks the ring — one condition, no counter. THE CLIPBOARD IS THE SYSTEM CLIPBOARD (editors' ctrl-exact Ctrl+C/X/V + the readout's Ctrl+C; empty answer = consumed no-op). STRICT MODIFIER VALIDATION: a modifier combination with no explicit binding is a no-op everywhere; three families deliberately untightened (`is_tab_cycle`, Ctrl+Z's shift bit, the editors' motion arm — alt excluded). SUPER IS DROPPED AT THE PLATFORM BOUNDARY (no key PRESS delivered while held; the modal arm's armed act still completes at its release — outside the GUI's providence, conventions.md). ALT IS THE FIVE Ctrl+Alt RENDER/PROPAGATE CHORDS AND NOTHING ELSE (Ctrl+Alt+/ joined 2026-08-20) — its pointer vocabulary is empty. Bare `e` is the left mouse button, translated at the platform boundary — never bind it in dispatch. TOUCH IS THE POINTER: wl_touch capability-gated, one finger translated into pointer deliveries behind the disambiguation window (pan-zone deadline `kTouchRegionHoldMs` = the region hold; elsewhere 60ms/8px), two fingers = seated-pivot pinch zoom ONLY (downgrades to the survivor's pan), one hard-end contract for cancel/capability loss; touch.md is authoritative. EVERY CHROME BUTTON ACTS AT THE LIFT (`AppState::ChromePress`: arm tracks the pointer, lift re-hits the published rect and re-asks every gate; modifiers read at the press); the three menu anchors keep the press (reasoned exception). THE HOLD-REPEATING BUTTONS are the four cardinal arrows (2026-08-16, reversing their 2026-08-13 deletion) and, since 2026-08-26, the WAVEFORM MAGNIFICATION PAIR — its third button, the reset, is idempotent and does not repeat (membership = `kToolbarChords`' `repeats` column, never a second list): first fire at `kHoldBeatMs`, later fires at the COMPOSITOR'S advertised rate read per fire (0 = no repeat), the first fire the burst's undo opener and a fired burst suppressing the lift act; the burst rides the arm and its edge inventory is at `AppState::ChromePress`. ALL PRODUCT HOLD THRESHOLDS READ `kHoldBeatMs`=575 (gui_input.h — the chrome shift hold and the touch region hold; 575 is the architect's labwc `<repeatDelay>` matched BY CONVENTION, and the key delay itself stays `wl_keyboard.repeat_info`'s, never hard-coded). The menu row: File / Edit / Settings dropdowns over ONE popup state (`AppState::Dropdown`), hover-crossing switches, the armed menu-row mode, items never grey; the Edit menu (2026-08-20) is the FIVE propagate commands' one pointer home, IconCopy/IconPaste deleted with the move (a RELOCATION under the no-second-road doctrine). All text proper capitalization, never a double space. Ctrl+Shift+Tab means the PAIRED MARCH in every state and hold-repeats in every state (the `h` view marches its diff-flag cycle); Ctrl+Tab stays one-shot everywhere. Bare 1/2/3 are absolute view selectors; digits 4..9 unbound.

### The GitHub Recheck (→ github-recheck.md)
Bare `h` toggles the READ-ONLY HISTORY VIEW: the marker lane paints only the viewed checkpoint's delta (green added / red removed), `,`/`.` walk the piece's whole eligible history — uncapped, background-prefetched (`GuiHistoryPrefetch`), LOAD-GATED by `load_commit_sidecars_strict` (membership gates nothing but membership; an empty walk opens at 0/0 and Save-and-Commit is live there). The source's parent folder under the clone's `projects/` is the project directory — the whole bootstrap rule. TWO COMPARE READINGS (iterative = forward toward now, the default; cumulative = vs the frozen live side; bare `u`, session bit `history_cumulative`, never serialized); the WALK selector is the ICON ROW'S OWN RADIO PAIR on bare `g` (`HistoryWalkGit` / `HistoryWalkSession` over the one switch owner `set_history_reading`; Local = the session's undo/redo timeline as states, `GuiHistoryLocalWalk`, both stacks frozen while the view stands) and ROW 3 IS THE A/B TABS IN EVERY STATE — Ctrl+Tab switches them inside the view too, needing no re-bind, and Ctrl+Shift+Tab IS THE MODE'S OWN PAIRED MARCH there (the mode's Tab act, the tab switch, the mode's Tab act again, over `cycle_history_diff_flag_focus`), lit on the bottom row and hold-repeating in every state. `'` loads the viewed member in place (Remote: any commit spelling, strict sidecar load via RAII scratch; Local: the member number, typed snapshots) — one undo entry on top, mode closes. Mode-local multi-selection over the diff flags + CTRL+H REVERT (each flag's inverse applied to the live store, fully manual and always forcing, one undo entry, closes the view). CTRL+S IN THE MODE IS SAVE AND COMMIT: the commit-title editor, then `GuiSaveOps::save`, then the STRICT-SANCTIONED seven-step act on a background worker (single in flight, every save refused while publishing; failures decide on the advisory return or an observation, success only on an observation; failures write the PERMANENT CRITICAL CHIP on the bottom row — paint-only, cleared by a later clean act or program close; NO in-app retry, a failed push is fixed in the terminal). The head delta greys the act when empty. LOCAL GIT ONLY: `run_git_capture` (reads) / `run_git_mutate` (add/commit/push, the one fenced writer); `projects_repo` guard at init and again before the push, the push URL pinned into the child's own config. THE VIEW OWNS NO NAVIGATION STATE: no entry snapshot, no exit restore, no edge that frames — entering and leaving disturb nothing, which is what lets one tab stand framed wide while the other stands zoomed; THE TRIM BAR SHOWS THE TAB'S REAL TRIM in every state (the display-only diff-span substitution is deleted, so paint equals hit), and its double-click runs the ORDINARY `run_span_framing_command` — the view's one framing gesture, its own two framing owners deleted with the substitution; playback is removed from the view whole.

### Validation Topology (architect-blessed 2026-07-17)
Every guard classifies under docs/engineering/validation_topology.md — five classes, one owner each; a duplicate predicate needs an (a)/(b)/(c) justification; an error arm exists iff a producer exists. A new guard that fits no class is a design smell to resolve, not a row to force.

### Type Names / Naming Symmetry
Marker and related GUI types carry the `Gui` prefix; new marker-side types follow. Warp and phase reset are co-equal axes: new pipeline surfaces ship both columns or record the asymmetry in a comment at the site. "Phase reset" is one concept token, never shortened to "reset" in names; warp is never the unmarked default (recorded-asymmetry inventory in conventions.md).

### Rounding
`std::nearbyint` for banker's rounding wherever fractional values convert to POINTS ON A GRID (authored frames, lattice columns, sample indices). No epsilon nudge. For authored positions, `snap_authored_frame` (app_state.h) is the single conversion chokepoint; no other site may round or cast an authored position. SCREEN PIXELS ARE CELLS, NOT POINTS (architect 2026-08-25, landed 2026-08-26): a pixel x covers [x, x+1), so a surface coordinate names the pixel that CONTAINS it — floor, through the ONE owner `containing_pixel` (input_core.h), which every pointer, touch and capture coordinate passes through on its way to an int; before that owner the ABSOLUTE mouse path truncated (`wl_fixed_to_int`) while the finger AND the captured-pointer ledger (the nav drag's virtual position and its release write-back) rounded (`nearbyint`) — the mouse and finger one pixel apart on the 10px endcap bands and the 8px crossing; the ledger's move from rounding to floor is the one LAPTOP-visible consequence (≤1px on the grab-pan's delivered coordinates and on a motionless click right after a capture release), accepted as the rule's own reach into a continuous coordinate. The cell/point distinction is the whole rule; no site restates it.

### Comments
Comments describe current behavior and rationale only. No dead-process citations (Brief/chunk/Stage/spec tags or "per the brief" prose).

### Grep Before Asserting an Invariant
Before claiming "every site that does X" or "no other caller does Y" inside a brief or a code change, grep it — AND AT RETELLS, NOT ONLY AT CREATION: an inventory retell RE-GREPS membership, never edits the inherited list in place. Structural fix preferred over discipline: an exhaustive enumeration lives at exactly ONE authoritative site per concept, and every other site states only its own class plus a pointer.
